#!/bin/sh
set -eu

if [ "${ALLOW_PUSH:-}" = "1" ] && (echo >/dev/tty) 2>/dev/null; then
  exit 0
fi

echo "Push blocked: 이 저장소는 명시적 수동 push만 허용합니다." >&2
echo "승인 시:  ALLOW_PUSH=1 git push ..." >&2
exit 1
