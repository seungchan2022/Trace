# trace-study 따라잡기 진행 상태

> **For agentic workers:** 이 파일은 실행 계획이 아니라 **학습 진행 상태**다.
> `/trace-study`(인자 없음)가 이 파일에서 다음 덩어리를 고르고, 세션이 끝나면 체크박스를 켠다.
> `/trace-init`이 이 경로의 `- [ ]`를 읽어 "몇 번까지 했고 다음은 몇 번인지"를 복원한다.

**Goal:** 바이브코딩으로 만들어진 Trace를 기능 단위 9덩어리로 나눠, 각 부품이 **무엇을 받아 무엇을
보장하는지 + 어떤 순서로 이어지는지**(깊이 C) 이해하고, 잊어도 10분이면 복원되는 노트로 남긴다.

**설계 근거:** `docs/superpowers/specs/2026-07-31-trace-study-scope-design.md`
**절차:** `.agents/skills/trace-study/SKILL.md`
**산출물:** `docs/study/<번호>-<슬러그>/` — `note.html`(사용자) + `agent-log.md`(에이전트)

**한 덩어리가 한 세션에 안 끝나는 것이 정상이다** (2026-08-10 수정 — 원래 "한 덩어리 = 한 세션"이라고
적혀 있었으나 청크 1은 4일 걸렸다). 그 전제가 *"오늘 안에 끝내야 한다"*는 압박을 만들어 **막힌 것을
그냥 넘기게 했다.** 파트 몇 개씩 끊고 닫는다 — 노트는 파트마다 저장·발행되므로 이어 붙는 데 문제없다.

순서는 데이터가 흐르는 순서(만든다 → 붙인다 → 보여준다 / 잰다 → 살려둔다 → 쓴다)를
따르고, 0번이 맨 앞인 이유는 나머지 8개가 전부 그 골격 위에 얹혀 있기 때문이다.

---

## 진행 상태

> **한 청크 = 폴더 하나.** `docs/study/<번호>-<슬러그>/` 안에 둘이 있다 —
> **`note.html`**(사용자 학습 자료 · 발행 대상)과 **`agent-log.md`**(에이전트용 작업 기록).
> **이 파일에는 진행 상태만 둔다.** 청크별 메모·팩트체크 내역·집필 이력은 그 청크의
> `agent-log.md`로 간다(2026-08-10 개편 — 그전까지 이 파일에 다 쌓여 352줄이 됐다).
>
> ⚠️ **파일을 고쳤으면 반드시 다시 발행한다.** 사용자가 보는 것은 저장소 파일이 아니라
> **발행된 URL**이다. 경로가 바뀌었을 때는 `url` 인자로 기존 URL을 넘겨야 링크가 유지된다.

- [x] **0. 앱 구조** → [`0-app-architecture/`](../../study/0-app-architecture/) (2026-08-03 · 7파트 + 확인 문제 6)
- [x] **1. 손으로 그은 선이 도로 경로가 되는 법** → [`1-drawn-path-to-route/`](../../study/1-drawn-path-to-route/) (2026-08-10 · 소개 4절 + 7파트 + 마무리 절 + 확인 문제 7)
- [x] **2. 구간을 이어붙이고 재는 법** → [`2-segment-stitching/`](../../study/2-segment-stitching/) (2026-08-15 · 소개 3절 + 7파트 + 마무리 4절 + 확인 문제 8)
- [ ] **3. 지도 위에 표시하는 법** → [`3-map-rendering/`](../../study/3-map-rendering/)
      · 🔴 **진행 중 — 2026-08-18에 구성을 개편했다. 파트를 열기 전에 `agent-log.md`의
        「🔴 방향 재검토」와 「✅ 구성 개편」을 먼저 읽는다.** 그 논의를 반복하지 말 것(사용자 요청).
        **축이 「만드는 순서」에서 「화면에 보이는 기능」으로 바뀌었다** — 파트 1~6은 화면 장면
        하나씩이고, 화면에 안 보이는 것(감싸는 골격 · 다시 그리는 조건)은 뒤쪽 **「골격」**으로 뺐다.
        옛 파트 2(얹는 것의 타입) 골격은 삭제했고 그 내용은 파트 1~4로 흩어졌다.
        **소개 3절 + 파트 1 완료(구간 ①②③).**  ✅ **`(d)` 파트 1 검증 완료(2026-08-18) — 단 자체 검증이다.** advisor는 세 번 다 구간별
        결과를 못 줘서, **코드와 Apple 문서로 직접 대조**했다(스킬이 정한 대체 경로).
        **고침 1건** — 「MapKit은 나중에 추가한 오버레이를 위에 얹는다」가 **iOS 문서에 없는 단정**이라
        「이 앱은 그 전제로 만들어져 있다」로 낮췄다. 내역은 노트 「팩트체크 수정 내역」.
        🔴 **미검증 둘이 남아 있다** — 구간 ③의 「다시 만들지 않는 이유」(기록 없음, 코드 구조에서 읽은
        추론) · 구간 ②의 「케이싱 = 밝기 대비」(커밋 메시지가 유일 근거). **둘 다 본문에 표시했다.**
        다음에 advisor가 되면 이 둘부터 건다.
        ✅ **자체 확인한 것(우선순위 낮음):** `SegmentPalette`의 `?? .systemBlue` 폴백 ·
        `MKPolyline`이 `boundingMapRect`를 갖는다(같은 파일 `WaypointDotsOverlay:110`이
        `MKOverlay` 요구사항으로 직접 구현) · 「먼저 추가한 것이 아래」는 코드 주석(`:52`)이 뒷받침. 검증이 필요한 주장: ①구간 ③의 「선택이 바뀔 때 오버레이를
        다시 만들지 않는 이유」는 **기록이 없어 코드 구조에서 읽은 것**이다 ②구간 ②의 「케이싱 도입 이유
        = 밝기 대비」는 **커밋 메시지가 유일한 근거**다 ③구간 ③의 ~~`setNeedsDisplay` 이후 사슬~~ → **✅ Apple 문서로 확인됨**
        (*"causing it to be redrawn during the next update cycle"*, `MKOverlayRenderer.setNeedsDisplay()`).
        **검증 필요한 것은 이제 ①②** 둘이다.
        또 실패하면 파트 2를 그대로 진행하고 청크 끝 팩트체크에서 함께 건다.
        🔴 **구간을 쓰기 전에 `agent-log.md`의 「✅ 파트 1 구간 ①② 완료」를 읽는다** —
        「알고 갈 것」을 다섯 번 고친 판별 목록과 신설 규칙 6건이 거기 있다.
        **미결: 남은 6덩어리(4~8)의 깊이 배분.**
      · ✅ **노트 용어 일괄 수정 완료·발행(2026-08-18)** — 「얹는다」37곳→「추가한다」 ·
        「실어 보낸다」5→「저장해 둔다」 · 의인화 표현 10여 곳(「답할 때」·「지도가 물어보나」 등).
        **파트 1 본문까지 함께 고쳤다**(사용자 결정). 내역은 `agent-log.md`.
        **8파트** 구성(7파트에서 개정 — 감싸기 파트를 신설하고 색↔타입 순서를 뒤집었다) ·
        브랜치 `docs/study-chunk-3` · 발행
        <https://claude.ai/code/artifact/8a7a02b4-6ed3-4214-9436-3291a7886aed>
      · ✅ **파트 1 팩트체크 완료(2026-08-17).** 고침 3건 — ①구간 ⑤의 「`rendererFor`는
        오버레이가 새로 추가될 때 불린다」가 **확인 안 된 주장이라 「그 시점에 렌더러가 이미 있다」로
        낮췄다**(파트 2가 그 위에 서므로 막고 있던 건) ②0.0001도 미터 환산이 위도에만 맞음
        ③Apple 문서 인용 다섯에 출처 표기 없음. 내역은 노트 「팩트체크 수정 내역」 절에 있다.
        **`(d)`를 파트 단위로도 걸도록 바꾼 첫 적용이다**(근거: 청크 2에서 고침 4건이 전부 마지막
        파트에 몰렸다).
      · 🔑 **파트 2를 열기 전에 `agent-log.md`의 「🔑 파트 2 착수 메모」를 읽는다.**
        능력 선언이 **실제 선택인지 확인한 결과(통과)** · 재료 경로 · 파트 1이 넘긴 것 ·
        소유권 경계 · **조심할 것 다섯**이 거기 있다. **재료는 착수 메모 말고 코드에서 다시 뽑는다**
        — 같은 실패가 세 번 났다.
      · ✅ **선행 작업 완료(2026-08-17)** — 「구간 ⑥ 네 번 무너짐」·「구간 ⑦ 제목이 결론」을
        파트 2를 열기 전에 읽었고, 거기서 나온 셋(능력 선언의 반대편 · 첫 조각 ·
        처음 나오는 이름 전수)을 `(b-4)` 입력에 실었다. **그 셋이 값을 했다** —
        구간 ①의 반대편 근거가 틀린 것(`syncHalo`는 반대편이 아니라 소비자)이 그 입력으로 걸렸다.
        내역은 `agent-log.md`「✅ 파트 2 구성 확정」.
      · ⚙️ **저장·발행 단위는 구간이다**(2026-08-16 사용자 결정) — 파트 끝까지 미루지 않는다.
        발행은 **이전 세션 URL이라 `url` 인자 + 사전 `WebFetch`**가 필요하다.
      · 🔴 **질의에서 나온 것은 「몇 주 뒤에 같은 자리에서 또 막힐 것인가」로 가른다**(2026-08-16).
        아니면 노트가 아니라 `agent-log.md`로 보낸다 — 파트 1이 청크 2 최대 파트의 **두 배**가 됐고,
        실측과 「쪼개지 않기로 한」 근거가 「📏 파트 1이 길어진 것」에 있다.
        **쪼갤 거면 `(b-5)` 시점에 — 구간을 쓰기 시작한 뒤에는 번호 참조 60곳이 걸린다.**
      · ⚠️ **도식은 mermaid가 아니다** — `diagram-design`으로 만든 **정적 인라인 SVG**
        (`SKILL.md`「노트 형식 · 도식」). **파트 1 구간 ④·⑤에 둘 넣었다.** 절차·판단은
        「🎨 첫 도식」에 있다: **`.diagram-design` 마커가 없어도 온보딩 질문 없이 통과** ·
        **`self_check.py`는 이 노트에 해당 없음**(모션 전제 · 독립 파일 대상) ·
        한글 13px 이상이 4px 그리드보다 우선.
        노트의 라이트 테마 색을 기준으로 넘기고 기본 주황 팔레트·모션은 쓰지 않는다.
        소개 1의 기존 mermaid는 그대로 둔다.
      · 📦 **청크 2 종료 시 쓴 착수 메모는 `agent-log.md`로 원문 이관했다**(2026-08-16).
        이미 지난 지시(브랜치를 새로 판다 · 낡은 파트 번호 · 발행은 새 URL)가 섞여 있어
        여기 두면 다음 세션을 잘못 이끈다. **유효한 것은 노트와 `agent-log.md`에 흡수됐다.**
- [ ] **4. GPS가 거리·페이스가 되는 법** → `docs/study/4-gps-to-stats/`
- [ ] **5. 백그라운드에서 계속 도는 법** → `docs/study/5-background-execution/`
- [ ] **6. 소리 내는 법** → `docs/study/6-audio-coaching/`
- [ ] **7. 잠금화면 위젯** → `docs/study/7-live-activity/`
- [ ] **8. 저장하는 법** → `docs/study/8-persistence/`


전부 `- [x]`가 되면 따라잡기는 끝이다. 그 뒤로는 `/trace-study MVP{N}`(따라가기)으로 전환하고,
이 파일과 설계 스펙의 처분(옮길지·지울지·둘지)을 그 시점에 판단한다.
