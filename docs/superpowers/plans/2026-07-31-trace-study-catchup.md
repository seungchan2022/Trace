# trace-study 따라잡기 진행 상태

> **For agentic workers:** 이 파일은 실행 계획이 아니라 **학습 진행 상태**다.
> `/trace-study`(인자 없음)가 이 파일에서 다음 덩어리를 고르고, 세션이 끝나면 체크박스를 켠다.
> `/trace-init`이 이 경로의 `- [ ]`를 읽어 "몇 번까지 했고 다음은 몇 번인지"를 복원한다.

**Goal:** 바이브코딩으로 만들어진 Trace를 기능 단위 9덩어리로 나눠, 각 부품이 **무엇을 받아 무엇을
보장하는지 + 어떤 순서로 이어지는지**(깊이 C) 이해하고, 잊어도 10분이면 복원되는 노트로 남긴다.

**설계 근거:** `docs/superpowers/specs/2026-07-31-trace-study-scope-design.md`
**절차:** `.agents/skills/trace-study/SKILL.md`
**산출물:** `docs/study/<번호>-<슬러그>.md`

**한 덩어리 = 한 세션.** 순서는 데이터가 흐르는 순서(만든다 → 붙인다 → 보여준다 / 잰다 → 살려둔다 → 쓴다)를
따르고, 0번이 맨 앞인 이유는 나머지 8개가 전부 그 골격 위에 얹혀 있기 때문이다.

---

## 진행 상태

> ⚠️ **파일을 고쳤으면 반드시 다시 발행한다.** 사용자가 보는 것은 저장소 파일이 아니라
> **발행된 URL**이다. 2026-08-04에 파일만 고치고 발행을 빼먹어 사용자 화면이 하루 전 상태로
> 남았다. **"파일 수정 = 완료"가 아니라 "재발행까지 = 완료"다.**
> 발행 URL과 favicon은 아래 각 덩어리 줄에 적어 둔다(적혀 있지 않으면 다음 세션이 존재조차 모른다).

- [x] **0. 앱 구조** → `docs/study/0-app-architecture.html` (2026-08-03 완료 · 7파트 + 확인 문제 6)
      · 발행: <https://claude.ai/code/artifact/3cdda364-790b-4ada-aa8a-df37847fc450> · favicon `🏗️`
      · **2026-08-04 갱신** — 이 학습이 찾아낸 잔재·구멍 5건을 정비 사이클
      `study-chunk-0-cleanup`으로 처리하고 그 결과를 노트에 덧썼다(코드 상자도 현재 코드로 교체).
      노트 맨 앞 "이 학습이 코드를 바꿨다" 표가 요약이다. 경위: `docs/roadmap.md`
- [ ] **1. 손으로 그은 선이 도로 경로가 되는 법** → `docs/study/1-drawn-path-to-route.html`
      · 발행: <https://claude.ai/code/artifact/a0008fdd-b877-4c9f-9bcf-388f542ec358> · favicon `✏️🛣️`
      · **진행 중 — 소개 4절 + 파트 1·2·3/7 완료. 다음은 파트 4 「쉬운 것부터 조립 — 탭 두 번」.**
      · **브랜치 `docs/study-chunk-1`에서 이어서 한다** (새 브랜치를 만들지 않는다).
      · ⚠️ **2026-08-05에 진행 규칙 두 개가 바뀌었다. `SKILL.md`를 반드시 다시 읽고 시작할 것:**
        ①**「한 번에 한 파트」→「한 번에 한 구간」**(구간 하나 설명하면 멈추고 확인받는다).
        파트 3을 한 답변에 통째로 쏟았다가 사용자가 멈춰 세웠고 파트를 처음부터 다시 열었다.
        ②**구간 끝에 「알고 갈 것」 한 줄**을 붙인다(`.takeaway` 상자). 앞의 "이걸 왜 여나"와 짝이다.
        ③절대 원칙 ②가 갈라졌다 — **없는 용어 만들기는 금지**, **비유는 "먼저" 안 쓰기**(순서 규칙).
      · ⚠️ **파트 2가 2026-08-05에 정정됐다(내용 오류).** 원래 *"전체 재계산이 캐시를 비껴가
        요청이 폭증해서 증분으로 갔다"*고 썼는데 **코드 확인 결과 틀렸다.** 바른 이유는
        **방향**이다 — `drawnStrokes.flatMap`이 그은 순서대로만 붙여서 **새 획을 코스 앞에
        붙이거나 뒤집을 수 없었고**, MVP3이 `StrokeDirectionResolver`(4쌍 거리 비교)를 넣으며
        증분으로 갔다. **캐시는 잘 먹고 있었다**(솎아내기가 앞에서부터 순서대로 돌아 앞 획
        채택점이 보존된다). 노트에 주황색 `.fix` 상자로 남겼다.
        **파트 6 판정의 전제가 이것이므로 다시 뒤집지 말 것.**
      · **파트 3에서 백로그 3건을 올렸다**(`docs/backlog.md` 「학습 중 발견 (2026-08-04, 따라잡기
        청크 1)」 섹션 끝): 탭/그리기 **에러 안내 불일치** · 라우팅 **캐시 상한 없음**(+히트율
        미측정) · **구현체에 테스트 없음**(테스트가 로직 복사본을 검증).
      · ⚠️ **노트 구조가 2026-08-04에 바뀌었다(`b566b51`·`cacb616`). 파트를 덧붙일 때 네 군데를 같이 고친다:**
        ①본문은 `<section>`이 아니라 **`<details class="part" id="p3">` + `<summary>`(part-tag + ptitle) + `<div class="pbody">`**.
        직전 파트의 `open` 속성을 떼고 **새 파트에만 `open`**을 준다(최신만 펼침).
        ②맨 위 목차(`#toc`)의 해당 줄을 `todo` → `class="on"` + 앵커로 바꾼다.
        ③**3분 복습**의 그 파트 `li`에서 `pending`을 떼고 `sum`을 채운다.
        ④**오른쪽 고정 목차**(`.floating-toc`)에 `<li>`를 추가한다 — 좁은 화면용 짧은 라벨(`.ks`)과 `title`,
        그리고 **`data-target="pN"`을 반드시 넣는다**(스크롤 추적 JS가 이 속성으로만 동작한다).
        ⑤**맨 아래 `.next` 상자**가 방금 쓴 파트를 예고하고 있으므로 **다음 파트로 바꾼다.**
        ⑥**구간마다 `.takeaway` 상자**를 그 구간 끝에 붙인다(2026-08-05 규칙). 곁가지가 섞였으면
        `.aside`에 "이건 몰라도 된다"까지 적는다. 정정이 생기면 `.fix` 상자(주황)를 쓴다 — 둘 다
        2026-08-05에 추가된 클래스다.
        · 소개 2절(`#range`)은 **mermaid 도식 때문에 `open` 고정**이다. 접으면 폭 0으로 깨질 수 있어 그대로 둔다.
        · 미해결: 사용자가 고정 목차에 대해 *"먼가 이상한데"*라고 했으나 증상은 미확인(2026-08-04, 진행 우선으로 넘김).
      · 파트 구성(만드는 순서): 1 도메인 타입 → 2 프로토콜 → 3 MapKit 어댑터 → 4 탭 조립 →
      5 다운샘플러 → 6 그리기 조립 → 7 제스처. 노트 위쪽 "3분 복습"에 능력 선언이 다 있다.
      · **파트 6에 이월된 판정**: 획 라우팅 반복문이 ViewModel에 있는데 Application 레이어의
      `CourseEditSession`이 더 맞는 자리로 보인다(사용자 지적, 2026-08-04). 파트 2에는
      "배치가 어정쩡하다"까지만 적었고 판정은 파트 6에서 코드를 놓고 한다.
      · **파트 7에 답할 질문이 예약돼 있다**(2026-08-04 사용자 질문): MapKit 기본 제스처 vs
      우리가 얹은 것 · 더블탭 확대 등과 어떻게 안 부딪히게 했나 · 탭 마커는 커스텀인가.
      · ⚠️ **스킬이 크게 바뀌었다. `SKILL.md`를 반드시 다시 읽고 시작할 것.**
      `366b089`(파트 축 = 만드는 순서) 이후 2026-08-04에 두 번 더 바뀌었다 —
      `93bcb0c`(선언 한 벌은 보여준다 · 부르는 사람 입장으로 통일)와
      **`65f0338`(「비전공자 중학생」 원칙 폐기)**. 독자는 Swift·iOS를 아는 사람이고,
      문법 강의와 지어낸 비유가 이 세션에서 파트 하나를 무너뜨렸다.
- [ ] **2. 구간을 이어붙이고 재는 법** → `docs/study/2-segment-stitching.html`
- [ ] **3. 지도 위에 표시하는 법** → `docs/study/3-map-rendering.html`
- [ ] **4. GPS가 거리·페이스가 되는 법** → `docs/study/4-gps-to-stats.html`
- [ ] **5. 백그라운드에서 계속 도는 법** → `docs/study/5-background-execution.html`
- [ ] **6. 소리 내는 법** → `docs/study/6-audio-coaching.html`
- [ ] **7. 잠금화면 위젯** → `docs/study/7-live-activity.html`
- [ ] **8. 저장하는 법** → `docs/study/8-persistence.html`

전부 `- [x]`가 되면 따라잡기는 끝이다. 그 뒤로는 `/trace-study MVP{N}`(따라가기)으로 전환하고,
이 파일과 설계 스펙의 처분(옮길지·지울지·둘지)을 그 시점에 판단한다.
