# trace-study 따라잡기 진행 상태

> **For agentic workers:** 이 파일은 실행 계획이 아니라 **학습 진행 상태**다.
> `/trace-study`(인자 없음)가 이 파일에서 다음 덩어리를 고르고, 세션이 끝나면 체크박스를 켠다.
> `/trace-init`이 이 경로의 `- [ ]`를 읽어 "몇 번까지 했고 다음은 몇 번인지"를 복원한다.

**Goal:** 바이브코딩으로 만들어진 Trace를 기능 단위 9덩어리로 나눠, 각 부품이 **무엇을 받아 무엇을
보장하는지 + 어떤 순서로 이어지는지**(깊이 C) 이해하고, 잊어도 10분이면 복원되는 노트로 남긴다.

**설계 근거:** `docs/superpowers/specs/2026-07-31-trace-study-scope-design.md`
**절차:** `.agents/skills/trace-study/SKILL.md`
**산출물:** `docs/study/<번호>-<슬러그>/` — `note.html`(사용자) + `agent-log.md`(에이전트)

**한 덩어리가 한 세션에 안 끝나는 것이 정상이다** (2026-08-10 수정 — 원래 "한 덩어리 = 한 세션"이라고
적혀 있었으나 청크 1은 4일 걸렸다). 그 전제가 *"오늘 안에 끝내야 한다"*는 압박을 만들어 **막힌 것을
그냥 넘기게 했다.** 파트 몇 개씩 끊고 닫는다 — 노트는 파트마다 저장·발행되므로 이어 붙는 데 문제없다.

순서는 데이터가 흐르는 순서(만든다 → 붙인다 → 보여준다 / 잰다 → 살려둔다 → 쓴다)를
따르고, 0번이 맨 앞인 이유는 나머지 8개가 전부 그 골격 위에 얹혀 있기 때문이다.

---

## 진행 상태

> **한 청크 = 폴더 하나.** `docs/study/<번호>-<슬러그>/` 안에 둘이 있다 —
> **`note.html`**(사용자 학습 자료 · 발행 대상)과 **`agent-log.md`**(에이전트용 작업 기록).
> **이 파일에는 진행 상태만 둔다.** 청크별 메모·팩트체크 내역·집필 이력은 그 청크의
> `agent-log.md`로 간다(2026-08-10 개편 — 그전까지 이 파일에 다 쌓여 352줄이 됐다).
>
> ⚠️ **파일을 고쳤으면 반드시 다시 발행한다.** 사용자가 보는 것은 저장소 파일이 아니라
> **발행된 URL**이다. 경로가 바뀌었을 때는 `url` 인자로 기존 URL을 넘겨야 링크가 유지된다.

- [x] **0. 앱 구조** → [`0-app-architecture/`](../../study/0-app-architecture/) (2026-08-03 · 7파트 + 확인 문제 6)
- [x] **1. 손으로 그은 선이 도로 경로가 되는 법** → [`1-drawn-path-to-route/`](../../study/1-drawn-path-to-route/) (2026-08-10 · 소개 4절 + 7파트 + 마무리 절 + 확인 문제 7)
- [x] **2. 구간을 이어붙이고 재는 법** → [`2-segment-stitching/`](../../study/2-segment-stitching/) (2026-08-15 · 소개 3절 + 7파트 + 마무리 4절 + 확인 문제 8)
- [ ] **3. 지도 위에 표시하는 법** → [`3-map-rendering/`](../../study/3-map-rendering/)
      · **진행 중 — 소개 3절 + 파트 1·2 완료(둘 다 advisor 팩트체크까지).**
        **파트 3 「출발·도착 핀」은 골격까지 세웠다** — 능력 선언 + 구간 표 6개를 `(b-4)` 검토를
        받아 확정하고 발행했다. ⚠️ **옛 구간 ③(「타입이 둘인 이유」)은 능력 선언에 답하지 않아
        2026-08-19에 접었다 — 6구간 → 5구간, 능력 선언도 모양·누르기까지 넓혔다.**
        **파트 3 완료 — 구간 다섯 + 3분 복습 「붙들 것」 13개 + `(d)` advisor 팩트체크까지.**
        **파트 4 「구간 경계의 흰 점」은 골격까지 세웠다(2026-08-19)** — 재료를 코드에서 전수로
        다시 뽑고(표면 25건, 빈칸 0), `(b-4)` advisor 구성 검토를 받아 **구간 5개**를 확정한 뒤
        파트 머리(한 줄 · 능력 선언 · 구간 표 전체)를 노트에 넣고 발행했다.
        ⚠️ **능력 선언을 바꿨다** — 옛것(*"왜 선이 아니라 따로 만든 오버레이인지"*)은 **선이 반대편이었던
        적이 없고** 오버레이 전환 이유는 **파트 2 구간 ④가 이미 답했다.** 새것은
        *"어디서 나온 좌표인지 + 화면에서 크기가 고정돼야 한다는 요구가 왜 직접 만들게 했는지 +
        그때 채워야 하는 것"*이고, 근거는 커밋 `fb1069f`(*"줌에 무관하게 화면상 고정 크기로 그림"*)다.
        **파트 4 구간 ① 완료(2026-08-19)** — 질의 셋을 받아 **덧붙이지 않고 다시 짰다**
        (사용자 요청: *"그냥 그대로 덧붙이는게 아니라 구간1을 더 이해하기 쉽게 내 질문과 이해바탕을
        같이 반영해서 재구성해야돼"*). 소제목 셋의 겨냥이 바뀌었다 — ①저장이 아니라 계산이고 뿌리는
        `entries` 하나 ②세는 단위가 「끝」이라 출발점은 애초에 후보가 아니고 `dropLast()`가 걸리는
        대상은 **구간 배열** ③변환은 뷰가 하되 모든 값이 그런 건 아니다. 도식 하나(구간 셋 → 흰 점 둘)를
        인라인 SVG로 넣었다.
        🔴 **파트 4 구간 순서를 바꿨다(2026-08-19, 사용자와 합의).** 옛 ②(오버레이 타입·박스)가
        **「그리는 코드를 실행할지 판정하는 값」**이라 **그리는 코드가 뭔지 모르면 설명할 대상이 없었다** —
        실제로 사용자가 `boundingMapRect`·`padding 2000`에서 여러 턴 막혔다. 새 순서:
        ②**어떻게 그려지나** → ③**크기가 왜 확대·축소와 무관한가** → ④**언제 실행되나(박스·`2000`)**.
        ①⑤와 능력 선언은 그대로. **고친 곳은 구간 표 다섯 줄뿐이다**(파트 1·2·3·골격은 「파트 4」까지만
        가리키고 구간 번호는 안 짚는 것을 grep으로 확인).
        **파트 4 구간 ② 완료(2026-08-19)** — 두 번 다시 썼다. 사용자 지적으로 버린 표현 둘:
        ❌*"이 앱이 더한 것"* ❌*"우리가 쓴 것"*(주어가 모호) → ✅**「`MKOverlay` 프로토콜이 요구하는
        프로퍼티 / 이 클래스가 스스로 정의한 프로퍼티」** · ✅**「애플이 준 것이 아니라 이 프로젝트 안에 있다」**.
        ❌*"반대편"*(스킬 내부 용어가 샜다) → ✅**「원래는 이렇게 되어 있었다」**.
        채우기·테두리를 나눠 말하던 것도 **「흰 점을 그린다」 한 줄로 합쳤다**(사용자 지적).
        **파트 4 머리에 「오버레이 ↔ 어노테이션」 리마인드 박스를 넣었다** — 사용자 요청이고,
        구간 ④에서도 필요해 구간마다 반복하지 않으려고 파트 머리에 뒀다.
        `CGContext`는 뺐다 — 능력 선언에 답하지 않고 과부하만 준다.
        **파트 4 구간 ③ 완료(2026-08-19)** — 🔑 **사용자 실기기 관찰이 내 설명을 뒤집었다.**
        내가 *"화면에서 보이는 크기는 항상 5"*로 썼는데, 사용자가 **핀치 중 크기가 단계적으로
        변한다**고 보고했다(*"처음에는 보이는 그대로 있다가 어느정도 지나면 약간 바뀌고 손을 놓으면
        바뀐 상태로"* · **점 전부가 동시에 변한다**). 코드로 확인하니 **줌 변화 시 다시 그리기를
        요청하는 곳이 없다** — `setNeedsDisplay()`는 구간 선택 두 자리(`:302`,`:306`)뿐이고
        `mapViewDidChangeVisibleRegion`(`:495`)은 region 되돌려주기만 한다. **관찰과 코드가 맞았고
        내 문장이 과했다.** 구간 ③에 소제목 하나(「완전히 고정되지는 않는다」)를 더하고
        **백로그에 항목을 올렸다**(「학습 중 발견」 절 · 확인 방법 포함).
        🔑 **사용자가 낸 일반 교훈을 💡 박스로 넣었다** — *"우리의 설명은 코드 베이스 기준이고
        실제 화면 렌더링과는 간극이 있을 수 있다."* 청크 3 전체에 걸리는 관점이다.
        **분량은 줄였다** — 나누기·곱하기 상쇄 산수는 능력 선언에 답하지 않아 한 문장으로 낮췄다
        (사용자: *"내부 과정도 이해를 해야돼?"* → 아니오).
        **파트 4 구간 ④ 완료(2026-08-19)** — **도식 둘을 인라인 SVG로 넣었다**(사용자 요청:
        *"이 구간은 특히 시각화에 대한 도식화를 표현하는게 좋을것 같아"*) — ①오버레이와 어노테이션이
        화면에 나오는 조건(덮는 박스 vs 기준점 하나) ②여유를 안 주면 화면 가장자리 점이 사라지는 장면.
        🔑 **`coordinate`가 막힌 자리였다** — *"오버레이를 그리기 위해서는 coordinate가 필요한 거
        아니야?"* 답은 **오버레이가 요구하는 값이 아니라 어노테이션에서 물려받은 요구**라는 것이고,
        오버레이가 더한 것은 `boundingMapRect` 하나뿐이다. **그리고 「coordinate가 무엇인가」 자체를
        다시 깔아줘야 했다**(위도·경도 한 쌍 · 파트 2에서 나왔으나 잊음) — **앞 파트에서 한 번 이어준
        것은 그 자리에 없다**는 「이름은 병기한다」 규칙이 여기서도 그대로 걸렸다.
        🔴 **구간 ⑤를 접었다 — 5구간 → 4구간(2026-08-19).** 사용자가 잡았다 —
        *"이게 프로젝트를 이해하려고 하는 스터디 입장에서 이것까지 알아야 하나?"* **능력 선언 셋 중
        어디에도 안 답했고**, 전환 사건은 파트 2 구간 ④가 이미 다뤘다. ⚠️ **(b-2)「깨지고 고친 과정」이
        (b-4)「능력 선언에 답하나」를 면제하지 않는다** — 그렇게 세워서 검사를 건너뛰었다.
        살린 것: **노트(구간 ④ 끝 곁줄)** 겹침 사실 · **백로그** 흰 점끼리 양보 없음 + QA 항목 누락 ·
        **`agent-log.md`** 집필 이력과 `collisionMode` 분석. 상세는 로그의 「🔴 파트 4 구간 ⑤를 접었다」.
        **파트 4 완료(2026-08-19)** — 4구간 + 3분 복습 「붙들 것」 14개 + `(d)` advisor 팩트체크까지.
        🔑 **팩트체크 범위를 「파트 4 본문만 · 구간 ①~④ 균등」으로 못박고, 발행본 전문을 컨텍스트에
        먼저 올린 뒤 걸었다**(사용자 요청 — *"최근 대화에 치우치지 말고 파트 전체를 보면서"*).
        **효과가 있었다: 지적 6건 중 5건이 파트 머리와 구간 ①에 있었다.**
        고친 것 — ①구간 ③의 정정이 파트 머리·3분 복습에 안 옮겨져 **같은 절 안에서 반대말**을 하고 있었다
        (「한 번 고치면 이미 쓴 곳도 함께 고친다」 미이행) ②`point(for:)`를 구간 ②는 「화면 위치」,
        ③은 「화면 좌표가 아니다」로 불러 **애플 문서(`the overlay renderer's drawing area`) 기준 ②가 틀렸다**
        → **「그리는 영역 안의 위치」로 고정** ③파트 머리 앞참조 `(③④)`가 **구간 재배치 전 번호**였다
        (표 다섯 줄만 보고 이 줄은 안 봤다) ④*"저장분은 `entries` 하나"*가 과했다(`redoStack`·`nextOrder`가
        있다) → **「화면에 그려지는 것의 뿌리」로 좁힘** ⑤「왜 계산 프로퍼티인가」에 출처가 없었다
        → `git log -S waypointCoordinates`로 확인하니 **커밋 `158e6ac`에도 설계 문서에도 없다** →
        **「기록에 없다」로 명시** ⑥*"이음매에서 앞 끝과 뒤 시작이 겹쳐 있다"*가 미검증
        (그리기 모드는 맞춘 뒤 길찾기를 한 번 더 거친다) → **「거의 같은 지점」으로 낮추고 미검증 표시**.
        추가로 **`2000`의 실패 모드가 문서로 확인된 범위를 넘는다**는 한 줄을 곁줄에 더했다.
        🧹 **⚠️ 블록 정리(2026-08-19)** — 사용자 지적으로 **노트에서 곁줄 형식을 없앴다.**
        실측: 곁줄이 청크 1 구간당 0.19 → 청크 3 1.58(**7배**), 그중 **⚠️ 미검증 표시 15개는
        전부 청크 3**이고 청크 1·2에는 0개 — **`(d)` 팩트체크 시작 시점과 겹친다.**
        규칙은 `SKILL.md` 절대 원칙에 기록했다(코드의 사실→본문 / 주장의 한계→그 문장에 한 구절 /
        그 밖→백로그·`agent-log`, **숫자 상한은 두지 않는다**).
        청크 3에 적용: **노트 밖 6건 · 본문으로 32건 · 골격 옛 라벨 8건을 새 형식으로 변환** →
        **24구간 전부가 「이 구간이 다루는 것」 + 「알고 갈 것」**을 갖게 됐다.
        🔴 **청크 1·2는 소급하지 않기로 결정했다**(사용자) — 청크 2는 34구간 중 32개가 옛 라벨만
        있어 **재집필 규모**이고 내용은 이미 팩트체크를 통과했다. **뒤집는 조건: `/trace-study MVP{N}`으로
        그 청크에 덧쓸 일이 생기면 그때 함께 맞춘다.**
        **파트 5 골격까지 완료(2026-08-19).** 착수 메모의 선행 작업 둘을 처리했다 —
        ①**재료 전수를 코드에서 다시 뽑았고**(표면 27건, 빈칸 0 · 안 배정된 것은 전부 파트 1·골격이
        이미 다룬 것) ②**능력 선언을 다시 정했다.** 옛 문장(*"왜 굵어지고 후광이 깔리는지"*)은
        **굵기 쪽이 넓고 카메라 쪽이 좁았다** — 굵기는 파트 1 구간 ③이 숫자표까지 끝냈고,
        카메라 이동은 노트가 두 곳에서 파트 5 몫으로 예고해 뒀는데 문장에 없었다.
        새 문장은 *"고른 것이 **어디에 값으로 남아, 무엇을 바꾸고, 언제 풀리는지**(카메라·후광) +
        **후광만 왜 매번 지웠다 새로 만들어 케이싱 아래에 끼워지는지**"*다.
        🔴 **파트 제목을 바꿨다(사용자 승인)** — 「고른 구간이 굵어지고 후광이 깔린다」 →
        **「고른 구간이 표시되는 법 — 카메라가 옮겨가고 후광이 깔린다」**. 반영한 곳 넷:
        노트 목차·플로팅 목차·3분 복습·이 파일.
        `(b-4)` advisor 구성 검토로 **구간 5개**를 확정했고 지적 둘을 반영했다 —
        ①**「언제 풀리나」를 구간 ②에서 ⑤로 옮겼다**(파트 4에서 값을 치른 것과 같은 모양 —
        효과를 보기 전에 효과가 멈추는 조건을 먼저 설명하고 있었다) ②**후광을 ③④로 가른 기준을
        골격 구간 ⑦의 축**(「얹는 일과 그리는 방법은 갈라져 있다」)에 맞췄다.
        🔑 **`getCoordinates`의 「왜」는 기록에 없다** — 후광이 좌표를 케이싱 오버레이에서 다시
        꺼내오는데, `git log -S "getCoordinates"` 결과가 후광 커밋(`a4da480`) 하나뿐이고 그 메시지에
        좌표 이야기가 없다. 겹침 회피(`OverlapOffsetResolver`, `9dcf1a7` 2026-07-03)가 아홉 날
        먼저 있었으므로 **결과적으로 비켜 그린 좌표와 맞는 것**이고, 그렇게 고른 이유는 기록에 없다.
        **파트 5 구간 ① 완료(2026-08-21).** 🔑 **사용자 질의로 소제목이 하나 늘었고, 덧붙이지 않고
        다시 짰다**(요청: *"좀더 자연스럽게 기본 내용에 녹아들게 적어서 재구성"*). 질문은
        *"굳이 함수가 아니라 그냥 값을 대입하면 되는 거 아니야? `private(set)` 때문이야?"*였고,
        답의 핵심은 **순서가 반대**라는 것이다 — 함수를 쓰려고 `private(set)`을 붙인 게 아니라
        값을 못 바꾸게 정하고 나니 함수가 남은 것이다. 그리고 **막힌 것은 대입뿐이고 읽기는 열려 있다**
        (시트는 값을 직접 읽는다). ⚠️ **이득이 `selectSegment(at:)`에서는 안 보인다** — 바꿀 것이
        하나뿐이라 밋밋하다. `clear()`가 **일곱 가지를 함께** 바꾸는 것을 근거로 댔다.
        **관례 근거: 이 뷰모델에 `private(set)` 11개 + 다른 뷰모델 둘도 같은 방식.
        단 `docs/agent-rules/`에는 규칙으로 없다 — 코드에만 있다.**
        소제목 넷 — ①함수가 하는 일 ②왜 함수인가 ③번호는 배열 자리(파트 1의 `colorKey`와 대비 ·
        행은 두 번호를 다 들고 있다) ④세 곳이 각자 읽어 간다.
        **파트 5 구간 ② 완료(2026-08-21) — 세 번 다시 썼다.** 🔴 **첫 판이 구조부터 틀렸다.**
        `.onChange` 블록의 `guard` 조건 넷을 소제목 하나로 세웠는데, ①조건 3(`newIndex < count`)의
        **이유가 구간 ⑤에 있어서 「그럴 수 있다」만 말하고 근거를 못 댔고**(파트 4에서 같은 이유로
        순서를 바꿨는데 되풀이했다) ②조건 2·4는 능력 선언에 안 답하는 **방어 코드 나열**이라
        ③성격이 다른 넷을 한 줄에 세우니 흐름이 안 생겼다. 사용자 지적 — *"설명이 그럴 수 있다고
        하는데 정작 그런 이유가 없고 어떤 일이 일어난다는 식으로만 말하고 있어."*
        **소제목 2를 통째로 접었다** — 살린 것은 「선택이 풀려도 카메라는 안 돌아온다」 하나이고,
        조건 3은 **구간 ⑤로 보냈다**(이유와 코드가 같은 자리에 온다), 조건 2·4는 뺐다. **4소제목 → 3.**
        🔑 **둘째 판도 반려됐다** — *"이미 한 번 본 사람에 대한 추가설명 같은 느낌"*.
        **`regionFitting`이 무엇을 하는 함수인지 한 번도 안 말했다**(③-2 위반 · 청크 3 구간 ⑦과 같은 실패).
        셋째 판에서 명패 카드로 열고, **「지도는 좌표 목록을 못 받고 중심+크기만 받는다」를 먼저 세운 뒤**
        그것을 만드는 함수로 소개했다 — 그래야 `MKCoordinateRegion` 설명이 **왜 나오는지**가 선다.
        🔑 **분량을 사용자가 직접 잘랐다** — *"너무 자세하고 복잡해서 이해를 못하겠는데 핵심만 잡고
        넘어가고 싶어."* `1.6`·`0.003`을 세 문단 → **한 문장**으로 낮추고, 소제목 2 머리에
        **「이것만 알고 넘어가도 된다」 상자**를 새로 넣었다(*"고른 구간의 한가운데로, 그 구간보다 조금 넓게"*
        + 왜 한가운데인지 · 왜 넓게인지). **이 상자 형식은 다음 구간들에도 쓸 것.**
        ❌ **버린 표현: 「여유」** → ✅**「조금 넓게 잡는다」**. 파트 4가 `2000`(실제로 더하는 값)에
        이미 「여유」를 썼는데 여기 `1.6`은 **곱하는 값**이라, 같은 말로 부르면 같은 장치로 읽힌다.
        ❌ **되돌리기 3행 표**(후광/굵기/카메라) → ✅**한 구절.** 사용자 지적 —
        *"선택이 풀릴때도 무언가 액션을 취해야 한다는거야?"* **표로 나란히 놓으니 카메라만 결함인 것처럼
        읽혔다.** 백로그에도 없고 불편으로 지목된 적도 없다. 남긴 사실은 **「세 곳의 반응이 서로 다르다」**뿐.
        🔑 **시트 가림 한계에 한 줄을 더했다** — `sheetDetent`가 `CoursePlannerPage.swift:30`이고
        `regionFitting`이 같은 파일 `:345`라 **못 봐서가 아니라 안 보는 것**이다(코드로 확인).
        **파트 5 구간 ③ 완료(2026-08-21).** 구성은 셋 — ①화면에서 무엇인가(선이 세 겹이 된다)
        ②왜 나중에 더해졌나 ③어떻게 그려지나(색&#183;굵기&#183;둥근 끝).
        🔑 **「왜」의 근거를 세 단계 기록으로 찾았다** — MVP7 설계가 하이라이트를
        *"강조 lineWidth **또는 채도 상승**"*으로 **둘을 열어뒀고**(반대편이 문서에 실제로 있다),
        MVP12가 halo를 **P2로 미루면서 당길 조건**(*"카메라 핏만으로 선택 구간 식별이 어렵다는
        피드백이 나오면"*)을 적어뒀고, 2026-07-12에 **그 조건이 충족돼** 당겼다.
        **이 사슬이 구간 순서(②카메라 → ③후광)의 근거이기도 하다.**
        🔴 **사용자가 내 도식의 오류를 잡았다** — *"구간을 선택하면 기존 구간도 약간 크게 보이는데
        그거는 뭐야? 착각인가?"* 착각이 아니다. **단면 그림에 `7/11/18`만 적고 그것이 「고른 상태」의
        값이라는 말을 안 해서**, 평소에도 7&#183;11인 것처럼 읽혔다. **평소 / 고른 구간 두 열로 다시
        그렸고**(색 선 5.5→7 · 케이싱 9.5→11 · 후광 없음→18) **어느 것이 파트 1 몫이고 어느 것이
        파트 5 몫인지**를 표로 갈랐다. ⚠️ **숫자를 적을 때 그것이 어느 상태의 값인지 함께 적을 것.**
        **미기록으로 남긴 것** — 색 35%&#183;굵기 18&#183;둥근 끝을 **그 값으로 고른 이유는 어디에도 없다.**
        코드 주석의 *"케이싱보다 더 아래, 훨씬 넓고 옅은 accent 스트로크"*가 의도의 전부다.
        본문에 한 문단으로 합쳐 적었다(⚠️ 블록으로 세우지 않았다).
        **파트 5 구간 ④ 완료(2026-08-21).** 4소제목 — ①`syncHalo` 다섯 단계 ②왜 값만 못 고치나
        ③좌표는 케이싱에서 복사 ④어디에 끼우나.
        🔑 **애플 문서로 근거를 댔다** — `MKPolyline`(`MKMultiPoint` 상속)에 **좌표를 바꾸는 API가 없다.**
        만들 때 받고 `getCoordinates`로 읽는 것만 있다. 이것이 *"왜 굵기처럼 값만 고치지 않나"*의 답 절반이고,
        나머지 절반은 **후광이 고른 구간에만 있어 자리가 통째로 달라진다**는 것이다.
        🔑 **사용자 질의 셋이 구성을 바꿨다.** ①*"다섯 단계 중 3~5가 syncHalo야, 1~5 전부야?"* →
        **전부 함수 본문**인데 그 말을 안 했다. 도식을 `syncHalo 본문 ─┬` 형태로 바꿔 **함수 경계를
        눈에 보이게** 했다. ②*"이전 구간에서 구간 좌표로 후광을 그린 거 아니었어?"* → **카메라(구간 ②)는
        구간 좌표, 후광은 케이싱 좌표**인데 대비를 안 세워서 부딪혀 읽혔다. **표로 갈랐고, 갈리는 이유를
        「필요한 정확도가 다르다」**로 적었다(카메라는 수백 미터 범위라 4m가 무의미 / 후광은 선과 정확히 겹쳐야 함).
        ③*"계산도 모두 새로 하는 건가?"* → **아니다. 계산은 케이싱을 만들 때 이미 끝났고 여기서는 복사만 한다.**
        이게 케이싱에서 가져오는 방식의 **두 번째 이득**이라 본문에 넣었다.
        🔴 **사용자 요청: 「그냥 덧붙이지 말고 내용에 알맞게 재구성. 다른 구간들이 길어진 것 같다.」**
        질의 셋을 **덧붙이지 않고 소제목 안에 녹였고**, 소제목 2를 「첫째/둘째」 두 문단 → **한 문단**으로,
        `currentHaloOverlay`와 호출 자리를 **각각 한 구절**로 줄였다.
        ⚠️ **길이 관리가 이 파트의 상시 과제다** — 구간 ①~③이 길다는 지적이 이미 나왔다.
        **파트 5 완료(2026-08-21)** — 5구간 + 3분 복습 「붙들 것」 15개 + `(d)` advisor 팩트체크까지.
        구간 ⑤는 3소제목(어긋나는 모양 둘 · 여덟 자리 · 따라가게 할 수도 있었다)이고,
        🔑 **규칙이 하나로 정리됐다 — 「배열이 바뀌면 푼다」.** 여덟 자리가 전부 배열을 바꾸는
        자리이고, **배열이 안 바뀌는 attach 실패에서는 선택이 유지된다**(테스트가 못박음).
        🔴 **사용자 질문이 백로그 항목을 만들었다** — *"배열이 바뀌지 않았을때 선택한것을 해제할
        방법이 있어야 한다던가"* → 코드 확인 결과 **`selectSegment` 비테스트 호출부가 행 버튼 하나뿐이고
        토글이 아니라, 사용자가 직접 푸는 길이 없다.** 노트에는 사실만, 판정·처방은 백로그로 보냈다.
        **`(d)` 팩트체크 — 지적 5건 + 「안 열어본 파일 3건」.** 상세는 `agent-log.md`.
        🔴 **가장 값진 지적: 내가 `grep -l`로 뽑아놓고 안 연 파일을 advisor가 되짚었다**
        (회고 셋 — `260713_daily_retro` · `260713_mvp12_completion_retro` · `260701_mvp7_completion_retro`).
        전부 열었고 전부 침묵이라 **「기록에 없다」 세 문장은 그대로 섰다.**
        ⚠️ **교훈: 「기록에 없다」를 쓰려면 뽑은 후보 목록을 전부 열었는지 먼저 확인할 것.**
        고친 것 — ①*"바로 다음 줄에서 지운다"*가 둘에서 거짓(사이에 다른 대입이 있다) →
        **채팅에서 쓴 「같은 함수 안에서」가 맞았는데 노트로 옮기며 조여서 틀렸다** ②*"카메라 범위를
        만드는 곳은 여기뿐"* → `cameraRegion`은 세 곳에서 더 만들어진다 → **「좌표 목록을 감싸는 범위」로 좁힘**
        ③`clear()` 실패 예시가 **실제로는 안 깨진다**(`syncHalo`가 케이싱을 못 찾고 `return`) → 낮춤
        ④원문자 충돌(**단계 ②/⑤ ↔ 구간 ②/골격 구간 ⑤**) → 「단계」 접두어 ⑤「세 곳」↔「세 가지」가
        다른 셋인데 같이 읽힘 → **「세 층」**으로.
        🔑 **길이는 같은 패스에서 잡았다 — 「문장을 압축하지 말고 주장을 뺀다」**(사용자 지적 반영).
        고침 2·3이 각각 한 문단 → 한 구절이 됐고, 구간 ① 소제목 2(5→3문단)와
        구간 ③ 소제목 3(3→1문단)을 합쳤다.
        **파트 1 오류 3건 수정 + 재발행 완료(2026-08-24)** — 파트 6 착수 전 선행 작업을 소진했다.
        같은 날 **파트 2~5 팩트체크 내역을 노트에 채우고** 맨 아래 안내도 갱신했다.
        **파트 6 「되짚어 그리면 두 줄로 보인다」는 골격까지 세웠다(2026-08-24)** — 재료를 코드에서
        전수로 다시 뽑고(공개 함수 1 · private 6 · 파라미터 4 · 테스트 10 · 호출부 1),
        `(b-4)` 구성 검토를 **두 번** 받아(첫 번째는 **입력의 구멍 넷**, 두 번째는 구성 자체)
        **구간 5개**를 확정한 뒤 파트 머리를 노트에 넣고 발행했다.
        🔑 **능력 선언을 두 번 고쳤다** — 옛 문장의 「좌표가 같은」이 **10m 판정을 선언 밖으로 밀어내서**
        「같은 길을 되짚어 지나는」으로 넓혔고, 괄호 넷은 **3분 복습에서 구간 표를 반복하게 되므로** 걷었다.
        **파트 6 구간 ① 완료(2026-08-24) — 두 번 다시 짰다.** 첫 판이 소제목 셋이었는데 3번 하나에
        「평행 이동이 무엇인가 · 원본은 그대로인가 · 언제 도는가」 셋이 뭉쳐 있어 **질문이 다섯 턴** 나왔고,
        소제목을 **넷으로 갈라** 다시 썼다. 🔴 **「민다」가 통하지 않았다 — 설계 문서에 정식 이름
        (「평행 이동」)이 있는데 내 표현을 썼다.** 🔴 **가장 값진 지적: 가독성 문제에 형식을 더하려 했다** —
        「이것만 알고 넘어가도 된다」 상자를 소제목마다 넣자고 했다가 사용자가 물렸다(*"이러면 더 길어지는거
        아니야? 그냥 필요없는 내용들만 찾아서 정리하면 될것 같은데."*). **파트 5에서 통한 처방을 조건 없이
        옮긴 것**이 원인이고, 그 상자는 **내용이 어려울 때** 쓰는 것이지 **분량이 많을 때** 쓰는 것이 아니다.
        결국 **주장 여섯을 빼서** 줄였다. 상세는 `agent-log.md`의 「✅ 파트 6 구간 ① 완료」.
        **백로그 1건 등재** — 「구간 경계 흰 점만 깜빡인다」(원인 미상 · 핀 깜빡임과 다른 대상).
        **파트 6 구간 ② 완료(2026-08-24) — 초안을 버리고 처음부터 다시 열었다.**
        🔴 **판정 방법을 흐름보다 먼저 말한 것이 원인이다** — *"점 하나에서 상대 구간의 선분까지"*라고
        쓰면서 **어떤 점인지·어떤 상대인지**를 안 세웠고, 사용자가 *"설명들이 다 이해가 안돼"*로 막혔다.
        **흐름부터 다시 깔자**(계산 단계 ↔ 그리기 단계 분리 · 「앞서 처리한 구간」 목록이 쌓이는 방식)
        10m·선분이 통했다. 🔴 **사용자가 「그리면서 비교한다」로 읽고 있었다** — 「하나씩」이 두 단계에
        다 있어서 겹쳐 읽혔다. 🔴 **「연속 3개」는 사용자가 접었다**(*"솔직히 이해 못하겠는데 넘어가도
        되지?"*) — 능력 선언으로 판정하니 곁가지가 맞고, **내가 배치 그림 둘로 부풀린 것이 막힌 자리를
        만들었다.** 노트에는 한 문단으로만 넣었다.
        ⚠️ **구간 ①의 증상 그림에서 근거 없는 줄을 뺐다** — *"파란 라벨도 거의 같은 자리에"*는
        **설계 문서에 없고 재본 적도 없다**(문서는 덮인 구간의 라벨만 말한다). 사용자가 잡았다.
        상세는 `agent-log.md`의 「✅ 파트 6 구간 ② 완료」와 「⚠️ 구간 ① 그림 정정」.
        **파트 6 구간 ③ 완료(2026-08-24) — 재구성 한 번.** 🔴 **막힌 곳이 하나였다** —
        *"배열자리가 밀리는거랑 화면에서의 그림이 밀리는거랑 무슨 상관인지 모르겠어."*
        **「첫 번째로 꺼낸 것 = 도로 위에 남는 것」이라는 고리를 안 세우고** 배열 자리 이야기부터 한 것이
        원인이다. 재구성에서 그 고리를 1번으로 올렸다. **장면도 사용자가 스스로 든 예(`A′ → A → B`)로
        바꿨다** — A를 왕복하면 A′가 배열 앞에 꽂히므로, 배열 자리 기준이면 **한 일은 왕복인데 움직이는
        것은 A**가 된다. 상세는 `agent-log.md`의 「✅ 파트 6 구간 ③ 완료」.
        **진행 중 — 다음은 파트 6 구간 ④ 「얼마나 · 어느 쪽으로 밀고, 어디는 안 미나」.**
        🔑 착수 메모의 ⚠️ 조심할 것 3번(**원문자에 「무엇의 번호인지」 붙이기**)이 여기서 가장 위험하다 —
        **「3겹」**이 나오는 구간이고 구간 ②의 **「연속 3점」**과 이름을 갈라 두었다.
        🔑 **착수 전에 `agent-log.md`의 「✅ `(b-4)` 구성 검토 완료」를 읽는다** — 확정 구간 5개,
        이름 전수(명패 필요 8 · 재연결 2: `colorKey`·「케이싱」), ⚠️ **「연속 3점」↔「3겹」 이름 분리**,
        그리고 ②에 넣기로 한 *"무엇이 앞선 것인지는 ③에서 정한다"* 한 마디가 거기 있다.
      · 🔑 **파트 6을 열기 전에 `agent-log.md`의 「🔑 파트 6 착수 메모 (2026-08-21 작성)」를 읽는다.**
        재료 전수 후보(`OverlapOffsetResolver` 152줄 · 함수 7개 · 파라미터 4개 · 테스트 10개)와
        앞 파트가 넘긴 것 넷, ⚠️ 조심할 것 10개가 거기 있다.
        🔑 **파트 5와 달리 「왜」의 재료가 설계 문서에 이미 다 있다** —
        `history/mvp8/2026-07-02-overlap-offset-design.md`가 **기각한 대안 넷 + 채택 이유**를
        적어뒀고, 문제의 출발점(*"나중 구간이 앞 구간을 덮는데 거리 라벨은 원래 자리에 남아
        라벨 색과 선 색이 어긋났다"* — MVP7 실기기 피드백)까지 있다. **파트 2(거리 라벨)가 이 파트의 동기다.**
        ⚠️ **능력 선언을 먼저 판정한다** — 3분 복습의 옛 문장(*"좌표가 같은 두 구간이 왜 두 줄로
        보이는지"*)에 **「도메인 좌표는 안 건드린다」는 경계**와 **「줌아웃하면 다시 겹친다」는 한계**가
        빠져 있다. 파트 5에서 같은 판정을 먼저 한 것이 값을 했다.
      · 🔑 **파트 5 착수 메모의 「⚠️ 조심할 것」 11개도 그대로 유효하다** — 파트 6 메모는
        거기에 **파트 5에서 새로 값을 치른 것 넷**(「기록에 없다」 전 후보 열기 · 채팅 표현을 노트에서
        조이지 않기 · 원문자에 「무엇의 번호인지」 붙이기 · 같은 파트에 「셋」이 둘이면 이름 바꾸기)을 더한 것이다.
        — 파일 맨 아래에 있다. 재료 경로 11 · 앞 파트가 넘긴 것 셋 · **조심할 것 열하나**가 거기 있고,
        열하나는 전부 파트 4에서 값을 치른 것이다. ⚠️ **능력 선언은 아직 안 정했다** —
        발행본 3분 복습의 옛 문장이 파트가 다룰 것보다 좁거나 넓은지 `(b-4)`에서 먼저 판정한다.
      · ✅ **파트 1의 오류 셋을 고치고 재발행했다(2026-08-24) — 파트 6 착수 전 선행 작업으로 소진.**
        ⚠️ **원래 파트 4 구간 ④ 발행에 묶여 있었는데 그 발행이 지나가면서 묻혔고, 파트 5를
        통째로 넘겨서야 `/trace-init`이 집어냈다.** 고친 셋 — ①*"점 여러 개라"*(**개수가 아니라
        반경 단위**가 막았다) ②전환 사건을 파트 4에 귀속(**파트 2 구간 ④**가 맞다) ③*"오버레이를
        **처음부터** 만든"*(두 갈래로 읽힌다). 🔴 **①의 초안이 새 오류를 넣을 뻔했다** —
        *"MapKit이 그리는 코드를 안 줘서"*로 썼는데 **애플은 `MKCircleRenderer`까지 준다.**
        advisor가 잡았고, 정본 문구는 **노트 파트 4 「붙들 것」에서 가져왔다.**
        상세는 `agent-log.md`의 「✅ 구간 ④ 발행에 함께 고칠 것」.
      · ✅ **파트 4 착수 메모는 읽었고 `(b-4)`까지 소진했다** — `agent-log.md` 맨 아래.
        조심할 것 아홉은 구간 본문을 쓰는 동안 계속 적용한다(특히 🔴 도식은 채팅 설명 단계부터 함께 내기 ·
        지적은 구간 전체에 걸리는지 보고 한 번에 내기 · 곁줄로 결론을 하나 더 붙이지 않기).
      · <details><summary>옛 파트 3 착수 메모 안내 (완료됨)</summary>
        거기에 **재료 경로(파일 셋에 걸쳐 있다) · 파트 2가 넘긴 것 둘 · 조심할 것 여섯**이 있다.
        ✅ **선행 작업 둘은 끝났다(2026-08-19)** — `apple-docs` MCP 승인·검증(**`search_apple_docs`만
        깨졌고 우회 경로가 로그에 있다**)과 **`displayPriority` 판정**(**죽은 줄이 아니다 ·
        백로그 안 올린다 · 다시 열지 말 것**).
        ⛔️ **「폐기됨」·「소진됨」이라고 붙은 옛 메모는 쓰지 말 것.**
        ⚠️ **재료 전수는 메모 말고 코드에서 다시 뽑는다** — 메모에 기대 실패한 것이 세 번이다.
        </details>
      · ✅ **파트 1 advisor `(d)` 팩트체크 완료(2026-08-18).** 앞 세션이 네 번 실패한 것을
        **새 세션에서 받았다.** 🔑 **통한 방법: 파트 본문 전문 + 근거 코드를 컨텍스트에 먼저 올리고,
        요청 범위를 「그 파트 본문만 · 진행 방식은 범위 밖」으로 못박아 호출.**
        스킬 `(d)`의 *"파트를 끝낸 그 세션에서 건다"*가 실패했을 때의 **복구 경로가 이것이다.**
        고침 5건 + 근거 보강 2건 + 파트 밖 정리 1건을 반영·재발행했다.
      · ✅ **깊이 배분 미결은 폐기했다(2026-08-18 사용자 결정).** *"기준을 잡기가 너무 애매하잖아."*
        **미리 정할 수 없는 질문이었다** — 어느 덩어리에 결정이 많은지는 그 재료를 읽어야 안다.
        **대체:** `SKILL.md` `(b)`에 **「결정이 없는 것은 파트로 세우지 않는다 — 옆 파트에 한두 줄로
        붙인다」**를 넣었다. 청크마다 파트를 나눌 때 적용하면 짧아질 덩어리는 저절로 짧아진다.
        🔴 **「속도를 줄여야 한다」는 전제는 사용자 요구가 아니라 에이전트 판단이었다 — 다시 열지 말 것.**
        🔴 **「저장(CRUD)은 익숙하다」도 틀렸다** — *"swiftdata는 사용하지 않아서 봐서 잘 몰라."*
        **4~8 다섯이 전부 낯선 영역이다.** 낯선 정도로 가르는 축은 폐기.
      · 🔴 **파트 2에도 그대로 걸리는 것 셋** (파트 1에서 advisor가 잡은 것):
        ①**처음 나오는 이름에 한 줄 설명** — `(b-4)` 입력에 「설명 있음/없음」을 채운다.
        ②**도식은 그린 뒤 코드로 대조한다** — 파트 1에서 관문이 없는데 관문으로 그렸다.
        ③**앞참조(「파트 N에서 본다」)는 현재 번호표로 확인한다** — 개편 뒤 낡은 번호가 세 번 나왔다.
      · ⚙️ **저장·발행 단위는 구간이다**(2026-08-16 사용자 결정) — 파트 끝까지 미루지 않는다.
        발행은 **`url` 인자 + 사전 `WebFetch`**가 필요하다(2026-08-18 재확인).
      · ⚠️ **도식은 mermaid가 아니다** — `diagram-design`으로 만든 **정적 인라인 SVG**.
        절차·판단은 `agent-log.md`「🎨 첫 도식」에 있다: **`.diagram-design` 마커가 없어도 온보딩
        질문 없이 통과** · **`self_check.py`는 이 노트에 해당 없음** · 한글 13px 이상이 그리드보다 우선.
        노트의 라이트 테마 색을 기준으로 넘기고 기본 주황 팔레트·모션은 쓰지 않는다.
      · **6파트 + 골격 2 구성**(2026-08-18 개편 — 축이 「만드는 순서」에서 「화면에 보이는 기능」으로).
        파트 2 거리 라벨 · 3 핀 · 4 흰 점 · 5 선택 강조 · 6 겹친 선.
        브랜치 `docs/study-chunk-3` · 발행
        <https://claude.ai/code/artifact/8a7a02b4-6ed3-4214-9436-3291a7886aed>
- [ ] **4. GPS가 거리·페이스가 되는 법** → `docs/study/4-gps-to-stats/`
- [ ] **5. 백그라운드에서 계속 도는 법** → `docs/study/5-background-execution/`
- [ ] **6. 소리 내는 법** → `docs/study/6-audio-coaching/`
- [ ] **7. 잠금화면 위젯** → `docs/study/7-live-activity/`
- [ ] **8. 저장하는 법** → `docs/study/8-persistence/`


전부 `- [x]`가 되면 따라잡기는 끝이다. 그 뒤로는 `/trace-study MVP{N}`(따라가기)으로 전환하고,
이 파일과 설계 스펙의 처분(옮길지·지울지·둘지)을 그 시점에 판단한다.
