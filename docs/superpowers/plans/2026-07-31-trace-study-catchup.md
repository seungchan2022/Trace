# trace-study 따라잡기 진행 상태

> **상태: 중단 — 필요할 때 사용자가 `trace-study`를 명시적으로 호출해 선택적으로 재개한다.**
> 기존 노트·체크박스·재개 지점은 보존하지만, `trace-init`은 이 파일을 활성 작업이나 다음 작업으로 제안하지 않는다.

> **For agentic workers:** 이 파일은 실행 계획이 아니라 **학습 진행 상태**다.
> `/trace-study`(인자 없음)가 이 파일에서 다음 덩어리를 고르고, 세션이 끝나면 체크박스를 켠다.
> `/trace-init`이 이 경로의 `- [ ]`를 읽어 "몇 번까지 했고 다음은 몇 번인지"를 복원한다.

**Goal:** 바이브코딩으로 만들어진 Trace를 기능 단위 9덩어리로 나눠, 각 부품이 **무엇을 받아 무엇을
보장하는지 + 어떤 순서로 이어지는지**(깊이 C) 이해하고, 잊어도 10분이면 복원되는 노트로 남긴다.

**설계 근거:** `docs/superpowers/specs/2026-07-31-trace-study-scope-design.md`
**절차:** `.agents/skills/trace-study/SKILL.md`
**산출물:** `docs/study/<번호>-<슬러그>/` — `note.html`(사용자) + `agent-log.md`(에이전트)

**한 덩어리가 한 세션에 안 끝나는 것이 정상이다** (2026-08-10 수정 — 원래 "한 덩어리 = 한 세션"이라고
적혀 있었으나 청크 1은 4일 걸렸다). 그 전제가 *"오늘 안에 끝내야 한다"*는 압박을 만들어 **막힌 것을
그냥 넘기게 했다.** 파트 몇 개씩 끊고 닫는다 — 노트는 파트마다 저장·발행되므로 이어 붙는 데 문제없다.

순서는 데이터가 흐르는 순서(만든다 → 붙인다 → 보여준다 / 잰다 → 살려둔다 → 쓴다)를
따르고, 0번이 맨 앞인 이유는 나머지 8개가 전부 그 골격 위에 얹혀 있기 때문이다.

---

## 진행 상태

> **한 청크 = 폴더 하나.** `docs/study/<번호>-<슬러그>/` 안에 둘이 있다 —
> **`note.html`**(사용자 학습 자료 · 발행 대상)과 **`agent-log.md`**(에이전트용 작업 기록).
> **이 파일에는 진행 상태만 둔다.** 청크별 메모·팩트체크 내역·집필 이력은 그 청크의
> `agent-log.md`로 간다(2026-08-10 개편 — 그전까지 이 파일에 다 쌓여 352줄이 됐다).
>
> ⚠️ **저장·확인 절차를 이 파일에 옮겨 적지 않는다** &#8212; 집은
> `.agents/skills/trace-study/saving.md`이고 여기서는 가리키기만 한다.
> (2026-08-31: 아티팩트 발행을 걷어냈는데 **이 자리에 복사돼 있던 옛 절차가 남아**
> 매 세션 없어진 단계를 지시하고 있었다. `authoring.md`의 「한 규칙 한 곳」이 막으려던 것이다.)

- [x] **0. 앱 구조** → [`0-app-architecture/`](../../study/0-app-architecture/) (2026-08-03 · 7파트 + 확인 문제 6)
- [x] **1. 손으로 그은 선이 도로 경로가 되는 법** → [`1-drawn-path-to-route/`](../../study/1-drawn-path-to-route/) (2026-08-10 · 소개 4절 + 7파트 + 마무리 절 + 확인 문제 7)
- [x] **2. 구간을 이어붙이고 재는 법** → [`2-segment-stitching/`](../../study/2-segment-stitching/) (2026-08-15 · 소개 3절 + 7파트 + 마무리 4절 + 확인 문제 8)
- [x] **3. 지도 위에 표시하는 법** → [`3-map-rendering/`](../../study/3-map-rendering/) (2026-08-25 · 소개 3절 + 기능 파트 6 + 골격 1 + 마무리 4절 + 확인 문제 7)
      · **진행 이력은 `agent-log.md`로 옮겼다** — 파트 1~5는 「📦 진행 요약 아카이브」, 파트 6과
        `(d)`·`(e)`·`(f)`는 그 앞 절들에 있다.
      · 🔑 **이 청크에서 규칙 셋이 바뀌었다** — ①「파트당 구간 3개 이하」 폐기(깊이 결정에 얹혀
        들어간 숫자 상한이었다) ②노트의 팩트체크 절은 **「이미 발행된 것이 무엇으로 바뀌었나」만**
        (검증 과정은 `agent-log.md`) ③**골격 2를 접었다** — 새 깊이에서 남는 것이 없었다.
        경위는 `.agents/skills/trace-study/decision-log.md`와 이 청크 `agent-log.md`.
      · ⚠️ **다음 청크 착수 시 확인할 것 둘** — ①**확인 문제 정답 위치를 섞는다**(이번에 일곱 문제
        내리 B였다) ②**파트를 닫을 때 목차 부제와 노트 맨 아래 「다음」 안내를 함께 고친다**
        (본문 고칠 때 눈에 안 들어오는 자리라 여러 세션 낡아 있었다).
- [ ] **4. GPS가 거리·페이스가 되는 법** → [`docs/study/4-gps-to-stats/`](../../study/4-gps-to-stats/)
      · **진행 중 — 소개 3절 + 파트 1~6 완료·발행. 파트 7은 구간 ①까지 넣었다(구간 1/3).**
        🔴 **다음은 파트 7 구간 ②를 쓰는 것**이다 — 「「이번 주」는 시작일과 마지막 날을 구해 그
        사이인지 보는 것이 아니라, 두 날짜가 같은 주에 드는지를 달력에 묻는 것이다」.
        **골격과 구간 표는 노트에 이미 있으니 다시 정하지 말고 `(c)`-1 예고부터 들어간다.**
        🔴 **구간 ③까지 넣으면 그 세션에서 파트 7 `(d)`(「그 파트만」)를 먼저 걸고, 그다음이
        청크 닫기다.** `(d)`는 두 번 거는데 아래 ⚠️에 적힌 것은 **청크 끝의 「파트 간 일관성」
        하나뿐**이라, 이 줄이 없으면 파트 단위 검토를 건너뛰고 닫게 된다 — 파트 6에서
        **골격·능력 선언이 구간 ③의 결론과 어긋난 것을 잡아낸 것이 그 파트 단위 `(d)`였다.**
        `(d)`의 🔑대로 **파트를 끝낸 그 세션에서** 건다.
        🔴 **값 예시를 다시 만들지 말 것 — 여덟 줄이 `agent-log.md` 2026-09-01 「파트 7 구간 ①
        집필」에 표로 못박혀 있다.** 구간 ②가 바로 그 여덟을 다섯으로 줄이는 자리이고,
        이번 주(7/19~7/25) 다섯 줄 · 지난주(7/12~7/18) 세 줄 · 이번 달 여덟 줄 합계가 거기 있다.
        **「전체」와 「이번 달」이 같은 여덟 줄이라 값이 같아지는 것**도 적어 뒀다 —
        *"세 갈래가 언제 갈리고 언제 같아지나"*로 쓸 수 있다.
        🔑 **예측 질문에서 사용자가 맞히면 거기서부터가 설명의 시작점이다.**
        2026-09-01 구간 ①에서 맞힌 것을 처음부터 다시 설명해 **분량이 두 배가 됐고**
        사용자 지적(*"내가 알아야 할 내용보다 설명이 훨씬 긴것 같은데"*)으로 절반을 걷어냈다.
        🔴 **HTML로 옮기면서 새로 만든 것은 `(c)`-5 검토를 안 지난 것이다** — 도식이 그렇다.
        구간 ①에서 초안의 코드블록을 SVG로 다시 그리다 **목록을 거리순으로 그렸는데 실제로는
        최신순**(`fetchSummaries`)이었다. 넣은 뒤 `--note`가 26건으로 튄 것도 도식 캡션이었다.
        🔴 **구간 ①은 `(d)` 팩트체크를 안 지났다**(파트 단위라 파트 7이 다 찬 뒤가 정상 시점이지만,
        세션을 구간 ①에서 닫았으므로 남긴다). **확인받을 것은 둘이고 둘 다 코드가 아니라 문서
        기록으로만 뒷받침된다** — ①*"러닝 탭 대기 화면의 「이번 주 0회」 자리가 쓸 예정이었다가
        그 자리 자체가 걷히면서 `lastRun`만 남았다"*(근거는 킥오프 §4.1과
        `history/mvp17/2026-07-23-run-idle-polish-design.md`, 백로그 `:329`)
        ②*"러닝을 저장할 때 다섯 칸을 따로 떼어 둔다"*(근거는 `SavedRunSummary` 주석뿐이고
        **저장 구현은 청크 8 몫이라 안 열었다**). **둘 다 노트가 단정으로 적고 있다.**
        ⚠️ **이미 근거를 채운 것은 다시 재지 말 것**(우선순위 낮음) — 계산기 입력이
        `[SavedRunSummary]`뿐 · `RunStats` 세 필드와 고도 미포함 · `lastRun` 앱 호출부 0(grep 전수) ·
        목록이 최신순 · 합계가 계산 프로퍼티라 매번 다시 더한다 · 값 산술(12,400 m · 4,327초 ·
        삭제 후 8.98 km·52:36) · `--note` 24 유지. **전부 코드나 실행으로 확인했다.**
        🔑 **또 못 걸면 본 작업(구간 ②)은 그대로 진행하고 파트 7 뒤 정식 `(d)`에서 함께 건다.**
        🔑 **재료는 코드 넷이다** — `Domain/RunTracking/Entity/RunStats.swift` ·
        `Pages/HistoryPage/HistoryPageViewModel.swift` ·
        `Pages/HistoryPage/UIComponent/HistoryPage+DashboardComponent.swift` ·
        입력인 `SavedRunSummary`. **표면 전수와 구간 배정은 `agent-log.md` 2026-09-01에 있다.**
        ⚠️ **`averagePaceSecondsPerKm`은 파트 3 몫인데 노트에 이름이 없다** — 지금 손대지 말고
        「파트 간 일관성」 검토에서 판단한다.
        ⚠️ **`--note` baseline은 24다.** 25가 나오면 그 자리에서 원인을 찾는다 —
        지금까지 튄 원인 셋은 ①골격 힌트의 `②이 숫자`(원문자 표기) ②도식 라벨의 *"고쳐야 한다"*
        ③3분 복습의 *"시작 전 화면"*(고정된 화면 이름이 아니다)이었다.
        🔑 **다른 파트의 구간 표 힌트 다섯 곳은 아직 기호만 쓰고 조사도 갈려 있다** —
        「파트 간 일관성」 검토에서 함께 한다.
        🔴 **예측 질문에는 셋을 다 준다: ①실물 값 ②흐름 그림 ③각 선택지가 코드에서 어떻게 적히는지.**
        세 번 막혔고 원인이 매번 달랐다 — 배경 부족(파트 5 구간 ①) · 시간축이 둘(파트 5 구간 ②) ·
        **선택지끼리 무엇이 다른지가 안 잡힘**(파트 6 구간 ①). 값과 그림만으로는 부족하다.
        🔴 **채팅 구조(구간 머리 · 🎯 · 소제목 · ✅)는 `(c)`-3의 것이다. 예측 질문 자리로 끌어오지 않는다.**
        2026-09-01 파트 6 구간 ②에서 끌어다 썼더니 **설명 차례에 장면이 통째로 빠졌다.**
        조항은 `session-cycle.md` `(c)`-3, 경위는 `decision-log.md` 2026-09-01.
        **다시 열 때는 앞 답변을 참조하지 않고 통째로 다시 쓴다**(「흐름 ①·②가」가 또 끊었다).
        ⚠️ **그림에서 갈래 둘을 위아래로 나란히 놓지 않는다** — 같은 러닝에 분모가 둘 있는 것으로
        읽혀서 *"5000과 1800은 뭐야?"*가 나왔다. **한 러닝에 하나뿐인 것은 그렇게 적는다.**
        ⚠️ **확인 문제는 청크가 끝난 뒤 한 번 낸다**(파트마다가 아니다 &#8212; 2026-08-30 사용자 확인,
        `session-cycle.md` `(e)`에 명시).
        노트: `docs/study/4-gps-to-stats/note.html` · 상세 이력: 같은 폴더 `agent-log.md`
        (**완료된 파트 1~6의 진행 이력은 거기 「📦 진행 요약 아카이브」에 있다.**)
      · 🔴 **구간 진행 순서(2026-08-25~26에 굳었다)** — 어기면 되돌리는 값이 크다.
        **①초안을 파일로 쓴다 → ②`python3 .agents/skills/trace-study/check-draft.py <파일>`을 통과시킨다
        → ③채팅으로 먼저 보인다 → ④질의를 받아 고친다 → ⑤그다음 노트에 넣고 발행한다.**
        **만들자마자 노트에 넣지 않는다**(2026-08-25에 그래서 구간 하나를 통째로 되돌렸다).
        그리고 **첫 구간을 설명하기 전에 파트 골격(한 줄·능력 선언·구간 표 전체)을 노트에 먼저 넣는다.**
      · 🔴 **사용자가 시킨 범위만 한다 — 2026-08-31에 또 어겼다.** 지시는 *"구간3을 노트에 넣어줘"*였는데
        3분 복습·목차 부제·「다음」 안내까지 고쳐서 되돌렸다. **깨진 자리는 `saving.md`가 「파트가 다 차면
        3분 복습을 채운다」고 적어 둔 것을 「지금 채워야 한다」로 읽은 것이다** &#8212;
        **절차가 시키는 것과 사용자가 시킨 것이 다르면 사용자 쪽이 범위다.**
      · 🔴 **질의를 노트에 덧붙이지 말고 재구성한다 &#8212; 2026-08-31에 또 어겼다.**
        사용자 원문 *"내 질문에 대한 답을 그대로 덧 붙히는게 아니라 이해하기 쉽게 재구성하라는거야."*
        이번에 옮긴 것은 문장이 아니라 **논증 순서**였다(대안을 세우고 반박하는 형태).
        🔑 **판별법을 하나 더 쓴다 — 「그 순서가 대화에서 온 것인가」.** 「기능 설명인가 내 해명인가」로는
        못 잡는다. 내용은 기능 설명이 맞았고 순서가 논증이었다. 고친 법은 **축을 갈아끼우는 것**이다
        (「무엇을 반박하나」 → 「두 규칙이 각각 무엇을 재나」). 경위는 `agent-log.md` 2026-08-31.
      · 🔴 **문서와 코드를 대조한 결과를 채팅·노트에 내지 않는다 &#8212; 2026-09-01에 어겼다.**
        파트를 여는 답변에 *"킥오프 §4의 그림과 지금 코드가 어긋난다"*를 한 문단으로 냈더니
        사용자가 *"내가 그것까지 알아야 하는거야? 어쩌피 나는 너가 정리하고 방향성을 잡아준
        상태에서 이것을 진행하는데?"*로 되물었다. **대조 결과는 `agent-log` 몫**이고, 노트와
        채팅에 나가는 것은 *지금 코드가 무엇을 하는가*와 `(b-2)` 형태의 기능 언어뿐이다.
        `check-draft.py`가 이제 이 형태를 잡는다. 🔑 **설계 문서를 근거로 인용하는 것은 `(b-1)`이
        요구하는 것이라 안 잡는다** — 첫 패턴이 그것까지 잡아 노트에서 21건을 오탐했다(24→45).
      · 🔴 **`check-draft.py --note` 건수를 이전 커밋과 비교한다** &#8212; 편집할 때마다.
        **SVG를 viewBox 크기로 찾아 바꾸다 남의 구간을 덮어쓴 사고를 잡아낸 경로가 그것 하나였다**(2026-08-30).
        구간 id(`<details class="seg" id="pNsM">`)로 먼저 잘라내고 그 안에서 바꾼다.
        검사는 **구간 밖도 본다** — 소개 `<section>` · 파트 골격 · 3분 복습.
      · 🔑 **`check-draft.py`에 구간별 전수 모드가 있다** —
        `python3 .agents/skills/trace-study/check-draft.py --note docs/study/4-gps-to-stats/note.html`
        **advisor가 뒤쪽 구간에 치우치는 것을 막는 자리**이고, 「사용자가 볼 필요 없는 것」도 검사한다.
        🔴 **사용자가 새 지적을 하면 그 자리에서 검사 항목을 하나 늘린다** — 두 번 찾게 하지 않는 것이 목적이다.
        🔑 **`(d)`를 걸기 전에 앞 구간들의 코드 근거와 도식 값을 먼저 대조해 대화에 올린다**(2026-08-31 사용자 요청) —
        안 그러면 검토가 방금 쓴 구간으로 쏠린다.
      · 🔑 **파트 표 7개는 확정돼 있다**(노트 소개 3에 있음) — 1 거리 / 2 시간 / 3 페이스 /
        4 고도 상승 / **5 km별 페이스** / 6 목표 진행률 / 7 주·달 합계.
        판정 셋도 끝났다 — 기록 탭 집계는 이 청크에 두고, 목표는 파트 6, 포인트 구간거리는 파트 1.
      · 🔑 **계속 쓰는 형식 셋.** ①**「💬 한 줄로 잡으면」**(`.mywords`) — 사용자가 자기 말로 정리한
        문장을 구간 머리 다음, 첫 소제목 앞에 남긴다. ②**설명을 「사용자 장면」으로 연다**
        (2026-08-26 사용자 확인 — *"훨씬더 해당 기능에 대해서 상황이 몰입되고 하니까 좋은것 같아."*).
        ③**이름 둘은 고정이다 — 「러닝 탭 화면」과 「잠금화면」.** `check-draft.py`가 「어느 화면인지」를 검사한다.
        형식은 `note-format.md`, 경위는 `agent-log.md`.
      · ⚠️ **파트 1·2에 옛 형식 잔여가 남아 있다** &#8212; `--note` 24건이 **전부 소개 절과 파트 1·2·3 몫**이고
        파트 4는 0건이다(「어느 화면인지」 · 「그림 배치」 · 「문단 첫머리 지시어」 등 형식 세대 차이).
        **청크 4를 닫기 전 「파트 간 일관성」 검토에서 함께 판단한다.**
      · 🔑 **파트 7의 값 여덟 줄은 앞 파트 값에 얹혀 있다** — `5,240 m`/`1,695초`는 파트 1·2의
        화면 예시이고 `3,420 m`/`1,171초`는 파트 6의 68% 분자다. **「파트 간 일관성」 검토에서 이
        넷 중 하나라도 고치면 파트 7의 표 여덟 줄과 합계 셋이 전부 따라 움직인다.**
      · ⚠️ **청크 4를 닫기 전에 「파트 간 일관성」 검토를 따로 건다.** 파트별 검토로는 구조적으로 안 걸린다 —
        2026-08-26에 **「1초에 한 번 / 5m마다」 충돌이 소개 절과 구간에 걸쳐** 있었다.
        🔴 **같은 자리에서 「팩트체크 수정 내역」 절을 만들어** 2026-08-31 파트 5 정정을 옮긴다 —
        **마지막 스플릿 줄에 뜨는 값이 구간 시간이 아니라 1km당 환산(`5'52"`)이라는 것**이고,
        **사용자가 이미 읽은 것이 바뀌었다.** 고친 자리 열하나는 `agent-log.md` 2026-08-31
        「파트 5 `(d)` 팩트체크」에 있다.
      · ⚠️ **아직 값을 치르지 않은 것 둘**(청크 3의 교훈. ②「목차 부제와 「다음」 안내를 함께 고친다」는
        2026-08-31 파트 4에서 치렀다)
        ①**확인 문제 정답 위치를 섞는다**(청크 3은 일곱 문제가 내리 B였다).
        ②**공간·자세를 머릿속에서 돌리게 하는 설명은 통하지 않는다** — 화살표 대응표처럼 **그려서** 보인다.
      · 🔑 **`trace-study` 파일 구성** — `SKILL.md`에는 목표 깊이·절대 원칙·절차 뼈대만 있다.
        **청크를 열 때 `session-cycle.md` · 노트를 쓸 때 `note-format.md` · 저장·발행할 때 `saving.md`**를 읽는다.
        뒤집힌 결정 이력은 `decision-log.md`.
- [ ] **5. 백그라운드에서 계속 도는 법** → `docs/study/5-background-execution/`
- [ ] **6. 소리 내는 법** → `docs/study/6-audio-coaching/`
- [ ] **7. 잠금화면 위젯** → `docs/study/7-live-activity/`
- [ ] **8. 저장하는 법** → `docs/study/8-persistence/`


전부 `- [x]`가 되면 따라잡기는 끝이다. 그 뒤로는 `/trace-study MVP{N}`(따라가기)으로 전환하고,
이 파일과 설계 스펙의 처분(옮길지·지울지·둘지)을 그 시점에 판단한다.
