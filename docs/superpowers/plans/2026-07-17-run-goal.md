# run-goal (MVP14 사이클 3) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 러닝 시작 시 목표 선택(자유/거리/시간) + 트래킹 중 진행률 표시 + 절반·달성 음성 안내(각 1회, 달성 후에도 트래킹 계속) + 기록에 목표 저장·상세 표시를 구현한다.

**Architecture:** `RunGoal` 값 타입(Domain)을 `RunSession`이 보유하고, 샘플 적산 시점에 절반/달성 플래그를 올린다(각 1회 이벤트는 소비자가 전이로 감지). `RunAudioCoach`(사이클 2)가 그 플래그를 구독해 발화를 추가한다 — 세션은 여전히 오디오를 모른다(스펙 §3.3 소비자 원칙). 저장은 payload DTO v3(additive) — 컬럼·스키마 변경 없음.

**Tech Stack:** Swift 6(기본 nonisolated + UI/상태 타입 명시 `@MainActor`), SwiftUI, Observation, XCTest. 새 프레임워크 없음(오디오 인프라는 사이클 2 그대로 재사용).

**스펙:** `docs/superpowers/specs/2026-07-15-run-experience-design.md` §3.4, §4-3 — 결정 6(3모드, 달성 후 트래킹 계속), 결정 7(km 안내는 모드 무관 — 목표 안내는 그 위에 추가).

**사이클 무게: 경량** (`docs/agent-rules/workflow.md` — 열린 결정 없음·범위 좁음·새 레이어 없음). 생략: 플랜 서브에이전트 리뷰(자체 리뷰만), 최종 브랜치 리뷰. Task별 리뷰는 수행.

## ⚠️ 전제 — 이 플랜은 사이클 2 코드 기반 위에 작성됨

이 플랜은 `feature/run-splits-audio`의 커밋 `3780e87`(사이클 2 Task 6까지) 기준으로 작성됐다
(2026-07-17, 사이클 2의 최종 검증·실기기 QA·머지 전). **구현 착수는 사이클 2가 `main`에
머지된 뒤** — Task 0(합류 + 대조)이 그 절차다. 사이클 2가 아직 main에 없으면 착수를 보류한다.

## 플랜 단계에서 확정한 결정 (전부 플랜 레벨 세부 — 자동 진행)

- **목표 저장 위치 = payload DTO v3** (컬럼 아님). 목표는 기록 상세에서만 표시하고(스펙 §4-3),
  상세는 이미 payload를 해독하므로 컬럼·마이그레이션이 전혀 필요 없다. v2 이하 blob은 goal
  부재 → `.open`으로 해독(하위호환).
- **절반·달성이 한 샘플에 동시에 걸리면 달성만 발화** — 사이클 2의 "여러 km 경계는 최신만"
  원칙과 동일(밀린 발화 연쇄 방지).
- **시간 목표 판정은 샘플 도착 시점**(샘플 타임스탬프 기준 활동 시간) — 별도 타이머를 도입하지
  않는다(세션은 샘플 구동이라는 기존 원칙 유지). 발화 지연은 최대 샘플 간격(약 1초)로 무시 가능.
- **발화 순서: 상태 전환 → km 경계 → 목표** — km 경계와 목표 달성이 동시면 km 먼저(스펙 §3.3
  "순서대로 읽는다"). 직렬화는 AVSpeechSynthesizer 큐가 담당(사이클 2).
- **입력 UI**: 대기 화면에 세그먼트(자유/거리/시간) + 값 휠 피커(거리 1~42km 정수·기본 5,
  시간 5~180분 5분 단위·기본 30). 자유 입력(키보드)은 범위 밖.
- **진행률 UI**: 트래킹 스탯 패널에 "5 km 목표 · 64%" 라벨 + `ProgressView` 한 줄, 1로 캡
  (달성 후 100% 고정).

## Global Constraints

- 브랜치: `feature/run-goal` (`feature/run-splits-audio`의 `3780e87`에서 분기, 이미 생성됨 —
  worktree `.claude/worktrees/run-goal`). 커밋은 `scripts/trace-commit.sh`로 경로 명시 스테이징.
  `git add -A`/`git push` 금지.
- 커밋 전 3종 통과 + 스탬프 갱신 필수(`docs/agent-rules/testing.md`): build/test/lint 각각 성공 후
  `.git/trace-verify-{build,test,lint}.ok` touch. 테스트는 반드시 `-parallel-testing-enabled NO`,
  iOS 26.5 시뮬레이터 하나만 사용(세션 중 교체 금지), XcodeBuildMCP `test_sim` 금지.
- 검증 명령 (아래 각 Task의 "Run"은 이 형식을 따른다. `$SIM_UDID`는 세션 시작 시 한 번 결정):

```bash
xcodebuild -project Trace.xcodeproj -scheme Trace -configuration Debug \
  -destination "platform=iOS Simulator,id=$SIM_UDID" \
  -parallel-testing-enabled NO \
  -only-testing:TraceTests/<TestClass> test
swiftlint
```

- force unwrap/cast/try 금지(린트 에러), IUO(`var x: T!`) 금지 — 테스트 픽스처는 `private let` +
  `lazy var` 필드 초기화 패턴(RunSessionTests 참고).
- 값 타입은 `Equatable, Sendable`. 도메인/DTO에 SwiftData·UIKit import 금지. 필요 없는 곳에
  어노테이션을 남기지 않는다.
- 새 파일은 pbxproj 수정 불필요(fileSystemSynchronizedGroups — 폴더에 놓으면 자동 등록).
- 커밋 메시지: `tag: 한국어 제목` + 본문 3~4줄(한국어), Co-Authored-By 금지.
- **이 사이클 범위 밖(구현 금지):** 코스 연동(다음 MVP), Live Activity/잠금화면의 목표 진행률
  표시, 잠금화면 인터랙티브 버튼, 목표값 자유 입력(휠 범위 밖), 달성 후 자동 종료·반복 안내,
  음성 on/off 토글, 목표 이력 통계.

---

### Task 0: 사이클 2 합류(rebase) + 플랜 대조 패스

**Files:** 없음(git 작업 + 플랜 문서 갱신 가능성만)

- [x] **Step 1: 사이클 2 머지 확인**

Run: `git log --oneline main | head -12`
Expected: `run-splits-audio` 커밋들(`오디오 코치 배선`, `러닝 오디오 코치`, `km 스플릿 엔진` 등)이
main에 보인다. **안 보이면 여기서 중단** — 사이클 2 완료(실기기 QA + 머지)까지 착수 보류.

- [x] **Step 2: main 위로 rebase**

```bash
git checkout feature/run-goal
git rebase main
```

Expected: 충돌 없이 완료(이 브랜치의 커밋은 플랜 문서뿐). 사이클 2가 ff-only로 머지되므로
`3780e87`은 main 히스토리에 그대로 포함된다.

- [x] **Step 3: 플랜 ↔ 실제 코드 대조**

사이클 2의 최종 검증·리뷰·QA에서 코드가 바뀌었을 수 있다. 아래 참조 지점이 이 플랜의 코드
블록과 일치하는지 확인하고, 어긋나면 **플랜의 해당 블록을 실제 코드에 맞게 고쳐 커밋**한다:

- `RunSession.start()` 시그니처와 `ingest(_:sessionStart:)` 끝부분 (Task 2가 수정)
- `RunAudioCoach.observeOnce()/sync()` 구조와 `(.idle, .acquiring)` 리셋 분기 (Task 4가 수정)
- `RunAnnouncementBuilder`의 `spokenDistance/spokenDuration` 존재 (Task 3이 사용)
- `RunPersistenceDTO.currentVersion == 2`와 `Run` 구조 (Task 6이 v3으로 올림)
- `SwiftDataRunRecordRepository.decodeRunPayload` 반환 튜플 `(samples, pauses)` (Task 6이 goal 추가)
- `RunStatsPanel` body의 `isPaused` 분기·버튼 HStack 위치 (Task 5가 사이 삽입)

**수행 결과 (2026-07-17, 플랜 작성 세션에서 완료):** 사이클 2는 `e250c89`로 main 머지 확인,
rebase 완료(`279d5af`). `3780e87` 이후 코드 변경은 `f306791`(`RunSplitCalculator.splits()`에
`sessionStart:totalActiveSeconds:` 파라미터 추가) 하나뿐 — 이 플랜은 `splits()`를 호출하지
않으므로 영향 없음. Task 6의 `RunRecordDetailView` 삽입 지점도 유효. 플랜 수정 불필요,
**구현은 Task 1부터 바로 시작하면 된다.**

---

### Task 1: RunGoal — 목표 값 타입 (Domain 순수 로직)

**Files:**
- Create: `Trace/Domain/RunTracking/Entity/RunGoal.swift`
- Test: `TraceTests/RunGoalTests.swift`

**Interfaces:**
- Consumes: 없음(순수 값 타입)
- Produces: `enum RunGoal: Equatable, Sendable { case open; case distance(meters: Double); case time(seconds: TimeInterval) }`,
  `func progressFraction(distanceMeters: Double, activeSeconds: TimeInterval) -> Double?`
  (open·0 이하 목표값이면 nil, 1 초과 = 초과분. Task 2·5가 사용)

- [ ] **Step 1: 실패하는 테스트 작성**

`TraceTests/RunGoalTests.swift`:

```swift
import XCTest
@testable import Trace

final class RunGoalTests: XCTestCase {
    func test_자유목표는_진행률이_없다() {
        XCTAssertNil(RunGoal.open.progressFraction(distanceMeters: 3000, activeSeconds: 900))
    }

    func test_거리목표_진행률은_거리_비율이다() {
        let goal = RunGoal.distance(meters: 5000)
        XCTAssertEqual(goal.progressFraction(distanceMeters: 2500, activeSeconds: 0), 0.5)
        XCTAssertEqual(goal.progressFraction(distanceMeters: 6000, activeSeconds: 0), 1.2)
    }

    func test_시간목표_진행률은_활동시간_비율이다() {
        let goal = RunGoal.time(seconds: 1800)
        XCTAssertEqual(goal.progressFraction(distanceMeters: 0, activeSeconds: 900), 0.5)
        XCTAssertEqual(goal.progressFraction(distanceMeters: 0, activeSeconds: 1800), 1.0)
    }

    func test_0이하_목표값은_진행률이_없다() {
        XCTAssertNil(RunGoal.distance(meters: 0).progressFraction(distanceMeters: 100, activeSeconds: 0))
        XCTAssertNil(RunGoal.time(seconds: 0).progressFraction(distanceMeters: 0, activeSeconds: 100))
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunGoalTests`)
Expected: 컴파일 실패 — `cannot find 'RunGoal' in scope`

- [ ] **Step 3: 최소 구현**

`Trace/Domain/RunTracking/Entity/RunGoal.swift`:

```swift
import Foundation

/// 러닝 목표 — 자유/거리/시간 3모드(스펙 §3.4). 달성해도 트래킹은 계속되고 종료는 사용자가 한다.
enum RunGoal: Equatable, Sendable {
    case open
    case distance(meters: Double)
    case time(seconds: TimeInterval)

    /// 진행률(0…, 1 초과 = 초과분) — open·0 이하 목표값은 nil.
    /// 시간 목표는 활동 시간(일시정지 제외, 스펙 §3.1) 기준.
    func progressFraction(distanceMeters: Double, activeSeconds: TimeInterval) -> Double? {
        switch self {
        case .open:
            return nil
        case .distance(let meters):
            guard meters > 0 else { return nil }
            return distanceMeters / meters
        case .time(let seconds):
            guard seconds > 0 else { return nil }
            return activeSeconds / seconds
        }
    }
}
```

- [ ] **Step 4: 통과 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunGoalTests`) + `swiftlint`
Expected: 4개 전부 PASS, 린트 위반 0

- [ ] **Step 5: 커밋**

```bash
scripts/trace-commit.sh -m "feat: 러닝 목표 값 타입 추가

- 자유/거리/시간 3모드 RunGoal과 진행률 계산을 도입한다
- 시간 목표는 활동 시간(일시정지 제외) 기준이다
- 세션 판정·UI 진행률·저장이 이 타입 하나를 공유한다" \
  -- Trace/Domain/RunTracking/Entity/RunGoal.swift TraceTests/RunGoalTests.swift
```

---

### Task 2: RunSession — 목표 보유 + 절반/달성 플래그

**Files:**
- Modify: `Trace/Application/RunTracking/RunSession.swift` (start·ingest·리셋 경로)
- Test: `TraceTests/RunSessionGoalTests.swift` (신규 — RunSessionTests의 `MockRunLocationStream` 재사용)

**Interfaces:**
- Consumes: `RunGoal.progressFraction(distanceMeters:activeSeconds:)` (Task 1)
- Produces: `RunSession.start(goal: RunGoal = .open)` (기존 `start()` 호출부는 기본값으로 그대로 컴파일),
  `private(set) var goal: RunGoal`, `private(set) var goalHalfReached: Bool`,
  `private(set) var goalAchieved: Bool` — 한 번 true면 러닝 끝까지 유지(각 1회 발화는
  소비자의 전이 감지 담당, Task 4). Task 5·6이 `goal`을 사용.

- [ ] **Step 1: 실패하는 테스트 작성**

`TraceTests/RunSessionGoalTests.swift`:

```swift
import XCTest
@testable import Trace

@MainActor
final class RunSessionGoalTests: XCTestCase {
    private let stream = MockRunLocationStream()
    private let recordRepository = MockRunRecordRepository()
    private lazy var session = RunSession(locationStream: stream, recordRepository: recordRepository)

    private func sample(at date: Date, metersNorth: Double = 0) -> RunSample {
        RunSample(
            timestamp: date,
            latitude: 37.5666 + metersNorth / 111_320.0,
            longitude: 126.9784,
            altitudeMeters: 10,
            speedMetersPerSecond: 3,
            horizontalAccuracyMeters: 5,
            verticalAccuracyMeters: 5
        )
    }

    /// 조건이 참이 될 때까지 짧은 간격으로 폴링한다(RunSessionTests와 동일 패턴)
    private func waitUntil(
        timeout: TimeInterval = 1,
        file: StaticString = #filePath,
        line: UInt = #line,
        _ condition: () -> Bool
    ) async {
        let deadline = Date().addingTimeInterval(timeout)
        while condition() == false {
            if Date() >= deadline {
                XCTFail("timed out waiting for condition", file: file, line: line)
                return
            }
            try? await Task.sleep(nanoseconds: 2_000_000)
        }
    }

    private func startTracking(goal: RunGoal) async -> Date {
        await session.start(goal: goal)
        let start = Date()
        stream.yield(sample(at: start))
        await waitUntil { session.state == .tracking }
        return start
    }

    func test_시작시_목표를_보유하고_기본은_자유다() async {
        await session.start(goal: .distance(meters: 5000))
        XCTAssertEqual(session.goal, .distance(meters: 5000))
        session.finishAcquiringCancelled()
        await session.start()
        XCTAssertEqual(session.goal, .open)
    }

    func test_거리목표_절반과_달성_플래그가_순서대로_켜진다() async {
        let start = await startTracking(goal: .distance(meters: 1000))
        XCTAssertFalse(session.goalHalfReached)

        stream.yield(sample(at: start.addingTimeInterval(150), metersNorth: 505))
        await waitUntil { session.track.totalDistanceMeters > 500 }
        XCTAssertTrue(session.goalHalfReached)
        XCTAssertFalse(session.goalAchieved)

        stream.yield(sample(at: start.addingTimeInterval(300), metersNorth: 1010))
        await waitUntil { session.track.totalDistanceMeters > 1000 }
        XCTAssertTrue(session.goalAchieved)
        XCTAssertEqual(session.state, .tracking) // 달성 후에도 트래킹 계속(결정 6)
    }

    func test_시간목표는_샘플_타임스탬프_기준_활동시간으로_판정한다() async {
        let start = await startTracking(goal: .time(seconds: 600))
        stream.yield(sample(at: start.addingTimeInterval(301), metersNorth: 10))
        await waitUntil { session.track.samples.count >= 2 }
        XCTAssertTrue(session.goalHalfReached)
        XCTAssertFalse(session.goalAchieved)

        stream.yield(sample(at: start.addingTimeInterval(601), metersNorth: 20))
        await waitUntil { session.track.samples.count >= 3 }
        XCTAssertTrue(session.goalAchieved)
    }

    func test_시간목표는_일시정지_시간을_제외한다() async {
        let start = await startTracking(goal: .time(seconds: 800))
        session.pause(now: start.addingTimeInterval(100))
        session.resume(now: start.addingTimeInterval(700)) // 600초 일시정지
        // 벽시계 1300초 경과, 활동 시간 700초 < 800초 — 아직 미달성
        stream.yield(sample(at: start.addingTimeInterval(1300), metersNorth: 30))
        await waitUntil { session.track.samples.count >= 2 }
        XCTAssertFalse(session.goalAchieved)
    }

    func test_요약을_닫으면_목표가_리셋된다() async {
        let start = await startTracking(goal: .distance(meters: 1000))
        stream.yield(sample(at: start.addingTimeInterval(300), metersNorth: 1010))
        await waitUntil { session.goalAchieved }
        session.finish()
        session.dismissSummary()
        XCTAssertEqual(session.goal, .open)
        XCTAssertFalse(session.goalHalfReached)
        XCTAssertFalse(session.goalAchieved)
    }
}
```

- [ ] **Step 2: 실패 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunSessionGoalTests`)
Expected: 컴파일 실패 — `extra argument 'goal' in call` / `has no member 'goal'`

- [ ] **Step 3: 최소 구현**

`Trace/Application/RunTracking/RunSession.swift` 수정 4곳:

① 프로퍼티 추가 (`completedPauses`/`pausedAt` 선언부 아래):

```swift
    /// 이번 러닝의 목표 — start(goal:)에서 정해지고 요약을 닫을 때 리셋된다(스펙 §3.4)
    private(set) var goal: RunGoal = .open
    /// 절반/달성 플래그 — 한 번 true면 러닝 끝까지 유지(각 1회 발화는 소비자가 전이로 감지)
    private(set) var goalHalfReached = false
    private(set) var goalAchieved = false
```

② `start()` 시그니처 변경 + 목표 초기화 (`state = .acquiring` 직전에 추가):

```swift
    func start(goal: RunGoal = .open) async {
        guard state == .idle else { return }
        lastStartFailure = nil
        // …기존 정확도 확인 로직 그대로…
        self.goal = goal
        goalHalfReached = false
        goalAchieved = false
        state = .acquiring
        // …기존 스트림 시작 로직 그대로…
    }
```

③ `ingest(_:sessionStart:)` 끝(`if state == .acquiring { state = .tracking }` 다음)에 판정 추가:

```swift
        track.append(sample)
        isSignalWeak = false
        if state == .acquiring { state = .tracking }
        updateGoalProgress(now: sample.timestamp)
```

```swift
    /// 샘플 도착 시점 판정 — 시간 목표도 샘플 타임스탬프 기준(별도 타이머 없음, 지연 ≤ 샘플 간격)
    private func updateGoalProgress(now: Date) {
        guard goal != .open, goalAchieved == false else { return }
        guard let fraction = goal.progressFraction(
            distanceMeters: track.totalDistanceMeters,
            activeSeconds: activeElapsedSeconds(now: now) ?? 0
        ) else { return }
        if fraction >= 1 { goalAchieved = true }
        if fraction >= 0.5 { goalHalfReached = true }
    }
```

④ 리셋 경로 — `dismissSummary()`와 `finishAcquiringCancelled()`의 기존 초기화 블록에 추가:

```swift
        goal = .open
        goalHalfReached = false
        goalAchieved = false
```

- [ ] **Step 4: 통과 확인 + 기존 세션 테스트 회귀**

Run: 검증 명령(`-only-testing:TraceTests/RunSessionGoalTests -only-testing:TraceTests/RunSessionTests`) + `swiftlint`
Expected: 전부 PASS(기존 start() 호출은 기본값으로 무변경), 린트 위반 0

- [ ] **Step 5: 커밋**

```bash
scripts/trace-commit.sh -m "feat: 러닝 세션 목표 보유·절반/달성 판정

- start(goal:)로 목표를 받고 샘플 적산 시점에 진행률을 판정한다
- 절반/달성 플래그는 러닝 끝까지 유지, 발화 1회 보장은 소비자 몫
- 달성 후에도 트래킹은 계속된다(결정 6), 요약 닫기에서 리셋" \
  -- Trace/Application/RunTracking/RunSession.swift TraceTests/RunSessionGoalTests.swift
```

---

### Task 3: RunAnnouncementBuilder — 목표 발화 문구

**Files:**
- Modify: `Trace/Application/RunTracking/RunAnnouncementBuilder.swift`
- Test: `TraceTests/RunAnnouncementBuilderTests.swift` (기존 파일에 추가)

**Interfaces:**
- Consumes: 기존 `spokenDistance(_:)`, `spokenDuration(_:)`
- Produces: `static let goalHalf: String`, `static func goalAchieved(distanceMeters: Double, totalSeconds: TimeInterval) -> String` (Task 4가 사용)

- [ ] **Step 1: 실패하는 테스트 작성**

`TraceTests/RunAnnouncementBuilderTests.swift`에 추가:

```swift
    func test_목표_절반_문구() {
        XCTAssertEqual(RunAnnouncementBuilder.goalHalf, "절반 왔습니다")
    }

    func test_목표_달성_문구() {
        XCTAssertEqual(
            RunAnnouncementBuilder.goalAchieved(distanceMeters: 5000, totalSeconds: 1750),
            "목표 달성! 5킬로미터, 29분 10초"
        )
    }
```

- [ ] **Step 2: 실패 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunAnnouncementBuilderTests`)
Expected: 컴파일 실패 — `has no member 'goalHalf'`

- [ ] **Step 3: 최소 구현**

`RunAnnouncementBuilder.swift`의 `resume` 선언 아래에 추가:

```swift
    static let goalHalf = "절반 왔습니다"

    /// "목표 달성! 5킬로미터, 29분 10초" — 모드 무관하게 실제 총거리·활동시간을 읽는다(스펙 §3.4)
    static func goalAchieved(distanceMeters: Double, totalSeconds: TimeInterval) -> String {
        "목표 달성! \(spokenDistance(distanceMeters)), \(spokenDuration(totalSeconds))"
    }
```

- [ ] **Step 4: 통과 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunAnnouncementBuilderTests`) + `swiftlint`
Expected: 전부 PASS, 린트 위반 0

- [ ] **Step 5: 커밋**

```bash
scripts/trace-commit.sh -m "feat: 목표 절반·달성 발화 문구 추가

- 절반은 고정 문구, 달성은 실제 총거리·활동시간을 읽는다
- 기존 spokenDistance/spokenDuration 포맷을 재사용한다" \
  -- Trace/Application/RunTracking/RunAnnouncementBuilder.swift TraceTests/RunAnnouncementBuilderTests.swift
```

---

### Task 4: RunAudioCoach — 목표 발화 (각 1회, km 다음 순서)

**Files:**
- Modify: `Trace/Application/RunTracking/RunAudioCoach.swift`
- Test: `TraceTests/RunAudioCoachTests.swift` (기존 파일에 추가)

**Interfaces:**
- Consumes: `session.goalHalfReached`/`goalAchieved`/`goal` (Task 2), `RunAnnouncementBuilder.goalHalf`/`goalAchieved(distanceMeters:totalSeconds:)` (Task 3)
- Produces: 없음(발화 부수효과만)

- [ ] **Step 1: 실패하는 테스트 작성**

`TraceTests/RunAudioCoachTests.swift`의 `startTracking()` 헬퍼를 목표를 받도록 변경:

```swift
    /// 시작 → 첫 샘플 수용(tracking 진입)까지 진행하고 발화 로그를 비운다
    private func startTracking(goal: RunGoal = .open) async {
        await session.start(goal: goal)
        coach.sync()
        stream.yield(sample(at: Date()))
        await waitUntil { session.state == .tracking }
        coach.sync()
        announcer.announced.removeAll()
    }
```

테스트 추가:

```swift
    func test_목표_절반은_한번만_발화() async {
        await startTracking(goal: .distance(meters: 1000))
        let start = Date()
        stream.yield(sample(at: start.addingTimeInterval(150), metersNorth: 505))
        await waitUntil { session.track.totalDistanceMeters > 500 }
        coach.sync()
        XCTAssertEqual(announcer.announced, ["절반 왔습니다"])

        // 절반 이후 추가 샘플 — 중복 발화 없음
        stream.yield(sample(at: start.addingTimeInterval(160), metersNorth: 520))
        await waitUntil { session.track.totalDistanceMeters > 515 }
        coach.sync()
        XCTAssertEqual(announcer.announced, ["절반 왔습니다"])
    }

    func test_목표달성은_km발화_다음_한번만_절반을_덮는다() async {
        // 한 샘플에 1km 경계와 절반·달성이 모두 걸림: km 먼저, 달성이 절반을 덮어 2건만
        await startTracking(goal: .distance(meters: 1000))
        let start = Date()
        stream.yield(sample(at: start.addingTimeInterval(300), metersNorth: 1005))
        await waitUntil { session.track.totalDistanceMeters > 1000 }
        coach.sync()
        XCTAssertEqual(announcer.announced.count, 2)
        XCTAssertTrue(announcer.announced[0].hasPrefix("1킬로미터"))
        XCTAssertTrue(announcer.announced[1].hasPrefix("목표 달성!"))

        // 달성 후 추가 샘플 — 재발화 없음
        stream.yield(sample(at: start.addingTimeInterval(310), metersNorth: 1020))
        await waitUntil { session.track.totalDistanceMeters > 1015 }
        coach.sync()
        XCTAssertEqual(announcer.announced.count, 2)
    }

    func test_자유러닝은_목표발화가_없다() async {
        await startTracking()
        let start = Date()
        stream.yield(sample(at: start.addingTimeInterval(300), metersNorth: 1005))
        await waitUntil { session.track.totalDistanceMeters > 1000 }
        coach.sync()
        XCTAssertEqual(announcer.announced.count, 1) // km 발화뿐
        XCTAssertTrue(announcer.announced[0].hasPrefix("1킬로미터"))
    }

    func test_새러닝을_시작하면_목표발화_카운터가_리셋된다() async {
        await startTracking(goal: .distance(meters: 1000))
        let start = Date()
        stream.yield(sample(at: start.addingTimeInterval(150), metersNorth: 505))
        await waitUntil { session.track.totalDistanceMeters > 500 }
        coach.sync()
        session.finish()
        coach.sync()
        session.dismissSummary()
        coach.sync()

        // 두 번째 러닝: 절반이 다시 나와야 한다
        await startTracking(goal: .distance(meters: 1000))
        let restart = Date()
        stream.yield(sample(at: restart.addingTimeInterval(150), metersNorth: 505))
        await waitUntil { session.track.totalDistanceMeters > 500 }
        coach.sync()
        XCTAssertEqual(announcer.announced, ["절반 왔습니다"])
    }
```

- [ ] **Step 2: 실패 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunAudioCoachTests`)
Expected: 신규 4개 FAIL(목표 발화 없음 — `announced`가 비거나 km 발화만), 기존 테스트는 PASS 유지

- [ ] **Step 3: 최소 구현**

`RunAudioCoach.swift` 수정 4곳:

① 발화 카운터 프로퍼티 추가(`lastAnnouncedKm` 아래):

```swift
    private var goalHalfAnnounced = false
    private var goalAchievedAnnounced = false
```

② `observeOnce()`의 관찰 대상에 플래그 추가:

```swift
        withObservationTracking {
            _ = session.state
            _ = session.track.totalDistanceMeters
            _ = session.goalHalfReached
            _ = session.goalAchieved
        } onChange: { … 기존 그대로 … }
```

③ `sync()`에 목표 발화 추가(km 다음 — 동시 발생 시 km 먼저 읽는다, 스펙 §3.3):

```swift
    func sync() {
        announceStateTransitionIfNeeded()
        announceKilometerIfNeeded()
        announceGoalIfNeeded()
        lastState = session.state
    }
```

④ `(.idle, .acquiring)` 분기에 카운터 리셋 추가 + 목표 발화 메서드:

```swift
        case (.idle, .acquiring):
            lastAnnouncedKm = 0 // 새 러닝 — km 카운터 리셋
            goalHalfAnnounced = false
            goalAchievedAnnounced = false
            announcer.announce(RunAnnouncementBuilder.start)
```

```swift
    private func announceGoalIfNeeded() {
        guard session.state == .tracking else { return }
        if session.goalAchieved, goalAchievedAnnounced == false {
            goalAchievedAnnounced = true
            // 한 sync에 절반·달성이 같이 걸린 극단 케이스는 달성만 읽는다(밀린 발화 연쇄 방지)
            goalHalfAnnounced = true
            let elapsed = session.activeElapsedSeconds() ?? 0
            announcer.announce(RunAnnouncementBuilder.goalAchieved(
                distanceMeters: session.track.totalDistanceMeters,
                totalSeconds: elapsed
            ))
        } else if session.goalHalfReached, goalHalfAnnounced == false {
            goalHalfAnnounced = true
            announcer.announce(RunAnnouncementBuilder.goalHalf)
        }
    }
```

- [ ] **Step 4: 통과 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunAudioCoachTests`) + `swiftlint`
Expected: 기존 + 신규 전부 PASS, 린트 위반 0

- [ ] **Step 5: 커밋**

```bash
scripts/trace-commit.sh -m "feat: 오디오 코치 목표 절반·달성 발화

- 세션의 절반/달성 플래그 전이를 감지해 각 1회만 읽는다
- km 경계와 동시면 km 먼저, 절반·달성 동시면 달성만(연쇄 방지)
- 새 러닝 시작 시 발화 카운터를 리셋한다" \
  -- Trace/Application/RunTracking/RunAudioCoach.swift TraceTests/RunAudioCoachTests.swift
```

---

### Task 5: 대기 화면 목표 선택 UI + 트래킹 진행률 표시

**Files:**
- Create: `Trace/Pages/RunPage/RunGoalFormatter.swift`
- Modify: `Trace/Pages/RunPage/RunPageViewModel.swift` (목표 선택 상태 + 진행률)
- Modify: `Trace/Pages/RunPage/RunPage.swift` (idle 분기에 목표 피커)
- Modify: `Trace/Pages/RunPage/UIComponent/RunPage+StatsPanelComponent.swift` (진행률 줄)
- Test: `TraceTests/RunGoalFormatterTests.swift` (신규), `TraceTests/RunPageViewModelTests.swift` (추가)

**Interfaces:**
- Consumes: `RunGoal`(Task 1), `session.start(goal:)`/`session.goal`(Task 2)
- Produces: `enum RunGoalMode: String, CaseIterable, Identifiable { case open, distance, time }`(뷰모델 파일),
  `RunPageViewModel.goalMode/goalDistanceKm/goalTimeMinutes/composedGoal/goalProgressFraction`,
  `RunGoalFormatter.label(_ goal: RunGoal) -> String?` (Task 6이 기록 상세에서 재사용)

- [ ] **Step 1: 실패하는 테스트 작성**

`TraceTests/RunGoalFormatterTests.swift`:

```swift
import XCTest
@testable import Trace

final class RunGoalFormatterTests: XCTestCase {
    func test_자유목표는_라벨이_없다() {
        XCTAssertNil(RunGoalFormatter.label(.open))
    }

    func test_거리목표_라벨() {
        XCTAssertEqual(RunGoalFormatter.label(.distance(meters: 5000)), "5 km 목표")
        XCTAssertEqual(RunGoalFormatter.label(.distance(meters: 7500)), "7.5 km 목표")
    }

    func test_시간목표_라벨() {
        XCTAssertEqual(RunGoalFormatter.label(.time(seconds: 1800)), "30분 목표")
    }
}
```

`TraceTests/RunPageViewModelTests.swift`에 추가(기존 픽스처의 `viewModel`/`stream` 사용 —
픽스처 이름이 다르면 그 파일의 기존 명명을 따른다):

```swift
    func test_목표모드와_값으로_RunGoal을_조립한다() {
        viewModel.goalMode = .distance
        viewModel.goalDistanceKm = 10
        XCTAssertEqual(viewModel.composedGoal, .distance(meters: 10000))
        viewModel.goalMode = .time
        viewModel.goalTimeMinutes = 30
        XCTAssertEqual(viewModel.composedGoal, .time(seconds: 1800))
        viewModel.goalMode = .open
        XCTAssertEqual(viewModel.composedGoal, .open)
    }
```

- [ ] **Step 2: 실패 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunGoalFormatterTests -only-testing:TraceTests/RunPageViewModelTests`)
Expected: 컴파일 실패 — `cannot find 'RunGoalFormatter'` / `has no member 'goalMode'`

- [ ] **Step 3: 구현**

`Trace/Pages/RunPage/RunGoalFormatter.swift`:

```swift
import Foundation

/// 목표 표시 라벨 — 발화(RunAnnouncementBuilder)와 달리 화면용 짧은 표기
enum RunGoalFormatter {
    /// .open은 nil(라벨 자체를 안 그린다), 5000 → "5 km 목표", 1800초 → "30분 목표"
    static func label(_ goal: RunGoal) -> String? {
        switch goal {
        case .open:
            return nil
        case .distance(let meters):
            return "\(distanceText(meters)) 목표"
        case .time(let seconds):
            return "\(Int(seconds / 60))분 목표"
        }
    }

    private static func distanceText(_ meters: Double) -> String {
        let km = meters / 1000
        if km == km.rounded() { return "\(Int(km)) km" }
        return String(format: "%.1f km", km)
    }
}
```

`RunPageViewModel.swift` — 파일 상단에 모드 enum, 클래스에 상태·진행률 추가,
`startTapped()`의 `session.start()`를 `session.start(goal: composedGoal)`로 변경:

```swift
/// 대기 화면 목표 선택 세그먼트의 3모드 — 조립 결과는 RunGoal(Domain)
enum RunGoalMode: String, CaseIterable, Identifiable {
    case open
    case distance
    case time

    var id: String { rawValue }

    var label: String {
        switch self {
        case .open: "자유"
        case .distance: "거리"
        case .time: "시간"
        }
    }
}
```

```swift
    // 목표 선택 상태(대기 화면) — 러닝 시작 시 composedGoal로 조립해 세션에 넘긴다
    var goalMode: RunGoalMode = .open
    var goalDistanceKm = 5
    var goalTimeMinutes = 30

    var composedGoal: RunGoal {
        switch goalMode {
        case .open: .open
        case .distance: .distance(meters: Double(goalDistanceKm) * 1000)
        case .time: .time(seconds: TimeInterval(goalTimeMinutes * 60))
        }
    }

    /// 트래킹 중 목표 진행률(1로 캡 — 달성 후 100% 고정 표시). 자유 러닝은 nil
    var goalProgressFraction: Double? {
        guard let fraction = session.goal.progressFraction(
            distanceMeters: session.track.totalDistanceMeters,
            activeSeconds: session.activeElapsedSeconds() ?? 0
        ) else { return nil }
        return min(1, fraction)
    }
```

```swift
    func startTapped() async {
        await session.start(goal: composedGoal)
        // …이하 기존 그대로…
    }
```

`RunPage.swift` — `controls`의 `.idle` 분기를 `startButton`에서 피커 포함 스택으로 교체:

```swift
        case .idle:
            startControls
```

```swift
    private var startControls: some View {
        VStack(spacing: 16) {
            goalPicker
            startButton
        }
    }

    private var goalPicker: some View {
        VStack(spacing: 10) {
            Picker("목표", selection: $viewModel.goalMode) {
                ForEach(RunGoalMode.allCases) { mode in
                    Text(mode.label).tag(mode)
                }
            }
            .pickerStyle(.segmented)
            switch viewModel.goalMode {
            case .open:
                EmptyView()
            case .distance:
                Picker("목표 거리", selection: $viewModel.goalDistanceKm) {
                    ForEach(1...42, id: \.self) { km in
                        Text("\(km) km").tag(km)
                    }
                }
                .pickerStyle(.wheel)
                .frame(height: 96)
            case .time:
                Picker("목표 시간", selection: $viewModel.goalTimeMinutes) {
                    ForEach(Array(stride(from: 5, through: 180, by: 5)), id: \.self) { minutes in
                        Text("\(minutes)분").tag(minutes)
                    }
                }
                .pickerStyle(.wheel)
                .frame(height: 96)
            }
        }
        .padding(DesignToken.Size.sheetPadding)
        .background(DesignToken.Color.surface, in: RoundedRectangle(cornerRadius: DesignToken.Corner.chrome))
        .padding(.horizontal, DesignToken.Size.screenMargin)
        .accessibilityIdentifier("run.goalPicker")
    }
```

`RunPage+StatsPanelComponent.swift` — `RunStatsPanel` body에서 "일시정지됨" 분기와
버튼 `HStack` 사이에 진행률 줄 추가:

```swift
            if let label = RunGoalFormatter.label(viewModel.session.goal),
               let fraction = viewModel.goalProgressFraction {
                VStack(spacing: 4) {
                    HStack {
                        Text(label)
                            .font(DesignToken.Typography.chip)
                            .foregroundStyle(DesignToken.Color.ink2)
                        Spacer()
                        Text("\(Int(fraction * 100))%")
                            .font(DesignToken.Typography.chip)
                            .monospacedDigit()
                            .foregroundStyle(DesignToken.Color.ink2)
                    }
                    ProgressView(value: fraction)
                        .tint(DesignToken.Color.accent)
                }
            }
```

- [ ] **Step 4: 통과 확인 + 빌드 + 시뮬레이터 육안 확인**

Run: 검증 명령(`-only-testing:TraceTests/RunGoalFormatterTests -only-testing:TraceTests/RunPageViewModelTests`) + `swiftlint`
Expected: 전부 PASS, 린트 위반 0

시뮬레이터로 앱을 빌드·실행해 육안 확인: ① 대기 화면에 세그먼트가 뜨고 거리/시간 선택 시
휠이 나타난다 ② 거리 5km 목표로 시작하면(시뮬레이터 위치 시뮬레이션) 스탯 패널에
"5 km 목표 · n%" 줄과 진행 바가 보인다 ③ 자유 모드로 시작하면 진행률 줄이 없다.

- [ ] **Step 5: 커밋**

```bash
scripts/trace-commit.sh -m "feat: 목표 선택 UI와 트래킹 진행률 표시

- 대기 화면에 자유/거리/시간 세그먼트 + 값 휠 피커를 추가한다
- 스탯 패널에 목표 라벨·퍼센트·진행 바를 표시한다(1로 캡)
- 시작 시 조립된 RunGoal을 세션에 넘긴다" \
  -- Trace/Pages/RunPage/RunGoalFormatter.swift Trace/Pages/RunPage/RunPageViewModel.swift \
     Trace/Pages/RunPage/RunPage.swift Trace/Pages/RunPage/UIComponent/RunPage+StatsPanelComponent.swift \
     TraceTests/RunGoalFormatterTests.swift TraceTests/RunPageViewModelTests.swift
```

---

### Task 6: 목표 저장(DTO v3) + 기록 상세 표시

**Files:**
- Modify: `Trace/Domain/RunTracking/Entity/SavedRun.swift` (goal 필드)
- Modify: `Trace/Infrastructure/Persistence/SwiftData/RunPersistenceDTO.swift` (v3 + Goal)
- Modify: `Trace/Infrastructure/Persistence/SwiftData/SwiftDataRunRecordRepository.swift` (매핑)
- Modify: `Trace/Application/RunTracking/RunSession.swift` (`startRecordSave`에 goal 전달)
- Modify: `Trace/Pages/RunPage/UIComponent/RunPage+HistoryComponent.swift` (상세 목표 줄)
- Test: `TraceTests/SwiftDataRunRecordRepositoryTests.swift` (추가)

**Interfaces:**
- Consumes: `RunGoal`(Task 1), `session.goal`(Task 2), `RunGoalFormatter.label(_:)`(Task 5)
- Produces: `SavedRun.goal: RunGoal`(init 기본값 `.open` — 기존 생성부 무변경),
  `RunPersistenceDTO.currentVersion = 3`, `RunPersistenceDTO.Goal`,
  `decodeRunPayload` 반환 튜플에 `goal: RunGoal` 추가

- [ ] **Step 1: 실패하는 테스트 작성**

`TraceTests/SwiftDataRunRecordRepositoryTests.swift`에 추가(기존 픽스처의 SavedRun 생성
헬퍼가 있으면 그것을 쓰되, goal 인자만 지정):

```swift
    func test_목표가_저장되고_복원된다() async throws {
        let repository = SwiftDataRunRecordRepository(inMemory: true)
        let base = Date(timeIntervalSince1970: 1_700_000_000)
        let run = SavedRun(
            summary: SavedRunSummary(
                id: UUID(), startedAt: base, distanceMeters: 5000,
                duration: 1750, elevationGainMeters: 12
            ),
            samples: [
                SavedRunSample(timestamp: base, latitude: 37.5666, longitude: 126.9784,
                               altitudeMeters: 10, speedMetersPerSecond: 3),
                SavedRunSample(timestamp: base.addingTimeInterval(1750), latitude: 37.6115, longitude: 126.9784,
                               altitudeMeters: 12, speedMetersPerSecond: 3)
            ],
            pauses: [],
            goal: .distance(meters: 5000)
        )
        try await repository.save(run)
        let loaded = await repository.fetchRun(id: run.summary.id)
        XCTAssertEqual(loaded?.goal, .distance(meters: 5000))
    }

    func test_구버전_blob은_자유목표로_해독된다() {
        // v2(사이클 2) 포맷 — goal 필드 자체가 없다
        let json = """
        {"version":2,"samples":[{"t":700000000,"lat":37.5,"lon":127.0,"alt":10,"spd":3}],"pauses":[]}
        """
        let decoded = SwiftDataRunRecordRepository.decodeRunPayload(Data(json.utf8))
        XCTAssertNotNil(decoded)
        XCTAssertEqual(decoded?.goal, .open)
    }
```

주의: 기존 테스트 중 `decodeRunPayload` 반환 튜플을 통째로 비교하는 게 있으면 `goal` 추가에
맞춰 갱신한다(필드별 비교면 무변경).

- [ ] **Step 2: 실패 확인**

Run: 검증 명령(`-only-testing:TraceTests/SwiftDataRunRecordRepositoryTests`)
Expected: 컴파일 실패 — `extra argument 'goal'` / `has no member 'goal'`

- [ ] **Step 3: 최소 구현**

① `SavedRun.swift` — `SavedRun`에 필드 추가(기본값으로 기존 생성부 호환):

```swift
    /// 이번 러닝의 목표 — 상세 화면 "목표 5 km" 표시용(스펙 §4-3). 자유 러닝은 .open
    let goal: RunGoal

    init(
        summary: SavedRunSummary, samples: [SavedRunSample],
        pauses: [RunPauseInterval] = [], goal: RunGoal = .open
    ) {
        self.summary = summary
        self.samples = samples
        self.pauses = pauses
        self.goal = goal
    }
```

② `RunPersistenceDTO.swift` — 버전 승격 + Goal DTO + 매핑:

```swift
    // v3: goal 추가(additive). v2 이하 blob은 goal 부재 → .open으로 해독(하위호환).
    static let currentVersion = 3
```

```swift
    struct Goal: Codable {
        let type: String // "distance" | "time"
        let value: Double
    }

    struct Run: Codable {
        let version: Int
        let samples: [Sample]
        let pauses: [Pause]?
        let goal: Goal?
    }
```

```swift
extension RunPersistenceDTO.Goal {
    init?(_ goal: RunGoal) {
        switch goal {
        case .open: return nil
        case .distance(let meters): self.init(type: "distance", value: meters)
        case .time(let seconds): self.init(type: "time", value: seconds)
        }
    }

    var domain: RunGoal {
        switch type {
        case "distance": .distance(meters: value)
        case "time": .time(seconds: value)
        default: .open // 알 수 없는 타입은 목표 표시만 포기(우아한 강등)
        }
    }
}
```

③ `SwiftDataRunRecordRepository.swift` — `save`의 DTO 조립에 goal 추가, `fetchRun`·
`decodeRunPayload`에 goal 매핑 추가:

```swift
        let dto = RunPersistenceDTO.Run(
            version: RunPersistenceDTO.currentVersion,
            samples: run.samples.map(RunPersistenceDTO.Sample.init),
            pauses: run.pauses.map(RunPersistenceDTO.Pause.init),
            goal: RunPersistenceDTO.Goal(run.goal)
        )
```

```swift
    static func decodeRunPayload(
        _ data: Data
    ) -> (samples: [SavedRunSample], pauses: [RunPauseInterval], goal: RunGoal)? {
        guard let dto = try? JSONDecoder().decode(RunPersistenceDTO.Run.self, from: data),
              dto.version <= RunPersistenceDTO.currentVersion else { return nil }
        return (dto.samples.map(\.domain), (dto.pauses ?? []).map(\.domain), dto.goal?.domain ?? .open)
    }
```

`fetchRun`의 `SavedRun` 조립에 `goal: payload.goal` 추가.

④ `RunSession.swift` — `startRecordSave()`의 `SavedRun` 생성에 `goal: goal` 추가:

```swift
            samples: track.samples.map(SavedRunSample.init),
            pauses: completedPauses,
            goal: goal
```

⑤ `RunPage+HistoryComponent.swift` — `RunRecordDetailView`의 `statsGrid` 아래
(스플릿 섹션 위)에 목표 줄 추가:

```swift
                if let loadedRun, let goalLabel = RunGoalFormatter.label(loadedRun.goal) {
                    Label(goalLabel, systemImage: "target")
                        .font(DesignToken.Typography.chip)
                        .foregroundStyle(DesignToken.Color.ink2)
                        .padding(.bottom, 8)
                }
```

- [ ] **Step 4: 통과 확인 + 관련 회귀**

Run: 검증 명령(`-only-testing:TraceTests/SwiftDataRunRecordRepositoryTests -only-testing:TraceTests/RunSessionTests -only-testing:TraceTests/SavedRunTests`) + `swiftlint`
Expected: 전부 PASS, 린트 위반 0

- [ ] **Step 5: 커밋**

```bash
scripts/trace-commit.sh -m "feat: 러닝 목표 저장·기록 상세 표시

- payload DTO v3에 목표 타입·값을 additive로 저장한다(컬럼 변경 없음)
- v2 이하 blob은 자유 목표로 해독된다(하위호환)
- 기록 상세에 목표 라벨을 표시한다" \
  -- Trace/Domain/RunTracking/Entity/SavedRun.swift \
     Trace/Infrastructure/Persistence/SwiftData/RunPersistenceDTO.swift \
     Trace/Infrastructure/Persistence/SwiftData/SwiftDataRunRecordRepository.swift \
     Trace/Application/RunTracking/RunSession.swift \
     Trace/Pages/RunPage/UIComponent/RunPage+HistoryComponent.swift \
     TraceTests/SwiftDataRunRecordRepositoryTests.swift
```

---

### Task 7: 최종 검증 + 실기기 QA 체크리스트 (경량 — 최종 브랜치 리뷰 생략)

- [ ] **Step 1: 전체 스위트 + 린트 최종 실행**

Run:

```bash
xcodebuild -project Trace.xcodeproj -scheme Trace -configuration Debug \
  -destination "platform=iOS Simulator,id=$SIM_UDID" \
  -parallel-testing-enabled NO test
swiftlint
```

Expected: 전체 PASS(사이클 2 머지 시점 전체 + 신규 ~20개), 린트 위반 0. 통과 후 스탬프 갱신.

- [ ] **Step 2: 실기기 QA 체크리스트 작성**

`docs/qa/2026-07-XX-run-goal-device-checklist.md` 작성(날짜는 작성일).
`docs/agent-rules/testing.md`의 시나리오 카드 템플릿을 따르고, **처음 쓰는 유저 기준 평이한
언어**(내부 용어 금지)로 쓴다. 반드시 포함할 시나리오:

1. **거리 목표 러닝(핵심)**: 1km 목표로 시작 → 500m쯤에서 "절반 왔습니다", 1km에서
   "1킬로미터…" 안내에 이어 "목표 달성!…" 안내가 각각 딱 1번씩 나오는가
2. **달성 후 계속**: 목표 달성 안내 후에도 기록이 계속 쌓이고, 종료는 직접 버튼으로만 되는가
   (초과분이 요약·기록에 포함되는가)
3. **시간 목표 러닝**: 10분 목표 → 5분에 절반, 10분에 달성 안내가 나오는가
4. **일시정지 낀 시간 목표**: 멈춘 시간은 목표 시간에 포함되지 않는가 (5분 뛰고 2분 멈추고
   다시 뛰면 달성 안내는 시작 후 12분이 아니라 "뛴 시간 10분"에 나오는가)
5. **자유 러닝**: 목표 없이 시작하면 절반/달성 안내가 없고 km 안내만 나오는가
6. **주머니 시나리오**: 화면 잠금 + 음악 재생 중에도 절반/달성 안내가 (음악이 살짝 작아지며)
   나오는가
7. 트래킹 화면에 목표와 진행률(퍼센트·바)이 보이고, 달성 후 100%에서 멈추는가
8. 방금 뛴 기록 상세에 "5 km 목표" 표시가 나오는가 / 자유 러닝 기록에는 안 나오는가
9. **과거 기록**: 이번 업데이트 전에 저장된 기록 상세가 그대로 열리는가 (목표 줄만 없음)

- [ ] **Step 3: 체크리스트 커밋**

```bash
scripts/trace-commit.sh -m "docs: run-goal 실기기 QA 체크리스트 작성

- 거리·시간 목표의 절반/달성 각 1회 발화를 중심으로 구성한다
- 달성 후 계속 기록·일시정지 제외·과거 기록 호환 확인을 포함한다" \
  -- docs/qa/2026-07-XX-run-goal-device-checklist.md
```

- [ ] **Step 4: 마무리**

경량 사이클이므로 최종 브랜치 리뷰는 생략(Task별 리뷰로 갈음, `docs/agent-rules/workflow.md`).
실기기 QA는 사용자 수행 — 통과 보고를 받으면 `docs/roadmap.md`의 `run-goal`을 `[x]`로 갱신하고
MVP14 아카이빙(`/trace-archive`) 대기로 보고한다. 통합(rebase + `--ff-only`)은 사용자 지시 시
`scripts/trace-integrate.sh`로.

---

## Self-Review 결과 (플랜 작성 시 수행)

- **스펙 커버리지 (§3.4)**: `RunGoal` 값 타입 ✓(T1), 대기 화면 모드 선택+값 입력 ✓(T5),
  세션이 목표 보유·절반/달성 이벤트 각 1회 ✓(T2 플래그 + T4 전이 감지), 절반/달성 발화 문구 ✓(T3),
  달성 후 트래킹 유지 ✓(T2 — 상태 전이 없음, 테스트로 고정), 진행률 표시 ✓(T5),
  목표 타입·값 옵셔널 저장 + 상세 표시(§4-3) ✓(T6). 결정 7(km 안내는 모드 무관) —
  기존 km 발화 경로 무변경, T4 자유 러닝 테스트로 회귀 고정 ✓.
- **플레이스홀더 스캔**: TBD/TODO/"적절히 처리" 없음 — 모든 코드 스텝에 실제 코드 포함 ✓.
- **타입 정합**: `RunGoal.progressFraction(distanceMeters:activeSeconds:)`(T1)를 T2(세션 판정)·
  T5(진행률)가 동일 시그니처로 사용, `start(goal:)`(T2)를 T4 테스트 헬퍼·T5 `startTapped()`가 사용,
  `goalAchieved(distanceMeters:totalSeconds:)`(T3)를 T4가 사용, `RunGoalFormatter.label(_:)`(T5)를
  T6 상세 뷰가 사용, `decodeRunPayload` 튜플 `(samples, pauses, goal)`(T6)과 `fetchRun` 조립 일치 ✓.
- **사이클 2 의존 지점**: `RunSession`(start/ingest/startRecordSave), `RunAudioCoach`(observeOnce/sync),
  `RunAnnouncementBuilder`(spoken*), `RunPersistenceDTO`(v2), `decodeRunPayload`, `RunStatsPanel` —
  전부 Task 0 대조 목록에 수록 ✓.
