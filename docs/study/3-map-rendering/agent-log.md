# 청크 3 작업 기록 — 지도 위에 표시하는 법

> **이 파일은 에이전트용이다. 사용자 학습 자료는 `note.html`이다.**
> 여기 담는 것: 팩트체크 대조 내역 · 노트 집필 이력(무엇을 썼다가 왜 뒤집혔나) ·
> 진행 방식 기록(무너진 것과 통한 순서) · 파트별 범위 합의 · 실측 절차 참조.
> **기능이 어떻게 동작하는가는 여기 적지 않는다** — 그건 `note.html` 몫이다.

- 노트: [`note.html`](note.html)
- 발행: <https://claude.ai/code/artifact/8a7a02b4-6ed3-4214-9436-3291a7886aed> · favicon `🗺️🎨`
- 브랜치: `docs/study-chunk-3`
- 상태: 🔄 진행 중 — **소개 3절 + 파트 1 완료**(2026-08-17, 8구간 전부).
  다음은 **파트 2 「지도에 얹는 것을 어떤 타입으로 만드나」**
- 🔴 **파트 2를 열기 전에 「구간 ⑥ — 네 번 무너졌다」와 「구간 ⑦ — 제목이 결론이었다」를 읽을 것.**
- ⚙️ **저장 단위가 파트 → 구간으로 내려갔다**(2026-08-16 사용자 결정, 아래 「진행 방식」 5번)

---

## 청크 구성 확정 (2026-08-15)

### 🆕 `(b-5)` 조항을 신설했다 — 청크 구성에도 advisor 검토를 건다

사용자 결정 — *"파트에서 구간을 만들때만 순서나 이런것들을 검토하는게 아니라, 이 청크를 만들때도
이런것들이 제대로 반영이 되는지 검토를 진행해줘."* `SKILL.md` `[3]`의 `(b-4)` 바로 뒤에 넣었다.
`(b-4)`가 **파트 안의 구간 배정**을 보는 자라면, `(b-5)`는 **청크 안의 파트 배정**을 본다.

### 검토를 두 번 걸었고, 걸린 것

> ⚠️ **이 절과 아래 「새로 걸린 사실 셋」의 파트 번호는 7파트 기준이다** (2026-08-16 확인).
> 두 표 모두 **개정 전**에 쓰였고, 8파트 매핑은 아래 「최종 8파트」 표에 있다 —
> 2→2 · 1→3 · 3→4 · 4→5 · 5→6 · 6→7 · 7→8.
> **번호를 일괄 수정하지 않는 이유**는 인계 원문을 훼손하지 않기 위해서다.
> 반면 아래 「소유권」 절은 개정 **후**에 쓰여 신 번호다 — **한 파일 안에 두 체계가 있다.**

**1차 (초안 작성 전)** — 파트 순서와 개념 소유권.

| 지적 | 처리 |
|---|---|
| 파트 1/2 순서가 뒤집혀 있다 — 서브클래싱의 동기가 *"색을 정했는데 그리는 쪽이 그 색을 모른다"*라 **번호 둘을 안 깔면 파트 2를 열 수가 없다** | 색 파트를 1번으로 올림 |
| `colorKey`/`segmentIndex`가 파트 1·3·6·7에 걸친다 | 파트 1에서 못박고 이후는 *"이 자리는 어느 번호를 쓴다"* 한 줄로만 |
| `MapPin.==`의 `role` 비교는 diff 쪽이 본론 | 파트 7 (파트 5는 필드만) |
| halo 일체(`SegmentHaloPolyline`·`syncHalo`·`insertOverlay(below:)`) | 파트 3에 통째로. 파트 7은 *"선택이 바뀌면 다시 부른다"* 한 줄 |

**2차 (파트 표 확정 전 — `(b-5)`)** — 표면 전수에서 **배정 안 된 줄 여섯**이 나왔다.
원인은 **착수 메모의 재료 4건만 보고 표를 짠 것**이다(`(b-4)`가 2026-08-14에 겪은 것과 같은 실패).

| 배정 안 됐던 표면 | 위치 | 배정 |
|---|---|---|
| `configureRenderer` | `MapViewRepresentable:346` | 파트 3 — `rendererFor`와 선택 갱신 루프 **양쪽**에서 불린다(파트 7로 가는 다리) |
| `mapViewDidChangeVisibleRegion` + 한 틱 미루기 | `:495` | 파트 7 — **방향이 반대**(지도 → SwiftUI)임을 명시 |
| `viewFor`의 `MKUserLocation → nil` | `:451` | 파트 5 — "안 그리는 핀" |
| `view.isEnabled = false` ↔ `pinHit(at:in:)` | `:471` / `:662` | 파트 5 — 핀은 선택 불가인데 히트 판정은 화면 24pt 반경으로 **따로 돈다** |
| `Coordinator.mergedBadgeTag` 정리 줄 | `:472` | 파트 5 (아래 「새로 걸린 사실 ①」과 같은 자리) |
| `Canvas` 획 미리보기 | `CoursePlannerPage:250` | **범위 밖 — 청크 1**. 노트 소개 1의 경계선에 명시 |

그 밖에 고친 것 셋:

- **파트 4의 능력 선언이 결정이 아니었다** — *"뷰를 직접 만들지 정할 수 있다"* →
  **"라벨을 배열 인덱스 절반에 찍을지 실거리 절반에 찍을지"**(커밋 `117f821`). 도식도 ✕ → ○.
- **`rendererFor`/`viewFor` 소유권을 노트 소개 3에 적었다** — 구조는 파트 2, 분기는 파트 3·4·5.
- **파트별 `(b-2)` 유형을 미리 태그했다** — 파트 6만 「기획대로」, 나머지는 「깨져서 고침」 또는
  「나중에 추가」. **파트 6이 유일하게 기획대로라는 대비 자체가 노트에서 값을 한다.**

### 새로 걸린 사실 셋 — 해당 파트에서 다룬다

1. **핀은 재사용을 안 하는데 재사용 대비 코드가 있다.** 같은 `viewFor` 안에서 거리 라벨은
   `dequeueReusableAnnotationView`(`:456`), **핀은 매번 새로 생성**(`:464`). 그런데 `:472`가
   *"기존 배지 서브뷰를 지운다"* — 갓 만든 뷰에는 있을 수 없으므로 **절대 안 도는 줄**이다.
   → 파트 5 재료 / 처방은 백로그.
2. **케이싱 색만 다크모드를 따른다.** `polylineCasingColor`(`:4-8`)는 다크 검정 0.35 /
   라이트 흰색 0.6인데 **팔레트 색은 고정**이다. → 파트 3 「세 겹」에서 겹마다 색 출처가 다르다는 점.
3. **`SegmentPalette`의 폴백이 조용하다.** `UIColor(named: "Seg\(index % 6)") ?? .systemBlue` —
   에셋이 하나라도 없으면 **전부 파랑**이 되고 신호가 없다. `SegmentPaletteTests`의 두 번째
   테스트가 간접적으로 막고 있다. → 파트 1 재료.

---

## 문서 ↔ 코드 대조 (`[2]`) 결과

멈출 사안 **없음.** 넷을 대조했고 전부 「깨져서 고친 과정」 재료로 파트 안에서 다룬다.

| 문서 | 코드 | 판정 |
|---|---|---|
| MVP7 §B *"색상을 세그먼트 순서에 따라 배정"* | `colorKey`(만든 순서) | MVP8 설계 문서가 **이미 스스로 정정**했다. 파트 1 재료 |
| MVP7 §B *"`SegmentPolyline`에 `segmentIndex` 추가"* | `segmentIndex` + `colorKey` **둘** | 같은 뿌리. 파트 1·2 재료 |
| MVP8 *"선택 하이라이트·핀 로직 변경 없음"* | halo가 나중에 추가됨(`a4da480`, 2026-07-12) | 파트 3 재료 |
| MVP7 §E *"해당 구간이 보이도록 카메라를 이동한다"* | **구현돼 있다** — 아래 오판 기록 참고 | 불일치 아님 |

### 🔴 내가 낸 오판 — 카메라 이동을 "미구현"으로 판정했다

`CoursePlannerPageViewModel.selectSegment`(`:324-326`)가 인덱스만 세팅하는 것을 보고
**"설계에 있는 카메라 이동이 코드에 없다"**고 판정해 사용자에게 `[2]`의 A/B/C를 냈다.
**틀렸다.** 실제 코드는 `CoursePlannerPage.swift:181-187`의
`.onChange(of: viewModel.selectedSegmentIndex)` → `regionFitting(:345-357)`에 있다.

- **원인:** 이 프로젝트는 **카메라 상태(`cameraRegion`)를 페이지 뷰가 `@State`로 소유**하고
  `MapViewRepresentable`에 `@Binding`으로 내려준다. 그래서 "선택되면 카메라를 옮긴다"도
  뷰모델이 아니라 **뷰의 `.onChange`**에 있다. 뷰모델 계층만 따라가서 놓쳤다.
- **교훈:** 이 청크에서 *"X를 하는 코드가 없다"*를 판정할 때는 **뷰모델과 뷰 양쪽을** 봐야 한다.
  표시층은 상태 소유자가 뷰 쪽인 경우가 있다.
- **곁증거를 흘렸다:** 백로그 154번(`구간 탭 카메라 프레이밍이 시트 medium 단계 높이를 반영하지 않음`)이
  **카메라 프레이밍이 있다는 전제**로 쓰여 있었다. 백로그를 먼저 훑었으면 그 자리에서 걸렸다.

### 절차 결정 — `[2]`의 A/B/C와 백로그는 별개다

사용자 질문 — *"문서에 있는데 제대로 반영이 안된거면 이후에 수정을 해야 하니까 백로그에 남겨줘야
하지 않을까? 그게 C인건가?"* 답: **A + 백로그**이고 C가 아니다.
A/B/C는 **노트를 무엇 기준으로 쓸까**만 정하고, 백로그 등재는 「사실은 노트에, 판정과 처방은
백로그에」에 따라 **어느 쪽을 골라도 항상** 한다. A와 C의 차이는 **노트에 그 한 줄을 넣느냐**뿐.

---

## 백로그 후보 (해당 파트를 쓸 때 등재)

- **`SegmentSnapshot.==`가 경도를 안 본다** — 구간마다 `coordinateCount` + 첫/끝 **위도**만
  비교한다(`:155-164`). 경도만 다른 변경과 중간 좌표 변경은 못 잡는다. → 파트 7
- **핀의 배지 정리 줄이 절대 안 돈다** — 위 「새로 걸린 사실 ①」. → 파트 5

이미 열려 있어 **다시 등재하지 않는 것**(노트에는 사실만 한 줄):
겹침 렌더링 α/β 후보 · `MapViewRepresentable` 688줄 분해(린트 ③) · 핀·경로선 전량 교체 ·
회색 "확인 중" 마커 깜빡임 · 구간 탭 카메라 프레이밍이 시트 높이 미반영(154번).

---

## 🔄 로드맵 개정 — 7파트 → 8파트 (2026-08-15, 사용자 제기 → `(b-5)` 재검토)

### 사용자가 제기한 것

*"결국에 지도에 관해서 화면에 표현하려면 UIViewRepresentable를 사용해야 하는데 이것에 대해서
어떻게 다루는지, 그리고 MKMapView 관련해서도 어떻게 표현해야 될지를 그래도 알아야 하지 않을까"* —
그리고 그 이유를 사용자가 직접 댔다: ***"그냥 swiftui뷰에 표현하는것이면은 아무렇지 않게 넘어갈것
같은데, 이 지도앱 만의 특성이라고 생각해서 그래. 지금은 지도앱이 swiftui로만 표현하기 힘들어서
우리가 MKMapView와 UIViewRepresentable를 가지고 커스텀에 맞게 수정을 해서 그것을 swiftui뷰에
올린것이기 때문에."***

**이 틀이 맞다.** 「UIKit 일반 지식」(범위 밖)이 아니라 **이 앱이 SwiftUI를 벗어나야 했던 이유**이고,
그건 절대 원칙 ①이 시키는 것 그 자체 — *"이 프로젝트가 왜 여기에 그걸 썼나"*.

### 🔴 내가 먼저 그은 선이 코드와 어긋났다

*"오토레이아웃은 안 한다 · UIKit 일반은 뺀다"*고 그었는데, **코드에 있다** —
`MapViewRepresentable:483`의 `NSLayoutConstraint.activate([...])`(병합 핀 배지),
`:78~99`의 `UILabel`+`addSubview`+`sizeToFit`, `:457`의 `dequeueReusableAnnotationView`.
**사용자가 잡았다.** 판정 기준을 바꿨다:

> **이 청크 코드에 실제로 나오는 UIKit은 전부 다룬다. 코드에 안 나오는 UIKit 일반만 뺀다.**

### 범위를 세 층으로 자른 결과

| 층 | 무엇 | 어디 |
|---|---|---|
| **다룬다** | 왜 SwiftUI를 벗어났나 · 세 함수가 언제 불리나 · `Coordinator`는 왜 · **델리게이트는 내가 안 부른다** | 파트 1 |
| **다룬다** | 조작 8종 — `addOverlay`/`addAnnotation` · `insertOverlay(_:below:)` · `setNeedsDisplay` · `UILabel`+`addSubview`+`NSLayoutConstraint` · `dequeueReusableAnnotationView` · `removeOverlays`/`removeAnnotations` · `setRegion` | 파트마다 |
| **뺀다** | 코드에 없는 것 — `UIViewController` 생명주기 · `UITableView`/`UICollectionView` · 오토레이아웃 전반 · 안 쓰는 `MKMapView` API | — |

**끝이 있다는 실측 근거:** 이 파일이 지도 인스턴스에 거는 것 **21종** · 구현한 델리게이트 **3개**
(`rendererFor`·`viewFor`·`mapViewDidChangeVisibleRegion`) · `UIViewRepresentable` 요구사항 **3개**.

### 🔁 순서가 뒤집혔다 — 색과 타입 (이번 개정의 핵심)

**전:** 색(1) → 타입(2). 근거는 *"색을 정했는데 그리는 쪽이 그 색을 모른다"*가 서브클래싱의 동기라는 것.
**후:** 타입(2) → 색(3). **감싸기 파트가 앞에 생기면서 그 동기가 옮겨갔다** — 서브클래싱을 강제하는 것은
색이 아니라 **`rendererFor`가 오버레이 하나만 건네받는다**는 사실이다.

**실측으로 확인했다 — `rendererFor`의 네 갈래 중 셋은 팔레트를 안 건드린다:**

| 갈래 | 색 출처 | 팔레트 |
|---|---|---|
| `WaypointDotsOverlay` | 렌더러가 직접 그림 | ✕ |
| `SegmentHaloPolyline` | `polylineHaloColor` **고정 상수** | ✕ |
| `SegmentCasingPolyline` | `polylineCasingColor` **고정 상수** | ✕ |
| `SegmentPolyline` | `configureRenderer(colorKey:)` | **○ 여기만** |

즉 **타입으로 갈리는 함수지 색으로 갈리는 함수가 아니다.** 동기가 세 겹으로 쌓이게 됐다 —
①델리게이트는 내가 안 부른다(1) → ②그래서 오버레이가 자기 정보를 들고 있어야 한다(2) →
③그럼 무슨 번호를 들려 보내나(3).

### 최종 8파트

| | 파트 | 이동 |
|---|---|---|
| 1 | SwiftUI 밖으로 나가기 — `MKMapView`를 SwiftUI에 얹는 법 | 🆕 |
| 2 | 지도에 얹는 것을 어떤 타입으로 만드나 | 2 → 2 |
| 3 | 번호가 둘이다 — 색을 정하는 규칙 | **1 → 3** |
| 4 | 선 하나를 세 겹으로 그린다 | 3 → 4 |
| 5 | 거리 라벨을 어디에 찍나 | 4 → 5 |
| 6 | 핀 목록을 만드는 규칙 | 5 → 6 |
| 7 | 겹치는 선을 비켜 그리기 | 6 → 7 |
| 8 | 언제 다시 그리나 | 7 → 8 |

⚠️ **파트 7(겹침)이 열어보면 또 쪼개질 수 있다 → 9파트.** 그 지점에 오면 *"겹침을 별도 청크로 뺄지"*를
그 자리에서 판단한다. **미리 정하지 않는다.**

### 소유권 (노트 소개 3에 적었다)

- **조작 모델** → 파트 1. 개별 호출은 각 파트.
- **`rendererFor`/`viewFor` 구조** → 파트 2. 분기는 파트 4·5·6.
- **z-order** → **보장**(오버레이는 항상 어노테이션 아래)은 파트 2, **명시 호출**(`insertOverlay(_:below:)`)은
  파트 4. 파트 4가 파트 2를 이름으로 부른다. **두 번 설명하게 되면 한 곳으로 합친다.**

---

## 🔑 파트 1 착수 메모 — 다음 세션은 여기서 시작한다

**파트 1 — SwiftUI 밖으로 나가기: `MKMapView`를 SwiftUI에 얹는 법**

### 능력 선언 (확정)

> SwiftUI만으로 안 되는 화면을 감쌀 때 **어떤 상태를 SwiftUI에 두고 어떤 상태를 `Coordinator`에 둘지**
> 정할 수 있다.

**왜 이 문장인가** — 이 앱이 실제로 **둘로 갈라놓았다.** `cameraRegion`은 페이지 뷰의 `@State`,
`lastSegmentSnapshots`·`lastSelectedIndex`·`currentHaloOverlay`는 `Coordinator`. 청크 1 노트가
그 이음매를 이미 한 줄 적어뒀다 — *"`Coordinator`는 UIKit 객체라 거기 값이 바뀌어도 SwiftUI가 모른다."*
**그 문장 위에 세운다.**

### 담을 것 다섯

1. **왜 SwiftUI `Map`을 버렸나** — hit-test 소유권. 설계 문서를 인용한다(아래 재료).
2. **`makeUIView`는 한 번만 불린다** — 제스처를 여기서 달기 때문에(`:200-231`), 나중에 켜고 끄려면
   `updateUIView`에서 손으로 해야 한다(`:332-343`). **그 처리 자체는 청크 1 영역 — 이유만 대고 넘긴다.**
3. **`updateUIView`는 값이 바뀔 때마다 불린다 — 카메라를 밀어도 불린다.** **파트 8의 전제**다.
   여기서 사실만 대고, 파트 8이 다시 받는다.
4. **`Coordinator`는 왜 있나** — `struct`는 델리게이트를 못 맡는다 + 편집 중 값을 들고 있어야 한다.
5. **델리게이트는 내가 부르지 않는다** — 파트 2의 동기.

### 재료 (경로 확인 완료 2026-08-15)

| 재료 | 경로 |
|---|---|
| 골격 | `MapViewRepresentable.swift` — `makeCoordinator:190` · `makeUIView:194-234` · `updateUIView:236-344` · `Coordinator:404-413` |
| 상태 분리 | `CoursePlannerPage.swift` — `cameraRegion` `@State` · `MapViewRepresentable(region: $cameraRegion …):234-249` |
| **설계 근거** | `history/mvp4/2026-06-26-mvp4-design.md:22` (hit-test 소유권) · `:26-37` (제스처 주의사항 · *"Coordinator가 `MKMapViewDelegate`로 처리"*) |
| 결정 기록 | `docs/agent-rules/project-decisions.md:106` |
| 깨진 것 | `history/mvp4/260627_mvp4_completion_retro.md` |

⚠️ **`history/mvp4/2026-06-26-mkmap-migration.md`는 플랜이라 재료가 아니다**(스킬 `[1]`).
설계 문서(`2026-06-26-mvp4-design.md`)를 쓴다. **둘을 헷갈리지 말 것.**

### 진행 순서

1. `(b-3)` — 이 파트는 분기가 여럿인 함수가 주인공이 아니라 **골격 소개**라 판정 구조 뽑기는
   해당 없을 수 있다. **`updateUIView`는 예외** — 그 안이 네 갈래 게이트(카메라 · 세그먼트 스냅샷 ·
   핀 diff · 그리기 모드)인데 **그중 셋은 파트 8·청크 1 것**이다. 파트 1에서는 *"갈래가 넷이고
   여기서는 구조만 본다"*까지만.
2. `(b-4)` — 구간 표 + **표면 전수 + 배정표**를 함께 넘긴다. 전수는 **코드에서 다시 뽑는다**(착수 메모 아님).
3. 구간 하나씩 채팅 → **승인** → 저장·발행.

### ⚠️ 이 파트에서 조심할 것

- **UIKit 강의로 번지지 않게.** 판정 기준은 하나 — **이 청크 코드를 읽는 데 필요한가.** 아니면 접는다.
- **문법을 설명하지 않는다**(절대 원칙 ①). `some View`·제네릭·`associatedtype`이 나와도 넘어간다.
  설명할 것은 **이 프로젝트가 왜 그렇게 썼나**뿐.
- **사용자는 SwiftUI는 알고 UIKit·`UIViewRepresentable`은 거의 안 써봤다**(본인 말).
  그러므로 **SwiftUI 쪽을 기준점으로 삼아 이어준다** — *"SwiftUI였다면 `body`가 다시 계산될 자리에,
  여기서는 `updateUIView`가 불린다"*처럼.
- **의인화 금지**(②-2). *"MapKit이 물어본다"* 대신 **함수 호출**로 쓴다.

### 🎨 도식 규칙이 바뀌었다 — mermaid로 그리지 말 것

커밋 `f2e89d6`(2026-08-15)이 `SKILL.md`「노트 형식 · 도식」을 고쳤다.

> 새 도식은 **`diagram-design`**으로 만든 **정적 인라인 SVG**를 `note.html`에 넣는다.
> 독립 HTML·PNG를 따로 만들지 않는다. 현재 노트의 **라이트 테마·한글 가독성 규칙을 색·글꼴의
> 기준으로 넘기고**, `diagram-design`의 **기본 주황 팔레트·모션은 쓰지 않는다.**
> 기존 Mermaid 도식은 **손댈 이유가 생길 때만** 교체한다.

- **파트 1은 도식 ○이므로 이 규칙에 바로 걸린다.** mermaid로 그리면 규칙 위반이다.
- `diagram-design`은 플러그인으로 설치돼 있다(`diagram-design@diagram-design`, 확인 2026-08-16).
  **호출이 실패하면 그 단계를 건너뛰지 말고 멈추고 사용자에게 알린다**(`docs/agent-rules/skills.md`).
- **소개 1의 mermaid 도식은 그대로 둔다** — 규칙이 서기 전에 만든 것이고 *"손댈 이유가 생길 때만
  교체"*에 해당한다. 노트 CSS의 mermaid 스타일도 그래서 남겨둔다.
- 색 기준: 노트의 `--accent #06B6A4` · `--deep #04796E` · `--ink #0E1817` · `--rule #DAE6E3`,
  라이트 고정. 한글 라벨은 **0.82rem 이상 · 자간 0.05em 이하 · 본문 글꼴**
  (`SKILL.md`「반복해서 틀린 것」).

---

## 파트 1 진행 기록 (2026-08-16)

### `(b-3)` — 착수 메모의 「네 갈래」가 실제로는 **다섯**이었다

`updateUIView`의 게이트를 코드에서 다시 뽑으니 **`lastSelectedIndex` 게이트(`:295-310`)가
착수 메모에서 빠져 있었다.** 다섯은 카메라(`:239`) · 스냅샷(`:247`) · **선택(`:295`)** ·
핀(`:312`) · 그리기 모드(`:332`).

구조상 확인한 것 셋 — ①**조기 반환이 아니다**(다섯이 배타적이지 않고 순서대로 전부 평가된다)
②**순서에 의미가 있다**(B가 오버레이를 재생성한 뒤 C가 렌더러를 만지므로, 둘 다 걸리면 C는 방금
만들어진 렌더러를 손본다. `syncHalo`는 B·C 양쪽에서 호출) ③**게이트의 이유는 무한루프**
(회고 `:26`, 고리는 `mapViewDidChangeVisibleRegion` → `parent.region` → `updateUIView` →
`setRegion` → 다시 델리게이트).

> **재료 목록을 착수 메모에 의존하면 빠진다** — `(b-4)`가 2026-08-14에, `(b-5)`가 2026-08-15에
> 겪은 것과 **같은 실패가 세 번째**다. 착수 메모는 앞 세션이 그때 본 것뿐이다.

### `(b-4)` — 구간 표를 내기 전 검토에서 셋이 걸렸다

| 지적 | 처리 |
|---|---|
| **⑤/⑥이 한 개념을 걸친다** — ⑤가 `Coordinator`의 존재 이유 둘(델리게이트 + 값 보관)을 대는데 뒤쪽이 ⑥ 전체고, 앞쪽 근거인 「델리게이트」는 ⑦에서야 열린다 | 자르는 선을 옮김 — ⑤=**`struct`는 매번 새로 만들어진다**(근거는 `:237` `parent = self`) · ⑥=어디에 두나 · ⑦=`NSObject` 채택과 델리게이트 수신 |
| **⑥이 규칙이 아니라 나열이었다** | 판별 규칙으로 바꿈 — ***「SwiftUI 쪽에서 그 값을 읽는 사람이 있나」***. `cameraRegion`은 있고(`.onChange` 둘 + 델리게이트), 표시 상태 셋은 없다 |
| **④가 파트 8의 몫까지 걸어간다** — 게이트 다섯을 순회하면 파트 8이 빈다 | ④는 *"몸통이 전부 「바뀌었나?」 검사다 + 왜(무한루프)"*까지. 게이트별 내용과 **게이트 도식은 파트 8**, ④의 도식은 **고리 하나** |

**배정 확정(질문 넷에 대한 답):** `showsUserLocation`은 파트 1에서 이름만·`viewFor → nil`은 파트 6 ·
`.onChange(of: selectedSegmentIndex)` → `regionFitting`은 **파트 1 구간 ⑥**(카메라 소유권의 결과다.
위 🔴 오판이 정확히 이걸 말한다) · `.onChange(of: scenePhase)` → `saveCameraPosition`은 ⑥의 근거로
한 줄만(`CameraStateStore`는 열지 않는다) · `SegmentSnapshot` 타입은 파트 8.

⚠️ **`Coordinator`에 있는 것이 「셋」이 아니다.** 표시 상태는 셋(`lastSegmentSnapshots` ·
`lastSelectedIndex` · `currentHaloOverlay`)이지만 인식기 참조 셋(`:519-521`) · `tapClassifier`(`:579`) ·
획 수집 배열(`:521-524`) · 보류 좌표(`:588-589`)가 더 있다. **구간 ⑥에서 "이 청크가 다루는 표시
상태 셋"으로 범위를 명시**하고 나머지는 청크 1로 자리만 알린다.

### `(b-2)` 유형이 하나가 아니라 **둘**이었다

`(b-5)` 사전 태그는 파트 1을 「깨져서 고침」 단일로 잡았는데 **틀렸다.**

- **기획대로 간 것** — 카메라를 `Binding`으로 주고받는 구조는 설계 문서
  `2026-06-26-mvp4-design.md:44-45`에 **처음부터 적혀 있다**(*"`MKCoordinateRegion`을 Binding으로
  주고받아 기존 `CameraStateStore` 저장/복원 로직 유지"*). **구간 ⑥의 1차 근거다.**
- **깨져서 고친 것** — 제스처. `makeUIView` 시점에 `MKMapView` 내부 인식기가 아직 없어
  `minimumNumberOfTouches = 2`가 **조용히 안 먹었다**(회고 `:13`) → `isScrollEnabled` 토글로 재설계.

**설계 문서에서 낡은 것은 `:28-33`(제스처 계획)뿐이다.** `:35-37`(오버레이 → `MKPolyline`,
*"Coordinator가 `MKMapViewDelegate`로 처리"*)와 `:44-45`는 지금 코드와 일치한다.
착수 메모가 *"`:26-37` 전체를 인용 안 함"*으로 잡은 것은 과했다.

### 🆕 구간 ⓪을 신설했다 — 사용자가 「감싼다」에서 먼저 막혔다

구간 ①을 열기 전에 *"감싼다는 표현에 대해서 이해를 하고 가야 할 것 같은데 이게 무슨 의미야?"*가
나왔다. **7구간 → 8구간.** 통한 순서는 ①`MKMapView`는 `UIView`라 `body`에 못 쓴다 →
②그래서 `UIViewRepresentable`을 채택한 `struct`를 만든다(선언 + 호출부 실물) →
③이름 둘을 가른다(애플 프로토콜 vs 우리 `struct`) → ④**변환이 아니라 둘이 따로 있고 수명이 다르다**.

- ④가 그냥 부연이 아니다 — 사용자가 *"…형태로 바꿀 수 있도록 한다는 것"*으로 정리했고,
  **「바꾼다」로 굳으면 구간 ⑤(`struct`는 매번 새로 만들어진다)가 안 통한다.** 그 자리에서 정정했다.

### 사용자 질의에서 나와 구간 ①에 들어간 것

1. 🔴 **시기를 안 가르면 통하지 않는다.** 사용자가 기억하는 것은 **MVP4~MVP16(두 손가락 이동)**인데
   설계 문서 인용문은 **MVP4 이전(SwiftUI `Map`)**이다. 인용문만 내밀었다가 *"지금도 결국 그리면서
   이동은 안 되는 걸로 알고 있어"* · *"아직도 정확한 이유를 모르겠어"*로 막혔다.
   **세 칸 표(MVP4 이전 / MVP4~16 / 지금)를 구간 ① 첫 조각으로 올려서 풀었다.**
2. **「해결된 것」을 잘못 말할 뻔했다** — 지금도 획을 긋는 동안에는 지도가 잠긴다(`:548`→`:568`).
   해결된 것은 *"그리면서 이동"*이 아니라 ***"이동하려고 모드를 나가는 것 + 나가면 초기화되는 것"***이다.
   **잠기는 단위가 모드 → 한 획으로 옮겨간 것**이고, 그 표를 노트에 넣었다.
3. **`Map`에 인식기가 없는 게 아니다** — 있는데 **우리가 못 만진다.** 세 층으로 정리했다:
   달 수는 있다(`.gesture`) / 안 불린다(안쪽이 먼저 가져간다) / 조정도 못 한다(`require(toFail:)`를
   걸려면 양쪽 인식기를 다 쥐어야 한다).
4. **부딪힌 것은 「한 손가락 드래그」다** — 내장 이동도, 그리기도 한 손가락 드래그라 정면 충돌이었다.
   사용자가 *"비슷한 제스처가 들어왔을 때"*라는 조건을 스스로 달았고 그게 정확한 자리다.
5. **미검증을 「불가」로 적지 않았다** — *"SwiftUI `Map`으로는 안 된다"*는 2026-06-25 리서치 판정이고
   (`project-decisions.md:106`) 그 뒤 재측정한 적 없다. 노트에 시점과 함께 명시했다.
   사용자가 *"이후에 지금 뷰를 map으로 표현해 보는 과정이 필요하다고 생각하는 거야?"*로 물었고,
   **아니라고 답했다** — 되돌릴 이유가 없고 능력 선언과도 무관한 곁가지다.

### 구간 ② — 순서를 틀렸고 사용자가 잡았다

🔴 **`Coordinator`가 뭔지 안 대고 `makeCoordinator`를 설명했다.** `(b-4)`가 `Coordinator`의 정체를
구간 ⑤·⑦로 미뤄놨는데, 구간 ②에 `makeCoordinator`가 나오므로 **거기서 이미 필요했다.**
사용자 지적 — *"Coordinator가 뭔지 모르겠는데 Coordinator를 만드는 함수인 makeCoordinator에
대해서 이해를 못하지 않을까?"* **③-2(처음 나오는 부품은 「무엇을 하나」부터)의 위반**이다.

- **처방: 구간 ②로 끌어올렸다.** `Coordinator`의 **정체와 두 역할**(델리게이트를 받는다 ·
  지난번 값을 기억한다)까지 ②에서 댄다.
- ⚠️ **그래서 ⑤·⑦의 경계를 다시 긋는다** — 겹치면 그 구간이 빈다.

| 구간 | 남은 몫 |
|---|---|
| ⑤ | **`MapViewRepresentable`(`struct`)이 왜 그 일을 못 맡나** — 매번 새로 만들어진다(`:237` `parent = self`가 증거). ②가 *"`Coordinator`가 한다"*까지 갔으므로 ⑤는 **왜 거기여야 하나**만 |
| ⑦ | **호출 방향이 뒤집힌다는 것과 그 결과** — ②는 *"델리게이트를 여기서 받는다"*까지. ⑦은 *"내가 부르지 않는다 → 그래서 오버레이가 자기 정보를 들고 있어야 한다"*(파트 2의 동기) |

### 구간 ②에서 정정한 것 둘

1. 🔴 **「무조건인 것은 둘」이 틀렸다.** Apple 문서 확인 결과 **셋 다 요구사항**이고
   `makeCoordinator`에만 기본 구현이 있다(*"Required method. Default implementation provided."*).
   **소개 1의 「요구사항 3개」는 맞았다** — 고치겠다고 했다가 철회했다.
   대신 **`updateUIView`에는 기본 구현이 없다**(빈 몸통으로라도 써야 한다)를 대비로 넣었다.
2. 🔴 **`Coordinator`를 「UIKit → SwiftUI 전달」로 좁히면 구간 ⑥이 반대로 읽힌다.**
   사용자가 그렇게 정리했고 그 자리에서 넓혔다 — `Coordinator`는 **`MKMapView`가 부르는 함수들이
   있는 곳**이고 **그중 일부만** SwiftUI를 고친다. 실측: `rendererFor`·`viewFor`는 안 고치고
   `mapViewDidChangeVisibleRegion`만 고친다(**셋 중 하나**). ⑥의 판정 규칙이
   *"SwiftUI 쪽에서 그 값을 읽는 사람이 있나"*라서, 안 넓히면 정반대로 적용된다.

### 🆕 절대 원칙 ②-4를 신설했다 — 어원·본뜻으로 연다

사용자 요청 — *"어떤 인스턴스나 프로퍼티 혹은 이런 자연스럽게 쓰이는 말들에 대해서 이해가 안 되면
이런 어원이나 이런 식으로 말해주면 이해가 잘 될 것 같아. … 어원이나 이런 것을 알면 더 이해하기 쉬어서."*
`Coordinator`에서 통한 것이 **coordinate = 조율하다** + Apple 문서 *"A type to coordinate with
the view"*였다. **비유가 아니라 원래 뜻이었다** — ②(나)의 *"막히면 비유"*로는 안 잡히는 종류다.
`SKILL.md` ②-3 뒤에 넣었고, **1차 출처(공식 문서)를 인용할 것**을 조건으로 달았다.

### 구간 ②에서 뺀 것과 넣은 것

- **뺐다 — `dismantleUIView`.** *"안 채운 칸도 세자"*로 넣었는데 사용자가 *"이 내용은 왜 들어가는
  거야?"*로 잡았다. **능력 선언에 아무 재료도 안 된다 → 곁가지.** (사실 관계로는 `dismantleUIView`도
  프로토콜 멤버가 맞다. 틀려서 뺀 게 아니라 **자리를 차지할 이유가 없어서** 뺐다.)
- **넣었다 — 카메라 미리보기 대조.** 사용자가 자기 다른 프로젝트 코드(`CameraPreview`)를 가져와
  *"여기에는 makeCoordinator가 없는데 이거는 어떻게 된 거야?"*라고 물었다. **「둘지 말지」 판정의
  안 두는 쪽 실물**이라 구간 ② 7번으로 편입했다(출처 명시). 판정 두 줄 — 델리게이트 안 받음 ·
  `updateUIView`가 비어 있어 기억할 것 없음.

### 구간 ③ — 질의로 정밀해진 것 셋

1. 🔴 **「인식기를 만들 때 무조건 꺼둔다」가 부정확했다.** 실제로는 **`drawGR`만** 모드를 안 보고
   `false`이고(`:209`), `tapGR`·`touchObserver`는 **그 시점의 모드에 맞춘다**(`:217`·`:228`,
   `!isDrawingMode`). 사용자가 *"앱을 켰을 때 설정이 탭 모드니까"*로 짚어서 확인했고,
   `CoursePlannerPageViewModel:35`가 `interactionMode = .tap`이라 **진입 시 탭 쪽 둘이 켜진 채로
   화면에 붙는다** — `updateUIView`를 기다리는 것이 아니다. **노트에 이 세 줄을 그대로 넣었다.**
   - `drawGR`의 비대칭은 진입 모드가 항상 탭이라 **결과가 같아서 드러나지 않는다.** 사실만 적고
     백로그로 보내지 않았다(동작 문제가 아니다).
2. ⚠️ **배타 활성화의 「이유」는 코드·문서에 없다.** 인식기 셋을 모드마다 배타적으로 켜고 끄는 것은
   사실이지만(청크 1 노트 표 + `:332-343`), **왜 그렇게 했는지 적힌 근거는 못 찾았다.**
   사용자가 *"충돌 때문이야?"*로 물었고 **추론이라고 밝히고 답했다.** 회전·기울이기 쪽만
   청크 1이 이유를 적어뒀다(*"둘 다 두 손가락 동작이라 그리기와 섞일 일이 없게"*).
3. 🆕 **`makeUIView`가 `Coordinator`에 하는 일은 「연결」이다.** 사용자 질문 —
   *"Coordinator는 updateUIView와 SwiftUI의 다리라면서 왜 makeUIView 안에서 쓰이는 거야?"*
   **좋은 모순 지적이었고**, 답은 `:196`·`:201`·`:211`이 전부 **값을 주고받는 것이 아니라 선을
   이어두는 것**이라는 점이다. 그래서 **`makeUIView`는 값이 오가는 고리에 없다** —
   이 정리를 구간 ③ 5번으로 넣었다.

### 📌 다음 세션이 알아둘 것 — 「감싼다 / 표현된다」가 두 번 흔들렸다

사용자가 *"MKMapView를 UIViewRepresentable로 감싸서 **MapViewRepresentable를** SwiftUI에
표현한다"*로 정리한 적이 있다. **표현되는 쪽은 `MKMapView`이고 `MapViewRepresentable`은
화면에 아무것도 그리지 않는다.** 그 자리에서 바로잡았지만 **같은 구분이 두 번 흔들렸다.**

- **구간 ⑤·⑥이 정확히 이 구분 위에 선다** — *"`struct`는 매번 새로 만들어진다"*(⑤)와
  *"무엇을 어디에 두나"*(⑥)는 **둘이 따로 있고 수명이 다르다**를 전제한다.
- **보강은 사용자가 접었다**(*"그냥 넘어가자"*, 2026-08-16). 구간 ⓪에 칸을 더하지 않았다.
  **⑤를 열 때 이 구분을 다시 한 줄 이어주는 것으로 대신한다**(「이름은 병기한다」와 같은 처방).

### 구간 ④ — 내 표현 셋이 막았고, 카메라 설명을 세 번 다시 열었다

🔴 **막은 것은 개념이 아니라 내가 쓴 말이었다.** 셋 다 사용자가 짚었다.

| 쓴 말 | 왜 막았나 | 바꾼 말 |
|---|---|---|
| **검사** | 지어낸 말 — *"검사라는 표현도 잘 모르겠어"* | **비교**(회고가 쓰는 말 — *"count+좌표 비교 게이트"*) |
| **몸통** | SwiftUI에 `body`가 있어 그걸로 읽힌다 — *"어떤 몸통을 말하는거지? body를?"* | **`updateUIView` 안** |
| **「비교가 없으면」** | 가정법이라 *"카메라만 비교를 안 한다는 거야?"*로 읽혔다 | 가정법을 빼고 **실제로 도는 대로** 쓴다 |

**세 번째가 가장 컸다.** 없는 상황을 가정해 설명하면 **그게 지금 상태로 읽힌다.**
🔴 **앞으로 「~이 없으면 이렇게 된다」로 메커니즘을 설명하지 않는다** — 있는 대로 쓰고,
막는 대상은 결과로 보인다.

**카메라 설명이 세 번 만에 통했다.** 통한 것은 **장면과 시제**였다:

- ⏱ **함수 이름의 시제** — `didChange`(이미 바뀌었다 = 알림) ↔ `set`(이렇게 맞춰라 = 명령).
  이걸 대자 *"이동은 이미 손가락이 시켰다"*가 바로 잡혔다. **②-4(어원·본뜻)의 두 번째 적용 사례다.**
- 🎯 사용자 문장으로 도착한 곳 — *"이미 지도가 움직였을 때 이동이 끝난 것이고 … 또
  updateUIView가 불려야 하는데 이러면 중복이 된다는 거지?"* **「중복」이 사용자 말이라 그대로 쓴다**
  (「고리」·「무한루프」는 회고 인용에서만).

**정정 하나 — 「해당하는 `if`만 실행된다」는 사용자 말이 맞았다.** 내가 *"하나만은 아니다"*로
과잉 정정했고 사용자가 되물었다(*"결국 내 말대로 …아니야?"*). **없는 차이를 만들었다.**
그 자리에서 인정하고 물러섰다.

### 🎨 첫 도식 — `diagram-design`으로 정적 SVG를 넣었다

**청크 3의 첫 인라인 SVG 도식이다**(소개 1의 mermaid는 규칙 이전 것). 절차와 판단:

- **게이트 처리** — 프로젝트에 `.diagram-design` 마커가 없지만 **팔레트는 이미 정해져 있다**
  (노트 라이트 테마). `trace-study`가 *"노트의 색·글꼴을 기준으로 넘긴다"*고 명시하므로
  온보딩 질문 없이 통과시켰다. **다음에도 같이 처리한다.**
- **타입** Flowchart · 노드 7 · 화살표 6 · accent 2(판단 마름모 + 끊기는 결과). 예산 안.
- **내용은 「손가락으로 민 경우」** — 그것이 이번에 통한 그림이라 그대로 그렸다.
  마지막 칸 아래에 굵은 가로선 + *"여기서 끊긴다"*로 정지를 드러냈다.
- ⚠️ **`self_check.py`는 이 노트에 적용되지 않는다.** 돌리면 두 건이 뜨는데
  (`data-motion-root` 없음 · `script 1` 속성) **둘 다 애니메이션 전제 검사**이고,
  스크립트는 **독립 도식 HTML 파일**을 대상으로 한다. 우리는 학습 노트 안에 인라인으로 넣고
  모션을 쓰지 않으므로 해당 없다. **대신 직접 확인한 것:** 접근성 계약(`role="img"` ·
  `aria-labelledby` · `<title>`이 `<svg>` 첫 자식 · 접두어 `p1s4-*`) · 분기 연결선의
  rounded elbow(`r=8`) · 라벨 마스크와 선 사이 12px · 마스크가 마름모·노드와 안 겹침.
- **글자 크기는 4px 그리드보다 우리 규칙이 우선** — 한글 13px 이상(스킬 권장은 12px).
  「반복해서 틀린 것」의 *"작은 라벨도 0.82rem 이상"*이 이긴다.

### 구간 ⑤ — 두 번 다시 짰다. 원인은 첫 조각을 「성질」로 연 것

🔴 **③-2 위반이었다.** 첫 조각을 *"매번 새로 만들어진다"*라는 **성질**로 열고,
「지난번 값」이 **무엇인지 안 보여준 채로** 그것을 어디 둘지부터 논했다. 사용자 지적 —
*"전체적으로 지금 해당 구간에서 무슨말을 하고 싶은지 전혀 모르겠고, 그래서 그 안에 설명들도
이해가 안가."* 그리고 ③-2가 경고한 그대로 **「이 구간을 알면 무엇을 정할 수 있나」가
「어디에 둬야 하는지」라는 설계 판단**이었다.

**통한 순서 (재구성 2):** ①「지난번 값」실물 셋 → ②**탭 한 번이 화면까지 가는 길**(도식) →
③그 흐름의 ⑥이 핵심(적는 쪽/적히는 곳) → ④`MapViewRepresentable`에 두면 왜 안 되나 →
⑤델리게이트는 별개 이유 → ⑥같은 대입이 반대 방향도.

- 🔑 **사용자가 먼저 도식을 요청했다** — *"흐름도나 도식도로 표현하는건 어떻게 생각해? …
  그 값들의 변화에 따라 뭐가 불리고 최종으로 어떻게 되는지에 대해서 흐름을 알아야."*
  구간 ⑤는 도식 ✕로 잡혀 있었으나 **값의 출처와 행선지가 이 구간의 전부**라 넣었다.
  **다음에도 「값이 어디서 나서 어디로 가나」가 주제면 도식을 미리 잡는다.**
- **이름을 바꿨다** — *"`struct`는 매번 새로 만들어진다"* → *"`MapViewRepresentable`은…"*.
  사용자 제안(*"굳이 struct라고 표현을 하는것보다 MapViewRepresentable라고 하는건 어떻게 생각해?"*).
  **부품 이름을 주어로 두는 것이 맞다.**

### 정정 셋 (구간 ⑤에서)

1. 🔴 **「`updateUIView`가 가지고 있다」가 틀렸다.** 사용자 질문 —
   *"coordinator가 바뀌는데 왜 updateUIView가 가지고 있다고 하는거야?"*
   **적는 쪽(동작)은 `updateUIView`, 적히는 곳(보관)은 `Coordinator`.** 「가지고 있다」를
   양쪽에 써서 헷갈리게 했다. 노트에 이 두 줄을 명시했다.
2. 🔴 **흐름 설명에서 `rendererFor`를 잘못 넣었다.** 선택만 바뀐 경우는 **오버레이가 그대로라
   렌더러도 그대로**이고, `uiView.renderer(for:)`로 **이미 있는 것을 가져와** 고친다.
   `rendererFor`는 **오버레이가 새로 추가될 때**만 불린다. 노트에 경고로 넣었다.
3. **「해당하는 `if`만 실행된다」는 사용자 말이 맞았다**(구간 ④ 기록과 같은 건).

### 🆕 절대 원칙 — 줄 번호를 주어·제목으로 쓰지 않는다

사용자 요청 — *"`:237`은 반대 방향도 맡는다 => 이런 표현은 넣지마 실제 코드를 적던가해.
이렇게 하면 내가 다시 파일을 열어보거나 해야 하잖아."* `SKILL.md`「코드 스니펫 나열 금지」
바로 앞에 넣었다. **판별법: 줄 번호를 지우면 문장이 무너지는가.**

### 📏 파트 1이 길어진 것 — 재보고 「그대로 둔다」로 결정

사용자 질문 — *"질의를 통해 재구성하면 파트가 길어지는데 어쩔 수 없는거지?"* **실측했다.**

| | 자수 | 구간당 평균 |
|---|---|---|
| 파트 1 (구간 ⓪~⑤, ⑥⑦ 남음) | 19,059 → **약 26,000 예상** | **3,177** |
| 청크 2에서 가장 길었던 파트 6 (구간 8) | 12,983 | 1,623 |

**구간 수가 아니라 구간 하나의 밀도가 두 배다.** 선택지 셋을 냈고
(A 파트 쪼개기 → 9파트 · B 그대로 · C 이미 쓴 것 덜어내기), **처음에 A를 추천했다가 비용을
재보고 B로 뒤집었다.**

- **비용:** 산문 안 「파트 N」 26곳 + 장면↔부품 배지 13개 + 3분 복습 8줄 + 목차 8줄 +
  floating-toc + `agent-log` 표 = **60곳 이상.** 게다가 **이미 발행해 사용자가 읽은
  「파트 8에서 본다」가 전부 어긋난다.** 이 파일에서 **7파트/8파트 혼재로 이미 한 번 데었다.**
- **내용은 덜어낼 것이 없었다** — 여섯 구간 전부 능력 선언에 재료를 댄다. 능력 선언으로
  판정한 결과다(**구별은 사용자가 아니라 가르치는 쪽 몫**, 「아직 안 배운 내용에 판정을
  요구하지 않는다」).
- **길이가 정당한 이유:** 사용자 말 — *"uikit에 관한 것을 잘 사용하지 않았었고
  UIViewRepresentable에 대해서도 많이 쓰지 않아서."* **파트 1은 이 청크에서 유일하게
  「안 써본 도구」를 다룬다.** 파트 2~8은 전부 이 앱이 쓴 코드라 이만큼 안 나온다.

**대신 붙인 것 셋:** ①3분 복습 파트 1 줄에 *"이 파트만 유독 길다 — 유일하게 UIKit 골격"* 명시
②구간 표에 **⓪~③ 감싸기 자체 / ④~⑦ 이 앱의 값 흐름** 성격 구분
③🔴 **파트 2부터는 질의에서 나온 것을 「몇 주 뒤에 같은 자리에서 또 막힐 것인가」로 가르고,
아니면 `agent-log.md`로 보낸다.** 지금까지는 거의 다 노트에 넣었다.

> **다음에 같은 상황이 오면 다시 재지 말 것** — 파트를 쪼개는 비용은 **번호 참조 60곳 + 발행본
> 어긋남**이다. 쪼갤 거면 **파트 표를 확정하는 `(b-5)` 시점**에 해야 하고, 구간을 쓰기 시작한
> 뒤에는 늦다.

### 🔴 구간 ⑥ — 네 번 무너졌다. 원인은 능력 선언이 「선택」이 아니었던 것

**이 청크에서 가장 크게 실패한 구간이다.** 사용자 말로 끝났다 —
*"솔직히 글이 잘 읽히지도 않는다. 그냥 이렇게하고 다음 구간 진행하자."*
**끝까지 통하지 못한 채로 닫았다.**

**무너진 순서와 그때마다 든 기준:**

| 회차 | 기준 | 사용자가 짚은 것 |
|---|---|---|
| 1 | 「무엇을 SwiftUI에, 무엇을 `Coordinator`에」 | *"이 파트에서 무엇을 말하고 싶은지 모르겠다. 상태는 ViewModel이 갖는 거 아니야?"* |
| 2 | 「SwiftUI 쪽에서 읽는 사람이 있나」 | *"카메라도 지도에서밖에 사용 안 하는 거 아니야? 기준과 설명이 논리적이지가 않아"* — **맞는 반박이었다.** 다섯 곳 중 넷은 읽기가 아니라 **쓰기**였다 |
| 3 | 「지도 밖으로 나가야 하는가」(advisor 제안) | *"@Binding으로 표현해도 어차피 현재 페이지에서 사용하는 것 똑같은데"* — **맞다.** `cameraRegion`도 `saveCameraPosition`도 **같은 `CoursePlannerPage` 안**이라 「밖으로 나간다」가 성립하지 않는다 |
| 4 | 카메라 논증을 접고 「사실 / 기록」만 남김 | 읽히지 않는다 → 그대로 닫음 |

🔴 **근본 원인: 능력 선언이 「정할 수 있다」인데 실제로 정할 것이 없었다.**
앱 상태는 ViewModel(범위 밖) · 카메라는 SwiftUI(다른 선택지 없음) · 기록은 `Coordinator`
(`struct`에 못 두니 선택지 없음). **셋 다 강제된 결과인데 「정하는 법」으로 포장했다.**
`(b-5)`가 파트 4에서 잡았던 것과 같은 종류인데 **`(b-4)`도 advisor도 못 잡았고, 써보고서야 드러났다.**

⚠️ **카메라 소유권의 진짜 이유는 `CameraStateStore`(저장·복원)인데 그것은 파트 1에서 열지 않기로
한 것이다.** 근거를 못 보여주니 결론만 네 번 떠 있었다. **범위 밖 근거에 기대는 결론은 구간에
넣지 말 것** — 넣으려면 근거를 열든지, 사실만 대고 *"이 파트 밖"*이라 명시하든지 둘 중 하나다.

**바꾼 것:**
- **파트 1 능력 선언 교체** — *"어떤 상태를 SwiftUI에 두고 어떤 상태를 `Coordinator`에 둘지
  정할 수 있다"* → **"무엇을 구현해야 하고, 어떤 값이 어디에 남아야 하는지 가를 수 있다"**.
  3분 복습 + 파트 머리 `goal` 두 곳. **파트 2~8의 선언은 확인해보니 전부 실제 선택이라 그대로 둔다**
  (파트 1만 골격 소개라 「선택」이 아니었다).
- **구간 ⑥ 주제 교체** — *"무엇을 SwiftUI에…"* → **"값은 세 군데에 나뉘어 있다"**.
- **버린 것** — 카메라 다섯 줄 표(`:숫자`만 있어 정보가 없었다) · *"반영 기록을 ViewModel에 둘 수
  없다"* 절(⑤ 반복) · 무한루프 논증(근거 부실) · 도식(⑤가 이미 흐름도를 갖고 있어 반복).
- **남은 것 하나가 이 구간의 값이다** — `selectedSegmentIndex`(앱이 아는 사실) ↔
  `lastSelectedIndex`(지도를 손본 기록). **이름이 비슷해 실제로 사용자가 막혔고, 코드를 열면 또 막힌다.**

**표현 지적 둘:** 「자」(잣대 뜻으로 썼다) → **「기준」** · 「지도 밖으로 나간다」 → 폐기.
그리고 **방금 만든 「줄 번호를 주어·제목으로 쓰지 않는다」를 표의 열 제목에서 그대로 어겼다**
(*"어디서: `:164`"*). **규칙을 만든 그 턴에 어겼다.**

🔑 **구간 ⑦에서 다르게 할 것** — ⑥이 네 번 다시 쓰이며 **정정·단서·「이 파트 밖」 표시가 본문에
계속 붙었고**, 그것이 읽히지 않은 직접 원인이다. ⑦은 **처음부터 실물로 열고, 단서를 본문에 섞지
말고, 짧게** 쓴다.

### 구간 ⑦ — 제목이 결론이었고, 주인공 함수를 설명 안 했다

**두 번 고쳐서 통했다.** 걸린 것 둘 다 사용자가 짚었다.

1. 🔴 **`rendererFor`가 무엇인지 한 번도 말한 적이 없다.** 구간 ②·③에서 이름만 스쳤는데
   ⑦이 그것을 주인공으로 삼았다. 사용자 지적 — *"rendererFor가 뭐야? 애플에 내장되어 있는
   함수인 거야? 각 함수들에 대한 설명들이 너무 부족해."*
   **통한 설명:** MapKit이 **오버레이(어디에 있나 · 좌표만)와 렌더러(어떻게 그리나 · 색·굵기)를
   갈라놨다** → 얹기만 해서는 아무것도 안 보인다 → `rendererFor`는 *"그릴 도구를 줘"*라는 물음.
   그리고 **②-4(어원)를 다시 썼다** — `render`(그리다) / `renderer`(그리는 것) /
   `rendererFor(overlay:)`(이 오버레이를 위한 렌더러). 사용자가 *"rendererFor가 렌더링하는
   역할"*로 정리했을 때 **직접 그리는 것은 돌려받은 렌더러**라고 한 줄 갈랐다.
   > 🔑 **파트 2부터: 처음 나오는 함수는 「무엇을 하는 함수인지」 한 줄을 반드시 붙인다.**
   > 이 노트에서 `viewFor`·`syncHalo`·`configureRenderer`가 아직 이름만 나와 있다.
2. 🔴 **제목이 결론이었다.** *"델리게이트는 내가 부르지 않는다"* → 사용자 —
   *"구간 제목부터 와닿지가 않아."* **결론은 읽고 나서 얻는 것이지 제목이 아니다.**
   → **「얹는 일과 그리는 방법은 갈라져 있다」**로 교체. 이것은 사용자가 스스로 정리한 문장
   (*"updateUIView가 하는 일은 얹는 것까지이고, 그 특성에 대한 설정은 다른 곳에서 한다"*)과 같다.
   **사용자 표현을 제목으로 쓰니 그 자리에서 통했다.**

**범위도 좁혔다** — 초안이 *"그래서 오버레이에 번호를 실어뒀다"*까지 갔는데 **그것은 파트 2의
주제**다(`(b-5)`가 *"서브클래싱을 강제하는 것은 `rendererFor`가 오버레이 하나만 건네받는다는
사실"*이라고 배정해 뒀다). ⑦은 **갈라져 있다는 사실과 그 이유**까지만 두고 파트 2로 넘겼다.

**표면 전수 재확인(사용자 요청 — *"이번 파트에서 다른 부분에 대한 것도 다 확인을 한 거야?"*):**
델리게이트 셋 중 `viewFor`가 파트 1 어디에도 안 나와 있었다. 소개 1이 **실측으로 「구현한
델리게이트 3개」**라고 적어뒀으므로 ⑦ 4번에 **셋을 세는 표**를 넣어 닫았다
(`rendererFor` 이 구간 · `viewFor` 파트 2·5·6 · `mapViewDidChangeVisibleRegion` 구간 ④).
나머지 파트 1 몫은 전부 배정돼 있었다.

### 파트 1 완료 (2026-08-17) — 8구간

3분 복습의 「붙들 것」 여덟 줄을 **구간 순서대로** 채웠다. 파트 1 한 줄과 능력 선언은
파트 머리의 것을 그대로 옮겼다(줄이지 않았다).

**이 파트에서 값을 치른 것 넷** — ①구간 ⓪ 신설(「감싼다」에서 먼저 막혔다) ②구간 ⑤ 두 번 재구성
(첫 조각을 성질로 열었다) ③**구간 ⑥ 네 번 무너짐**(능력 선언이 선택이 아니었다) ④구간 ⑦ 두 번
(주인공 함수 미설명 + 제목이 결론). **넷 다 「무엇을 먼저 보여주느냐」 문제였다.**

### 검토·저장 이력

- `(b-4)` 자료(판정 구조 + 표면 전수 + 배정표 + 구간 표 초안): 세션 스크래치패드에 작성 후 advisor 호출.
- 발행: 같은 URL 갱신(2026-08-16). **이전 세션 발행본이라 `url` 인자가 필요했고, 그 전에
  `WebFetch`로 최신 버전을 한 번 읽어야 발행이 통과했다** — 다음 세션도 같은 순서를 밟는다.

---

## 📦 인계 원문 보존 — 청크 2 종료 시 쓴 착수 메모 (2026-08-15)

> **진행 상태 파일에서 이리로 옮겼다**(2026-08-16). 옮긴 이유: `/trace-init`이 `🔑` 블록을
> **재개 지점 맨 앞에 원문 그대로** 싣는데, 이 메모가 **이미 지난 지시**를 담고 있어 다음 세션을
> 잘못 이끈다 — *"브랜치를 새로 판다"*(이미 팠다) · 재료 4건(파트 1에 필요한 MVP4 설계 문서가 없다) ·
> 파트 번호(④핀 → 파트 2라고 적혀 있으나 **8파트 개정 후 파트 6**) · *"발행은 새 URL"*(이미 발행).
> **지우지 않고 옮긴 것은** 앞 세션이 좁혀둔 확인 대상이 여기 적혀 있어서다.

- 🔑 **착수 메모 (2026-08-15 청크 2 종료 시 작성) — 열기 전에 읽을 것.**
  **브랜치를 새로 판다** — 청크 2는 `docs/study-chunk-2`에서 했고 **푸시는 사용자 몫**이라
  아직 안 됐을 수 있다. `main`에서 `docs/study-chunk-3`을 새로 만든다.
- 📌 **재료 (실측 줄 수 · 스펙 §3 기준, 경로 확인 완료 2026-08-15):**
  `MapViewRepresentable.swift` **688줄**(`Trace/Pages/CoursePlannerPage/` 바로 아래 —
  `UIComponent/`가 아니다) · `OverlapOffsetResolver.swift` 152줄 · `SegmentPalette.swift` 13줄 ·
  `CoursePlannerPage+MapPinsComponent.swift` 68줄(`UIComponent/`).
  테스트는 `SegmentPaletteTests` · `OverlapOffsetResolverTests` · `CourseCoordinateGeoTests`.
  설계 문서는 `history/mvp7/`(구간 패널) · `history/mvp8/`(겹침 오프셋).
  ⚠️ **688줄짜리는 통째로 읽지 말 것** — `rendererFor`·`viewFor` 등 **해당 함수만**(`SKILL.md` `[1]`).
- 📌 **청크 2가 넘긴 연결 고리 — 다시 파지 말고 이어받을 것:**
  ①**`CourseCoordinate+Geo`의 함수 셋**(`distanceMeters(toSegment:)` · `headingVector` ·
  `offset`)은 실측상 `OverlapOffsetResolver` 한 곳에서만 쓰인다 — **청크 2가 "이건 청크 3"이라고
  명시하고 넘겼다**(노트 소개 1). `CourseCoordinateGeoTests` 7개도 전부 이쪽.
  ②**색은 붙인 순서 번호를 따른다**(`segmentColorKeys`, 파트 3) — 코스 앞쪽에 새 구간이 꽂혀도
  기존 구간 색이 안 바뀌는 이유가 거기 있다.
  ③**왕복 구간은 대상 구간과 좌표가 완전히 같다** — 화면에서 두 줄로 보이는 것은 **겹침 오프셋이
  옮겨 그리는 것**이고, 청크 2 파트 7 구간 ③이 그 사실을 **청크 3 예고로 남겨뒀다.**
  ④**핀은 좌표 한 줄의 첫/끝 점**이고 20m 안이면 「출발/도착」 하나로 합쳐진다(파트 2).
- 📌 **백로그에 이 영역 항목이 여럿 있다**(착수 전에 훑을 것) — 겹침 렌더링 α/β 후보 ·
  `MapViewRepresentable` 688줄 분해(린트 ③) · 핀·경로선 **전량 교체**로 핀이 매번 다시 떨어지는 것 ·
  회색 "확인 중" 마커 깜빡임. **학습 중 발견은 백로그로 보내고 코드는 고치지 않는다.**
- ⚠️ **진행 방식 — 청크 2에서 값을 치른 것들. 그대로 이어간다:**
  ①**저장·발행은 승인받고 한다** — 청크 2에서 구간 ①을 안 보여주고 발행했다가 사용자가 잡았다.
  **구간마다 채팅에 먼저 내고, 승인받고, 저장·발행한다.**
  ②**용어는 활성/비활성**(「켜진다·꺼진다」 금지, `SKILL.md` 절대 원칙 ②-3). 지도 쪽은
  「그린다/다시 그린다」처럼 **이미 통용되는 말**을 쓸 것.
  ③**구간 표를 내기 전에 `(b-3)` 판정 구조 뽑기 → `(b-4)` advisor 검토**(표면 전수 + 배정표를
  함께 넘긴다). 청크 2 파트 7에서 이 절차가 **한 개념이 두 구간에 걸친 것**을 잡아냈다.
  ④**확인 문제는 청크 전체에서 파트당 하나씩** 뽑는다 — 청크 2에서 한 파트에 몰렸다가 지적받았다.
- 🔑 **노트 형식은 이미 정해져 있다** — 두 층 접기 · 파트 머리 구간 표 · 번호 접두어 · 3분 복습 ·
  마무리 4절(어디를 열면 되나 · 왜 이렇게 됐나 · 팩트체크 수정 내역 · 확인 문제 기록).
  **다시 정하지 말고 청크 2와 같은 골격으로 만든다.** 발행은 **새 URL**(청크마다 아티팩트가 다르다) —
  첫 발행 뒤 URL을 `agent-log.md` 머리에 적어두고, 이후에는 같은 파일 경로로 다시 발행한다.

**소화 결과 — 위 메모 중 지금도 유효한 것은 이렇게 흡수됐다:**
연결 고리 넷 → 노트 소개 1 표(파트 번호는 8파트 기준으로 갱신) · 진행 방식 넷 → 이 파일 맨 아래 ·
노트 형식 → 이미 적용 · 백로그 항목 → 이 파일 「백로그 후보」 절.

---

## 진행 방식 — 청크 2에서 값을 치른 것들, 그대로 이어간다

1. **저장·발행은 승인받고 한다** — 구간마다 채팅에 먼저 내고, 승인받고, 저장·발행.
2. **용어는 활성/비활성**(「켜진다·꺼진다」 금지). 지도 쪽은 「그린다/다시 그린다」처럼
   **이미 통용되는 말**을 쓴다.
3. **구간 표를 내기 전에 `(b-3)` 판정 구조 뽑기 → `(b-4)` advisor 검토**(표면 전수 + 배정표를 함께).
4. **확인 문제는 청크 전체에서 파트당 하나씩.**
5. 🆕 **저장·발행 단위는 파트가 아니라 구간이다** (2026-08-16 사용자 결정).
   사용자 표현 — *"이전에는 파트 전체로 했는데 구간을 진행하면서 질의도 하다 보면 거기서 내가
   이해한 것을 바탕으로 파일을 정리하고 싶은데 넘어가게 되면 그게 잘 안 되는 것 같더라고."*
   **질의에서 나온 것이 그 구간 자리에 바로 들어가야 하고, 파트 끝까지 미루면 유실된다.**
   2026-08-05에 「한 번에 한 구간」을 파트 → 구간으로 내린 것과 **같은 처방**이다.
