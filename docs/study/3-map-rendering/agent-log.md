# 청크 3 작업 기록 — 지도 위에 표시하는 법

> **이 파일은 에이전트용이다. 사용자 학습 자료는 `note.html`이다.**
> 여기 담는 것: 팩트체크 대조 내역 · 노트 집필 이력(무엇을 썼다가 왜 뒤집혔나) ·
> 진행 방식 기록(무너진 것과 통한 순서) · 파트별 범위 합의 · 실측 절차 참조.
> **기능이 어떻게 동작하는가는 여기 적지 않는다** — 그건 `note.html` 몫이다.

- 노트: [`note.html`](note.html)
- 발행: <https://claude.ai/code/artifact/8a7a02b4-6ed3-4214-9436-3291a7886aed> · favicon `🗺️🎨`
- 브랜치: `docs/study-chunk-3`
- 상태: 🔄 진행 중 — **소개 3절 + 파트 1 완료**(2026-08-18 · 구간 ①②③ · advisor 팩트체크까지).
  **다음은 파트 2 「선 한가운데 거리 라벨」을 여는 것부터.**
- 🔑 **시작점: 아래 「🔑 파트 2 착수 메모 (2026-08-18 작성)」.** 재료 경로·진짜 결정·
  실기기 QA에서 나온 「깨져서 고친 것」 셋·조심할 것 다섯이 거기 있다.
  ⛔️ **그 바로 아래 「폐기됨」 메모는 삭제된 옛 파트 2의 것이니 쓰지 말 것.**
  구성 규칙 교훈은 「구간 ⑥ — 네 번 무너졌다」·「구간 ⑦ — 제목이 결론이었다」에서 읽는다.
- ✅ **깊이 배분 미결은 폐기됐다**(2026-08-18) — 대체 규칙이 `SKILL.md` `(b)`에 들어갔다.
  **「속도를 줄여야 한다」 논의를 다시 열지 말 것.**
- ⚙️ **저장 단위가 파트 → 구간으로 내려갔다**(2026-08-16 사용자 결정, 아래 「진행 방식」 5번)

---

## 청크 구성 확정 (2026-08-15)

### 🆕 `(b-5)` 조항을 신설했다 — 청크 구성에도 advisor 검토를 건다

사용자 결정 — *"파트에서 구간을 만들때만 순서나 이런것들을 검토하는게 아니라, 이 청크를 만들때도
이런것들이 제대로 반영이 되는지 검토를 진행해줘."* `SKILL.md` `[3]`의 `(b-4)` 바로 뒤에 넣었다.
`(b-4)`가 **파트 안의 구간 배정**을 보는 자라면, `(b-5)`는 **청크 안의 파트 배정**을 본다.

### 검토를 두 번 걸었고, 걸린 것

> ⚠️ **이 절과 아래 「새로 걸린 사실 셋」의 파트 번호는 7파트 기준이다** (2026-08-16 확인).
> 두 표 모두 **개정 전**에 쓰였고, 8파트 매핑은 아래 「최종 8파트」 표에 있다 —
> 2→2 · 1→3 · 3→4 · 4→5 · 5→6 · 6→7 · 7→8.
> **번호를 일괄 수정하지 않는 이유**는 인계 원문을 훼손하지 않기 위해서다.
> 반면 아래 「소유권」 절은 개정 **후**에 쓰여 신 번호다 — **한 파일 안에 두 체계가 있다.**

**1차 (초안 작성 전)** — 파트 순서와 개념 소유권.

| 지적 | 처리 |
|---|---|
| 파트 1/2 순서가 뒤집혀 있다 — 서브클래싱의 동기가 *"색을 정했는데 그리는 쪽이 그 색을 모른다"*라 **번호 둘을 안 깔면 파트 2를 열 수가 없다** | 색 파트를 1번으로 올림 |
| `colorKey`/`segmentIndex`가 파트 1·3·6·7에 걸친다 | 파트 1에서 못박고 이후는 *"이 자리는 어느 번호를 쓴다"* 한 줄로만 |
| `MapPin.==`의 `role` 비교는 diff 쪽이 본론 | 파트 7 (파트 5는 필드만) |
| halo 일체(`SegmentHaloPolyline`·`syncHalo`·`insertOverlay(below:)`) | 파트 3에 통째로. 파트 7은 *"선택이 바뀌면 다시 부른다"* 한 줄 |

**2차 (파트 표 확정 전 — `(b-5)`)** — 표면 전수에서 **배정 안 된 줄 여섯**이 나왔다.
원인은 **착수 메모의 재료 4건만 보고 표를 짠 것**이다(`(b-4)`가 2026-08-14에 겪은 것과 같은 실패).

| 배정 안 됐던 표면 | 위치 | 배정 |
|---|---|---|
| `configureRenderer` | `MapViewRepresentable:346` | 파트 3 — `rendererFor`와 선택 갱신 루프 **양쪽**에서 불린다(파트 7로 가는 다리) |
| `mapViewDidChangeVisibleRegion` + 한 틱 미루기 | `:495` | 파트 7 — **방향이 반대**(지도 → SwiftUI)임을 명시 |
| `viewFor`의 `MKUserLocation → nil` | `:451` | 파트 5 — "안 그리는 핀" |
| `view.isEnabled = false` ↔ `pinHit(at:in:)` | `:471` / `:662` | 파트 5 — 핀은 선택 불가인데 히트 판정은 화면 24pt 반경으로 **따로 돈다** |
| `Coordinator.mergedBadgeTag` 정리 줄 | `:472` | 파트 5 (아래 「새로 걸린 사실 ①」과 같은 자리) |
| `Canvas` 획 미리보기 | `CoursePlannerPage:250` | **범위 밖 — 청크 1**. 노트 소개 1의 경계선에 명시 |

그 밖에 고친 것 셋:

- **파트 4의 능력 선언이 결정이 아니었다** — *"뷰를 직접 만들지 정할 수 있다"* →
  **"라벨을 배열 인덱스 절반에 찍을지 실거리 절반에 찍을지"**(커밋 `117f821`). 도식도 ✕ → ○.
- **`rendererFor`/`viewFor` 소유권을 노트 소개 3에 적었다** — 구조는 파트 2, 분기는 파트 3·4·5.
- **파트별 `(b-2)` 유형을 미리 태그했다** — 파트 6만 「기획대로」, 나머지는 「깨져서 고침」 또는
  「나중에 추가」. **파트 6이 유일하게 기획대로라는 대비 자체가 노트에서 값을 한다.**

### 새로 걸린 사실 셋 — 해당 파트에서 다룬다

1. **핀은 재사용을 안 하는데 재사용 대비 코드가 있다.** 같은 `viewFor` 안에서 거리 라벨은
   `dequeueReusableAnnotationView`(`:456`), **핀은 매번 새로 생성**(`:464`). 그런데 `:472`가
   *"기존 배지 서브뷰를 지운다"* — 갓 만든 뷰에는 있을 수 없으므로 **절대 안 도는 줄**이다.
   → 파트 5 재료 / 처방은 백로그.
2. **케이싱 색만 다크모드를 따른다.** `polylineCasingColor`(`:4-8`)는 다크 검정 0.35 /
   라이트 흰색 0.6인데 **팔레트 색은 고정**이다. → 파트 3 「세 겹」에서 겹마다 색 출처가 다르다는 점.
3. **`SegmentPalette`의 폴백이 조용하다.** `UIColor(named: "Seg\(index % 6)") ?? .systemBlue` —
   에셋이 하나라도 없으면 **전부 파랑**이 되고 신호가 없다. `SegmentPaletteTests`의 두 번째
   테스트가 간접적으로 막고 있다. → 파트 1 재료.

---

## 문서 ↔ 코드 대조 (`[2]`) 결과

멈출 사안 **없음.** 넷을 대조했고 전부 「깨져서 고친 과정」 재료로 파트 안에서 다룬다.

| 문서 | 코드 | 판정 |
|---|---|---|
| MVP7 §B *"색상을 세그먼트 순서에 따라 배정"* | `colorKey`(만든 순서) | MVP8 설계 문서가 **이미 스스로 정정**했다. 파트 1 재료 |
| MVP7 §B *"`SegmentPolyline`에 `segmentIndex` 추가"* | `segmentIndex` + `colorKey` **둘** | 같은 뿌리. 파트 1·2 재료 |
| MVP8 *"선택 하이라이트·핀 로직 변경 없음"* | halo가 나중에 추가됨(`a4da480`, 2026-07-12) | 파트 3 재료 |
| MVP7 §E *"해당 구간이 보이도록 카메라를 이동한다"* | **구현돼 있다** — 아래 오판 기록 참고 | 불일치 아님 |

### 🔴 내가 낸 오판 — 카메라 이동을 "미구현"으로 판정했다

`CoursePlannerPageViewModel.selectSegment`(`:324-326`)가 인덱스만 세팅하는 것을 보고
**"설계에 있는 카메라 이동이 코드에 없다"**고 판정해 사용자에게 `[2]`의 A/B/C를 냈다.
**틀렸다.** 실제 코드는 `CoursePlannerPage.swift:181-187`의
`.onChange(of: viewModel.selectedSegmentIndex)` → `regionFitting(:345-357)`에 있다.

- **원인:** 이 프로젝트는 **카메라 상태(`cameraRegion`)를 페이지 뷰가 `@State`로 소유**하고
  `MapViewRepresentable`에 `@Binding`으로 내려준다. 그래서 "선택되면 카메라를 옮긴다"도
  뷰모델이 아니라 **뷰의 `.onChange`**에 있다. 뷰모델 계층만 따라가서 놓쳤다.
- **교훈:** 이 청크에서 *"X를 하는 코드가 없다"*를 판정할 때는 **뷰모델과 뷰 양쪽을** 봐야 한다.
  표시층은 상태 소유자가 뷰 쪽인 경우가 있다.
- **곁증거를 흘렸다:** 백로그 154번(`구간 탭 카메라 프레이밍이 시트 medium 단계 높이를 반영하지 않음`)이
  **카메라 프레이밍이 있다는 전제**로 쓰여 있었다. 백로그를 먼저 훑었으면 그 자리에서 걸렸다.

### 절차 결정 — `[2]`의 A/B/C와 백로그는 별개다

사용자 질문 — *"문서에 있는데 제대로 반영이 안된거면 이후에 수정을 해야 하니까 백로그에 남겨줘야
하지 않을까? 그게 C인건가?"* 답: **A + 백로그**이고 C가 아니다.
A/B/C는 **노트를 무엇 기준으로 쓸까**만 정하고, 백로그 등재는 「사실은 노트에, 판정과 처방은
백로그에」에 따라 **어느 쪽을 골라도 항상** 한다. A와 C의 차이는 **노트에 그 한 줄을 넣느냐**뿐.

---

## 백로그 후보 (해당 파트를 쓸 때 등재)

- **`SegmentSnapshot.==`가 경도를 안 본다** — 구간마다 `coordinateCount` + 첫/끝 **위도**만
  비교한다(`:155-164`). 경도만 다른 변경과 중간 좌표 변경은 못 잡는다. → 파트 7
- **핀의 배지 정리 줄이 절대 안 돈다** — 위 「새로 걸린 사실 ①」. → 파트 5

이미 열려 있어 **다시 등재하지 않는 것**(노트에는 사실만 한 줄):
겹침 렌더링 α/β 후보 · `MapViewRepresentable` 688줄 분해(린트 ③) · 핀·경로선 전량 교체 ·
회색 "확인 중" 마커 깜빡임 · 구간 탭 카메라 프레이밍이 시트 높이 미반영(154번).

---

## 🔄 로드맵 개정 — 7파트 → 8파트 (2026-08-15, 사용자 제기 → `(b-5)` 재검토)

### 사용자가 제기한 것

*"결국에 지도에 관해서 화면에 표현하려면 UIViewRepresentable를 사용해야 하는데 이것에 대해서
어떻게 다루는지, 그리고 MKMapView 관련해서도 어떻게 표현해야 될지를 그래도 알아야 하지 않을까"* —
그리고 그 이유를 사용자가 직접 댔다: ***"그냥 swiftui뷰에 표현하는것이면은 아무렇지 않게 넘어갈것
같은데, 이 지도앱 만의 특성이라고 생각해서 그래. 지금은 지도앱이 swiftui로만 표현하기 힘들어서
우리가 MKMapView와 UIViewRepresentable를 가지고 커스텀에 맞게 수정을 해서 그것을 swiftui뷰에
올린것이기 때문에."***

**이 틀이 맞다.** 「UIKit 일반 지식」(범위 밖)이 아니라 **이 앱이 SwiftUI를 벗어나야 했던 이유**이고,
그건 절대 원칙 ①이 시키는 것 그 자체 — *"이 프로젝트가 왜 여기에 그걸 썼나"*.

### 🔴 내가 먼저 그은 선이 코드와 어긋났다

*"오토레이아웃은 안 한다 · UIKit 일반은 뺀다"*고 그었는데, **코드에 있다** —
`MapViewRepresentable:483`의 `NSLayoutConstraint.activate([...])`(병합 핀 배지),
`:78~99`의 `UILabel`+`addSubview`+`sizeToFit`, `:457`의 `dequeueReusableAnnotationView`.
**사용자가 잡았다.** 판정 기준을 바꿨다:

> **이 청크 코드에 실제로 나오는 UIKit은 전부 다룬다. 코드에 안 나오는 UIKit 일반만 뺀다.**

### 범위를 세 층으로 자른 결과

| 층 | 무엇 | 어디 |
|---|---|---|
| **다룬다** | 왜 SwiftUI를 벗어났나 · 세 함수가 언제 불리나 · `Coordinator`는 왜 · **델리게이트는 내가 안 부른다** | 파트 1 |
| **다룬다** | 조작 8종 — `addOverlay`/`addAnnotation` · `insertOverlay(_:below:)` · `setNeedsDisplay` · `UILabel`+`addSubview`+`NSLayoutConstraint` · `dequeueReusableAnnotationView` · `removeOverlays`/`removeAnnotations` · `setRegion` | 파트마다 |
| **뺀다** | 코드에 없는 것 — `UIViewController` 생명주기 · `UITableView`/`UICollectionView` · 오토레이아웃 전반 · 안 쓰는 `MKMapView` API | — |

**끝이 있다는 실측 근거:** 이 파일이 지도 인스턴스에 거는 것 **21종** · 구현한 델리게이트 **3개**
(`rendererFor`·`viewFor`·`mapViewDidChangeVisibleRegion`) · `UIViewRepresentable` 요구사항 **3개**.

### 🔁 순서가 뒤집혔다 — 색과 타입 (이번 개정의 핵심)

**전:** 색(1) → 타입(2). 근거는 *"색을 정했는데 그리는 쪽이 그 색을 모른다"*가 서브클래싱의 동기라는 것.
**후:** 타입(2) → 색(3). **감싸기 파트가 앞에 생기면서 그 동기가 옮겨갔다** — 서브클래싱을 강제하는 것은
색이 아니라 **`rendererFor`가 오버레이 하나만 건네받는다**는 사실이다.

**실측으로 확인했다 — `rendererFor`의 네 갈래 중 셋은 팔레트를 안 건드린다:**

| 갈래 | 색 출처 | 팔레트 |
|---|---|---|
| `WaypointDotsOverlay` | 렌더러가 직접 그림 | ✕ |
| `SegmentHaloPolyline` | `polylineHaloColor` **고정 상수** | ✕ |
| `SegmentCasingPolyline` | `polylineCasingColor` **고정 상수** | ✕ |
| `SegmentPolyline` | `configureRenderer(colorKey:)` | **○ 여기만** |

즉 **타입으로 갈리는 함수지 색으로 갈리는 함수가 아니다.** 동기가 세 겹으로 쌓이게 됐다 —
①델리게이트는 내가 안 부른다(1) → ②그래서 오버레이가 자기 정보를 들고 있어야 한다(2) →
③그럼 무슨 번호를 들려 보내나(3).

### ~~최종 8파트~~ — ⛔️ **폐기된 구성이다 (2026-08-18 개편으로 대체)**

> 🔴 **아래 표의 파트 번호를 쓰지 말 것.** 2026-08-18에 축이 「만드는 순서」에서
> **「화면에 보이는 기능」**으로 바뀌면서 **6파트 + 골격 2**가 됐다(「✅ 구성 개편 실행」 참고).
> 현재 구성: 1 색 선 · 2 거리 라벨 · 3 핀 · 4 흰 점 · 5 선택 강조 · 6 겹친 선 · 골격 1·2.
> 아래는 **왜 그 축을 접었는지의 이력**으로만 남긴다.


| | 파트 | 이동 |
|---|---|---|
| 1 | SwiftUI 밖으로 나가기 — `MKMapView`를 SwiftUI에 얹는 법 | 🆕 |
| 2 | 지도에 얹는 것을 어떤 타입으로 만드나 | 2 → 2 |
| 3 | 번호가 둘이다 — 색을 정하는 규칙 | **1 → 3** |
| 4 | 선 하나를 세 겹으로 그린다 | 3 → 4 |
| 5 | 거리 라벨을 어디에 찍나 | 4 → 5 |
| 6 | 핀 목록을 만드는 규칙 | 5 → 6 |
| 7 | 겹치는 선을 비켜 그리기 | 6 → 7 |
| 8 | 언제 다시 그리나 | 7 → 8 |

⚠️ **파트 7(겹침)이 열어보면 또 쪼개질 수 있다 → 9파트.** 그 지점에 오면 *"겹침을 별도 청크로 뺄지"*를
그 자리에서 판단한다. **미리 정하지 않는다.**

### 소유권 (노트 소개 3에 적었다)

- **조작 모델** → 파트 1. 개별 호출은 각 파트.
- **`rendererFor`/`viewFor` 구조** → 파트 2. 분기는 파트 4·5·6.
- **z-order** → **보장**(오버레이는 항상 어노테이션 아래)은 파트 2, **명시 호출**(`insertOverlay(_:below:)`)은
  파트 4. 파트 4가 파트 2를 이름으로 부른다. **두 번 설명하게 되면 한 곳으로 합친다.**

---

## ✅ 파트 1 착수 메모 (2026-08-16 작성 · **파트 1 완료로 소진됨**)

> **다음 세션의 시작점이 아니다.** (그 아래 「파트 2 착수 메모」도 아니다 — 2026-08-18 폐기.)
> 이 절은 **당시 무엇을 계획했고 실제로 어떻게 달라졌는지**를 대조하려고 남긴다
> (예: 「담을 것 다섯」이 8구간이 됐고, 「네 갈래 게이트」는 실제로 다섯이었다).

**파트 1 — SwiftUI 밖으로 나가기: `MKMapView`를 SwiftUI에 얹는 법**

### 능력 선언 (확정)

> SwiftUI만으로 안 되는 화면을 감쌀 때 **어떤 상태를 SwiftUI에 두고 어떤 상태를 `Coordinator`에 둘지**
> 정할 수 있다.

**왜 이 문장인가** — 이 앱이 실제로 **둘로 갈라놓았다.** `cameraRegion`은 페이지 뷰의 `@State`,
`lastSegmentSnapshots`·`lastSelectedIndex`·`currentHaloOverlay`는 `Coordinator`. 청크 1 노트가
그 이음매를 이미 한 줄 적어뒀다 — *"`Coordinator`는 UIKit 객체라 거기 값이 바뀌어도 SwiftUI가 모른다."*
**그 문장 위에 세운다.**

### 담을 것 다섯

1. **왜 SwiftUI `Map`을 버렸나** — hit-test 소유권. 설계 문서를 인용한다(아래 재료).
2. **`makeUIView`는 한 번만 불린다** — 제스처를 여기서 달기 때문에(`:200-231`), 나중에 켜고 끄려면
   `updateUIView`에서 손으로 해야 한다(`:332-343`). **그 처리 자체는 청크 1 영역 — 이유만 대고 넘긴다.**
3. **`updateUIView`는 값이 바뀔 때마다 불린다 — 카메라를 밀어도 불린다.** **파트 8의 전제**다.
   여기서 사실만 대고, 파트 8이 다시 받는다.
4. **`Coordinator`는 왜 있나** — `struct`는 델리게이트를 못 맡는다 + 편집 중 값을 들고 있어야 한다.
5. **델리게이트는 내가 부르지 않는다** — 파트 2의 동기.

### 재료 (경로 확인 완료 2026-08-15)

| 재료 | 경로 |
|---|---|
| 골격 | `MapViewRepresentable.swift` — `makeCoordinator:190` · `makeUIView:194-234` · `updateUIView:236-344` · `Coordinator:404-413` |
| 상태 분리 | `CoursePlannerPage.swift` — `cameraRegion` `@State` · `MapViewRepresentable(region: $cameraRegion …):234-249` |
| **설계 근거** | `history/mvp4/2026-06-26-mvp4-design.md:22` (hit-test 소유권) · `:26-37` (제스처 주의사항 · *"Coordinator가 `MKMapViewDelegate`로 처리"*) |
| 결정 기록 | `docs/agent-rules/project-decisions.md:106` |
| 깨진 것 | `history/mvp4/260627_mvp4_completion_retro.md` |

⚠️ **`history/mvp4/2026-06-26-mkmap-migration.md`는 플랜이라 재료가 아니다**(스킬 `[1]`).
설계 문서(`2026-06-26-mvp4-design.md`)를 쓴다. **둘을 헷갈리지 말 것.**

### 진행 순서

1. `(b-3)` — 이 파트는 분기가 여럿인 함수가 주인공이 아니라 **골격 소개**라 판정 구조 뽑기는
   해당 없을 수 있다. **`updateUIView`는 예외** — 그 안이 네 갈래 게이트(카메라 · 세그먼트 스냅샷 ·
   핀 diff · 그리기 모드)인데 **그중 셋은 파트 8·청크 1 것**이다. 파트 1에서는 *"갈래가 넷이고
   여기서는 구조만 본다"*까지만.
2. `(b-4)` — 구간 표 + **표면 전수 + 배정표**를 함께 넘긴다. 전수는 **코드에서 다시 뽑는다**(착수 메모 아님).
3. 구간 하나씩 채팅 → **승인** → 저장·발행.

### ⚠️ 이 파트에서 조심할 것

- **UIKit 강의로 번지지 않게.** 판정 기준은 하나 — **이 청크 코드를 읽는 데 필요한가.** 아니면 접는다.
- **문법을 설명하지 않는다**(절대 원칙 ①). `some View`·제네릭·`associatedtype`이 나와도 넘어간다.
  설명할 것은 **이 프로젝트가 왜 그렇게 썼나**뿐.
- **사용자는 SwiftUI는 알고 UIKit·`UIViewRepresentable`은 거의 안 써봤다**(본인 말).
  그러므로 **SwiftUI 쪽을 기준점으로 삼아 이어준다** — *"SwiftUI였다면 `body`가 다시 계산될 자리에,
  여기서는 `updateUIView`가 불린다"*처럼.
- **의인화 금지**(②-2). *"MapKit이 물어본다"* 대신 **함수 호출**로 쓴다.

### 🎨 도식 규칙이 바뀌었다 — mermaid로 그리지 말 것

커밋 `f2e89d6`(2026-08-15)이 `SKILL.md`「노트 형식 · 도식」을 고쳤다.

> 새 도식은 **`diagram-design`**으로 만든 **정적 인라인 SVG**를 `note.html`에 넣는다.
> 독립 HTML·PNG를 따로 만들지 않는다. 현재 노트의 **라이트 테마·한글 가독성 규칙을 색·글꼴의
> 기준으로 넘기고**, `diagram-design`의 **기본 주황 팔레트·모션은 쓰지 않는다.**
> 기존 Mermaid 도식은 **손댈 이유가 생길 때만** 교체한다.

- **파트 1은 도식 ○이므로 이 규칙에 바로 걸린다.** mermaid로 그리면 규칙 위반이다.
- `diagram-design`은 플러그인으로 설치돼 있다(`diagram-design@diagram-design`, 확인 2026-08-16).
  **호출이 실패하면 그 단계를 건너뛰지 말고 멈추고 사용자에게 알린다**(`docs/agent-rules/skills.md`).
- **소개 1의 mermaid 도식은 그대로 둔다** — 규칙이 서기 전에 만든 것이고 *"손댈 이유가 생길 때만
  교체"*에 해당한다. 노트 CSS의 mermaid 스타일도 그래서 남겨둔다.
- 색 기준: 노트의 `--accent #06B6A4` · `--deep #04796E` · `--ink #0E1817` · `--rule #DAE6E3`,
  라이트 고정. 한글 라벨은 **0.82rem 이상 · 자간 0.05em 이하 · 본문 글꼴**
  (`SKILL.md`「반복해서 틀린 것」).

---

## 파트 1 진행 기록 (2026-08-16)

### `(b-3)` — 착수 메모의 「네 갈래」가 실제로는 **다섯**이었다

`updateUIView`의 게이트를 코드에서 다시 뽑으니 **`lastSelectedIndex` 게이트(`:295-310`)가
착수 메모에서 빠져 있었다.** 다섯은 카메라(`:239`) · 스냅샷(`:247`) · **선택(`:295`)** ·
핀(`:312`) · 그리기 모드(`:332`).

구조상 확인한 것 셋 — ①**조기 반환이 아니다**(다섯이 배타적이지 않고 순서대로 전부 평가된다)
②**순서에 의미가 있다**(B가 오버레이를 재생성한 뒤 C가 렌더러를 만지므로, 둘 다 걸리면 C는 방금
만들어진 렌더러를 손본다. `syncHalo`는 B·C 양쪽에서 호출) ③**게이트의 이유는 무한루프**
(회고 `:26`, 고리는 `mapViewDidChangeVisibleRegion` → `parent.region` → `updateUIView` →
`setRegion` → 다시 델리게이트).

> **재료 목록을 착수 메모에 의존하면 빠진다** — `(b-4)`가 2026-08-14에, `(b-5)`가 2026-08-15에
> 겪은 것과 **같은 실패가 세 번째**다. 착수 메모는 앞 세션이 그때 본 것뿐이다.

### `(b-4)` — 구간 표를 내기 전 검토에서 셋이 걸렸다

| 지적 | 처리 |
|---|---|
| **⑤/⑥이 한 개념을 걸친다** — ⑤가 `Coordinator`의 존재 이유 둘(델리게이트 + 값 보관)을 대는데 뒤쪽이 ⑥ 전체고, 앞쪽 근거인 「델리게이트」는 ⑦에서야 열린다 | 자르는 선을 옮김 — ⑤=**`struct`는 매번 새로 만들어진다**(근거는 `:237` `parent = self`) · ⑥=어디에 두나 · ⑦=`NSObject` 채택과 델리게이트 수신 |
| **⑥이 규칙이 아니라 나열이었다** | 판별 규칙으로 바꿈 — ***「SwiftUI 쪽에서 그 값을 읽는 사람이 있나」***. `cameraRegion`은 있고(`.onChange` 둘 + 델리게이트), 표시 상태 셋은 없다 |
| **④가 파트 8의 몫까지 걸어간다** — 게이트 다섯을 순회하면 파트 8이 빈다 | ④는 *"몸통이 전부 「바뀌었나?」 검사다 + 왜(무한루프)"*까지. 게이트별 내용과 **게이트 도식은 파트 8**, ④의 도식은 **고리 하나** |

**배정 확정(질문 넷에 대한 답):** `showsUserLocation`은 파트 1에서 이름만·`viewFor → nil`은 파트 6 ·
`.onChange(of: selectedSegmentIndex)` → `regionFitting`은 **파트 1 구간 ⑥**(카메라 소유권의 결과다.
위 🔴 오판이 정확히 이걸 말한다) · `.onChange(of: scenePhase)` → `saveCameraPosition`은 ⑥의 근거로
한 줄만(`CameraStateStore`는 열지 않는다) · `SegmentSnapshot` 타입은 파트 8.

⚠️ **`Coordinator`에 있는 것이 「셋」이 아니다.** 표시 상태는 셋(`lastSegmentSnapshots` ·
`lastSelectedIndex` · `currentHaloOverlay`)이지만 인식기 참조 셋(`:519-521`) · `tapClassifier`(`:579`) ·
획 수집 배열(`:521-524`) · 보류 좌표(`:588-589`)가 더 있다. **구간 ⑥에서 "이 청크가 다루는 표시
상태 셋"으로 범위를 명시**하고 나머지는 청크 1로 자리만 알린다.

### `(b-2)` 유형이 하나가 아니라 **둘**이었다

`(b-5)` 사전 태그는 파트 1을 「깨져서 고침」 단일로 잡았는데 **틀렸다.**

- **기획대로 간 것** — 카메라를 `Binding`으로 주고받는 구조는 설계 문서
  `2026-06-26-mvp4-design.md:44-45`에 **처음부터 적혀 있다**(*"`MKCoordinateRegion`을 Binding으로
  주고받아 기존 `CameraStateStore` 저장/복원 로직 유지"*). **구간 ⑥의 1차 근거다.**
- **깨져서 고친 것** — 제스처. `makeUIView` 시점에 `MKMapView` 내부 인식기가 아직 없어
  `minimumNumberOfTouches = 2`가 **조용히 안 먹었다**(회고 `:13`) → `isScrollEnabled` 토글로 재설계.

**설계 문서에서 낡은 것은 `:28-33`(제스처 계획)뿐이다.** `:35-37`(오버레이 → `MKPolyline`,
*"Coordinator가 `MKMapViewDelegate`로 처리"*)와 `:44-45`는 지금 코드와 일치한다.
착수 메모가 *"`:26-37` 전체를 인용 안 함"*으로 잡은 것은 과했다.

### 🆕 구간 ⓪을 신설했다 — 사용자가 「감싼다」에서 먼저 막혔다

구간 ①을 열기 전에 *"감싼다는 표현에 대해서 이해를 하고 가야 할 것 같은데 이게 무슨 의미야?"*가
나왔다. **7구간 → 8구간.** 통한 순서는 ①`MKMapView`는 `UIView`라 `body`에 못 쓴다 →
②그래서 `UIViewRepresentable`을 채택한 `struct`를 만든다(선언 + 호출부 실물) →
③이름 둘을 가른다(애플 프로토콜 vs 우리 `struct`) → ④**변환이 아니라 둘이 따로 있고 수명이 다르다**.

- ④가 그냥 부연이 아니다 — 사용자가 *"…형태로 바꿀 수 있도록 한다는 것"*으로 정리했고,
  **「바꾼다」로 굳으면 구간 ⑤(`struct`는 매번 새로 만들어진다)가 안 통한다.** 그 자리에서 정정했다.

### 사용자 질의에서 나와 구간 ①에 들어간 것

1. 🔴 **시기를 안 가르면 통하지 않는다.** 사용자가 기억하는 것은 **MVP4~MVP16(두 손가락 이동)**인데
   설계 문서 인용문은 **MVP4 이전(SwiftUI `Map`)**이다. 인용문만 내밀었다가 *"지금도 결국 그리면서
   이동은 안 되는 걸로 알고 있어"* · *"아직도 정확한 이유를 모르겠어"*로 막혔다.
   **세 칸 표(MVP4 이전 / MVP4~16 / 지금)를 구간 ① 첫 조각으로 올려서 풀었다.**
2. **「해결된 것」을 잘못 말할 뻔했다** — 지금도 획을 긋는 동안에는 지도가 잠긴다(`:548`→`:568`).
   해결된 것은 *"그리면서 이동"*이 아니라 ***"이동하려고 모드를 나가는 것 + 나가면 초기화되는 것"***이다.
   **잠기는 단위가 모드 → 한 획으로 옮겨간 것**이고, 그 표를 노트에 넣었다.
3. **`Map`에 인식기가 없는 게 아니다** — 있는데 **우리가 못 만진다.** 세 층으로 정리했다:
   달 수는 있다(`.gesture`) / 안 불린다(안쪽이 먼저 가져간다) / 조정도 못 한다(`require(toFail:)`를
   걸려면 양쪽 인식기를 다 쥐어야 한다).
4. **부딪힌 것은 「한 손가락 드래그」다** — 내장 이동도, 그리기도 한 손가락 드래그라 정면 충돌이었다.
   사용자가 *"비슷한 제스처가 들어왔을 때"*라는 조건을 스스로 달았고 그게 정확한 자리다.
5. **미검증을 「불가」로 적지 않았다** — *"SwiftUI `Map`으로는 안 된다"*는 2026-06-25 리서치 판정이고
   (`project-decisions.md:106`) 그 뒤 재측정한 적 없다. 노트에 시점과 함께 명시했다.
   사용자가 *"이후에 지금 뷰를 map으로 표현해 보는 과정이 필요하다고 생각하는 거야?"*로 물었고,
   **아니라고 답했다** — 되돌릴 이유가 없고 능력 선언과도 무관한 곁가지다.

### 구간 ② — 순서를 틀렸고 사용자가 잡았다

🔴 **`Coordinator`가 뭔지 안 대고 `makeCoordinator`를 설명했다.** `(b-4)`가 `Coordinator`의 정체를
구간 ⑤·⑦로 미뤄놨는데, 구간 ②에 `makeCoordinator`가 나오므로 **거기서 이미 필요했다.**
사용자 지적 — *"Coordinator가 뭔지 모르겠는데 Coordinator를 만드는 함수인 makeCoordinator에
대해서 이해를 못하지 않을까?"* **③-2(처음 나오는 부품은 「무엇을 하나」부터)의 위반**이다.

- **처방: 구간 ②로 끌어올렸다.** `Coordinator`의 **정체와 두 역할**(델리게이트를 받는다 ·
  지난번 값을 기억한다)까지 ②에서 댄다.
- ⚠️ **그래서 ⑤·⑦의 경계를 다시 긋는다** — 겹치면 그 구간이 빈다.

| 구간 | 남은 몫 |
|---|---|
| ⑤ | **`MapViewRepresentable`(`struct`)이 왜 그 일을 못 맡나** — 매번 새로 만들어진다(`:237` `parent = self`가 증거). ②가 *"`Coordinator`가 한다"*까지 갔으므로 ⑤는 **왜 거기여야 하나**만 |
| ⑦ | **호출 방향이 뒤집힌다는 것과 그 결과** — ②는 *"델리게이트를 여기서 받는다"*까지. ⑦은 *"내가 부르지 않는다 → 그래서 오버레이가 자기 정보를 들고 있어야 한다"*(파트 2의 동기) |

### 구간 ②에서 정정한 것 둘

1. 🔴 **「무조건인 것은 둘」이 틀렸다.** Apple 문서 확인 결과 **셋 다 요구사항**이고
   `makeCoordinator`에만 기본 구현이 있다(*"Required method. Default implementation provided."*).
   **소개 1의 「요구사항 3개」는 맞았다** — 고치겠다고 했다가 철회했다.
   대신 **`updateUIView`에는 기본 구현이 없다**(빈 몸통으로라도 써야 한다)를 대비로 넣었다.
2. 🔴 **`Coordinator`를 「UIKit → SwiftUI 전달」로 좁히면 구간 ⑥이 반대로 읽힌다.**
   사용자가 그렇게 정리했고 그 자리에서 넓혔다 — `Coordinator`는 **`MKMapView`가 부르는 함수들이
   있는 곳**이고 **그중 일부만** SwiftUI를 고친다. 실측: `rendererFor`·`viewFor`는 안 고치고
   `mapViewDidChangeVisibleRegion`만 고친다(**셋 중 하나**). ⑥의 판정 규칙이
   *"SwiftUI 쪽에서 그 값을 읽는 사람이 있나"*라서, 안 넓히면 정반대로 적용된다.

### 🆕 절대 원칙 ②-4를 신설했다 — 어원·본뜻으로 연다

사용자 요청 — *"어떤 인스턴스나 프로퍼티 혹은 이런 자연스럽게 쓰이는 말들에 대해서 이해가 안 되면
이런 어원이나 이런 식으로 말해주면 이해가 잘 될 것 같아. … 어원이나 이런 것을 알면 더 이해하기 쉬어서."*
`Coordinator`에서 통한 것이 **coordinate = 조율하다** + Apple 문서 *"A type to coordinate with
the view"*였다. **비유가 아니라 원래 뜻이었다** — ②(나)의 *"막히면 비유"*로는 안 잡히는 종류다.
`SKILL.md` ②-3 뒤에 넣었고, **1차 출처(공식 문서)를 인용할 것**을 조건으로 달았다.

### 구간 ②에서 뺀 것과 넣은 것

- **뺐다 — `dismantleUIView`.** *"안 채운 칸도 세자"*로 넣었는데 사용자가 *"이 내용은 왜 들어가는
  거야?"*로 잡았다. **능력 선언에 아무 재료도 안 된다 → 곁가지.** (사실 관계로는 `dismantleUIView`도
  프로토콜 멤버가 맞다. 틀려서 뺀 게 아니라 **자리를 차지할 이유가 없어서** 뺐다.)
- **넣었다 — 카메라 미리보기 대조.** 사용자가 자기 다른 프로젝트 코드(`CameraPreview`)를 가져와
  *"여기에는 makeCoordinator가 없는데 이거는 어떻게 된 거야?"*라고 물었다. **「둘지 말지」 판정의
  안 두는 쪽 실물**이라 구간 ② 7번으로 편입했다(출처 명시). 판정 두 줄 — 델리게이트 안 받음 ·
  `updateUIView`가 비어 있어 기억할 것 없음.

### 구간 ③ — 질의로 정밀해진 것 셋

1. 🔴 **「인식기를 만들 때 무조건 꺼둔다」가 부정확했다.** 실제로는 **`drawGR`만** 모드를 안 보고
   `false`이고(`:209`), `tapGR`·`touchObserver`는 **그 시점의 모드에 맞춘다**(`:217`·`:228`,
   `!isDrawingMode`). 사용자가 *"앱을 켰을 때 설정이 탭 모드니까"*로 짚어서 확인했고,
   `CoursePlannerPageViewModel:35`가 `interactionMode = .tap`이라 **진입 시 탭 쪽 둘이 켜진 채로
   화면에 붙는다** — `updateUIView`를 기다리는 것이 아니다. **노트에 이 세 줄을 그대로 넣었다.**
   - `drawGR`의 비대칭은 진입 모드가 항상 탭이라 **결과가 같아서 드러나지 않는다.** 사실만 적고
     백로그로 보내지 않았다(동작 문제가 아니다).
2. ⚠️ **배타 활성화의 「이유」는 코드·문서에 없다.** 인식기 셋을 모드마다 배타적으로 켜고 끄는 것은
   사실이지만(청크 1 노트 표 + `:332-343`), **왜 그렇게 했는지 적힌 근거는 못 찾았다.**
   사용자가 *"충돌 때문이야?"*로 물었고 **추론이라고 밝히고 답했다.** 회전·기울이기 쪽만
   청크 1이 이유를 적어뒀다(*"둘 다 두 손가락 동작이라 그리기와 섞일 일이 없게"*).
3. 🆕 **`makeUIView`가 `Coordinator`에 하는 일은 「연결」이다.** 사용자 질문 —
   *"Coordinator는 updateUIView와 SwiftUI의 다리라면서 왜 makeUIView 안에서 쓰이는 거야?"*
   **좋은 모순 지적이었고**, 답은 `:196`·`:201`·`:211`이 전부 **값을 주고받는 것이 아니라 선을
   이어두는 것**이라는 점이다. 그래서 **`makeUIView`는 값이 오가는 고리에 없다** —
   이 정리를 구간 ③ 5번으로 넣었다.

### 📌 다음 세션이 알아둘 것 — 「감싼다 / 표현된다」가 두 번 흔들렸다

사용자가 *"MKMapView를 UIViewRepresentable로 감싸서 **MapViewRepresentable를** SwiftUI에
표현한다"*로 정리한 적이 있다. **표현되는 쪽은 `MKMapView`이고 `MapViewRepresentable`은
화면에 아무것도 그리지 않는다.** 그 자리에서 바로잡았지만 **같은 구분이 두 번 흔들렸다.**

- **구간 ⑤·⑥이 정확히 이 구분 위에 선다** — *"`struct`는 매번 새로 만들어진다"*(⑤)와
  *"무엇을 어디에 두나"*(⑥)는 **둘이 따로 있고 수명이 다르다**를 전제한다.
- **보강은 사용자가 접었다**(*"그냥 넘어가자"*, 2026-08-16). 구간 ⓪에 칸을 더하지 않았다.
  **⑤를 열 때 이 구분을 다시 한 줄 이어주는 것으로 대신한다**(「이름은 병기한다」와 같은 처방).

### 구간 ④ — 내 표현 셋이 막았고, 카메라 설명을 세 번 다시 열었다

🔴 **막은 것은 개념이 아니라 내가 쓴 말이었다.** 셋 다 사용자가 짚었다.

| 쓴 말 | 왜 막았나 | 바꾼 말 |
|---|---|---|
| **검사** | 지어낸 말 — *"검사라는 표현도 잘 모르겠어"* | **비교**(회고가 쓰는 말 — *"count+좌표 비교 게이트"*) |
| **몸통** | SwiftUI에 `body`가 있어 그걸로 읽힌다 — *"어떤 몸통을 말하는거지? body를?"* | **`updateUIView` 안** |
| **「비교가 없으면」** | 가정법이라 *"카메라만 비교를 안 한다는 거야?"*로 읽혔다 | 가정법을 빼고 **실제로 도는 대로** 쓴다 |

**세 번째가 가장 컸다.** 없는 상황을 가정해 설명하면 **그게 지금 상태로 읽힌다.**
🔴 **앞으로 「~이 없으면 이렇게 된다」로 메커니즘을 설명하지 않는다** — 있는 대로 쓰고,
막는 대상은 결과로 보인다.

**카메라 설명이 세 번 만에 통했다.** 통한 것은 **장면과 시제**였다:

- ⏱ **함수 이름의 시제** — `didChange`(이미 바뀌었다 = 알림) ↔ `set`(이렇게 맞춰라 = 명령).
  이걸 대자 *"이동은 이미 손가락이 시켰다"*가 바로 잡혔다. **②-4(어원·본뜻)의 두 번째 적용 사례다.**
- 🎯 사용자 문장으로 도착한 곳 — *"이미 지도가 움직였을 때 이동이 끝난 것이고 … 또
  updateUIView가 불려야 하는데 이러면 중복이 된다는 거지?"* **「중복」이 사용자 말이라 그대로 쓴다**
  (「고리」·「무한루프」는 회고 인용에서만).

**정정 하나 — 「해당하는 `if`만 실행된다」는 사용자 말이 맞았다.** 내가 *"하나만은 아니다"*로
과잉 정정했고 사용자가 되물었다(*"결국 내 말대로 …아니야?"*). **없는 차이를 만들었다.**
그 자리에서 인정하고 물러섰다.

### 🎨 첫 도식 — `diagram-design`으로 정적 SVG를 넣었다

**청크 3의 첫 인라인 SVG 도식이다**(소개 1의 mermaid는 규칙 이전 것). 절차와 판단:

- **게이트 처리** — 프로젝트에 `.diagram-design` 마커가 없지만 **팔레트는 이미 정해져 있다**
  (노트 라이트 테마). `trace-study`가 *"노트의 색·글꼴을 기준으로 넘긴다"*고 명시하므로
  온보딩 질문 없이 통과시켰다. **다음에도 같이 처리한다.**
- **타입** Flowchart · 노드 7 · 화살표 6 · accent 2(판단 마름모 + 끊기는 결과). 예산 안.
- **내용은 「손가락으로 민 경우」** — 그것이 이번에 통한 그림이라 그대로 그렸다.
  마지막 칸 아래에 굵은 가로선 + *"여기서 끊긴다"*로 정지를 드러냈다.
- ⚠️ **`self_check.py`는 이 노트에 적용되지 않는다.** 돌리면 두 건이 뜨는데
  (`data-motion-root` 없음 · `script 1` 속성) **둘 다 애니메이션 전제 검사**이고,
  스크립트는 **독립 도식 HTML 파일**을 대상으로 한다. 우리는 학습 노트 안에 인라인으로 넣고
  모션을 쓰지 않으므로 해당 없다. **대신 직접 확인한 것:** 접근성 계약(`role="img"` ·
  `aria-labelledby` · `<title>`이 `<svg>` 첫 자식 · 접두어 `p1s4-*`) · 분기 연결선의
  rounded elbow(`r=8`) · 라벨 마스크와 선 사이 12px · 마스크가 마름모·노드와 안 겹침.
- **글자 크기는 4px 그리드보다 우리 규칙이 우선** — 한글 13px 이상(스킬 권장은 12px).
  「반복해서 틀린 것」의 *"작은 라벨도 0.82rem 이상"*이 이긴다.

### 구간 ⑤ — 두 번 다시 짰다. 원인은 첫 조각을 「성질」로 연 것

🔴 **③-2 위반이었다.** 첫 조각을 *"매번 새로 만들어진다"*라는 **성질**로 열고,
「지난번 값」이 **무엇인지 안 보여준 채로** 그것을 어디 둘지부터 논했다. 사용자 지적 —
*"전체적으로 지금 해당 구간에서 무슨말을 하고 싶은지 전혀 모르겠고, 그래서 그 안에 설명들도
이해가 안가."* 그리고 ③-2가 경고한 그대로 **「이 구간을 알면 무엇을 정할 수 있나」가
「어디에 둬야 하는지」라는 설계 판단**이었다.

**통한 순서 (재구성 2):** ①「지난번 값」실물 셋 → ②**탭 한 번이 화면까지 가는 길**(도식) →
③그 흐름의 ⑥이 핵심(적는 쪽/적히는 곳) → ④`MapViewRepresentable`에 두면 왜 안 되나 →
⑤델리게이트는 별개 이유 → ⑥같은 대입이 반대 방향도.

- 🔑 **사용자가 먼저 도식을 요청했다** — *"흐름도나 도식도로 표현하는건 어떻게 생각해? …
  그 값들의 변화에 따라 뭐가 불리고 최종으로 어떻게 되는지에 대해서 흐름을 알아야."*
  구간 ⑤는 도식 ✕로 잡혀 있었으나 **값의 출처와 행선지가 이 구간의 전부**라 넣었다.
  **다음에도 「값이 어디서 나서 어디로 가나」가 주제면 도식을 미리 잡는다.**
- **이름을 바꿨다** — *"`struct`는 매번 새로 만들어진다"* → *"`MapViewRepresentable`은…"*.
  사용자 제안(*"굳이 struct라고 표현을 하는것보다 MapViewRepresentable라고 하는건 어떻게 생각해?"*).
  **부품 이름을 주어로 두는 것이 맞다.**

### 정정 셋 (구간 ⑤에서)

1. 🔴 **「`updateUIView`가 가지고 있다」가 틀렸다.** 사용자 질문 —
   *"coordinator가 바뀌는데 왜 updateUIView가 가지고 있다고 하는거야?"*
   **적는 쪽(동작)은 `updateUIView`, 적히는 곳(보관)은 `Coordinator`.** 「가지고 있다」를
   양쪽에 써서 헷갈리게 했다. 노트에 이 두 줄을 명시했다.
2. 🔴 **흐름 설명에서 `rendererFor`를 잘못 넣었다.** 선택만 바뀐 경우는 **오버레이가 그대로라
   렌더러도 그대로**이고, `uiView.renderer(for:)`로 **이미 있는 것을 가져와** 고친다.
   `rendererFor`는 **오버레이가 새로 추가될 때**만 불린다. 노트에 경고로 넣었다.
3. **「해당하는 `if`만 실행된다」는 사용자 말이 맞았다**(구간 ④ 기록과 같은 건).

### 🆕 절대 원칙 — 줄 번호를 주어·제목으로 쓰지 않는다

사용자 요청 — *"`:237`은 반대 방향도 맡는다 => 이런 표현은 넣지마 실제 코드를 적던가해.
이렇게 하면 내가 다시 파일을 열어보거나 해야 하잖아."* `SKILL.md`「코드 스니펫 나열 금지」
바로 앞에 넣었다. **판별법: 줄 번호를 지우면 문장이 무너지는가.**

### 📏 파트 1이 길어진 것 — 재보고 「그대로 둔다」로 결정

사용자 질문 — *"질의를 통해 재구성하면 파트가 길어지는데 어쩔 수 없는거지?"* **실측했다.**

| | 자수 | 구간당 평균 |
|---|---|---|
| 파트 1 (구간 ⓪~⑤, ⑥⑦ 남음) | 19,059 → **약 26,000 예상** | **3,177** |
| 청크 2에서 가장 길었던 파트 6 (구간 8) | 12,983 | 1,623 |

**구간 수가 아니라 구간 하나의 밀도가 두 배다.** 선택지 셋을 냈고
(A 파트 쪼개기 → 9파트 · B 그대로 · C 이미 쓴 것 덜어내기), **처음에 A를 추천했다가 비용을
재보고 B로 뒤집었다.**

- **비용:** 산문 안 「파트 N」 26곳 + 장면↔부품 배지 13개 + 3분 복습 8줄 + 목차 8줄 +
  floating-toc + `agent-log` 표 = **60곳 이상.** 게다가 **이미 발행해 사용자가 읽은
  「파트 8에서 본다」가 전부 어긋난다.** 이 파일에서 **7파트/8파트 혼재로 이미 한 번 데었다.**
- **내용은 덜어낼 것이 없었다** — 여섯 구간 전부 능력 선언에 재료를 댄다. 능력 선언으로
  판정한 결과다(**구별은 사용자가 아니라 가르치는 쪽 몫**, 「아직 안 배운 내용에 판정을
  요구하지 않는다」).
- **길이가 정당한 이유:** 사용자 말 — *"uikit에 관한 것을 잘 사용하지 않았었고
  UIViewRepresentable에 대해서도 많이 쓰지 않아서."* **파트 1은 이 청크에서 유일하게
  「안 써본 도구」를 다룬다.** 파트 2~8은 전부 이 앱이 쓴 코드라 이만큼 안 나온다.

**대신 붙인 것 셋:** ①3분 복습 파트 1 줄에 *"이 파트만 유독 길다 — 유일하게 UIKit 골격"* 명시
②구간 표에 **⓪~③ 감싸기 자체 / ④~⑦ 이 앱의 값 흐름** 성격 구분
③🔴 **파트 2부터는 질의에서 나온 것을 「몇 주 뒤에 같은 자리에서 또 막힐 것인가」로 가르고,
아니면 `agent-log.md`로 보낸다.** 지금까지는 거의 다 노트에 넣었다.

> **다음에 같은 상황이 오면 다시 재지 말 것** — 파트를 쪼개는 비용은 **번호 참조 60곳 + 발행본
> 어긋남**이다. 쪼갤 거면 **파트 표를 확정하는 `(b-5)` 시점**에 해야 하고, 구간을 쓰기 시작한
> 뒤에는 늦다.

### 🔴 구간 ⑥ — 네 번 무너졌다. 원인은 능력 선언이 「선택」이 아니었던 것

**이 청크에서 가장 크게 실패한 구간이다.** 사용자 말로 끝났다 —
*"솔직히 글이 잘 읽히지도 않는다. 그냥 이렇게하고 다음 구간 진행하자."*
**끝까지 통하지 못한 채로 닫았다.**

**무너진 순서와 그때마다 든 기준:**

| 회차 | 기준 | 사용자가 짚은 것 |
|---|---|---|
| 1 | 「무엇을 SwiftUI에, 무엇을 `Coordinator`에」 | *"이 파트에서 무엇을 말하고 싶은지 모르겠다. 상태는 ViewModel이 갖는 거 아니야?"* |
| 2 | 「SwiftUI 쪽에서 읽는 사람이 있나」 | *"카메라도 지도에서밖에 사용 안 하는 거 아니야? 기준과 설명이 논리적이지가 않아"* — **맞는 반박이었다.** 다섯 곳 중 넷은 읽기가 아니라 **쓰기**였다 |
| 3 | 「지도 밖으로 나가야 하는가」(advisor 제안) | *"@Binding으로 표현해도 어차피 현재 페이지에서 사용하는 것 똑같은데"* — **맞다.** `cameraRegion`도 `saveCameraPosition`도 **같은 `CoursePlannerPage` 안**이라 「밖으로 나간다」가 성립하지 않는다 |
| 4 | 카메라 논증을 접고 「사실 / 기록」만 남김 | 읽히지 않는다 → 그대로 닫음 |

🔴 **근본 원인: 능력 선언이 「정할 수 있다」인데 실제로 정할 것이 없었다.**
앱 상태는 ViewModel(범위 밖) · 카메라는 SwiftUI(다른 선택지 없음) · 기록은 `Coordinator`
(`struct`에 못 두니 선택지 없음). **셋 다 강제된 결과인데 「정하는 법」으로 포장했다.**
`(b-5)`가 파트 4에서 잡았던 것과 같은 종류인데 **`(b-4)`도 advisor도 못 잡았고, 써보고서야 드러났다.**

⚠️ **카메라 소유권의 진짜 이유는 `CameraStateStore`(저장·복원)인데 그것은 파트 1에서 열지 않기로
한 것이다.** 근거를 못 보여주니 결론만 네 번 떠 있었다. **범위 밖 근거에 기대는 결론은 구간에
넣지 말 것** — 넣으려면 근거를 열든지, 사실만 대고 *"이 파트 밖"*이라 명시하든지 둘 중 하나다.

**바꾼 것:**
- **파트 1 능력 선언 교체** — *"어떤 상태를 SwiftUI에 두고 어떤 상태를 `Coordinator`에 둘지
  정할 수 있다"* → **"무엇을 구현해야 하고, 어떤 값이 어디에 남아야 하는지 가를 수 있다"**.
  3분 복습 + 파트 머리 `goal` 두 곳. **파트 2~8의 선언은 확인해보니 전부 실제 선택이라 그대로 둔다**
  (파트 1만 골격 소개라 「선택」이 아니었다).
- **구간 ⑥ 주제 교체** — *"무엇을 SwiftUI에…"* → **"값은 세 군데에 나뉘어 있다"**.
- **버린 것** — 카메라 다섯 줄 표(`:숫자`만 있어 정보가 없었다) · *"반영 기록을 ViewModel에 둘 수
  없다"* 절(⑤ 반복) · 무한루프 논증(근거 부실) · 도식(⑤가 이미 흐름도를 갖고 있어 반복).
- **남은 것 하나가 이 구간의 값이다** — `selectedSegmentIndex`(앱이 아는 사실) ↔
  `lastSelectedIndex`(지도를 손본 기록). **이름이 비슷해 실제로 사용자가 막혔고, 코드를 열면 또 막힌다.**

**표현 지적 둘:** 「자」(잣대 뜻으로 썼다) → **「기준」** · 「지도 밖으로 나간다」 → 폐기.
그리고 **방금 만든 「줄 번호를 주어·제목으로 쓰지 않는다」를 표의 열 제목에서 그대로 어겼다**
(*"어디서: `:164`"*). **규칙을 만든 그 턴에 어겼다.**

🔑 **구간 ⑦에서 다르게 할 것** — ⑥이 네 번 다시 쓰이며 **정정·단서·「이 파트 밖」 표시가 본문에
계속 붙었고**, 그것이 읽히지 않은 직접 원인이다. ⑦은 **처음부터 실물로 열고, 단서를 본문에 섞지
말고, 짧게** 쓴다.

### 구간 ⑦ — 제목이 결론이었고, 주인공 함수를 설명 안 했다

**두 번 고쳐서 통했다.** 걸린 것 둘 다 사용자가 짚었다.

1. 🔴 **`rendererFor`가 무엇인지 한 번도 말한 적이 없다.** 구간 ②·③에서 이름만 스쳤는데
   ⑦이 그것을 주인공으로 삼았다. 사용자 지적 — *"rendererFor가 뭐야? 애플에 내장되어 있는
   함수인 거야? 각 함수들에 대한 설명들이 너무 부족해."*
   **통한 설명:** MapKit이 **오버레이(어디에 있나 · 좌표만)와 렌더러(어떻게 그리나 · 색·굵기)를
   갈라놨다** → 얹기만 해서는 아무것도 안 보인다 → `rendererFor`는 *"그릴 도구를 줘"*라는 물음.
   그리고 **②-4(어원)를 다시 썼다** — `render`(그리다) / `renderer`(그리는 것) /
   `rendererFor(overlay:)`(이 오버레이를 위한 렌더러). 사용자가 *"rendererFor가 렌더링하는
   역할"*로 정리했을 때 **직접 그리는 것은 돌려받은 렌더러**라고 한 줄 갈랐다.
   > 🔑 **파트 2부터: 처음 나오는 함수는 「무엇을 하는 함수인지」 한 줄을 반드시 붙인다.**
   > 이 노트에서 `viewFor`·`syncHalo`·`configureRenderer`가 아직 이름만 나와 있다.
2. 🔴 **제목이 결론이었다.** *"델리게이트는 내가 부르지 않는다"* → 사용자 —
   *"구간 제목부터 와닿지가 않아."* **결론은 읽고 나서 얻는 것이지 제목이 아니다.**
   → **「얹는 일과 그리는 방법은 갈라져 있다」**로 교체. 이것은 사용자가 스스로 정리한 문장
   (*"updateUIView가 하는 일은 얹는 것까지이고, 그 특성에 대한 설정은 다른 곳에서 한다"*)과 같다.
   **사용자 표현을 제목으로 쓰니 그 자리에서 통했다.**

**범위도 좁혔다** — 초안이 *"그래서 오버레이에 번호를 실어뒀다"*까지 갔는데 **그것은 파트 2의
주제**다(`(b-5)`가 *"서브클래싱을 강제하는 것은 `rendererFor`가 오버레이 하나만 건네받는다는
사실"*이라고 배정해 뒀다). ⑦은 **갈라져 있다는 사실과 그 이유**까지만 두고 파트 2로 넘겼다.

**표면 전수 재확인(사용자 요청 — *"이번 파트에서 다른 부분에 대한 것도 다 확인을 한 거야?"*):**
델리게이트 셋 중 `viewFor`가 파트 1 어디에도 안 나와 있었다. 소개 1이 **실측으로 「구현한
델리게이트 3개」**라고 적어뒀으므로 ⑦ 4번에 **셋을 세는 표**를 넣어 닫았다
(`rendererFor` 이 구간 · `viewFor` 파트 2·5·6 · `mapViewDidChangeVisibleRegion` 구간 ④).
나머지 파트 1 몫은 전부 배정돼 있었다.

### 파트 1 완료 (2026-08-17) — 8구간

3분 복습의 「붙들 것」 여덟 줄을 **구간 순서대로** 채웠다. 파트 1 한 줄과 능력 선언은
파트 머리의 것을 그대로 옮겼다(줄이지 않았다).

**이 파트에서 값을 치른 것 넷** — ①구간 ⓪ 신설(「감싼다」에서 먼저 막혔다) ②구간 ⑤ 두 번 재구성
(첫 조각을 성질로 열었다) ③**구간 ⑥ 네 번 무너짐**(능력 선언이 선택이 아니었다) ④구간 ⑦ 두 번
(주인공 함수 미설명 + 제목이 결론). **넷 다 「무엇을 먼저 보여주느냐」 문제였다.**

## ✅ 파트 1 팩트체크 (2026-08-17) — `(d)`를 파트 단위로 건 첫 사례

**사용자 제안으로 `(d)`를 파트 단위로도 걸기로 하고**(근거: 청크 2에서 고침 4건이 **전부 파트 7**에
몰렸다 — 전체를 한 번에 받으면 마지막에 읽은 것만 선명하게 걸린다) **파트 1에 소급 적용했다.**

**고침 3건 — 내역은 노트 「팩트체크 수정 내역」 절.**

1. 🔴 **구간 ⑤ — 「`rendererFor`가 불리는 것은 오버레이가 새로 추가될 때다」가 확인 안 된 주장.**
   MapKit의 호출 시점은 보장된 바가 없다. **이 코드가 실제로 기대는 것은 「그 시점에 렌더러가 이미
   있다」뿐**(`renderer(for:)`가 nil이 아닌 것을 돌려준다). **파트 2가 구간 ⑦ 위에 서므로 이것만
   파트 2를 막고 있었다.**
2. 0.0001도 → **미터 환산이 위도에만 맞다.** 경도는 서울에서 9m쯤이고 `spanDiff`는 거리가 아니라
   **다른 축**이다.
3. **Apple 문서 인용 다섯에 출처가 없었다.** 코드는 전부 `파일:줄`을 달았는데 문서만 비어 있었다.

**통과:** 구간 ②의 요구사항 셋 · 구간 ③의 회고 인용 · 구간 ④의 게이트 다섯과 조건 줄 ·
구간 ⑤의 `:237`·`:291`·`:296` · 구간 ⑥의 `cameraRegion` 쓰기 넷 읽기 하나.

> 🔑 **왜 파트 1 팩트체크를 그 세션에서 바로 돌렸나** — advisor가 **그 세션의 대화 기록 전체**를
> 본다. 파트를 쓰면서 **무엇을 코드로 확인했고 무엇을 추론으로 적었는지**가 거기 남아 있어서
> 「이 문장의 근거가 무엇인가」를 가릴 수 있다. **새 세션은 노트만 보고 검토하게 되어 얕아진다.**
> **파트를 끝낸 그 세션에서 거는 것을 기본으로 한다.**

---

## ✅ 파트 2 착수 메모 (2026-08-18 작성 · **파트 2 완료로 소진됨**)

> **옛 메모(「지도에 얹는 것을 어떤 타입으로 만드나」)는 폐기됐다.** 이것이 유효한 메모다.

**파트 2 — 선 한가운데 거리 라벨** (장면 ②)

### 화면에서 무엇인가

구간 선 한가운데에 뜨는 **작은 색 라벨**(`"320m"`). 배경색이 **그 구간의 선 색과 같다**.
파트 1이 「선」이었다면 파트 2는 **그 선 위에 붙는 글자**다.

### 재료 — 경로 확인 2026-08-18 (⚠️ **전수는 `(b-4)`에서 코드로 다시 뽑는다**)

전부 `Trace/Pages/CoursePlannerPage/MapViewRepresentable.swift` 한 파일 안에 있다.

| 무엇 | 줄 | 하는 일 |
|---|---|---|
| `SegmentDistanceAnnotation` | `:65` | `MKAnnotation` 채택. `coordinate` · `distanceText` · `color` 셋만 가진다 |
| `SegmentDistanceAnnotationView` | `:77` | `MKAnnotationView` 상속. 안에 `UILabel` 하나. `canShowCallout = false` |
| `configure(text:color:)` | `:95` | 텍스트·배경색 넣고 `sizeToFit` → `bounds = label.bounds` |
| 만들어 추가하는 곳 | `:281` | `midpointAlongPath(coords)` · `"%.0fm"` · `SegmentPalette.color(at: colorKey)` |
| 지우는 곳 | `:258` | `annotations.filter { $0 is SegmentDistanceAnnotation }` — **오버레이 전량 교체와 같은 게이트 안** |
| `midpointAlongPath` | `:374` | 누적 거리를 재서 **실거리 절반 지점을 보간**해 돌려준다 |
| `viewFor`의 라벨 분기 | `:454` | `dequeueReusableAnnotationView` → 없으면 새로 만들고 `configure` |

### 🔑 능력 선언 — **진짜 결정이 무엇인지 이미 확인됐다**

청크 3 `(b-5)` advisor 검토에서 **옛 파트 4(= 지금 파트 2)의 능력 선언이 결정이 아니라고 걸렸다** —
*"뷰를 직접 만들지 정할 수 있다"*는 반대편이 없었고, **실제 결정은
「라벨을 배열 인덱스 절반에 찍을지, 실거리 절반에 찍을지」**였다.
**그 반대편은 실재한다 — 인덱스 절반이 실제로 먼저 있었고 갈아엎었다.**

### (b-2) 깨져서 고친 것 — **이 파트의 핵심 재료 셋. 전부 실기기 QA에서 나왔다**

1. **라벨 위치가 랜덤해 보였다** — 배열 인덱스 절반이었는데 **라우팅 좌표는 밀도가 균일하지 않다**
   (커브에 촘촘, 직선에 듬성). 실거리 절반 보간으로 교체(`117f821`, 2026-07-04).
   **암묵적 가정이 깨진 사례**라 `260704_daily_retro.md`에 회고까지 있다.
   코드 주석(`:371-373`)이 이유를 적어 뒀다.
2. **경유점이 라벨을 가렸다** — `zPosition`과 추가 순서 재정렬을 **두 번 다 실기기에서 실패**하고,
   **경유점을 어노테이션에서 오버레이로 옮겨** 해결(2026-07-04). 근거는 *"MapKit은 오버레이를 항상
   어노테이션보다 아래에 그린다"*는 **구조적 보장**이고 주석(`:104-107`)에 있다.
   🔑 **파트 1에서 확인 안 됐던 「나중에 추가한 것이 위」와 달리, 이건 층이 다른 이야기다** — 섞지 말 것.
   소개 3 소유권 절이 이 사건을 **파트 4(흰 점)** 몫으로 배정해 뒀다. **파트 2는 「라벨이 왜 안 가려지나」까지만.**
3. **라벨과 출발/도착 핀이 겹치면 핀이 사라졌다** — MapKit 기본 `collisionMode`가 핀을 감췄다.
   `displayPriority = .required` · `collisionMode = .none`으로 고쳤다(MVP7).
   **핀 쪽 이야기라 파트 3 몫**이지만, **원인이 라벨과의 충돌**이라 파트 2에서 한 줄 짚고 넘긴다.

문서: `history/mvp9/2026-07-04-edit-consistency-device-checklist.md:15-21,70-72` ·
`history/mvp7/260701_mvp7_completion_retro.md:50` · `history/daily-retro/260704_daily_retro.md:20,35`

### 파트 1이 넘긴 것 — 이어서 받는다

- **`SegmentPalette.color(at: colorKey)`를 라벨도 쓴다** — 선과 라벨이 같은 색인 이유가 이것이다.
  파트 1 구간 ①에서 이미 세웠으므로 **다시 세우지 말고 이어받는다.**
- **`rendererFor`(오버레이) ↔ `viewFor`(어노테이션)** 갈래는 파트 1 구간 ①과 골격에서 이름과 역할을
  이미 밝혔다. **파트 2는 `viewFor`의 자기 분기만 가져간다**(소개 3 소유권 절).
- **어노테이션은 오버레이와 다른 것**이라는 구분은 **파트 2가 처음 여는 자리다** —
  ③-2대로 **「무엇을 하나 → 없으면 뭐가 깨지나」**로 연다.

### ⚠️ 조심할 것 — 파트 1에서 advisor가 잡은 것이 그대로 걸린다

1. **처음 나오는 이름에 한 줄 설명을 붙인다.** 파트 2에서 처음 나오는 것:
   `MKAnnotation` · `MKAnnotationView` · `dequeueReusableAnnotationView` · `canShowCallout` ·
   `sizeToFit`/`bounds`. **`(b-4)` 입력에 「한 줄 설명 있음/없음」을 채운다.**
2. **도식을 그렸으면 코드로 한 번 대조한다.** 파트 1에서 관문이 없는데 관문으로 그렸다.
3. **앞참조(「파트 N에서 본다」)를 쓸 때 현재 파트 번호표로 확인한다.** 구성 개편 뒤 낡은 번호가
   표·본문·진행 상태 파일에서 세 번 나왔다.
4. **저장·발행은 구간 단위**이고, 발행은 **`url` 인자 + 사전 `WebFetch`**가 필요하다.
5. **`(d)` 팩트체크를 파트 2를 끝낸 그 세션에서 바로 건다.** 못 받으면 `🔴`로 남긴다 —
   **다른 세션에서 본문 전문 + 근거 코드를 다시 올려 걸면 통한다**(2026-08-18 실증).

---

## 📋 파트 2 재료 전수 — 코드에서 다시 뽑았다 (2026-08-18, `(b-4)` 입력)

> 착수 메모의 재료 7건은 **전부 줄 번호까지 맞았다.** 아래는 그것을 포함한 **전수**다.
> `(b-4)`가 요구하는 「표면 전수 + 배정」의 왼쪽 절반이고, **배정은 구간 표를 짜면서 채운다.**

### 표면 전수 — `MapViewRepresentable.swift` (688줄)

| # | 표면 | 줄 |
|---|---|---|
| 1 | `SegmentDistanceAnnotation` 선언 — `NSObject, MKAnnotation` 채택 | `:65` |
| 2 | 프로퍼티 셋 `coordinate`·`distanceText`·`color` (전부 `let`) | `:66-68` |
| 3 | `init(coordinate:distanceText:color:)` | `:70-74` |
| 4 | `SegmentDistanceAnnotationView` 선언 — `MKAnnotationView` 상속 | `:77` |
| 5 | `label: UILabel` 스타일 — 11pt semibold · 흰 글자 · 가운데 · `cornerRadius 4` · `masksToBounds` | `:78-87` |
| 6 | `canShowCallout = false` | `:88` |
| 7 | `init?(coder:)` → `fatalError` | `:91-93` |
| 8 | `configure(text:color:)` — 텍스트 양옆 공백 · 배경색 · `sizeToFit()` · `bounds = label.bounds` · `centerOffset = .zero` | `:95-101` |
| 9 | 라벨 생성·추가 — `midpointAlongPath(coords)` · `String(format: "%.0fm", segment.distanceMeters)` · `SegmentPalette.color(at: colorKey)` · `addAnnotation` | `:281-287` |
| 10 | 라벨 제거 — `removeAnnotations(uiView.annotations.filter { $0 is SegmentDistanceAnnotation })` | `:258` |
| 11 | 스냅샷 게이트 `lastSegmentSnapshots != currentSnapshots` — 9·10이 **이 안에 있다** | `:256` |
| 12 | `midpointAlongPath` — 누적거리 배열 → 절반 → 두 좌표 사이 선형 보간 · 폴백 `coords[count / 2]` | `:374-397` |
| 13 | `viewFor`의 라벨 분기 — identifier `"segmentDistance"` · `dequeueReusableAnnotationView` · `view.annotation` 재대입 · `configure` | `:454-462` |
| 14 | `viewFor`가 **먼저** 걸러내는 것 — `annotation is MKUserLocation` → `nil` | `:451-453` |
| 15 | 층 주석 — *"MapKit은 오버레이를 항상 애노테이션보다 아래에 그린다"* | `:104-107` |
| 16 | 핀 쪽 `displayPriority = .required` · `collisionMode = .none` (주석이 **거리 라벨을 원인으로 명시**) | `:468-470` |
| 17 | 라벨이 쓰는 좌표는 `displayCoordinates`(겹침 오프셋된 **표시 좌표**)다 — 도메인 좌표가 아니다 | `:261-266` |
| 18 | 라벨 텍스트의 값 출처 — `CourseSegment`의 연관값 `distanceMeters` | `CourseSegment.swift:3-6` |
| 19 | 색 출처 — `SegmentPalette.color(at:)` = `UIColor(named: "Seg\(index % 6)") ?? .systemBlue` | `SegmentPalette.swift:5-7` |

### 🔑 새로 확인한 사실 셋 (착수 메모에 없던 것)

1. **`SegmentDistanceAnnotation`·`midpointAlongPath`는 이 파일 밖에서 한 번도 쓰이지 않는다.**
   앱 전체 grep 결과 참조 0건. **테스트도 0건**이다(`TraceTests` 어디에서도 안 나온다).
   → 「사실은 노트에」 규칙에 따라 **한 줄로 남긴다.**
2. **라벨 좌표는 표시 좌표(`displayCoordinates`) 기준이다.** 겹침 오프셋으로 옆으로 비켜 그린
   좌표를 그대로 받으므로, **라벨도 비켜난 선을 따라간다.** 파트 6(겹친 선)과 이어지는 자리다.
3. **`viewFor`는 `ColoredPinAnnotation`도 아니면 `nil`을 돌려준다**(`:463`) — MapKit 기본 뷰로 넘어간다.

### `(b-3)` 판정 — `viewFor`는 줄 순서 = 판정 순서다

분기 진입 조건 셋을 전부 뽑았다: ①`is MKUserLocation` → `nil` ②`as? SegmentDistanceAnnotation`
③`guard as? ColoredPinAnnotation else { return nil }`. **모든 분기에 공통으로 붙은 부정 조건이 없어서**
청크 2의 `attach`처럼 파일 아래쪽이 먼저 걸러지는 구조가 **아니다.**
**가장 먼저 걸러지는 것은 파란 점(`MKUserLocation`)이고 그것이 파일에서도 맨 위다.**

### `(b-2)` 셋 — 1차 출처를 전부 확인했다

1. **라벨 위치가 랜덤해 보였다** — 커밋 `117f821` *"거리 라벨 위치를 배열 인덱스 대신 실거리 절반 지점으로"*.
   원인은 *"라우팅 좌표 밀도가 불균일해(커브 촘촘, 직선 듬성)"*. 회고의 판정 —
   *"인덱스 기반 위치는 좌표 밀도가 균일하다는 암묵적 가정이 깨지는 순간 바로 틀어진다 …
   실거리 기준으로 바꿔 가정 자체를 없앰"*(`history/daily-retro/260704_daily_retro.md`).
   **반대편이 실재했고 갈아엎었다 — 이 파트의 진짜 결정이다.**
2. **경유점이 라벨을 가렸다** — 커밋 `fb1069f`. QA 문서에 *"두 번의 시도(zPosition, 추가 순서 재정렬)가
   모두 실기기에서 재발"* → *"애노테이션끼리의 z-order가 애초에 안정적으로 보장 안 된다"*로 파악하고
   층을 옮겼다. 회고의 교훈 — *"같은 층위 안에서 순서 조정보다 다른 층위로 옮기기가 더 안정적"*.
   **파트 4 몫. 파트 2는 「라벨이 왜 안 가려지나」까지만.**
3. **라벨과 핀이 겹치면 핀이 사라졌다** — `history/mvp7/260701_mvp7_completion_retro.md` —
   *"MapKit이 기본 `collisionMode`로 핀을 자동으로 숨겨버린다 … 원인이 라우팅 로직이 아니라
   순수 렌더링 레이어의 충돌 처리였다"*. **파트 3 몫. 원인이 라벨이라 파트 2에서 한 줄.**

### 처음 나오는 이름 — 노트 등장 횟수 실측 (2026-08-18)

**0회:** `MKAnnotation`(단독 0 — 기존 1건은 `MKAnnotationView`의 부분 문자열이었다) ·
`MKMarkerAnnotationView` · `canShowCallout` · `sizeToFit` · `centerOffset` ·
`displayPriority` · `collisionMode` · `removeAnnotations`
**1회:** `MKAnnotationView`(설명 있음 — *"그 어노테이션을 그릴 뷰"*) · `dequeueReusableAnnotationView` ·
`SegmentDistanceAnnotation` · `UILabel` · `distanceMeters` · `addAnnotation`
**2회 이상:** `midpointAlongPath`(2) · `어노테이션`(5) · `viewFor`(7 — **한 줄 설명이 이미 있다**,
*"어노테이션을 …"* `:1011`) · `SegmentPalette`(9) · `colorKey`(13)

⚠️ **착수 메모의 *"`viewFor`는 이름만 7번 나오고 무엇을 하는 함수인지는 한 번도 안 나왔다"*는
지금은 사실이 아니다** — 파트 1에서 `rendererFor`와 나란히 설명이 들어갔다. **다시 처음부터
열지 말고 이어받는다.**

### 다음 — 여기서 이어서 한다

**구간 표 초안을 짜고 `(b-4)` advisor 검토를 건다.** 검토에 넘길 것은 다섯이다 —
①구간 표 ②위 표면 전수 19건 + **각 줄이 어느 구간에 배정됐는지** ③각 구간 능력 선언의 「반대편」
④각 구간의 첫 조각 ⑤위 등장 횟수 + 「한 줄 설명 있음/없음」.
**통과한 뒤에** 사용자에게 내고, 골격(파트 머리 + 구간 표)을 노트에 넣어 발행한다
(`url` 인자 + 사전 `WebFetch`). 그 발행이 `note.html:3146`의 낡은 예고
(*"다음 — 파트 2 「지도에 추가하는 것을 어떤 타입으로 만드나」"*)를 함께 걷어낸다.

---

## ✅ 파트 2 구성 확정 (2026-08-19) — 4구간. `(b-4)`는 절반만 받았다

### `(b-4)` advisor 검토 — **받은 것 하나, 못 받은 것 넷**

입력 다섯(구간 표 5구간 · 표면 전수 19건 배정 · 반대편 · 첫 조각 · 이름 등장 실측)을 갖춰 걸었다.
**응답이 내 구간 표가 아니라 다른 대상을 보고 왔다** — note.html 구조를 읽으라는 지시, 3구간 구성,
발행 절차 이야기였다. 파트 1 `(d)`가 네 번 실패했을 때와 **증상이 같다**(요청 범위를 못박아도
다른 것을 본다).

🔑 **그런데 그 안에 유효한 지적이 하나 있었고, 그것이 정확했다.** advisor 본인이
*"One content item that survives whatever the structure turns out to be"*로 분리해 냈다:

> **층 규칙을 「라벨은 항상 위」로 일반화하면 안 된다.** 흰 점(경유점)과 선은 **둘 다 오버레이**라
> 라벨이 항상 위인 것이 맞지만, **라벨 대 핀은 어노테이션끼리**라 층 규칙이 **아무 말도 하지 않는다.**
> `displayPriority`/`collisionMode`가 존재하는 이유가 그것이다.
> 내가 그 줄을 *"파트 3 몫, 원인만 한 줄"*로 배정했는데 — **그 줄은 장식이 아니라 결론을
> 한정하는 줄이다**(*"that line is load-bearing, not garnish"*).

→ **반영했다.** 마지막 구간의 「알고 갈 것」은 **층 규칙이 닿는 범위까지만** 말하고,
**핀은 그 규칙 밖이라는 것을 그 자리에서 밝힌다.** 착수 메모의 경고
(*"파트 1에서 확인 안 됐던 「나중에 추가한 것이 위」와 달리, 이건 층이 다른 이야기다 — 섞지 말 것"*)와
같은 것을 advisor가 **반대편에서** 짚은 셈이다.

🔴 **못 받은 것 넷 — 다음 세션이 새 세션에서 재시도할 값이 있다.**
①구간 순서가 자연스러운가 ②한 개념이 두 구간에 걸치지 않았나 ③빠진 표면이 있나
④**구간 ③(색)의 반대편이 약한 문제** — 아래처럼 자체 판단으로 처리했으므로 **재시도해서
뒤집히면 그때 고친다.** 파트를 다 쓴 뒤 `(d)`를 걸 때 함께 확인해도 된다.
**`(b-4)`는 파트당 한 번이므로 이 세션에서는 다시 걸지 않는다.**

### 🔻 구간 ③(색)을 없애고 구간 ①에 붙였다 — 5구간 → **4구간**

`(b)`의 **「결정이 없는 것은 파트로 세우지 않는다 — 옆에 한두 줄로 붙인다」를 구간 단위로 적용**했다.
근거 셋:

1. **반대편이 약하다.** 라벨 색을 흰/검 고정으로 두는 길이 있었겠지만 **코드에도 문서에도 흔적이 없다.**
   다른 구간 넷은 반대편이 전부 코드나 커밋에 실재한다.
2. **파트 1에서 이미 배운 것이다.** `SegmentPalette` 9회 · `colorKey` 13회로 **설명까지 끝나 있다.**
   파트 2가 새로 여는 것은 *"라벨도 그것을 쓴다"* 한 줄뿐이다.
3. **착수 메모가 그렇게 지시했다** — *"파트 1 구간 ①에서 이미 세웠으므로 다시 세우지 말고 이어받는다."*

⚠️ **능력 선언의 「그 색으로」는 버리지 않는다** — 구간 ①이 그 자리에서 답한다.

### 최종 4구간

| | 구간 | 이 구간이 다루는 것 |
|---|---|---|
| ① | 선 위의 글자는 선이 아니라 따로 얹은 것이다 | 라벨은 `MKAnnotation`이다. 선(오버레이)과 갈래가 다른 이유 + **색을 어디서 받는지(이어받기)** |
| ② | 라벨을 선의 어디에 찍나 — 인덱스 절반과 실거리 절반 | `midpointAlongPath`. **이 파트의 진짜 결정** |
| ③ | 그 데이터를 화면의 글자로 바꾸는 것은 누가 하나 | `viewFor` + `SegmentDistanceAnnotationView`. 기성품이냐 직접 만드냐 |
| ④ | 라벨이 경유점에 안 가려지는 이유 | 어노테이션 층은 오버레이 층보다 항상 위다. **핀은 그 규칙 밖이다** |

**순서 근거:** ①이 「이게 무엇이고 무엇을 담나」, ②가 「어디에 놓나」, ③이 「그것을 뭘로 그리나」,
④가 「그려진 것이 어느 층에 놓이나」. ③을 뒤에 둔 것은 파트 1이 `rendererFor`(그리는 쪽)를
뒤에 둔 것과 같은 축이다. ④는 ①(어노테이션 층)과 ③(뷰)을 다 알아야 성립한다.

### 표면 배정 — 19건 전부, 빈칸 없음

①: 1·2·3·9(무엇을 만드나)·18·19 / ②: 9(좌표)·12·17 / ③: 4·5·6·7·8·13·14 /
④: 15 + **16(핀은 층 규칙 밖 — 결론을 한정하는 줄)** / 골격 2로 보냄: 10·11(파트 2는 한 줄로 짚는다)

### 각 구간 첫 조각 — 넷 다 실물 (③-2 위반 없음)

① `SegmentDistanceAnnotation` 선언 3줄(프로퍼티가 셋뿐) / ② 실기기에서 라벨이 엉뚱한 곳에 찍히던
장면 + 그때 코드였던 `coords.count / 2` / ③ `SegmentDistanceAnnotationView` 선언
(`MKAnnotationView` 상속 + 안에 `UILabel` 하나) / ④ 경유점이 거리 라벨을 가리던 실기기 장면.

### 한 줄 설명을 반드시 붙일 이름 — 실측 결과 대부분이 「이름만 나열」이었다

**설명 없음(0~1회, 전부 표 안의 나열):** `MKAnnotation`(0) · `SegmentDistanceAnnotation`(1) ·
`CourseSegment.distanceMeters`(0 — 노트의 `distanceMeters(toSegment:_:)`는 **다른 함수**) ·
`addAnnotation`(1) · `midpointAlongPath`(2, 둘 다 나열) · `MKMarkerAnnotationView`(0) ·
`dequeueReusableAnnotationView`(1) · `UILabel`(1) · `canShowCallout`(0) ·
`sizeToFit`/`bounds`/`centerOffset`(0) · `displayPriority`/`collisionMode`(0)
**이어받는 것(설명 있음):** `SegmentPalette`(9) · `colorKey`(13) · `MKAnnotationView`(1) · `viewFor`(7)

### 다음 — 골격을 노트에 넣고 발행한 뒤 구간 ①

파트 머리(한 줄 · 능력 선언 · **구간 표 4개 전부**)를 `note.html`에 넣고 목차·플로팅 목차·3분 복습의
파트 2 줄을 켠다. **`note.html`을 먼저 커밋하고 발행한다**(발행이 실패해도 마크업이 남는다).
발행은 `WebFetch`로 <https://claude.ai/code/artifact/8a7a02b4-6ed3-4214-9436-3291a7886aed>를
먼저 읽고 `url` 인자 + 같은 favicon으로. **`force`는 쓰지 않는다.**
그 발행이 `note.html`의 낡은 예고(*"다음 — 파트 2 「지도에 추가하는 것을 어떤 타입으로 만드나」"*)를
함께 걷어낸다 — **사용자에게 이미 보고했다.**

---

## ✅ `(b-4)` 재시도 — 두 번째에 제대로 받았다 (2026-08-19, 사용자 지시)

**사용자가 되물어서 다시 걸었다** — *"그러면 제대로된 검토가 안되었으니 다시 한번 진행해야 되는거 아니야?"*
맞는 지적이다. 검토를 못 받은 채로 그 위에 파트 전체를 쌓으면 **`(b-4)`의 존재 이유(되돌리는 비용)가
사라진다.** 스킬의 *"파트당 한 번"*은 **advisor가 느려서 진행이 눌리는 것**을 막는 조항이라,
**검토를 못 받은 경우까지 한 번으로 치는 규칙이 아니다.**

🔑 **같은 세션 두 번째 시도에서 통했다.** 파트 1 `(d)`는 같은 세션에서 다섯 번 실패하고 새 세션에서
통했는데, 여기는 **두 번째에 왔다.** 차이는 **범위를 「무엇이 범위 밖인지」까지 열거하고,
답을 받고 싶은 질문 넷을 번호로 못박은 것**이다. 다음에 빗나가면 이 형태를 먼저 쓴다.
⚠️ **다만 이번에도 첫 응답은 빗나갔다** — 앞 응답을 미련 없이 버리고(유효한 한 줄만 건지고)
다시 거는 것이 맞았다.

### 🔴 지적 1 — **발행된 능력 선언이 파트의 절반을 안 덮는다.** 골격 발행을 막는 문제다

`note.html:647`(이미 발행됨) — *"거리 라벨(선 위의 「320m」)이 **왜 그 자리에 그 색으로 붙는지**
설명할 수 있다"*

| 구간 | 능력 선언이 덮나 |
|---|---|
| ① 어노테이션이다 + 색을 어디서 받나 | **「그 색으로」** ✅ |
| ② 어디에 찍나 | **「그 자리에」** ✅ |
| ③ 무엇으로 그리나 (기성품이냐 직접 만드냐) | ❌ **안 덮인다** |
| ④ 어느 층에 놓이나 (핀은 예외) | ❌ **안 덮인다** |

**스킬이 「곁가지인지 아닌지는 능력 선언으로 판정한다」고 정해 뒀으므로, 문자 그대로 읽으면
파트의 절반이 곁가지다.** 넓히거나, ③④가 그 안에 든다는 것을 보여야 한다.

⚠️ **이 줄은 이미 사용자가 읽었다.** 이 세션의 선례가 *"이미 발행돼 사용자가 읽은 문장이라
조용히 못 고친다 → 그 자리에 넣고 발행 후 채팅으로 알렸다"*이므로 **사용자 승인을 먼저 받는다.**

**수정안(사용자 승인 대기):**
> 거리 라벨(선 위의 「320m」)이 **무엇으로 만들어져 왜 그 자리에 그 색으로 붙고,
> 왜 다른 것에 안 가려지는지** 설명할 수 있다

- **원문 「왜 그 자리에 그 색으로 붙는지」를 통째로 안에 보존**했다 — 사용자가 읽은 문장이 사라지지
  않고 앞뒤가 붙는 형태다.
- 「무엇으로 만들어져」가 ①(어노테이션)과 ③(직접 만든 뷰)을, 「안 가려지는지」가 ④를 덮는다.
- **파트 4의 능력 선언이 이미 타입 이야기를 담고 있다**(*"왜 선이 아니라 따로 만든 오버레이인지"*)
  므로 형태가 어긋나지 않는다.

### 🔴 지적 2 — ④가 ③에 의존한다는 **순서 근거가 근거 없다**

내가 *"④는 ①과 ③을 다 알아야 성립한다"*고 썼는데, **④의 첫 조각(경유점이 라벨을 가리던 장면)은
`viewFor`도 `MKAnnotationView`도 건드리지 않는다.** 어노테이션끼리의 z-order가 불안정해서 층을
옮긴 이야기라 **뷰를 어떻게 만드는지와 무관**하다. 내 판별법에 그대로 걸린다 —
*"그 구간의 첫 조각이 앞 구간을 한 번도 안 부르면 연결을 안 깐 것이다."*

**순서는 그대로 두고 근거 줄을 고쳤다**(`(b-0)`이 구간 표 아래 요구하는 그 줄이다):

> ①②③이 **라벨 하나를 만드는 순서**다 — ①이 무엇이고 무엇을 담나, ②가 어디에 놓나,
> ③이 무엇으로 그리나. ④는 **다 만들어 놓은 뒤 다른 것과 겹칠 때** 무슨 일이 생기는지라 마지막이다.
> **④가 필요로 하는 것은 ①(라벨이 어노테이션이라는 것)뿐이고 ③과는 무관하다.**

### ✅ advisor가 문제없다고 확인한 것 — 다시 열지 않는다

**색 구간 흡수**(5→4구간) · **핀 예외를 ④의 필수 요소로 승격** · **표면 19건 배정에 빈칸 없음**.
*"Everything else in the 4구간 … holds up. Don't reopen those."*

---

## 📤 파트 2 골격 저장·발행 (2026-08-19)

**사용자 승인을 받고 능력 선언을 고쳤다** — *"응 그렇게 수정하고 골격 저장하고 발행해줘."*

`note.html` 다섯 곳을 고쳤다:

1. **3분 복습 능력 선언** — *"왜 그 자리에 그 색으로 붙는지"* →
   *"**무엇으로 만들어져** 왜 그 자리에 그 색으로 붙고, **왜 다른 것에 안 가려지는지**"*.
   **원문을 통째로 안에 보존**해 이미 읽은 문장이 사라지지 않게 했다. `pending`도 풀고 `sum`을 넣었다
   (「붙들 것」은 구간이 다 찬 뒤에 채운다).
2. **파트 2 골격** — 파트 1과 골격 1 **사이**에 넣었다(`id="p2"`, `open`).
   머리(이 파트가 만드는 화면 · 능력 선언) + 구간 표 4개 + `hint`(순서 근거).
3. **목차** — 파트 2 줄을 `todo` → `on` + 링크로.
4. **플로팅 목차** — 파트 2 항목 신설(파트 1 뒤).
5. **`<div class="next">`** — 삭제된 옛 파트 2 제목(*"지도에 추가하는 것을 어떤 타입으로 만드나"*)을
   가리키던 것을 현재 구성으로 바꿨다.

**검증:** `details` 14/14 · `section` 4/4 · `div` 160/160 균형. 파트 2가 `:1586-1625`에 들어가
파트 1(`:1042-1584`)과 골격 1(`:1627`) 사이에 정확히 위치.

**절차:** ①`note.html`을 **먼저 커밋**(`375e749`)하고 ②`WebFetch`로 기존 URL을 읽은 뒤
③`url` 인자 + favicon `🗺️🎨`로 재발행. **`force`는 쓰지 않았다.**
같은 URL이 갱신됐다 — <https://claude.ai/code/artifact/8a7a02b4-6ed3-4214-9436-3291a7886aed>

⚠️ **`WebFetch`가 실제로 필요했다** — 이전 세션 발행본이라 안 읽으면 거절된다(3회 연속 재확인).

### 다음 — 구간 ①부터

**골격은 노트에 있다. 다시 만들지 말고 그 아래에 구간을 하나씩 붙인다.**
저장 단위는 구간이고 매번 재발행한다(`url` + 사전 `WebFetch`).
구간 ①의 첫 조각은 **`SegmentDistanceAnnotation` 선언 3줄**(프로퍼티가 `coordinate`·`distanceText`·`color`
셋뿐)이고, `MKAnnotation`은 **노트 등장 0회**라 한 줄 설명을 반드시 붙인다.

---

## ✅ 파트 2 구간 ① 완료 (2026-08-19) — 저장·발행됨

**질의에서 정정 넷이 나왔고 전부 본문에 반영했다.** 사용자 요청으로 **재구성한 뒤 저장**했다
(*"이전내용과 질의를 포함해서 내용을 이해하기 쉽게 재구성해줘 ... 내용이 너무 길어지면 안되고"*).

| # | 무엇이 걸렸나 | 어떻게 고쳤나 |
|---|---|---|
| 1 | 🔴 **프로퍼티를 셋이라 해놓고 둘만 말했다** — *"글자와 색만 있다"*에서 **좌표가 빠졌다.** 사용자가 *"2번에서 말하는 어노테이션은 다른 건가?"*까지 물었다 | *"들고 있는 것은 셋, **없는 것은 생김새**"*로 바꿨다. 하려던 말은 「적다」가 아니라 **「성격이 한쪽뿐이다」**였다 |
| 2 | 🔴 **「두 갈래」가 지어낸 표현이었다** — 사용자가 *"독립적이라는 건가?"*로 되물었다. **뜻이 정반대**다 | **`protocol MKOverlay : MKAnnotation`** 을 그대로 보이고 **상속**이라고 썼다. 3분 복습 요약의 같은 표현도 함께 고쳤다 |
| 3 | **`coordinate`·`boundingMapRect`의 출처를 안 밝혔다** | **둘 다 MapKit이 정한 이름**이고 `distanceText`·`color`만 이 앱이 추가한 것임을 1번에 넣었다 |
| 4 | 🔑 **「320m 하나인데 왜 타입이 여럿이냐」가 안 풀렸다** — *"실제 사용하는 곳에서 어떻게 사용이 되는지를 이해하고 싶은 것뿐이야. (이 부분은 코드적으로 표현된 부분을 보여주는 게 좋을 것 같아.)"* | **사용자가 코드를 요청했으므로 보여줬다**(스킬의 「메커니즘을 산문으로 먼저, 물으면 그때 코드」 순서 그대로). `:281` 생성 코드 + 타입 셋 표 |

### 🔴 착수 메모가 틀렸던 것 — 어노테이션은 오버레이와 「다른 것」이 아니다

메모에 *"어노테이션은 오버레이와 다른 것이라는 구분은 파트 2가 처음 여는 자리다"*라고 적혀 있었는데,
**`MKOverlay`가 `MKAnnotation`을 상속한다.** 애플 문서 —
*"Because the MKOverlay protocol conforms to the MKAnnotation protocol, all overlay objects can also
function as annotations."* **갈리는 것은 「다른 종류」가 아니라 요구사항 하나**(`boundingMapRect`)이고,
그래서 축이 **「점(location)이냐 영역(region)이냐」**로 정확해졌다.
🔑 **1차 출처를 `context7`로 조회해서 잡았다** — 메모대로 썼으면 틀린 채로 나갔다.

### 근거 (전부 1차 출처)

`MKAnnotation` 정의·필수 `coordinate`·*"do not provide the visual representation themselves … data
source"* · `MKOverlay` 정의·필수 둘 · `protocol MKOverlay : MKAnnotation` —
모두 `context7`(`/websites/developer_apple_mapkit`)로 확인.

### 다음 — 구간 ②

**「라벨을 선의 어디에 찍나 — 인덱스 절반과 실거리 절반」.** 이 파트의 **진짜 결정**이고
**반대편이 실재한다**(인덱스 절반이 먼저 있었고 `117f821`로 갈아엎었다).
📌 **코드를 읽다 찾은 것:** `midpointAlongPath`의 마지막 줄 폴백이 **`coords[coords.count / 2]`**,
곧 **갈아엎은 그 방식**이다. 그런데 `cumulative.last >= half`가 항상 참이라 **도달할 수 없는 줄**로
보인다 — 구간 ②에서 확인해 사실이면 남긴다(「이 코드가 무엇을 보장하지 않는지도 구조의 일부」).

---

## ✅ 파트 2 구간 ②③ 완료 (2026-08-19) — 값을 크게 치른 것 둘

### 구간 ② — 제목에서 옛 방식을 뺐다

사용자 지적 — *"인덱스 절반 방식에 대해서 사용하고 있지 않는데 왜 구간 제목이나 설명에 넣는 거야?"*
**맞는 지적이다.** 제목이 *"라벨을 선의 어디에 찍나 — 인덱스 절반과 실거리 절반"*이었는데
**지금 안 쓰는 방식이 제목의 절반**이었고, 읽기 전에는 「인덱스 절반」이 뭔지도 모른다.
→ **「라벨이 붙을 좌표는 어떻게 정하나」**로 바꿨다(구간 표도 함께).

**깨지고 고친 이야기는 본문에 남겼다.** `(b-2)` 때문만이 아니라 **실질적 이유**가 있다 —
그 이야기가 없으면 스무 줄짜리 `midpointAlongPath`가 왜 필요한지 설명이 어디에도 안 남고,
나중에 코드를 열었을 때 *"`count / 2` 한 줄이면 되는데"*가 된다.

📌 **폴백 줄(`coords[coords.count / 2]`)은 통째로 뺐다.** 도달 불가능한 줄인 것은 맞지만
**능력 선언에 안 걸리는 곁가지**였고, 사용자가 그 대목에서 막혔다(*"무슨 말인지 이해를 못하겠어.
더 알아야 하는 거야?"*). **깊이 질문에는 「몰라도 된다」를 먼저 답한다.**

### 구간 ③ — 소제목 하나를 통째로 없앴다

사용자 지적 — *"3번부터 이해가 약간 안 돼. 일단 소제목부터 무엇을 하고 싶은 이야기인지 모르겠고.
어쩌라는 거지 느낌?"* **판정을 요청받아 판정했고, 없애는 것이 맞았다.** 원인 둘:
①소제목(*"구간 ①에서 「없다」고 한 생김새가 전부 여기 있다"*)이 **위치만 가리키고 답이 없었다**
②내용(글꼴 11pt · 흰 글자 · 모서리 4)에 **정할 것이 없었다** — 왜 그렇게 정했는지가 코드에도
문서에도 없는 나열이다. **`(b)`의 「결정이 없는 것은 세우지 않는다」를 소제목 단위로 적용**했다.
→ 살린 것은 `canShowCallout = false` 하나(**기본값을 끈 것**이라 이유가 있다).

### 🔴 값을 크게 치른 것 1 — 내가 만든 갈래에 사용자가 막혔다

구간 ①에서 *"왜 데이터와 뷰를 안 합치나"*를 던지고 **구간 ③으로 미뤄뒀다.**
**사용자가 물은 적이 없는 질문이다.** 그걸 설명하려니 **UIKit 뷰 재사용**이라는 범위 밖 주제를
끌고 와야 했고, 사용자가 **바닥에서 막혔다** — *"애초에 이것에 대해서 기본 지식? 개념이 없어서
이해가 잘 안 되네? 그냥 그리고 놔두면 그대로인 거 아니야?"* · *"전반적으로 이해가 그냥 아예
처음부터 상황이 안 되는 것 같아."*

- **판별법: 「구간 N에서 본다」로 미루기 전에, 그 질문을 사용자가 물었는지 본다.** 내가 만든
  질문이면 미룰 것이 아니라 **애초에 안 여는 것**이다.
- **복구는 접는 것이었다** — 능력 선언(*"무엇으로 만들어져 왜 그 자리에 그 색으로 붙고 왜 안
  가려지는지"*)에 재사용이 **없다.** 곁가지 판정 후 두 문단으로 줄였다.
- 🔑 **사용자가 스스로 정리한 것을 채점해 준 것이 통했다** — *"결국 맵킷에 대한 방식에 대해서
  우리가 좀 더 효율적이게 사용해보기 위해서 재사용되도록 하기 위했던 거였어?"* →
  **절반만 맞다. `dequeueReusableAnnotationView`는 MapKit이 제공하는 표준 사용법이지
  이 앱의 결정이 아니다.** 이 구분이 들어가자 풀렸다.

### 🔴 값을 크게 치른 것 2 — 노트에 「내가 알아야 할 것」이 섞였다

사용자 지적 — *"먼가 내가 알아야 할것과 너가 알아야 할것들이 같이 들어간 느낌이랄까?"*
**규칙은 이미 있다**(`[4]` — *"`note.html`이 담는 것은 기능이 어떻게 동작하는가뿐"*).
**있는데도 깨졌다.** 섞여 있던 것:
*"붙들 것은 아니다"* · *"이 파트의 다른 결정들과 성격이 다르다"* · *"코드를 열었을 때 걸리지
않도록"* 이라는 제목 · 곁가지 판정.

- **판별법: 그 문장이 「무엇이 일어나는가」인가, 「어디까지 가르칠지」인가.**
  뒤엣것이면 `agent-log.md`로 간다.
- ⚠️ **앞참조(「파트 N이 다룬다」)는 남긴다** — 그건 소유권 안내라 노트에 필요하다.
  걷어낼 것은 **학습 운영 판정**이다.
- 규칙 텍스트를 늘리지 않았다. **판별법 한 줄을 여기 남긴다.**

### 다음 — 구간 ④

**「라벨이 경유점에 안 가려지는 이유」.** `(b-4)`가 잡은 것이 여기 걸린다 —
🔴 **「라벨은 항상 위」로 일반화하면 안 된다.** 흰 점과 선은 **둘 다 오버레이**라 층 규칙이
성립하지만, **라벨과 핀은 어노테이션끼리**라 층 규칙이 아무 말도 하지 않는다.
`displayPriority`/`collisionMode`(`:468-470`)가 존재하는 이유가 그것이고,
**그 줄은 곁다리가 아니라 결론을 한정하는 줄**이다.

---

## ✅ 파트 2 완료 (2026-08-19) — 4구간 + `(d)` 팩트체크 반영·재발행

🔑 **`(d)`를 파트를 끝낸 그 세션에서 받았다** — 스킬이 정한 자리 그대로이고, 파트 1처럼 새 세션으로
넘기지 않았다. **사용자가 범위를 미리 못박아 준 것이 컸다** — *"최근에 대화한 마지막 구간 위주가
아니라 전체적으로 확인을 해줘야 해."* 청크 2에서 **고침 4건이 전부 마지막 파트에서 나온** 전례를
막는 지시다. 결과도 그렇게 나왔다 — **구간 ①②④에 하나씩, 구간 ③은 깨끗**했다.

🔑 **통한 방법: 채팅이 아니라 「저장된 최종본」을 파일에서 추출해 올렸다.** 이 세션 채팅에는
재구성 **전** 버전이 섞여 있어서 그대로 넘기면 **이미 고친 것을 지적받는다.** 태그를 벗겨
텍스트 266줄로 만들어 넘겼다.

### 고침 3건 (전부 코드로 재확인함)

1. 🔴 **색 설명이 파트 1과 부딪혔다.** *"같은 곳에서 같은 값을 받는다"*로 뭉갰는데,
   **`SegmentPolyline`에는 `color`가 없다**(실측: `segmentIndex`·`colorKey` 둘 다 `Int`).
   선은 **번호만 싣고** `configureRenderer`가 `renderer.strokeColor`에 색을 넣는데,
   **라벨은 만들 때 `UIColor`를 얻어 들고 간다**(`let color: UIColor`).
   파트 1의 붙들 것이 *"색을 들고 있는 것은 오버레이가 아니라 렌더러"*라고 못박아 둬서
   **파트 1을 읽고 내려온 독자가 걸리는 자리**였다.
   → 결론(같은 함수·같은 번호·같은 색)은 두고 **「얻는 시점이 다르다」 문단을 붙였다.**
2. 🔴 ***"어떻게 보일지는 하나도 없다"*가 틀렸다** — **`color`가 예외다.**
   → *"무슨 색으로 칠할지는 들고 있지만 어떻게 생길지는 하나도 없다"*로 좁혔다.
   ⚠️ **사용자가 구간 ①에서 잡아낸 것과 같은 종류다**(*"셋이라 해놓고 둘만 말했다"*).
3. 🔴 **`MKDirections`가 설명 없이 처음 나왔다** — 노트 등장 실측 **1회 = 그 줄 하나뿐**.
   → *"애플이 준 길찾기 — 출발점과 도착점을 주면 도로를 따라가는 좌표 목록을 돌려준다
   (청크 1 파트 2에서 다룬 그것)"* 한 줄을 붙였다.

### 🔴 `(b-4)` 입력의 결함 — 다음 파트에서 고칠 것

**이름 등장 횟수 목록을 어노테이션 계열로만 뽑아서 `MKDirections`가 빠졌다.**
`(b-4)`를 두 번이나 돌았는데 안 걸린 이유가 이것이다 — **입력에 없으면 안 걸린다**(청크 3 파트 1의
교훈과 같은 구조).
🔑 **처방: 다음 파트부터 처음 나오는 이름을 「구간별로」 뽑는다.** 파트 주제(어노테이션)에서
연상되는 것만 뽑으면 **다른 구간이 끌어오는 이름이 통째로 샌다.**

### ✅ `displayPriority` 판정 — **죽은 줄이 아니다. 백로그에 올리지 않는다** (2026-08-19 완료)

> 이 항목은 원래 📌 *"`displayPriority`는 기본값과 같다"*였다. **전제가 틀렸다.**
> **트리거(「죽은 줄이면 백로그에 올린다」)는 발동하지 않았다 — 다시 열지 말 것.**

**무엇이 틀렸나.** 근거였던 *"Defaults to `required`"*는 **`MKAnnotationView`(베이스 클래스)
문서의 문장**이고, 코드가 실제로 만드는 것은 **`MKMarkerAnnotationView`**(`:464`)다.
그 클래스 문서는 이렇게 적는다:

> The default `displayPriority` for an instance of this class is `defaultLow`.

→ `view.displayPriority = .required`는 **`.defaultLow`를 `.required`로 올리는 살아 있는 줄이다.**
애플 문서의 두 문장은 서로 모순이 아니다 — **서브클래스 재정의를 못 보면 죽은 줄로 보인다.**

**🔴 MapKit JS 근거는 폐기한다.** *"collisionMode가 `.none`이면 priority를 무시한다"*는
**MapKit JS 문서의 문장이고 UIKit MapKit의 근거가 될 수 없다**(다른 프레임워크·다른 구현).
iOS `.none` 문서는 *"A constant indicating that collisions can't occur."* 한 줄뿐이고
**priority를 무시한다는 말은 어디에도 없다.**

**따라서 두 줄은 각자 문서화된 것만 적는다:**

| 줄 | 문서가 말하는 것 |
|---|---|
| `collisionMode = .none` | 이 뷰를 **어노테이션 겹침 판정에서 뺀다** |
| `displayPriority = .required` | 표시 순위를 **서브클래스 기본값 `.defaultLow` 위로 올린다** |

⚠️ **둘 중 하나가 다른 하나를 덮는지는 iOS MapKit 문서에 없다.** 「무시된다」고 쓰지 않는다
(`unverified-is-not-impossible` — 안 해본 것을 단정하지 않는다). **실측 프로브도 돌리지 않는다**:
학습 스킬은 코드를 건드리지 않고, 의도는 `:467` 주석에 이미 적혀 있다.
**이것을 대체 백로그 항목으로 올리지도 않는다** — 사용자에게 보이는 증상이 없고 트리거도 없다.

🔑 **재사용할 교훈: 기본값은 베이스 클래스 페이지가 아니라 「실제로 만드는 클래스」의 문서에서
확인한다.** 이것은 **「재료 전수는 코드에서 다시 뽑는다」와 같은 가족의 실패**다 —
한 단계 위에서 읽고 끝냈다.

⚠️ **이 판정 때문에 파트 2 발행본 한 줄을 고쳤다** — 바로 아래 참고.

### ✅ 파트 2 발행본 수정 — 두 줄의 역할 배분 (2026-08-19)

발행본이 *"**겹침을 실제로 막는 것은 `collisionMode` 쪽**이고 나머지는 파트 3에서 본다"*라고
**둘의 우열을 단정하고 있었다.** 위 판정대로 **그 우열은 iOS 문서에 없다**(게다가
`.required`는 베이스 클래스 문서가 *"always visible on the map"*이라 적는다).
→ 우열을 빼고 **`displayPriority`가 무엇을 하는 줄인지만** 적는 문장으로 바꿔 재발행했다.
**「나머지는 파트 3에서」라는 이어받기는 그대로 살렸다.**

### 🔌 `apple-docs` MCP 검증 결과 — **쓴다. 갈아끼우지 않는다** (2026-08-19)

`.mcp.json` 등록 + `settings.local.json` 활성화까지 끝나 `claude mcp list`에서 `✔ Connected`.
**실제 조회로 검증했고 절반만 동작한다.**

**7개를 찔러봤고 깨진 것은 하나뿐이다** (처음엔 3개만 보고 "검색이 깨졌다"고 적었다가
사용자 지적으로 범위를 넓혔다 — `unverified-is-not-impossible`의 반대 실수였다).

| 도구 | 결과 |
|---|---|
| `search_apple_docs` | ❌ **유일하게 깨졌다.** `"MKAnnotationView"` 같은 **있을 수밖에 없는 이름도 0건** |
| `get_apple_doc_content` | ✅ 본문·선언·플랫폼 가용성까지 최신으로 온다 |
| `search_framework_symbols` | ✅ `framework` + `namePattern`으로 심볼과 URL을 준다 |
| `get_related_apis` | ✅ **이 작업에는 검색보다 낫다** — URL 하나로 상속·준수·See Also를 67건 준다 |
| `list_technologies` | ✅ 카테고리별 프레임워크 목록 |
| `search_wwdc_content` | ✅ 전사·코드 전문 검색 |
| `get_documentation_updates` | ✅ 프레임워크별 업데이트 페이지 |

**판정: 유지한다.** 깨진 것은 자유 검색 한 개뿐이고, **결정적 사실을 가져온 것은 동작하는
쪽**이었다. `apple-doc-mcp-server`로 갈아끼우면 **사용자 기준이던 「실시간 최신」을 캐시
스냅샷과 맞바꾸게 된다** — 우회 가능한 절반 때문에 그러지 않는다.

🔑 **우회 경로 — 셋 다 검증했다. `search_apple_docs`는 아예 안 쓴다.**
1. **심볼 이름을 안다**(학습은 코드에서 뽑으니 대개 이쪽) → URL을 직접 조립해 `get_apple_doc_content`.
2. **한 타입의 주변을 훑고 싶다** → `get_related_apis(apiUrl:)`. **이번 판정에서 가장 유용했을 경로다** —
   `MKAnnotationView` 하나로 `MKMarkerAnnotationView`·`collisionMode`·`displayPriority`가 한 번에 나온다.
3. **이름이 흐릿하다** → `search_framework_symbols(framework:, namePattern:)`로 URL을 얻고 1번으로.
⚠️ **`/documentation/mapkitjs/**`는 이 서버로 404다** — MapKit JS 문서는 여기서 못 읽는다.

### 낮은 우선순위 (고치지 않음 — 판단 근거를 남긴다)

- 구간 ②의 *"직선 800m에 20개, 커브 100m에 80개"*는 **만든 숫자**다. *"생각하면 된다 /
  들어갈 수 있다"*로 가정임이 드러나 있어 두었다.
- 구간 ①의 *"거리가 달라지면 지우고 새로 만든다"*는 실제로는 **`SegmentSnapshot` 게이트**
  (좌표 수·첫·끝)에 걸려 있다. 좌표가 같으면 거리도 같으니 실질적으로 참이고,
  **보장의 근거는 골격 2**에 있다.

### 다음 — 파트 3 「출발·도착 핀」

파트 2가 넘긴 것 둘: ①~~`collisionMode`/`displayPriority` 두 줄의 판정~~ → **✅ 2026-08-19 완료**
(위 「✅ `displayPriority` 판정」 — **살아 있는 줄**. 결과를 파트 3 본문에 그대로 쓴다)
②**어노테이션끼리는 층 규칙 밖**이라는 사실(파트 2 구간 ④에서 세웠다 — 다시 세우지 말고 이어받는다).

---

## 🔑 파트 3 착수 메모 — 다음 세션은 여기서 시작한다 (2026-08-19 작성)

**파트 3 — 출발·도착 핀** (장면 ③⑤)

### ✅ 먼저 할 것 — `apple-docs` MCP 승인 → **끝났다 (2026-08-19)**

> **승인·연결·실제 조회 검증까지 완료.** 결과와 우회 경로는 위 「🔌 `apple-docs` MCP 검증 결과」에
> 있다 — 요지는 **`search_apple_docs`만 깨졌고 나머지는 정상, 서버는 유지**다.
> **아래는 붙일 당시의 기록으로만 남긴다.**

`.mcp.json`에 **`@kimsungwhee/apple-docs-mcp`를 추가했다**(2026-08-19 사용자 요청).
**세션을 시작하면 승인 프롬프트가 뜬다** — 승인해야 도구가 붙는다(`claude mcp list`에서
`⏸ Pending approval`로 확인됨).

- **고른 이유:** 이 서버는 **Apple 공식 search API를 실시간 조회**한다(경쟁 후보
  `apple-doc-mcp-server`는 캐시 스냅샷). 사용자 기준이 *"가장 최신 것까지 커버"*였다.
- ⚠️ **npm 마지막 수정이 2025-09로 11개월 전이다.** 실시간 조회는 Apple 사이트 구조가 바뀌면
  깨지므로, **도구가 뜨는지 · 실제 조회가 되는지 한 번 확인하고 쓴다.**
  안 되면 `apple-doc-mcp-server`(= `github.com/MightyDillah/apple-doc-mcp`)로 갈아끼운다.
- **context7은 그대로 둔다** — Apple 밖 라이브러리는 계속 그쪽이 맡는다.

### 화면에서 무엇인가

지도 위의 **출발·도착 핀**. 물방울 모양 마커에 아이콘이 들어 있고, 상황에 따라
**몇 개가 서는지와 모양이 달라진다**(출발/도착/병합/대기/확인 중). 능력 선언(발행됨) —
*"출발·도착 핀이 **어떤 상태에 몇 개 서고 언제 하나로 합쳐지는지** 설명할 수 있다."*

### ⚠️ 재료가 한 파일이 아니다 — 파트 2와 다른 점

파트 2는 `MapViewRepresentable.swift` 한 파일 안에서 끝났는데 **파트 3은 셋에 걸쳐 있다.**
(경로 확인 2026-08-19 · ⚠️ **전수는 `(b-4)`에서 코드로 다시 뽑는다**)

| 무엇 | 어디 |
|---|---|
| `MapPin` 선언 + `==` 커스텀 비교(*"좌표가 같아도 스타일 전환을 diff가 감지"*) | `MapViewRepresentable.swift:14-27` |
| `ColoredPinAnnotation` 선언 | `MapViewRepresentable.swift:29-43` |
| `viewFor`의 핀 분기 — `MKMarkerAnnotationView` · `markerTintColor` · `glyphImage` · `animatesWhenAdded` · `isEnabled = false` · 병합 배지 | `MapViewRepresentable.swift:463-491` |
| `displayPriority` · `collisionMode` | `MapViewRepresentable.swift:468-470` |
| 핀 전량 교체 판정(`pinsChanged`) — **어노테이션을 타입만으로 걸러 되만들어 비교한다** | `MapViewRepresentable.swift:312-318` |
| 핀 히트 판정 `pinHit`(24pt) | `MapViewRepresentable.swift:662` |
| **핀 목록을 만드는 곳** | `CoursePlannerPage+MapPinsComponent.swift` (`mapPins`) |
| `CoursePinRole` 정의 | `CoursePlannerPageViewModel.swift:11` |

### 파트 2가 넘긴 것 둘 — 이어서 받는다

1. ✅ **`displayPriority = .required`의 판정 — 끝났다 (2026-08-19).**
   **죽은 줄이 아니다.** `MKMarkerAnnotationView`의 기본값이 `.defaultLow`라서 이 줄이
   `.required`로 올린다. *"Defaults to `required`"*는 **베이스 클래스 쪽 문장**이었다.
   **백로그 트리거는 발동하지 않았다 — 다시 열지 말 것.** MapKit JS 근거는 폐기.
   전문과 파트 3 본문에 쓸 표현은 위 「✅ `displayPriority` 판정」에 있다.
2. **어노테이션끼리는 층 규칙 밖**이라는 사실은 **파트 2 구간 ④에서 이미 세웠다.**
   다시 세우지 말고 **이어받는다** — 핀이 사라졌던 사건의 원인(거리 라벨과의 겹침)도 거기 있다.
   **파트 3은 「핀 쪽에서 무엇을 했나」를 가져간다.**

### ⚠️ 조심할 것

1. 🔴 **처음 나오는 이름을 「구간별로」 뽑는다.** 파트 2에서 이름 목록을 **어노테이션 계열로만**
   뽑아 `MKDirections`가 통째로 샜고, `(b-4)`를 두 번 돌았는데도 안 걸렸다
   (**입력에 없으면 안 걸린다**). 파트 3에서 0회일 이름: `MKMarkerAnnotationView` ·
   `markerTintColor` · `glyphImage` · `animatesWhenAdded` · `displayPriority` · `collisionMode`.
2. **파트 1·2와 부딪히지 않는지 본다.** 파트 2 팩트체크에서 걸린 것이 **파트 1의 붙들 것과
   충돌**이었다(*"색을 들고 있는 것은 오버레이가 아니라 렌더러"*). **앞 파트의 「붙들 것」을
   먼저 읽고 쓴다.**
3. **저장·발행 단위는 구간**이고, 발행은 **`url` 인자 없이** 같은 파일 경로로 재발행하면 된다
   (이 세션에서 발행한 URL이면). **다른 세션이면 `url` + 사전 `WebFetch`가 필요하다.**
4. **`(d)` 팩트체크는 파트를 끝낸 그 세션에서 건다.** 🔑 **통한 방법(2026-08-19):
   채팅이 아니라 「저장된 최종본」을 파일에서 추출해 올리고, 범위를
   「그 파트 전체 · 마지막 구간에 쏠리지 말 것 · 무엇이 범위 밖인지」까지 열거해 호출.**
5. **노트에는 기능만 쓴다.** *"붙들 것이 아니다"* 같은 **학습 운영 판정은 `agent-log.md`로.**
   **판별법: 그 문장이 「무엇이 일어나는가」인가 「어디까지 가르칠지」인가.**
6. 🔴 **내가 만든 질문을 「구간 N에서 본다」로 미루지 않는다.** 파트 2에서 *"왜 데이터와 뷰를
   안 합치나"*를 스스로 던지고 미뤘다가, 그걸 설명하느라 **범위 밖 주제(UIKit 뷰 재사용)로
   끌려가 사용자가 바닥에서 막혔다.** **미루기 전에 그 질문을 사용자가 물었는지 본다.**

---

## ⛔️ 파트 2 착수 메모 — **폐기됨. 이 메모의 파트는 삭제됐다** (2026-08-17 작성 · 2026-08-18 폐기)

> 🔴 **이 메모는 「옛 파트 2 — 지도에 얹는 것을 어떤 타입으로 만드나」의 것이고, 그 파트는
> 2026-08-18 구성 개편에서 삭제됐다**(「✅ 구성 개편 실행」 참고 — *"옛 파트 2는 삭제했다.
> 내용은 파트 1~4가 각자 자기 기능이 쓰는 타입을 그 자리에서 다룬다"*).
> **새 파트 2는 「선 한가운데 거리 라벨」**이고, 재료도 능력 선언도 완전히 다르다
> (`SegmentDistanceAnnotation` · `midpointAlongPath` · `viewFor`).
>
> **아래를 새 파트 2의 착수 메모로 쓰지 말 것.** 능력 선언(*"오버레이로 둘지 어노테이션으로
> 둘지"*)도, *"구간 ⑦이 파트 2의 동기를 만들어뒀다"*는 이어받기 지시도 **전부 옛 구조 기준**이다.
> **살아남은 것은 「전수는 코드에서 다시 뽑는다」는 절차 교훈 하나뿐**이고, 그것은 진행 상태
> 파일과 `SKILL.md` `(b-4)`에 이미 들어가 있다.
>
> 원문은 **왜 그 파트를 접었는지의 기록**으로만 남긴다.

**(옛) 파트 2 — 지도에 얹는 것을 어떤 타입으로 만드나**

### 능력 선언 — **선택인지 확인했다 (통과)**

> 오버레이로 둘지 어노테이션으로 둘지, 거기에 무슨 정보를 실어야 하는지 정할 수 있다

**파트 1과 달리 이건 진짜 선택이다.** 반대편이 실재한다 —
①**경유점이 어노테이션이었다가 오버레이로 옮겨갔다**(소개 3 소유권 절에 적어둔 사건).
②정보를 싣는 대신 **오버레이 목록에서 찾는** 길도 있었다(구간 ⑦에서 이미 언급했다).
**이 확인을 `(b-4)`에서 다시 하되, 여기서 한 번 통과했다는 것을 알고 시작한다.**

### 파트 1이 넘긴 것 — 이어서 받는다

- 🔗 **구간 ⑦이 파트 2의 동기를 만들어뒀다** — *"`rendererFor`는 오버레이 하나만 건네받는다.
  얹을 때 알던 것이 답할 때는 없다."* **파트 2는 그 해결부터 연다.** 다시 세우지 말고 이어받는다.
- 파트 1 노트에서 이미 쓴 것: 오버레이(어디에 있나) ↔ 렌더러(어떻게 그리나) 분리 ·
  델리게이트 셋 표 · `SegmentPolyline`의 번호 둘(이름만 나왔고 **왜 그 둘인지는 파트 2·3**).

### 재료 (경로 확인 2026-08-17 · **전수는 코드에서 다시 뽑는다**)

| 재료 | 경로 |
|---|---|
| 오버레이 타입 넷 | `SegmentPolyline:45` · `SegmentCasingPolyline:53` · `SegmentHaloPolyline:61` · `WaypointDotsOverlay:108` |
| 어노테이션 타입 둘 + 뷰 | `ColoredPinAnnotation:29` · `SegmentDistanceAnnotation:65` · `SegmentDistanceAnnotationView:77` |
| 직접 만든 렌더러 | `WaypointDotsRenderer:133` |
| 델리게이트 **구조** | `rendererFor:417` · `viewFor:450` — **구조만.** 분기는 파트 3·4·5·6 |
| 값 타입 | `MapPin:14` (`Equatable` — `role` 비교는 **파트 8** diff 쪽이 본론) |
| 경유점 전환 사건 | `history/mvp9/2026-07-03-edit-consistency.md` · `history/mvp12/2026-07-11-design-apply.md` |

**소유권(소개 3에 이미 적혀 있다):** `rendererFor`/`viewFor` **구조**는 파트 2, 분기는 파트 4·5·6 ·
z-order **보장**(오버레이는 항상 어노테이션 아래)은 파트 2, **명시 호출**(`insertOverlay(_:below:)`)은
파트 4. **두 번 설명하게 되면 한 곳으로 합친다.**

### `(b-4)`에 넘길 것 — **이제 다섯이다**

구간 표 · 표면 전수 + 배정표 · 🆕**각 구간 능력 선언의 반대편** · 🆕**각 구간의 첫 조각** ·
🆕**처음 나오는 이름 전수(설명 있음/없음)**. 셋이 신설된 이유가 파트 1의 실패 넷이다.

### ⚠️ 파트 1에서 값을 치른 것 — 그대로 재발할 수 있다

1. **처음 나오는 함수·타입은 「무엇을 하나」 한 줄 필수.** 이 노트에서 **`viewFor`·`syncHalo`·
   `configureRenderer`가 아직 이름만 나와 있다.** 파트 2가 `viewFor`를 처음 여는 자리다.
2. **첫 조각은 실물** — 성질·설계 판단으로 열면 무너진다(구간 ⑤·⑥).
3. **제목은 결론이 아니라 주제** — 안 읽은 사람이 무엇에 관한 이야기인지 알 수 있어야 한다.
   **사용자가 자기 말로 요약해 주면 그 문장이 제목 1순위다.**
4. **범위 밖 근거에 기대는 결론을 넣지 않는다**(구간 ⑥이 `CameraStateStore`로 네 번 떴다).
   넣으려면 근거를 열든지, 사실만 대고 *"이 파트 밖"*이라 명시하든지 둘 중 하나.
5. **질의에서 나온 것은 「몇 주 뒤에 같은 자리에서 또 막힐 것인가」로 가른다.** 아니면 이 파일로.
   파트 1이 청크 2 최대 파트의 **두 배**가 됐다(「📏 파트 1이 길어진 것」).

---

### 검토·저장 이력

- `(b-4)` 자료(판정 구조 + 표면 전수 + 배정표 + 구간 표 초안): 세션 스크래치패드에 작성 후 advisor 호출.
- 발행: 같은 URL 갱신(2026-08-16). **이전 세션 발행본이라 `url` 인자가 필요했고, 그 전에
  `WebFetch`로 최신 버전을 한 번 읽어야 발행이 통과했다** — 다음 세션도 같은 순서를 밟는다.

---

## ⛔️ 파트 2 구성 확정 (2026-08-17) — **폐기됨. 이 구성의 파트는 삭제됐다** (2026-08-18 폐기)

> 🔴 **아래 6구간과 「🔑 구간별 착수 재료」 표는 「옛 파트 2 — 지도에 얹는 것을 어떤 타입으로
> 만드나」의 것이고, 그 파트는 2026-08-18 구성 개편에서 삭제됐다**(「✅ 구성 개편 실행」 참고 —
> *"옛 파트 2는 삭제했다. 내용은 파트 1~4가 각자 자기 기능이 쓰는 타입을 그 자리에서 다룬다"*).
> **새 파트 2는 「선 한가운데 거리 라벨」이고, 유효한 착수 메모는 위의
> 「🔑 파트 2 착수 메모 — 다음 세션은 여기서 시작한다 (2026-08-18 작성)」다.**
>
> ⚠️ **이 표가 이 파일에서 가장 상세한 착수 재료라 먼저 눈에 띈다 — 그래서 이 표시를 붙였다**
> (2026-08-18 `/trace-init`에서 발견). 위의 ⛔️ 표시는 바로 앞 「착수 메모」에만 붙어 있었다.
> **구간 구성과 재료 표는 쓰지 말 것.** 남긴 이유는 `(b-4)` 검토에서 걸린 것 둘
> (근거가 반대편이 아니라 소비자였던 사례 · *"그 구간을 여는 시점에 독자가 어노테이션이 뭔지 아는가"*
> 라는 순서 판별 질문)이 **다음 파트 구성을 짤 때 다시 쓸 재료**여서다.

**선행 `🔴` 둘 다 처리했다** — 「구간 ⑥ 네 번 무너짐」·「구간 ⑦ 제목이 결론」을 파트 2를 열기 전에
읽었고, 거기서 나온 셋(능력 선언의 반대편 · 첫 조각 · 처음 나오는 이름 전수)을 `(b-4)` 입력에 실었다.
**그 셋이 실제로 값을 했다 — 아래 걸린 것 둘 중 하나가 그 입력이 없었으면 안 걸렸다.**

### `(b-4)` 검토에서 걸린 것 둘

1. 🔴 **구간 ①의 「반대편」 근거가 틀렸다.** 「번호를 안 싣고 찾는 길」의 실물로 `syncHalo`
   (`:359-361`)를 댔는데, 그건 **실어 보낸 `segmentIndex`를 읽어서** 찾는 코드라 **반대편이 아니라
   소비자**였다. 사용자가 코드를 열면 그 자리에서 무너지는 종류다 — **파트 1 구간 ⑥과 같은 실패**
   (근거가 결론을 못 받친다).
   **진짜 반대편은 핀 비교(`:312-318`)다** — `ColoredPinAnnotation`을 타입만으로 걸러
   **객체 자신의 다섯 필드로 `MapPin`을 되만들어** 비교한다. 번호를 안 싣는다.
   → 구간 ①의 축이 이렇게 정직해졌다: **객체 자신으로 되찾을 수 있으면 찾고(핀),
   되찾을 수 없는 것만 번호를 싣는다(`segmentIndex`·`colorKey`).**
2. 🔁 **순서가 틀렸다 — 어노테이션을 배우기 전에 「오버레이냐 어노테이션이냐」를 뒀다.**
   `MKAnnotation`은 노트 등장 **0회**인데 그 구간이 어노테이션 도입보다 앞에 있었다.
   **`(b-5)`에서 색↔타입이 뒤집힌 것과 같은 종류.** 판별 질문 하나로 걸린다 —
   *"그 구간을 여는 시점에 독자가 어노테이션이 뭔지 아는가."*

### 최종 6구간

| | 구간 | 이 구간을 알면 무엇을 정할 수 있나 |
|---|---|---|
| ① | 답할 때 없는 것을 어떻게 아나 | 되찾을지 · 실어 보낼지 |
| ② | 이미 있는 타입을 물려받는다 | 상속으로 만들지 · 무엇을 얹을지 |
| ③ | 어노테이션 둘은 무엇을 싣나 | 무슨 정보를 실을지 |
| ④ | 선이 아닌 것은 처음부터 만든다 | `MKOverlay`를 직접 채택할지 |
| ⑤ | 오버레이냐 어노테이션이냐 | 어느 레이어에 둘지 |
| ⑥ | 뷰는 누가 만드나 | 기성품 뷰냐 직접 만드냐 |

**순서의 근거:** ②(오버레이·쉬운 길) → ③(어노테이션·다른 길) → ④(오버레이인데 물려받을 게 없다 =
②③을 합친 자리) → ⑤(④에서 만든 그 물건이 원래 어노테이션이었다 = ③④를 다 알아야 성립).

### 통과한 것

- 각 구간 첫 조각 여섯 **전부 실물** — ③-2 위반 없음.
- `MapPin`을 구간 ③에 셋째로 두는 것 — 「넷이면 파트」 기준에 안 걸림.
- `WaypointDotsRenderer.draw`를 구간 ④가 갖는 것 — 「직접 채택하면 렌더러까지 내 몫」이
  능력 선언의 재료다.

### 🔴 배정 누락 둘을 찾아 메웠다

1. **장면 ④(경계의 흰 점)가 소개 3의 배정 문장에서 빠져 있었다** — *"색은 파트 3, 선은 파트 4,
   라벨은 파트 5"*에 흰 점만 없었다. **이미 발행돼 사용자가 읽은 문장**이라 조용히 못 고친다.
   → **「④의 흰 점은 파트 2가 통째로 갖는다」를 그 자리에 넣고 발행 후 채팅으로 알렸다.**
2. **`polylineCasingColor`·`polylineHaloColor`(`:4-10`)가 어느 파트에도 없었다.**
   `UIColor { traits in ... }` 다크 대응이라 개정된 판정 기준(*"코드에 나오는 UIKit은 전부 다룬다"*)에
   걸린다. → **파트 4로 보낸다.** 파트 4 착수 시 재료 목록에 실을 것.

### 저장·발행

- 골격(파트 한 줄 · 능력 선언 · 구간 표 6개 + hint)을 `note.html`에 넣고 **같은 URL로 발행**했다.
  목차·플로팅 목차의 파트 2 줄, 3분 복습 파트 2의 `sum`도 함께 켰다. 파트 1은 접었다(`open` 제거).
- **이전 세션 발행본이라 `url` 인자 + 사전 `WebFetch`가 필요했다** — 착수 메모에 적힌 그대로였다.
- 🔑 **사용자가 순서를 잡아줬다** — *"파일과 노트를 만들고 나서 구간을 진행하는 게 낫지 않아?"*
  스킬 `[4]`의 「파트를 처음 저장할 때 골격을 함께 만든다」와 같은 말인데 **내가 구간 ①부터
  열려고 했다.** 골격을 먼저 세우면 구간마다 붙일 자리가 이미 있다.

### ⛔️ 구간별 착수 재료 — **폐기됨. 위 ⛔️ 표시 참고 (2026-08-18)**

**골격은 이미 노트에 있다.** 파트 머리(한 줄·능력 선언·구간 표 6개)는 저장·발행됐으므로
**다시 만들지 말고 그 아래에 구간을 하나씩 붙인다.** 저장 단위는 구간이고 매번 재발행한다
(`url` 인자 + 사전 `WebFetch`).

**각 구간의 첫 조각 — `(b-4)`에서 여섯 전부 「실물」로 통과했다. 이 성격을 바꾸지 말 것:**

| 구간 | 첫 조각 (실물) | 재료 |
|---|---|---|
| ① | `addOverlay(polyline)` 자리에서는 `index`를 알고, `rendererFor(overlay:)`에는 `overlay`만 온다 — **두 코드 자리를 나란히** | `:274,279` ↔ `:417` · 반대편 `:312-318` |
| ② | `SegmentPolyline` 선언 3줄 (`MKPolyline` + 저장 프로퍼티 둘) | `:45-49` · `:53-56` · `:61-63` |
| ③ | `ColoredPinAnnotation`·`SegmentDistanceAnnotation` 선언 둘 나란히 | `:29-43` · `:65-75` · `MapPin :14-27` |
| ④ | 장면 ④ — 경계의 흰 점. `MKPolyline`으로는 안 되는 이유(점 여럿은 선이 아니다) | `:108-131` · `:133-148` |
| ⑤ | 실기기에서 거리 라벨이 흰 점에 가려지던 장면(2026-07-04) | `:104-107` 주석 · `history/mvp9/` |
| ⑥ | `viewFor`가 **무엇을 하는 함수인가** 한 줄 + 라벨이 화면에 뭘로 보이나 | `:450-491` · `:77-102` |

⚠️ **구간 ⑥의 `viewFor`가 이 파트에서 가장 위험한 자리다** — 노트에 이름만 7번 나와 있고
**무엇을 하는 함수인지는 한 번도 안 나왔다.** 구간 ⑦이 `rendererFor`로 똑같이 값을 치렀다.

**처음 나오는 이름 — 한 줄 설명을 반드시 붙일 것(노트 등장 0~2회 실측):**
`MKAnnotation`(0) · `MKAnnotationView`(0) · `MKMarkerAnnotationView`(0) · `boundingMapRect`(0) ·
`SegmentDistanceAnnotationView`(0) · `MapPin`(0) · `colorKey`·`segmentIndex`(0) · 서브클래싱(0) ·
`viewFor`(이름만) · `dequeueReusableAnnotationView`(1) · `WaypointDotsOverlay`/`Renderer`(1).

**뒤로 넘길 것 (구간에서 흘러넘치면 여기로):** `colorKey`↔`segmentIndex`가 왜 둘인가 → 파트 3 ·
3겹과 `insertOverlay(_:below:)`·`syncHalo` 본론 → 파트 4 · `midpointAlongPath` → 파트 5 ·
배지·`displayPriority`·`collisionMode`·`MKUserLocation → nil` → 파트 6 ·
`OverlapOffsetResolver` → 파트 7 · `MapPin.==` diff·`SegmentSnapshot` → 파트 8.

**파트 2를 다 쓰면 `(d)` 팩트체크를 그 세션에서 바로 건다**(파트 1에서 그렇게 했다).

---

## 🔴 방향 재검토 (2026-08-18) — 구간 ① 세 번 실패 뒤 스터디 전체를 다시 봤다

**다음 세션은 이 절을 먼저 읽는다. 여기 적힌 논의를 다시 하지 말 것** — 사용자가 명시적으로
요청했다(*"이런 대화를 다시 진행하면 그때 또 시간이 걸리고 하잖아"*).

### 무슨 일이 있었나

구간 ①을 **세 번** 썼고 세 번 다 막혔다. 원인 셋 — ①첫 조각을 **코드 대조**로 열었다
(③-2 위반, 실물이 아니다) ②지어낸 용어 넷 ③한 답변에 처음 나오는 이름 넷 + 코드 자리 여덟.
**셋 다 `agent-log.md`에 「파트 1에서 값을 치른 것」으로 내가 직접 적어둔 것들이다.**

그러다 사용자가 *"얹는다는 표현도 대체 무엇을 얹는다는 건지 모르겠어"*를 물었고,
**노트 37곳이 틀린 말이었음**이 드러났다. 이건 이번 세션 실수가 아니라 **파트 1을 쓸 때부터
있던 것**이다.

### ✅ 용어 일괄 수정 (완료 · 발행됨)

| 고친 말 | 바뀐 말 | 횟수 | 왜 |
|---|---|---|---|
| 얹는다/얹기 | **추가한다/추가** | 37 | `addOverlay`는 목록에 **추가**하는 것. "얹는다"는 화면에 올린다는 뜻으로 읽혀 **추가≠그리기**라는 파트 1 결론을 흐렸다 |
| 실어 보낸다 | **저장해 둔다** | 5 | 전송이 아니라 **저장 프로퍼티 대입**이다 |
| 답할 때/답하는 시점 | **`rendererFor`가 호출될 때** | 7 | 의인화(②-2) |
| 되찾기 | **객체에서 읽기** | 1 | 같은 이유 |
| 물건 | **객체** | 1 | ②-3 — 통용되는 말이 있다 |
| 들고 있다 | **가지고 있다** | 3 | 의인화 |
| 「지도가 무엇을 물어보나 / 도구를 줘」 | 「호출되면 무엇을 반환하나 / 렌더러」 | 표 4칸 | 델리게이트 표 전체가 의인화였다 |

**"자리"(32곳)는 전수 확인 후 그대로 뒀다** — 노트에서는 *"채울 자리"*(코드 위치)와
*"라벨의 자리"*(화면 위치) 두 뜻으로만 쓰였고 문맥으로 갈린다. 세 번째 뜻(저장 프로퍼티)을
만든 것은 채팅뿐이라 **앞으로 그 뜻으로 쓰지 않는다.**

### 🔴 사용자가 아홉 번 물었고 아홉 번 다 맞았다

이해할 수 있겠나 · 용어는 괜찮나 · "얹는다"가 뭔가 · 주제가 바뀐 건가 · 깊이를 내리는 게 맞나 ·
"못 읽나"가 맞나 · 파트를 줄이면 지나치는 것 아닌가 · 결국 어떻게 개선되나 ·
질의로 추가된 건 이해를 위한 건데.

**그중 셋은 내 판단이 틀렸다는 것을 잡아낸 것이다.** 사용자가 *"내가 너무 깊게 파고들어서
계속 길어지는 건가"*라고 자책했는데 **아니다.** 안 물었으면 틀린 노트가 계속 쌓였다.
**이 반응을 「진도를 늦추는 것」으로 취급하지 말 것.**

### 📏 실측 — 분량이 시간의 원인이다

| 청크 | 본문 | 파트간 참조 | 파트당 평균 | 최대 파트 | 기간 |
|---|---|---|---|---|---|
| 0. 앱 구조 | 41,737자 | 16회 | 약 6,000자 | — | 가장 짧게 끝남 |
| 1. 손으로 그은 선 | 92,191자 | 59회 | 5,110자* | 13,863자 | 7일 |
| 2. 구간 이어붙이기 | 93,440자 | 93회 | 9,748자 | 18,855자 | 5일 |
| 3. 지도 표시(파트 1까지) | 49,263자 | 11회 | 12,395자 | **22,876자** | 3일째 |

*(청크 1은 소개·마무리 절이 같은 형식이라 파트 수가 14로 잡혀 평균이 낮게 나왔다.)*

**파트 1 안의 구간별 분량은 고르다**(1,277~4,616자, 평균 2,769자). 즉 **질의로 부푼 게 아니라
파트 하나에 구간이 8개** 들어간 것이다. 개념으로 세도 여덟.

**개념 하나가 여러 파트에 흩어져 있다** — `rendererFor` 파트 1·2·4 · `colorKey` 파트 3·4·5·6·8 ·
`insertOverlay` 파트 1·2·4·5·6·8. **규칙("개념 하나가 두 파트에 걸치면 잘못 쪼갠 것") 위반 상태다.**

### 죽은 처방 / 살아남은 처방

**죽은 것 (전부 「속도」 제안이었다)**
- 깊이를 C→B로 내리기 — **틀렸다.** 오늘 걸린 다섯 중 깊이 탓인 것이 하나도 없었다.
- 목적 문장을 *"이걸 모르면 무엇을 못 읽나"*로 — 확인이 안 되는 문장이다.
- 파트 수 줄이기(8→4) — 내용이 안 줄면 파트당 분량만 2배가 된다.
- 이음새(파트간 참조) 제거 — 재보니 **전체의 3~5%**다.
- *"질의 답변이 쌓여 불어났다"* — **실측이 반박했다.** 구간별로 고르다.

**살아남은 것 (전부 「품질」 제안이다 — 반박되지 않았다)**
1. **목적 문장을 「~가 왜 이렇게 되어 있는지 설명할 수 있다」로.** *"정할 수 있다"*는 이미 다
   정해진 앱에 안 맞는다. 새 문장은 선택지를 살리면서 과거형이고 **확인이 된다**(설명해보면 된다).
   🔑 **사용자 확인: 「이 구간을 알면 무엇을 정할 수 있나」는 청크 0부터 지금까지 한 번도
   안 읽혔다.** *"이해를 못하고 그 아래 설명을 듣고 따라가기만 한 것 같아."* **이 스터디의
   핵심 장치가 작동한 적이 없다는 뜻이고, 오늘 가장 큰 발견이다.**
2. **MapKit이 강제한 것 ↔ 이 앱이 고른 것을 갈라 표시한다.** 앞엣것은 한 줄로 넘긴다.
   구간 ①이 그 예다 — *"왜 오버레이에 번호를 저장하나"* → `rendererFor`에 오버레이만 오니까.
   **고른 게 아니라 강제된 것이라 구간 하나가 될 이유가 없었다.**
   목적 문장을 1번으로 바꾸면 이 구분이 **저절로 따라온다**(답이 "MapKit이 그래서"로 끝나면 짧다).
3. **화면 장면에서 코드로 내려가는 축.** 만드는 순서 재구성은 **한 번도 겪지 않은 순서**다
   (바이브코딩으로 만들어졌으므로). 사용자가 아는 것은 화면이다.
4. **파트당 구간 수 제한.** 분량은 안 줄지만 **세션마다 하나씩 닫혀 진도가 보인다.**

### 🔴 미결 — 다음 세션이 정할 것

- **확정된 것:** 목적은 *"누군가에게 설명해야 하고, 개발자니까 설계와 구성을 더 자세히 알아야
  한다"*. **깊이 C 유지**(사용자 반문으로 확정, B로 내리자는 제안은 폐기).
- ⛔️ **미결 폐기 (2026-08-18 사용자 결정)** — ~~남은 6덩어리(4~8번)를 전부 같은 깊이로 볼 것인가.~~
  **미리 정할 수 없는 질문이었다.** 어느 덩어리에 결정이 많은지는 **그 덩어리의 재료를 읽어야**
  알 수 있는데(`[1]`), 배분은 **읽기 전에** 정하는 것이다. 대리 지표(문서 기록량)로 미리 알아내려
  했으나 *"몇 번 언급됐나"*는 *"결정이 몇 개였나"*와 다르다. 사용자 표현 — *"기준을 잡기가 너무 애매하잖아."*
  **대체:** `SKILL.md` `(b)`에 **「결정이 없는 것은 파트로 세우지 않는다」** 한 줄을 넣었다 —
  청크마다 파트를 나눌 때 적용하면 **짧아질 덩어리는 저절로 짧아진다.**
  🔴 **「속도를 줄여야 한다」는 전제 자체가 사용자 요구가 아니라 에이전트 판단이었다** — 기록에
  사용자가 그렇게 말한 흔적이 없다. **다시 이 논의를 열지 말 것.**
- (아래는 폐기된 미결의 근거 재료 — 기록으로만 남긴다.)
  근거 재료 — 사용자가 *"일반적인 crud와 같은 api 호출해서 받아오고 표현만 하는 게 아니라
  map이라는 특정 도메인을 사용하는 것도 있어서"*라고 했다. ~~**8번(저장)은 익숙한 영역에 가깝고**~~
  5·6·7(백그라운드·소리·잠금화면 위젯)은 iOS 특유라 낯선 쪽.

  🔴 **정정 (2026-08-18) — 「저장은 익숙하다」는 틀렸다.** 사용자 발언 —
  *"저장에 대해서도 내가 swiftdata는 사용하지 않아서 봐서 잘 몰라."*
  **CRUD 개념이 익숙한 것과 `SwiftData`를 아는 것은 다른 이야기였고, 내가 앞엣것에서
  뒤엣것을 넘겨짚었다.** 결과: **4~8 다섯이 전부 낯선 영역**이 되어
  **「사용자에게 낯선 정도」 축은 아무것도 가르지 못한다.** 이 축은 폐기한다.
- 🔴 **속도는 크게 못 줄인다는 것이 실측 결론이다.** 뺄 물이 없다. **남은 레버는 범위뿐이고,
  가르는 기준은 「그 기능에 이 앱 고유의 의사결정이 얼마나 있나」다**(roadmap·backlog·history의
  기록량으로 실측 가능). **"방식을 고치면 빨라진다"고 다시 주장하지 말 것 — 오늘 그렇게 말했다가
  실측으로 뒤집혔다.**

### ⛔️ 파트 2 현재 상태 — **폐기됨 (2026-08-18 개편으로 이 파트가 삭제됐다)**

> 아래는 **옛 파트 2**의 상태다. 새 파트 2 「선 한가운데 거리 라벨」은 **골격도 본문도 아직 없다.**

- 골격(한 줄·능력 선언·구간 표 6개)은 발행돼 있으나 **재구성 대상**이다. 위 1~4를 적용하면
  구간 구성이 바뀐다.
- 구간 ① 제목만 **「그리는 데 필요한 정보를 어디서 얻나」**로 고쳐뒀다
  (기존 *"답할 때 없는 것을 어떻게 아나"*는 의인화, 중간에 *"색을 정하는 코드는…"*으로 바꿨던 것은
  **예시가 제목을 차지한 것**이라 되돌렸다).
- **본문은 저장하지 않았다.** 세 번 쓴 것 전부 폐기.

---

## ✅ 구성 개편 실행 (2026-08-18) — 화면 기능 축으로 갈아엎고 파트 1 구간 ①까지 발행

### 새 파트 구성 (advisor `(b-5)` 검토 통과)

| 파트 | 화면 장면 |
|---|---|
| 1 | 구간마다 색이 다른 선 |
| 2 | 선 한가운데 거리 라벨 |
| 3 | 출발·도착 핀 |
| 4 | 구간 경계의 흰 점 |
| 5 | 고른 구간이 굵어지고 후광 |
| 6 | 되짚어 그리면 두 줄로 보인다 |
| 골격 1 | 지도를 SwiftUI에 올리는 뼈대 *(옛 파트 1 — 내용 그대로, 뒤로 이동)* |
| 골격 2 | 언제 다시 그리나 *(미작성)* |

**옛 파트 2(얹는 것의 타입)는 삭제했다** — 본문이 없어 손실 없음. 내용은 파트 1~4가 각자
자기 기능이 쓰는 타입을 그 자리에서 다룬다.

### advisor 검토에서 걸린 것 넷 (전부 반영)

1. **골격을 앞으로 옮기지 말 것** — 판별 기준은 **초안**이다. 기능 파트가 골격을 세 문단씩
   설명해야 하면 앞으로, 두 문장이면 뒤가 맞다. **구간 ①에서 두 문장으로 끝나 검증됐다.**
2. 🔴 **카메라가 세 파트에 걸쳐 있었다**(발행된 골격 + 파트 5 + 골격 2) — 「개념 하나가 두 파트에
   걸치면 잘못 쪼갠 것」 위반. **골격이 소유**(저장·복원·사용자 이동)하고 파트 5는 구간 선택 시
   이동만 가져가도록 정리했다.
3. **발행본의 상호참조가 옛 구조를 가리켰다** — 목차·3분 복습·소개 2/3 배정 문장·머리말
   *"만드는 순서로 8파트"*. 전부 새 번호로 고쳤다.
4. **`updateUIView` 소유권 선언** — **골격이 게이트 구조를 갖고 각 기능 파트는 자기 몫만** 본다.
   소개 3 소유권 절에 명시했다.

### 🔑 사용자가 준 규칙 둘 (SKILL.md·메모리 반영 완료)

1. **줄여 쓰지 말고 정확하게** — *"정확하게 표현을 하지 않으니 내가 그게 무엇인지 다시 유추하고
   너한테 질문을 하고 거기서 질의가 쌓이면서 전체 파트가 길어지는것 같아."* **소제목은 답을 담고,
   구간 제목에는 무엇에 대한 것인지를 넣고, 처음 나오는 타입은 「프레임워크가 준 것인지 우리가
   만든 것인지」까지 밝힌다.** ⚠️ 분량 감축과 충돌하지 않는다 — 깎을 것은 곁가지와 강제된 것이다.
2. **파트 번호는 숫자로**(A·B·C 같은 영문 금지).

### 걸렸던 것

- **「배경」이라는 이름이 안 통했다** — 사용자가 *"먼저 알아야 할 기본 개념이면 앞에 있어야 하는거
  아니야?"*로 되물었다. **「골격」으로 바꾸고 「화면에 보이는 것이 없는 파트 · 먼저 안 봐도 된다」를
  명시했다.** 이름 하나로 순서의 근거가 뒤집혀 읽힌 사례다.
- 앵커 정리: 옛 파트 1의 `p1*` 앵커 20곳을 `bg1*`로 옮기고 새 파트 1이 `p1`을 쓴다.

### 발행

같은 URL 갱신. 본문 50,860자(개편 전 49,263자 — 파트 1 구간 ① 추가분).

---

## ✅ 파트 1 구간 ①② 완료 + 노트 구조 규칙 6건 신설 (2026-08-18 후반)

**이 절이 오늘 세션의 결론이다. 구간 ③을 열기 전에 읽는다.**

### 쓴 것

- **파트 1 구간 ①**(구간마다 다른 색은 어디서 정해지나) · **구간 ②**(구간 선 하나가 실은 두 겹인 이유).
  둘 다 저장·발행 완료. 다음은 **구간 ③ 「굵기와 색을 실제로 칠하는 곳」**이고 그걸로 파트 1이 끝난다.
- 파트 1이 끝나면 **`(d)` 파트 단위 팩트체크를 그 세션에서 바로 건다.**

### 🔴 「이 구간에서 알고 갈 것」을 다섯 번 고쳤다 — 매번 다른 결함이었다

**오늘 가장 값을 치른 자리다. 구간 ③부터는 쓰기 전에 이 목록으로 먼저 확인한다.**

| # | 무엇이 잘못됐나 | 판별법 |
|---|---|---|
| 1 | **결론이 아니라 해명**이었다 — 그날 질의로 정정한 사실이 앞에 왔다 | 그 문장이 **파트의 목적 문장에 답하는가** |
| 2 | **세 문장**이 됐다 | 세 문장이면 이미 신호다 |
| 3 | **대화에서 나온 정정**이 또 섞였다(같은 실수 2회 연속) | 마무리 한 줄을 **구간을 쓰기 전에 먼저 정한다** |
| 4 | **지시어로 시작**했다(*"그 띠는…"*) — 앞을 읽어야 뜻이 통했다 | **3분 복습에서 그 줄만 봐도** 뜻이 서는가 |
| 5 | 「왜」와 「알 것」을 **한 곳에** 담아 이도 저도 아니게 됐다 | 앞뒤 **자리를 갈라** 담는다 |

**규칙 텍스트를 늘려서는 2·3이 안 막혔다.** 그래서 **쓰는 순서를 바꾸는 것**으로 걸었다
(마무리를 먼저 정하고 본문을 쓴다). 이게 이번 처방의 핵심이다.

### 🆕 SKILL.md에 신설한 규칙 6건

1. **`(b)` 파트 나누기 축 교체** — 「만드는 순서」 → **「화면에 보이는 기능」**. 화면에 없는 것은
   뒤쪽 **「골격」**으로(「배경」이라 부르지 않는다). 앞으로 옮길지는 **초안으로 판정**한다
   (세 문단씩 필요하면 앞으로, 두 문장이면 뒤).
2. **`(b-0)` 구간은 「이 구간이 다루는 것」으로 열고 「알고 갈 것」으로 닫는다** —
   머리는 **사실을 바로** 주고(질문만 던져 유추시키지 않는다), 마무리는 **자족적**이어야 한다.
   둘의 문장 형태를 다르게 한다(붙어 읽힌다). **머리 라벨을 「보는 이유」로 달지 않는다** —
   구간마다 존재 이유를 만들면 폐기한 「무엇을 정할 수 있나」처럼 공허해진다.
   대신 **①구간 표 아래 「왜 이 순서인가」 ②구간 머리 끝에 앞 구간과의 연결**을 비우지 않는다.
3. **`(b-1)` 구간마다 「무엇을」과 「왜」를 둘 다 담는다** — 「왜」가 없으면 미완.
   근거는 **`git log -S`로 그 코드가 들어온 커밋까지** 찾아서 대고, 없으면 「기록에 없다」고 밝힌다.
   ⚠️ **「프레임워크가 강제해서」로 넘기기 전에 정말 강제인지 확인한다.**
4. **줄여 쓰지 않는다** — 소제목은 **답을 담고**, 구간 제목에는 **무엇에 대한 것인지**를 넣고,
   처음 나오는 타입은 **프레임워크가 준 것인지 우리가 만든 것인지**까지 밝힌다.
   ⚠️ 분량 감축과 충돌하지 않는다 — 깎을 것은 **곁가지와 강제된 것**이다.
5. **목적 문장을 「~가 왜 이렇게 되어 있는지 설명할 수 있다」로** (「무엇을 정할 수 있나」 폐기).
6. **파트 번호는 숫자로**(A·B·C 금지).

### 🔴 오늘 값을 치른 것 — 그대로 재발할 수 있다

- **`MKPolyline`을 설명 없이 계속 썼다.** 명패에 *"좌표 배열일 뿐"*이라고 써두고도 사용자가
  *"그러면 무엇이라고 생각해야 하는거야?"*로 막혔다. 통한 것은 **「지도 위 어느 자리에 어떤 모양이
  있는지를 적어둔 것」** + **좌표 배열과 감싸는 사각형 둘을 들고 있다**는 서술이었다.
- 🔴 **이 프로젝트 코드에 안 나오는 API를 설명 재료로 꺼내지 않는다.** SwiftUI `Path`로 비유했다가
  *"path가 뭔지 몰라"*를 들었다. **「Swift·iOS를 안다」는 전제를 「SwiftUI API 전반을 안다」로
  넓히지 말 것** — 이 앱이 쓰는 것과 안 쓰는 것이 갈린다.
- **케이싱을 「MapKit이 강제해서」로 적었다가 정정했다.** 렌더러를 직접 만들면 가능했고
  **같은 파일 안에 그렇게 만든 것이 있었다**(`WaypointDotsRenderer`). 강제가 아니라 **선택**이었고,
  **그 선택의 대가를 적는 것이 곧 「왜」**였다.

### 근거로 찾은 사실 (다음 파트에서도 쓴다)

- **케이싱을 넣은 이유 = 밝기 대비.** 커밋 `7ffbc5e`(2026-07-12) 메시지 —
  *"폴리라인 아래에 **밝기 대비** halo를 깐다"*. 문서에는 없고 **커밋에만 있었다.**
- **`colorKey` == `CourseEditSession`의 `order`.** `segmentColorKeys`가
  `entries.map(\.order)`다. 이름이 둘인 것은 **층이 달라서**(세션 쪽 `order` / 지도 쪽 `colorKey`).
  **undo도 같은 `order`를 쓴다**(청크 2와 이어지는 자리).
- 굵기: 색 선 5.5 / 케이싱 9.5, 선택 시 7 / 11 — **차이 4pt 고정**, 한쪽 띠 2pt.

### ⛔️ 미결 (다음 세션이 정할 것) — **폐기됨 (2026-08-18 사용자 결정)**

> 🔴 **깊이 배분 미결은 폐기됐다.** 근거와 대체 처방은 위 「🔴 미결 — 다음 세션이 정할 것」에 있다
> (**미리 정할 수 없는 질문이었다** — 대체로 `SKILL.md` `(b)`에
> 「결정이 없는 것은 파트로 세우지 않는다」를 넣었다).
> **아래 두 전제는 틀린 것으로 확인됐으니 그대로 읽지 말 것** — ①*"속도를 줄여야 한다"*는
> 사용자 요구가 아니라 에이전트 판단이었다 ②*"CRUD는 익숙하다"*도 틀렸다
> (*"swiftdata는 사용하지 않아서 봐서 잘 몰라"* — 4~8 다섯이 전부 낯선 영역이다).

**남은 6덩어리(4~8)의 깊이 배분.** 오늘 실측으로 **속도는 방식 개선으로 크게 못 줄인다**는 것이
확인됐다(뺄 물이 없다). **남은 레버는 범위뿐**이고, 가르는 기준 후보는 **그 기능에 이 앱 고유의
의사결정이 얼마나 있나**다. 문서 기록량 실측: 백그라운드 47 · 저장 34 · 소리 31 · GPS 26 ·
잠금화면 위젯 14회. **사용자는 CRUD는 익숙하고 map 도메인이 낯설다고 했다.**

---

## 🔴 파트 1 검토 상태 — advisor를 못 받았다 (2026-08-18)

**파트 1(구간 ①②③)은 다 썼고 발행됐다. 남은 것은 검토뿐이다.**

### 무엇이 됐고 무엇이 안 됐나

| | 상태 |
|---|---|
| 구간 ①②③ 작성·발행·커밋 | ✅ 완료 |
| **내용이 맞는가** (사실 확인) | ✅ **자체 검증으로 함** — 코드·Apple 문서 대조, 고침 1건 |
| **흐름이 괜찮은가** | ❌ **아예 못 함** — 저자가 자기 흐름을 판정할 수 없다 |
| advisor `(d)` 팩트체크 | ❌ **못 받음** |

### advisor 시도 기록 — 네 번

1. **과부하** (응답 없음)
2. **과부하** (응답 없음)
3. 응답은 왔으나 **파트 1 내용이 아니라 세션 진행 방식 지적**(정정이 가속되고 있다 · 규칙을 여섯 개
   추가했는데 두 개는 한 구간 안에서 깨졌다)
4. 응답은 왔으나 **"검토 결과를 어떻게 적용하라"는 지침**만 있고 findings가 없었다

🔑 **원인 추정: advisor는 그 세션의 대화 전체를 보는데, 후반부가 온통 고치고 다시 쓰는 기록이라
그쪽을 더 급한 문제로 판단한 것으로 보인다.** 요청 범위를 명시적으로 못박아도 같았다.
**새 세션에서는 그 기록에 안 끌리므로 재시도할 값이 있다.**

### 자체 검증 결과 (스킬이 정한 대체 경로)

- 🔴 **고침 1건 — 구간 ②.** *"MapKit은 나중에 추가한 오버레이를 위에 얹으므로"*가
  **iOS MapKit 문서에 없는 단정**이었다(`addOverlay(_:)` 설명은 *"`aboveLabels` 레벨 그룹에
  추가한다"*까지만). *"이 앱은 그 전제로 만들어져 있다"*로 낮추고 간접 근거 둘을 적었다 —
  `insertOverlay(_:below:)`의 존재 · **MapKit JS 문서에는 명시**(*"overlays added later appear
  closer to the foreground"*). **2026-08-17 고침 1과 같은 종류다.**
- **코드로 확인:** `% 6`·`?? .systemBlue` · 에셋 `Seg0`~`Seg5` · 두 타입의 상속과 프로퍼티 ·
  **`colorKey` == `CourseEditSession`의 `order`** · **`prepend`가 배열 맨 앞에 넣으면서 `order`는
  `nextOrder`를 받는다**(구간 ①의 도식이 코드와 일치) · 굵기 5.5/7 · 9.5/11 ·
  케이싱 색 60%/35% · **케이싱 굵기 계산 두 곳 중복**(백로그 열어둠) ·
  `renderer(for:)`·`configureRenderer` 동작.
- **Apple 문서로 확인:** `setNeedsDisplay()` — *"causing it to be redrawn during the next update cycle"*.

### 🔴 여전히 미검증인 주장 둘 — 새 세션에서 이것부터 건다

1. **구간 ③ 「선택이 바뀔 때 오버레이를 다시 만들지 않는 이유」** — 커밋 메시지에도 주석에도
   기록이 없어 **코드 구조에서 읽은 추론**이다. 본문에 ⚠️로 표시해 뒀다.
2. **구간 ② 「케이싱 도입 = 밝기 대비」** — **커밋 `7ffbc5e` 메시지가 유일한 근거**다.

---

## ✅ 파트 1 advisor `(d)` 팩트체크 — 다섯 번째 시도에서 받았다 (2026-08-18, 새 세션)

**앞 세션의 원인 추정이 맞았다.** 새 세션에서 첫 시도에 정상 응답이 왔다 — 파트 1 내용에 대한
검토였고 세션 진행 방식 지적이 아니었다. 앞 세션이 네 번 실패한 것은 **그 대화 후반부가
정정의 연속이라 advisor가 그쪽을 더 급한 문제로 봤기 때문**으로 보인다.

🔑 **통한 방법 — 새 세션에서 파트 1 본문 전문 + 근거 코드를 컨텍스트에 먼저 올리고,
요청 범위를 「파트 1 본문만 · 진행 방식은 범위 밖」으로 못박아 호출했다.** 스킬 `(d)`의
*"파트를 끝낸 그 세션에서 건다"*는 원칙과 어긋나지만, **그 세션이 실패했을 때의 복구 경로**로는
이것이 통했다. 노트만 보고 얕아지는 것은 **본문 전문 + 코드를 다시 올려서** 막았다.

### 🔴 발행 전 차단 — 고쳐야 하는 것 셋 (전부 코드로 재확인함)

1. **구간 ① 1번 「함수 하나만 들어 있는 enum」이 사실 오류.** `SegmentPalette.swift`에
   `static func`가 **둘**이다 — `color(at:)`와 `swiftUIColor(at:)`. 파일 13줄은 맞다.
   두 번째 함수 주석이 *"SwiftUI Map/스와치용 — 기록 상세의 구간 폴리라인과 구간 표가 같은 색을
   쓴다(ui-direction §6)"*라, **이 팔레트가 MKMapView 경로와 기록 상세의 SwiftUI 경로에
   공유된다**는 사실까지 함께 가려졌다. 「돌려주는 것뿐이다」의 *뿐*도 같이 손봐야 한다.

2. 🔴 **구간 ③ 5번 흐름도가 판단 자리를 잘못 놓았다 — 메커니즘을 반대로 읽게 만든다.**
   도식은 `오버레이를 훑는다 → segmentIndex == 선택된 번호 ? → 짝인 렌더러를 찾는다`로
   **루프 안의 관문**처럼 그렸는데, **코드(`MapViewRepresentable.swift:295-306`)에 그 관문이 없다.**
   타입 캐스팅만 하고 **모든** 구간 오버레이의 렌더러를 찾아 `configureRenderer(...)`에
   `selected:`를 넘기며, 비교는 **`configureRenderer` 안**(`:348` — `segmentIndex == selected ? 7 : 5.5`)
   에서 일어난다. 케이싱도 루프 안에서 삼항연산자로 **전부** 다시 넣는다(`11 : 9.5`).
   - **왜 이해에 걸리나:** 관문이 있다면 **선택을 풀었을 때 직전에 굵어진 선이 그대로 남아야 한다.**
     전부 다시 넣기 때문에 5.5로 되돌아오는 것이고 **그게 이 구조의 요점**이다.
   - 구간 ③ 3번 본문(*"렌더러는 값을 받는 쪽이지 판단 재료가 아니다"*)은 **이미 맞게 썼다** —
     어긋난 것은 도식뿐이다.

3. **구간 ① 표의 `segmentIndex` 앞참조가 파트 1 안에서 자기모순.** 표는 용도를
   *"고른 구간을 굵게 (파트 5)"*로 보냈는데, **구간 ③ 3번이 바로 그 `segmentIndex`를 선택 판정의
   재료로 쓴다.** 첫 실사용은 파트 5가 아니라 **같은 파트의 구간 ③**이다(파트 5는 후광·카메라).
   → 「구간 ③, 그리고 파트 5」로 고친다.

### 미검증 둘 — 판정 결과

- **구간 ③ 6번(오버레이를 다시 만들지 않는 이유): ⚠️를 그대로 둔다.** 이번에 찾은
  `lastSegmentSnapshots` diff와 커밋 `28075e3`(*"카메라 이동마다 불필요하게 다시 그리지 않도록
  세그먼트 개수/좌표 스냅샷으로 diff한다"*)은 **오버레이 재생성 경로**를 감싸는 것이고
  **선택 경로(`lastSelectedIndex` 분기)의 이유가 아니다** — 인접 근거이지 그 결정의 기록이 아니다.
  MVP7 계획 문서 `history/mvp7/2026-07-01-course-edit-ux-panel.md:836-844`에도 **코드만 있고
  산문 근거가 없다**(직접 확인함). 「이 파일이 원래 바뀐 것만 건드리는 방식으로 쓰였다」를
  유추의 보강으로 한 줄 붙이되 *"기록에 없다"*는 지우지 않는다.

- ✅ **구간 ② 2번(케이싱 = 밝기 대비): 근거를 올린다. 「커밋 메시지가 유일 근거」는 내린다.**
  설계 문서 `history/mvp12/2026-07-10-design-direction-design.md:96-98`이 **값까지 확정**해 뒀다 —
  *"구간별 `seg` 색 폴리라인 + 케이싱(라이트 흰색 .6 / 다크 검정 .35 — 다크 글로우는 P2),
  선 폭 5.5(케이싱 9.5)"*. **목적이 대비라고 적은 것은 커밋 `7ffbc5e`뿐**이지만,
  **값 + 라이트/다크 반전 자체가 대비 목적의 방증**이라 그 조합으로 쓰면 정확하다.
  - ⚠️ **인용 함정 — 같은 설계 문서에서 「halo」가 두 가지를 가리킨다.** `:93`·`:158`의
    *"구간 선택 시 지도 halo 하이라이트"*는 **P2**이고 커밋 `a4da480`으로 나중에 당겨진 것 —
    **파트 5의 민트색 층**이다. 케이싱은 `:96`의 **P1**이다. `:93`을 케이싱 근거로 끌어오면
    층 둘이 섞인다.

### 🔑 Apple 문서로 새로 확인한 것 — 구간 ③ 2번에 빠진 조건이 있다

advisor가 *"「지도가 계속 보관한다」도 같은 종류의 단정 아닌가"*로 확인을 요구했고, 조회 결과
**보관한다는 쪽은 문서로 뒷받침된다** — `renderer(for:)` Discussion: *"This method returns the
renderer object that your map delegate provided in its `mapView(_:rendererFor:)` method."*
**앞의 둘(「나중에 추가한 것이 위」·「rendererFor는 새로 추가될 때 불린다」)과 달리 낮출 필요가 없다.**

🔴 **대신 노트에 없는 조건이 하나 있다** — 반환값 설명이
*"The renderer object in use for the specified overlay or **`nil` if the overlay is not onscreen**"*이다.
코드가 `if let`으로 받으므로 **화면 밖 오버레이는 렌더러가 `nil`이라 굵기 변경이 조용히 건너뛰어진다.**
노트 구간 ③ 2번은 *"나중에 달라고 하면 돌려준다"*로만 써서 이 조건이 빠져 있다.

### 흐름 검토 — 아무도 못 봤던 것 (저자가 자기 흐름을 판정할 수 없어 미실행이던 항목)

- ✅ **①→②→③ 큰 순서는 맞다.** 구간 표 아래 「①②는 미리 정해두고 ③은 꺼내 칠한다」가 실제 내용과
  일치하고, **마무리 세 줄 다 지시어 없이 자족적**이다(`(b-0)` 충족).
- 🔴 **걸린 것 하나 — `rendererFor` 콜백을 두 번 외상으로 쓰고 세 번째에 갚는다.**
  구간 ① 4번이 *"MapKit이 … 묻는 함수를 호출하는데 넘겨주는 것은 오버레이 객체 하나뿐"*으로 기대고,
  구간 ② 3번이 *"객체 하나만 건네주므로 타입이 유일한 표식"*으로 **같은 사실에 또 기댄다.**
  정작 오버레이/렌더러의 정의는 **구간 ③ 1번**에 있다. **③-2의 「앞 구간에서 이름만 스친 것은
  처음 나온 것으로 친다」위반이고, 청크 3 옛 구간 ⑦이 무너진 것과 같은 형태다.**
  - **싸게 고치는 법(advisor 권고):** 구간 ① 4번의 앞참조를 **①②가 공유하는 한 문장**으로 만들고
    (함수 이름을 대고 *"자세한 것은 구간 ③"*), 구간 ② 3번에서 되풀이하지 않는다.
    **구간 ③ 1번을 앞으로 옮기는 것은 권하지 않는다** — ①②가 「미리 정해둔다」로 묶인 구성이 깨진다.

### ✅ advisor가 문제없다고 확인한 것

`% 6` 순환(7번째 → `Seg0`) · `?? .systemBlue` · 굵기 차 4pt와 한쪽 띠 2pt(9.5−5.5, 11−7) ·
케이싱과 색 선이 값 동일·타입만 다름 · 케이싱 굵기 두 곳 중복(사실은 노트, 처방은 백로그로 갈린 것도 맞음) ·
구간 ② 3번의 「기성품 vs 직접 만들기」 대비와 `WaypointDotsRenderer` 인용 ·
`addOverlay` 순서 문장의 완화 표현. **후자는 프로젝트 내부 근거를 하나 더 붙일 수 있다** —
계획 문서 `history/mvp12/2026-07-11-design-apply.md:1380`: *"add the casing overlay **before** the
colored one so it renders underneath (MapKit draws later-added overlays on top, same convention
already used for `WaypointDotsOverlay`)"*.

### 🔴 파트 1 밖에서 발견 — 「장면 ↔ 맡은 부품」 표가 옛 8파트 구성 그대로다

2026-08-18 개편 때 *"발행본의 상호참조가 옛 구조를 가리켰다 — 목차·3분 복습·소개 2/3 배정 문장·
머리말. **전부 새 번호로 고쳤다**"*고 적었는데, **소개 3의 표 본체는 안 고쳤다.**
현재 표(`note.html:947-990`)의 파트 번호 전부가 옛 축이다:

| 표의 장면 | 표에 적힌 파트 | 새 구성의 실제 |
|---|---|---|
| (바탕) 지도를 SwiftUI에 추가 | 1 | **골격 1** |
| ① 구간마다 다른 색 선 | 2·3·4 | **1** |
| ④ 구간 경계 흰 점 | 2 | **4** |
| ⑥ 선택 강조 + 카메라 | 4 | **5**(+ 카메라 저장·복원은 골격) |
| ② 거리 라벨 | 5 | **2** |
| ③⑤ 핀 | 6 | **3** |
| ⑦ 겹친 선 갈라 그리기 | 7 | **6** |
| (안 보이는 것) 다시 그리는 조건 | 8 | **골격 2** |

표 아래 산문도 옛 축 언어다 — *"장면 ①의 파트 번호가 셋으로 흩어져 있다 … 추가할 타입 만들기 →
번호 정하기 → 그림으로 바꾸기 … **만드는 순서와 겪는 순서가 다르다**"*. **새 축(화면에 보이는 기능)
에서는 장면 ①이 파트 1 하나이므로 이 문단은 존재 이유가 없다.**

🔴 **소개 2와 소개 3이 서로 반대말을 한다** — 소개 2(`:890`)는 *"①색이 다른 선은 **파트 1**"*로
고쳐져 있는데 소개 3 표는 *"2·3·4"*라고 한다. 사용자가 소개를 읽고 파트 1로 내려오는 경로에
바로 걸린다.

---

## 📦 인계 원문 보존 — 청크 2 종료 시 쓴 착수 메모 (2026-08-15)

> **진행 상태 파일에서 이리로 옮겼다**(2026-08-16). 옮긴 이유: `/trace-init`이 `🔑` 블록을
> **재개 지점 맨 앞에 원문 그대로** 싣는데, 이 메모가 **이미 지난 지시**를 담고 있어 다음 세션을
> 잘못 이끈다 — *"브랜치를 새로 판다"*(이미 팠다) · 재료 4건(파트 1에 필요한 MVP4 설계 문서가 없다) ·
> 파트 번호(④핀 → 파트 2라고 적혀 있으나 **8파트 개정 후 파트 6**) · *"발행은 새 URL"*(이미 발행).
> **지우지 않고 옮긴 것은** 앞 세션이 좁혀둔 확인 대상이 여기 적혀 있어서다.

- 🔑 **착수 메모 (2026-08-15 청크 2 종료 시 작성) — 열기 전에 읽을 것.**
  **브랜치를 새로 판다** — 청크 2는 `docs/study-chunk-2`에서 했고 **푸시는 사용자 몫**이라
  아직 안 됐을 수 있다. `main`에서 `docs/study-chunk-3`을 새로 만든다.
- 📌 **재료 (실측 줄 수 · 스펙 §3 기준, 경로 확인 완료 2026-08-15):**
  `MapViewRepresentable.swift` **688줄**(`Trace/Pages/CoursePlannerPage/` 바로 아래 —
  `UIComponent/`가 아니다) · `OverlapOffsetResolver.swift` 152줄 · `SegmentPalette.swift` 13줄 ·
  `CoursePlannerPage+MapPinsComponent.swift` 68줄(`UIComponent/`).
  테스트는 `SegmentPaletteTests` · `OverlapOffsetResolverTests` · `CourseCoordinateGeoTests`.
  설계 문서는 `history/mvp7/`(구간 패널) · `history/mvp8/`(겹침 오프셋).
  ⚠️ **688줄짜리는 통째로 읽지 말 것** — `rendererFor`·`viewFor` 등 **해당 함수만**(`SKILL.md` `[1]`).
- 📌 **청크 2가 넘긴 연결 고리 — 다시 파지 말고 이어받을 것:**
  ①**`CourseCoordinate+Geo`의 함수 셋**(`distanceMeters(toSegment:)` · `headingVector` ·
  `offset`)은 실측상 `OverlapOffsetResolver` 한 곳에서만 쓰인다 — **청크 2가 "이건 청크 3"이라고
  명시하고 넘겼다**(노트 소개 1). `CourseCoordinateGeoTests` 7개도 전부 이쪽.
  ②**색은 붙인 순서 번호를 따른다**(`segmentColorKeys`, 파트 3) — 코스 앞쪽에 새 구간이 꽂혀도
  기존 구간 색이 안 바뀌는 이유가 거기 있다.
  ③**왕복 구간은 대상 구간과 좌표가 완전히 같다** — 화면에서 두 줄로 보이는 것은 **겹침 오프셋이
  옮겨 그리는 것**이고, 청크 2 파트 7 구간 ③이 그 사실을 **청크 3 예고로 남겨뒀다.**
  ④**핀은 좌표 한 줄의 첫/끝 점**이고 20m 안이면 「출발/도착」 하나로 합쳐진다(파트 2).
- 📌 **백로그에 이 영역 항목이 여럿 있다**(착수 전에 훑을 것) — 겹침 렌더링 α/β 후보 ·
  `MapViewRepresentable` 688줄 분해(린트 ③) · 핀·경로선 **전량 교체**로 핀이 매번 다시 떨어지는 것 ·
  회색 "확인 중" 마커 깜빡임. **학습 중 발견은 백로그로 보내고 코드는 고치지 않는다.**
- ⚠️ **진행 방식 — 청크 2에서 값을 치른 것들. 그대로 이어간다:**
  ①**저장·발행은 승인받고 한다** — 청크 2에서 구간 ①을 안 보여주고 발행했다가 사용자가 잡았다.
  **구간마다 채팅에 먼저 내고, 승인받고, 저장·발행한다.**
  ②**용어는 활성/비활성**(「켜진다·꺼진다」 금지, `SKILL.md` 절대 원칙 ②-3). 지도 쪽은
  「그린다/다시 그린다」처럼 **이미 통용되는 말**을 쓸 것.
  ③**구간 표를 내기 전에 `(b-3)` 판정 구조 뽑기 → `(b-4)` advisor 검토**(표면 전수 + 배정표를
  함께 넘긴다). 청크 2 파트 7에서 이 절차가 **한 개념이 두 구간에 걸친 것**을 잡아냈다.
  ④**확인 문제는 청크 전체에서 파트당 하나씩** 뽑는다 — 청크 2에서 한 파트에 몰렸다가 지적받았다.
- 🔑 **노트 형식은 이미 정해져 있다** — 두 층 접기 · 파트 머리 구간 표 · 번호 접두어 · 3분 복습 ·
  마무리 4절(어디를 열면 되나 · 왜 이렇게 됐나 · 팩트체크 수정 내역 · 확인 문제 기록).
  **다시 정하지 말고 청크 2와 같은 골격으로 만든다.** 발행은 **새 URL**(청크마다 아티팩트가 다르다) —
  첫 발행 뒤 URL을 `agent-log.md` 머리에 적어두고, 이후에는 같은 파일 경로로 다시 발행한다.

**소화 결과 — 위 메모 중 지금도 유효한 것은 이렇게 흡수됐다:**
연결 고리 넷 → 노트 소개 1 표(파트 번호는 8파트 기준으로 갱신) · 진행 방식 넷 → 이 파일 맨 아래 ·
노트 형식 → 이미 적용 · 백로그 항목 → 이 파일 「백로그 후보」 절.

---

## 진행 방식 — 청크 2에서 값을 치른 것들, 그대로 이어간다

1. **저장·발행은 승인받고 한다** — 구간마다 채팅에 먼저 내고, 승인받고, 저장·발행.
2. **용어는 활성/비활성**(「켜진다·꺼진다」 금지). 지도 쪽은 「그린다/다시 그린다」처럼
   **이미 통용되는 말**을 쓴다.
3. **구간 표를 내기 전에 `(b-3)` 판정 구조 뽑기 → `(b-4)` advisor 검토**(표면 전수 + 배정표를 함께).
4. **확인 문제는 청크 전체에서 파트당 하나씩.**
5. 🆕 **저장·발행 단위는 파트가 아니라 구간이다** (2026-08-16 사용자 결정).
   사용자 표현 — *"이전에는 파트 전체로 했는데 구간을 진행하면서 질의도 하다 보면 거기서 내가
   이해한 것을 바탕으로 파일을 정리하고 싶은데 넘어가게 되면 그게 잘 안 되는 것 같더라고."*
   **질의에서 나온 것이 그 구간 자리에 바로 들어가야 하고, 파트 끝까지 미루면 유실된다.**
   2026-08-05에 「한 번에 한 구간」을 파트 → 구간으로 내린 것과 **같은 처방**이다.


---

## ✅ 파트 3 구성 확정 — `(b-4)` 검토 반영 (2026-08-19)

**재료는 코드에서 다시 뽑았다.** 착수 메모와 다른 점 하나 — `mapPins`는
`CoursePlannerPage/UIComponent/CoursePlannerPage+MapPinsComponent.swift`(68줄)에 있다
(메모에는 `UIComponent/`가 빠져 있었다). **표면 21건을 뽑아 전부 구간에 배정했고 빈칸은 없었다.**

### advisor가 막은 것 넷 — 전부 반영

| 지적 | 반영 |
|---|---|
| ⑤(배지)를 ④에 접어라 — *"기성품을 그대로 쓴다"* 다음에 *"기성품에 없는 것"*이 따로 서면 앞을 부정한다 | ⑤ 「기성품이 주는 것과 안 주는 것」 하나로 합침 |
| `MapPin.==`를 ②→④로 — `color`·`systemImage`를 뺀 이유가 **diff 쪽에 있다** | ④로 이동(개념 하나가 두 구간에 걸치는 것을 막음) |
| ①을 쪼개라 — 표면 6개 + 버그 서사가 한 구간에 몰렸다 | **확정 핀(①) / 임시 핀(②)**로 분리 |
| E의 미정 셋을 지금 정하라 | 아래 표로 확정 |
| ③(구 ②)의 첫 조각이 ③-2 위반 — 제목이 설계 판단이라 첫 조각을 선언 열기로 잡았다 | 첫 조각을 **「없으면 뭐가 깨지나」**로 교체 |

### 🔑 코드로 확인한 것 둘 — **주석을 근거로 쓰지 않았다**

1. **「동시 최대 3개」 확인됨.** `pendingTapStart = coordinate`(`CoursePlannerPageViewModel:156`)는
   `nearestEndpoint`가 `nil`을 준 뒤에만 닿고, 그 함수는 `session.course`가 없을 때만 `nil`이다.
   → **코스가 있으면 첫 탭 대기 핀은 안 선다.** 최대는 **출발 + 도착 + 확인 중 = 3**.
   (`mapPins`의 주석이 같은 말을 하지만, 같은 저장소에 낡은 주석 백로그 항목이 열려 있어 코드로 봤다.)
2. **배지 정리 줄은 잔재가 아니라 방어 코드.** `git log -S mergedBadgeTag` →
   `166c6c7 feat: 닫힌 코스 병합 핀 표시`(2026-07-03)가 **배지와 정리 줄을 같이** 넣었고,
   뷰를 매번 새로 만드는 코드는 그보다 앞선 `ae5f662`에 이미 있었다.
   → **dequeue 시절의 잔재가 아니다.** 다만 새 뷰에 그 서브뷰가 있을 수 없어 **실행되지 않는다.**
   ⚠️ **노트 본문에 쓰지 않는다** — 배지 이야기의 본론은 *"기성품에 깃발이 없어 서브뷰로 얹었다"*이고
   정리 줄의 생사는 곁가지다. **처분은 파트 끝에 백로그로.**

### 처음 나오는 이름 — 「그 판정 자체는 어디 소관인가」

| 이름 | 한 줄 설명 | 판정은 |
|---|---|---|
| `isClosedCourse` | 코스의 첫·끝 좌표가 연결 임계값 안이면 참 | **청크 2**(구간 이어붙이기) |
| `pendingTapStart` | 두 번째 탭을 기다리는 첫 탭 좌표 | **청크 1**(탭이 구간이 되는 과정) |
| `pendingTapMarker` | 0.35초 판별 창을 기다리는 보류 탭 좌표 | **청크 1** |

`MKMarkerAnnotationView` · `markerTintColor` · `glyphImage` · `animatesWhenAdded` ·
`displayPriority` · `collisionMode` · `isEnabled` · `tag`는 전부 구간 ⑤⑥에서 첫 등장에 한 줄을 붙인다.

### ⑤ 구간 머리에 올릴 것 — 파트 2 대비

*"거리 라벨은 직접 만들었고(파트 2) 핀은 기성품을 그대로 쓴다"* — 같은 `viewFor` 안에서 갈리고,
`dequeueReusableAnnotationView`를 **라벨은 쓰고 핀은 안 쓴다**는 차이까지 같은 자리에 있다.
**본문에 묻지 말고 구간 머리의 연결 한 마디로 쓴다.**


## 🔴 구간 ①에서 걸린 것 — 질의 결과를 「뒤 구간 소관」으로 미루면 유실된다 (2026-08-19)

구간 ①을 저장한 뒤 사용자가 *"구간1에 대해서 나와 질의한것 까지 반영해서 됐다는거야? 아닌거
같은데?"*로 되짚었고, **재보니 넷 중 둘이 빠져 있었다.**

| 질의에서 나온 것 | 반영 |
|---|---|
| 출발·도착은 경로 **양 끝**에 하나씩 | ✅ |
| 출발 핀 좌표는 **길찾기가 도로에 붙인 첫 좌표** | ✅ |
| `count > 1`은 **앞단이 보장하는 방어 조건** | ✅ |
| **깜빡여 보이는 이유** — 다시 붙을 때 `animatesWhenAdded` 등장 동작이 매번 돈다 | ❌ **빠짐** |
| **첫 탭 대기 핀과 출발 핀은 다른 핀이다** | ❌ **빠짐** |

**빠진 둘 다 「그건 구간 ②/⑤에서 본다」로 미룬 것이다.** 재료의 소속으로만 보면 미룬 것이 맞다 —
`animatesWhenAdded`는 구간 ⑤의 재료이고 첫 탭 대기 핀은 구간 ②의 주인공이다.
**그런데 사용자가 그 자리에서 이해한 것이 통째로 노트에서 사라졌다.** 두 번째는
사용자가 *"첫 탭의 출발이 이후 코스에서의 같은 출발이 아닌게 이해가 됐어"*라고 **직접 확인한
지점**이었다.

🔑 **판별법: 「그 구간의 사실을 설명하는 데 필요한가」로 가른다 — 재료가 어느 구간 소속인지로
가르지 않는다.** 재료는 뒤에 있어도 **그 구간의 결론을 완성시키는 한 문장**이면 여기 남긴다.
자리만 알려주고 미룰 것은 **그 구간의 결론과 무관한 것**이다.
- 「핀은 이어지지 않는다」는 **왜 눈에 보이는가**까지 가야 완성된다 → 등장 동작 한 줄은 ①에 남긴다.
- 「출발 핀은 길찾기가 붙인 자리에 선다」는 **그럼 그 전에 있던 건 뭔가**가 따라붙는다 →
  「다른 핀이다」는 ①에 남긴다. **그 핀의 수명과 스타일 이력**은 ②로 미룬다.

⚠️ **직전 지적(「그 구간에서 안 쓰는 것은 꺼내지 마라」)을 너무 넓게 적용한 것이 원인이다.**
그 지적이 막으려던 것은 **내가 안 쓸 것을 꺼내놓고 미루는 것**이었는데, **사용자가 이미 이해한
것까지 잘라내는 데 썼다.** 둘은 방향이 반대다.

**반영:** 구간 ①에 두 문단을 추가하고 재발행했다(`part3-seg1-qa-fixes`).


## 🔴 구간 ①이 네 번 고쳐진 이유 — 지적받은 것만 최소로 고쳤다 (2026-08-19)

구간 ① 하나에 커밋이 넷 붙었다: 최초 작성 → 질의 결과 둘 추가 → 통째로 재구성 → 도식 둘 추가.
사용자 지적 — *"이거 몇번을 수정해야 하는거야?"*

**원인은 매번 「그 지적만」 고친 것이다.** 질의에서 빠진 것을 지적받자 **문단 둘만 덧붙였고**,
재구성하라는 지적을 받자 **글 구조만 다시 짰고**, 그러고도 **도식은 안 넣었다.**
도식 요구는 그보다 **먼저** 나왔고 메모리에까지 적었는데 노트에 반영하지 않았다
(`flow-explanations-need-visual-form`).

🔑 **처방: 지적을 받으면 그 지적이 「구간 전체에 어떻게 걸리는지」를 먼저 보고 한 번에 낸다.**
「덧붙일까 / 다시 짤까 / 형태를 바꿀까」를 매번 최소로 고르면 **사용자가 같은 구간을 네 번 본다.**

⚠️ **줄글만 이어지는 것 자체가 신호다.** 노트 형식 규칙에 *"줄글만 이어지면 한눈에 안 들어온다"*가
이미 있는데, 💡 박스·`.fix`·`.later`·`.takeaway`가 있으니 정박점이 있다고 판단했다.
**그것들은 곁가지 표시이지 본문의 구조가 아니다.** 본문에 흐름·갈림이 있으면 **도식**이다.

### 🎨 도식 — `diagram-design` 스킬이 이 세션에서 안 붙었다

`Skill(diagram-design)` · `Skill(diagram-design:diagram-design)` **둘 다 `Unknown skill`**.
(스킬 목록에는 떠 있으나 호출이 안 된다.)
→ **노트에 이미 있는 도식(`ch3-scope` 등)의 규격을 그대로 따라 손으로 인라인 SVG를 썼다.**
다음 세션도 같은 상황이면 이 경로로 간다. 규격:
`viewBox` + `width="100%"` + `max-width:600px` · `role="img"` + `aria-labelledby`로 `<title>`/`<desc>` ·
화살표 마커 `#A3BFBA` · 강조 박스 `fill #E3F1EE stroke #06B6A4` · 보통 박스 `fill #F7FAF9 stroke #DAE6E3` ·
글자 `#0E1817`/`#52625E`/`#04796E`, **한글 13px 이상** · `<figure>` + `<figcaption>`.

**구간 ①에 넣은 도식 둘:** ①「같은 자리, 다른 핀」(💡 박스 안 — 첫 탭 대기 핀이 버려지고 출발 핀이
도로에 붙어 새로 서는 전후) ②「핀이 몇 개 어디에 서는가」(3번 끝 — 임계값 판정으로 갈리는
열린 코스 2개 / 닫힌 코스 1개).


## 🔑 축을 바꿀 때의 판정 기준 — 「질문이 많았나」가 아니라 「능력 선언에 답하나」 (2026-08-19)

구간 ②를 질의 여섯 뒤에 통째로 다시 짰더니 사용자가 되물었다 —
*"궁금한게 축을 바꾸긴 했는데 이게 맞는 방향인거야? 그냥 내가 많이 물어봐서 바꾼거면 그럴필요없지
않아? 원래 설계된 방향성이 있고 알아야할게 있는거 아니였어?"* **정당한 견제이고, 답이 갈렸다.**

**축을 바꾼 것은 맞다.** 초안의 축은 **「부품을 하나씩 소개하는 순서」**였는데, 능력 선언이 묻는
것은 *"어떤 상태에 몇 개 서고 언제 합쳐지나"* &#8212; **조건과 수명**이다. 노트의 축(값이 어디
있나 → 언제 표시되나 → 언제 사라지나)이 **능력 선언에 더 가깝다.**
🔑 **증거: 「첫 탭 대기 핀은 코스가 없을 때만 만들어진다」가 초안에 아예 없었다.**
그건 능력 선언 한복판인데 부품 소개 축에서는 물어볼 자리가 안 나왔다.
**질문이 만들어낸 내용이 아니라, 축을 바꾸니 드러난 누락이다.**

**그런데 하나는 넘쳤다.** 4번에 **역할값 분기표**(병합→안내 / 도착→안내 / 출발→왕복)를 폈는데,
그건 *"몇 개가 서나"*가 아니라 *"눌렀을 때 무슨 일이 나나"*라 **구간 ⑥의 자리를 앞당겼고**
분기 내용 자체는 **탭 처리(청크 1) 소관**이다. *"왜 둘이 같은 역할값을 쓰나"*를 설명하는 데는
**「역할값은 눌렀을 때의 취급을 가르는 값이다」 한 줄이면 충분**했다.
→ **한 줄로 줄이고 구간 ⑥으로 자리만 넘겼다**(2026-08-19, 사용자 승인).

🔑 **그래서 판정 절차는 이렇다. 질의 뒤 재구성할 때 들어간 것마다 묻는다:**
1. **능력 선언에 답하는가** → 남긴다.
2. **코드를 잘못 읽게 만드는 것인가** → 남긴다(노트 규칙의 예외 ①).
3. **둘 다 아닌데 질문에 답하려고 들어왔나** → **줄이고 자리만 알려준다.**

⚠️ **재구성 자체를 겁내지 말 것.** 위험은 「축을 바꾸는 것」이 아니라 **「질문을 따라가다 범위가
번지는 것」**이고, 그건 축과 무관하게 매번 3번 항목으로 걸러야 한다.


## 🔴 구간 ③(옛 「타입이 둘인 이유」)을 접었다 — 능력 선언에 답하지 않았다 (2026-08-19)

채팅으로 구간 ③을 낸 직후 사용자가 막혔다 — *"이 2번에 대해서 이해도 안되고 뭐 어쩌라는 생각이
들어. 그래서 내가 무엇을 알아야 하고 왜 알아야 하는가 라는 생각이 떠오르는거야."*
**스킬이 예고한 신호 그대로다**(*"사용자가 「이걸 왜 하는 거지」라고 물으면 그 구간을 잘못 연 것"*).

**원인이 둘이었고 둘 다 내 쪽이다.**

1. **없는 말을 지어냈다** &#8212; *"지도 밖"*, *"지도 안"*, *"밖에서 만들어 안으로 넘긴다"*.
   코드에 그런 구분은 없다. 있는 것은 **타입 둘**(`CoursePlannerPage` / `MapViewRepresentable`)과
   **프로퍼티로 배열을 넘기는 것**뿐이다. ②(가) 위반.
   게다가 *"지도 안에 핀 정보가 없다"*는 **사실도 틀렸다** &#8212; `MKMapView`는 어노테이션을 실제로
   들고 있다. 사용자가 되물어 걸렸다(*"핀에 대한 정보를 지도에 있는거 아니야?"*).
2. 🔴 **능력 선언에 답하지 않는 구간이었다.** *"어떤 상태에 몇 개 서고 언제 합쳐지나"*에
   **타입이 둘이라는 사실은 아무 답도 하지 않는다.**

**같은 자로 재보니 ⑤⑥도 능력 선언 밖이었다.** 그런데 그 둘은 **화면에 보이는 기능**(모양·누르기)이라
이 파트가 다루는 것이 맞다 &#8212; **구간이 넘친 것이 아니라 능력 선언이 파트보다 좁았다.**

### 처분 (2026-08-19 사용자 승인)

- **구간 ③ 삭제.** 6구간 → **5구간**. 옛 ④⑤⑥이 새 ③④⑤가 됐다.
- **남긴 사실 한 문장** &#8212; *"핀은 두 번 만들어진다: SwiftUI 뷰가 값(`MapPin`)으로 만들고,
  지도에 올릴 때 MapKit이 요구하는 클래스(`ColoredPinAnnotation`)로 다시 만든다."*
  → **새 구간 ③ 맨 앞에 붙인다**(무엇을 비교하는지 말하려면 필요하다).
- **능력 선언을 넓혔다** &#8212; *"어떤 상태에 몇 개 표시되고, **왜 그 모양이며, 눌렀을 때 무엇이
  되는지** 설명할 수 있다."* `.goal`과 3분 복습 양쪽을 고쳤다.
- 구간 표 · 본문 앞참조(배지 ⑤→④, 누르기 ⑥→⑤) · 순서 설명 hint를 전부 맞췄다.

🔑 **교훈: 구간을 짤 때 「반대편이 있나」만 보고 「능력 선언에 답하나」를 안 봤다.**
`(b-4)` 입력에 반대편 표는 넣었지만 **각 구간이 능력 선언의 어느 조각에 답하는지는 안 넣었다.**
다음부터 그 열을 추가한다 &#8212; **빈칸이면 그 구간은 접거나, 능력 선언이 좁은 것이다.**


## 🔴 「다루는 것」·「알고 갈 것」이 파트마다 부풀었다 — 실측 (2026-08-19)

사용자 지적 — *"먼가 갈수록 「이 구간이 다루는 것」과 「이 구간에서 알고 갈 것」에 대한 내용들이
점점 늘어나고 굳이 필요없는 내용들이 들어가는것 같은데."* **재보니 정확했다.**

| | 다루는 것(평균) | 알고 갈 것(평균) |
|---|---|---|
| 파트 1 | 134자 | **95자** |
| 파트 2 | 181자 | **103자** |
| 파트 3 (수정 전) | **274자** | **244자** |
| 파트 3 (수정 후) | 170자 | 146자 |

**「알고 갈 것」이 파트 1의 2.5배였다.** 스킬에 *"마무리는 한 줄이어야 한다. 세 줄로 늘어나면
그 구간을 잘못 쪼갠 것이다"*가 있는데 지키지 않았다.

🔑 **원인은 곁줄(`<span class="aside">`)이다.** 질의에서 나온 것을 본문에 넣고
**마무리에도 한 번 더 곁줄로 달았다.** 세 구간 전부 그랬고, 그 곁줄이 사실상 **두 번째 결론**이라
마무리가 결론 하나로 읽히지 않게 됐다.
**`aside`는 「이건 몰라도 된다」는 곁가지 표시용이지 결론을 하나 더 붙이는 자리가 아니다.**

**처방:** 곁줄 셋을 전부 걷고, 「다루는 것」에서 앞 구간 연결을 한 마디로 줄였다.
구간 ③은 사전 지식(「핀은 두 번 만들어진다」)을 머리에서 **본문 1번으로 내렸다** — 머리는
*"무엇을 볼지"*만 예고하는 자리다.

⚠️ **분량은 매 구간 재는 것이 낫다.** 한 구간씩 볼 때는 안 보이고 **파트를 가로질러 재야 보인다.**


## 📌 표현 교정 둘 (2026-08-19) — 파트 1·2 소급 적용

1. **「기성품」 폐기.** 사용자 요청 — *"기성품이라는 표현을 다른 표현으로 해줘."* **내가 만든 비유**라
   ②(나) 위반이었다. **「애플이 제공하는 뷰」 · 「애플이 주는 렌더러」 · 「쓸 만한 것이 이미 있느냐」**로
   바꿨다. ⚠️ **파트 1·2에 11군데 있어서 함께 고쳤다**(「한 번 고정했으면 이미 쓴 곳도 함께 고친다」).
   같은 계열의 「완성품」·「기성 뷰」도 걷었다. **잔여 0건 확인.**
2. **「견주다」 → 「비교하다」.** 통용되는 말 쪽으로(②-3). 잔여 0건 확인.

## 🔑 도식은 채팅 설명 단계부터 보여준다 (2026-08-19 사용자 요청)

지금까지 채팅은 줄글로 설명하고 *"저장할 때 도식과 함께 넣겠다"*로 미뤘는데 사용자가 막았다 —
*"지금부터 설명을 할때 도식화에 대한 부분을 보여주면서 설명을 해야하는거 아니야? 물론 좋은 퀄리티의
도식화는 아니여도 틀은 같은 시각적 자료를 표현할 수 있잖아."*
→ **채팅에서도 같은 틀의 그림을 텍스트로 그려 함께 낸다.** 저장할 때 그것을 SVG로 다듬는다.
**미루면 노트에 안 들어간다** — 구간 ①에서 실제로 그렇게 유실됐다.

**구간 ④에 넣은 도식 셋:** ①`viewFor` 갈림(라벨은 클래스를 만들었고 핀은 안 만들었다)
②핀 해부도(어디까지 딸려 오고 어디부터 이 앱이 정했나) ③`displayPriority` 순위와 미확인 경고.


## 🔴 구간 ⑤ 초안이 틀렸다 — 「누가 판정하나」만 쓰고 「누르면 무엇이 되나」를 뺐다 (2026-08-19)

사용자 지적 — *"결국 맵킷에서 주어지는 핀을 선택해서 액션들을 처리하지 않고 그냥 우리가 좌표를
기준으로 액션을 정했다고 하는것 같은데 이것을 알아서 뭐 어쩌라는거야?"*

**틀린 것이 둘이다.**

1. 🔴 **능력 선언의 「눌렀을 때 무엇이 되는지」를 통째로 뺐다.** 초안은 `isEnabled` ·
   `pinHit` · 24pt로 **구현만** 다뤘고, **화면에서 무슨 일이 나는지**(출발 핀 → 왕복 생성 /
   도착·병합 → 안내 / 임시 → 아무 일 없음)가 없었다. **그 표는 구간 ②에서 「구간 ⑤에서 본다」로
   넘겨놓은 것인데 받지 않았다.**
2. 🔴 **`isEnabled = false`의 이유를 지어냈다.** 초안은 *"MapKit에게 맡기는 길 vs 직접 판정하는 길
   중 하나를 골랐다"*로 그렸는데 **이력이 그렇지 않다.**

### 커밋으로 확인한 실제 이력

| | 커밋 | 무엇 |
|---|---|---|
| — | `3c89225` | 출발핀 탭 왕복 분기 &#8212; **처음부터 직접 판정**(지도 거리 기준) |
| 07-03 | `f4d1e53` | 판정을 **화면 24pt**로 교체 &#8212; *"지도거리 기반 판정의 줌 종속 문제를 피한다"* |
| 07-04 | `69d1a9f` | **`isEnabled = false` 추가** &#8212; *"핀 탭 시 나오던 MapKit 기본 선택
애니메이션(확대+흔들림)을 비활성화한다"* (실기기 QA 4건 중 하나) |

→ **`isEnabled = false`는 「길을 고른 결정」이 아니라 「부작용을 끈 수정」이다.**
`didSelect`는 이 저장소에서 **한 번도 쓰인 적이 없다**(`git log -S didSelect` 0건).
⚠️ **「애초에 왜 MapKit 선택으로 안 만들었나」는 기록에 없다 — 추측으로 채우지 않는다.**

### 다시 짠 구성

1. **핀을 누르면 역할에 따라 갈린다**(도식 &#8212; 출발/도착/병합/임시)
2. **누른 것을 감지하는 것은 MapKit이 아니다**(24pt · 화면 좌표인 이유 도식 · 탭↔그리기 갈림)
3. **`isEnabled = false`는 선택을 안 쓰려고 넣은 줄이 아니다**(증상/원인/고침)

🔑 **교훈: 「화면 기능」 파트에서 구간 제목이 구현 용어(`isEnabled`, `pinHit`)로 잡히면 신호다.**
제목을 **사용자가 보는 결과**로 먼저 잡고 구현을 뒤에 붙인다.


## ✅ 파트 3 `(d)` advisor 팩트체크 — 완료 (2026-08-19)

**호출 방식(통한 방법 재확인):** 노트 파일에서 **파트 3 전문을 추출해 컨텍스트에 올린 뒤**
범위를 못박아 호출했다 &#8212; *"구간 ①~⑤ + 파트 머리 + 3분 복습 · **마지막 구간에 쏠리지 말 것** ·
범위 밖은 파트 1·2 본문/골격/청크 1·2 소관/진행 방식"*. 미확인 표시 셋도 함께 검증 요청했다.

### 걸린 것과 처분

| 지적 | 확인 | 처분 |
|---|---|---|
| 🔴 **구간 ②가 「구간 ①에서 「동시에 최대 3개」라고 한」이라며 없는 것을 인용** | 구간 ① 안 `3개` **0건** &#8212; 재구성 때 잘렸다 | 구간 ②를 **자립 문장**으로 고쳤다(*"여기서 화면에 동시에 뜰 수 있는 핀의 최대 개수도 정해진다"*) |
| 구간 ③의 「두 번 만들어진다」와 **비교용 복제**가 헷갈릴 수 있다 | 맞다 | *"비교하려고 잠깐 만드는 것이라 화면에 뜨지 않는다 &#8212; 「두 번」에는 안 들어간다"* 한 마디 추가 |
| 3분 복습 13개가 많아 보인다? | **아니다.** 13/5구간 = **2.6**, 파트 2는 10/4 = **2.5** | 그대로 둔다 |
| 미확인 #2(길찾기 전에 비우는 이유)는 **커밋 메시지 방법을 안 써봤다** | `git log -S 'pendingTapStart = nil'` → `c01b8a9`·`1f7280e` **둘 다 순서 언급 없음** | 방법이 소진됐다 &#8212; **「근거가 없다」 서술 유지** |
| 미확인 #1(`displayPriority`×`collisionMode`) · #3(왜 MapKit 선택 안 썼나) | 정당한 미확인 | 그대로 |

🔑 **재구성이 앞참조를 끊는다 &#8212; 번호뿐 아니라 「내용 참조」도.** 착수 메모의
*"앞참조는 현재 번호표로 확인한다"*는 **원문자 참조만** 잡는다. 이번에 끊긴 것은
**「①에서 ~라고 한」이라는 내용 인용**이었고, 원문자 검사로는 안 걸린다.
**처방: 구간을 재구성하면 「구간 N에서 ~라고 한」 형태의 문장을 전부 찾아 그 내용이 아직
거기 있는지 확인한다.**

### 함께 고친 잔여 표현
`3. 견준 결과는` → `3. 비교 결과는`(치환에서 「견준」 활용형이 빠졌다) ·
`못 비교한다` → `비교할 수 없다`(도식 텍스트 포함) · 원문자 뒤 조사 통일(`③은`·`⑤는`).


---

# 🔑 파트 4 착수 메모 — 다음 세션은 여기서 시작한다 (2026-08-19 작성)

**파트 4 — 구간 경계의 흰 점**

## 화면에서 무엇인가

코스를 여러 번 나눠 이으면 **구간과 구간이 만나는 자리마다 작은 흰 동그라미**가 찍힌다
(흰 채움 + 회색 테두리). 능력 선언(발행본 3분 복습에 이미 있음) &#8212;
*"구간 경계의 흰 점이 **왜 선이 아니라 따로 만든 오버레이인지** 설명할 수 있다."*
⚠️ **이 능력 선언이 파트가 실제로 다룰 것보다 좁은지 먼저 본다** &#8212; 파트 3에서 같은 문제로
구간 하나를 접고 선언을 넓혔다(위 기록 참고).

## 재료 경로 (2026-08-19 코드에서 확인 · ⚠️ 전수는 `(b-4)`에서 다시 뽑는다)

| 무엇 | 어디 |
|---|---|
| `WaypointDotsOverlay` 선언 &#8212; `MKOverlay` 채택, `boundingMapRect`를 **직접 계산**(padding 2000) | `MapViewRepresentable.swift:108-131` |
| `WaypointDotsRenderer` &#8212; **`MKOverlayRenderer`를 직접 상속해 `draw`를 구현** | `:133-152` |
| 반경·테두리를 `zoomScale`로 나눈다(`5 / zoomScale`) | `:136-137` |
| 흰 채움 + 회색 테두리 | `:141-143` |
| `waypoints: [CLLocationCoordinate2D]` 입력 프로퍼티 | `:183` |
| 점 목록이 비어 있지 않을 때만 오버레이 추가 | `:288-290` |
| `rendererFor`가 이 렌더러를 돌려준다 | `:418-420` |
| **점 좌표를 만드는 곳** &#8212; 구간이 2개 이상일 때 **마지막 구간을 뺀 각 구간의 끝 좌표** | `CoursePlannerPageViewModel.swift:68-71` |
| 뷰에서 넘기는 곳 | `CoursePlannerPage.swift:241` |

## 앞 파트가 넘긴 것 셋 &#8212; 이어서 받는다

1. 🔑 **파트 1과 정반대다.** 파트 1은 *"렌더러를 직접 만드는 길도 있었다 &#8212; 이 앱은 애플이 주는
   렌더러 둘을 겹치는 쪽을 골랐다"*였는데, **흰 점은 직접 만들었다.** 같은 파일 안에서 갈린
   선택이라 **이 대비가 파트 4의 가장 센 재료**다. 다시 세우지 말고 **이어받는다.**
2. **파트 3 구간 ④의 결론이 여기서 실현된다** &#8212; *"물방울 모양 자체는 값으로 못 바꾼다.
   바꾸려면 직접 만들어야 한다."* **흰 점이 실제로 그 길을 간 예다.**
3. **파트 2 구간 ④가 넘긴 것** &#8212; *"어노테이션끼리는 앞뒤가 보장되지 않는다. 흰 점을
   오버레이로 옮겨 보장이 있는 관계로 바꿔 풀었다."* **왜 오버레이인가**의 답이 거기 있다.
   파트 2에서 이미 세웠으니 **다시 세우지 말고 「점 쪽에서 무엇을 했나」를 가져간다.**

## ⚠️ 조심할 것 &#8212; 파트 3에서 값을 치른 것들

1. 🔴 **구간은 「능력 선언에 답하나」로 짠다.** `(b-4)` 입력에 **각 구간이 능력 선언의 어느 조각에
   답하는지 열을 넣는다.** 빈칸이면 **그 구간을 접거나 능력 선언이 좁은 것**이다.
   파트 3에서 「반대편이 있나」만 보고 구간을 세웠다가 하나를 통째로 접었다.
2. 🔴 **도식은 채팅 설명 단계부터 함께 낸다.** *"저장할 때 넣겠다"*로 미루면 **노트에 안 들어간다**
   (파트 3 구간 ①에서 실제로 유실). 채팅에서는 텍스트로 틀만 그려도 된다.
3. 🔴 **지적을 받으면 「구간 전체에 어떻게 걸리는지」 보고 한 번에 낸다.**
   파트 3 구간 ①은 지적마다 최소로 고쳐서 **사용자가 같은 구간을 네 번 봤다.**
4. **질의 반영 판정 3단계** &#8212; ①능력 선언에 답하는가 → 남긴다 ②없으면 코드를 잘못 읽게
   만드는가 → 남긴다 ③둘 다 아닌데 질문에 답하려고 들어왔나 → **줄이고 자리만 알려준다.**
5. **분량 기준** &#8212; 「이 구간이 다루는 것」 **~180자**, 「이 구간에서 알고 갈 것」 **~140자**.
   🔴 **곁줄(`aside`)로 결론을 하나 더 붙이지 않는다** &#8212; 파트 3에서 그것 때문에 2.5배로 부풀었다.
6. **재구성하면 「구간 N에서 ~라고 한」 형태의 내용 참조를 전부 확인한다.** 원문자 검사로는 안 걸린다.
7. **표현 금지어(사용자 지적)** &#8212; ❌기성품 ❌견주다 ❌핀이 선다 →
   ✅**애플이 제공하는 뷰** ✅**비교한다** ✅**표시된다**. 그리고 **「탭」 앞에는 목적을 붙인다**
   (*"경로에 지점을 찍는 탭"*). ⚠️ **한 번 고치면 앞 파트에 이미 쓴 것도 함께 고친다.**
8. **`diagram-design` 스킬이 이 저장소 세션에서 호출되지 않는다**(`Unknown skill`).
   → 노트의 인라인 SVG 규격을 그대로 따른다(위 「🎨 도식」 항목에 규격 정리해 둠).
9. **재료 전수는 착수 메모가 아니라 코드에서 다시 뽑는다.** 이번에도 메모의 파일 경로 하나가
   실제와 달랐다(`UIComponent/` 누락).

---

## ✅ 파트 4 `(b-4)` 구성 검토 — 구간 5개 확정 + 골격 발행 (2026-08-19)

착수 메모를 읽고 **재료를 코드에서 전수로 다시 뽑았다**(메모에 기대 실패한 것이 세 번 —
이번에도 메모의 경로가 실제와 달랐다: `UIComponent/`가 아니라 `Trace/Pages/CoursePlannerPage/`).
표면 25건, **배정 빈칸 0**, 범위 밖으로 보낸 것은 `removeOverlays` 전량 교체 · `SegmentSnapshot`
게이트 하나(골격 구간 ④ 소유).

### 능력 선언을 바꿨다 — 옛것이 좁고 한 조각은 이미 소진됐다

| | |
|---|---|
| **옛것(발행본)** | *"구간 경계의 흰 점이 **왜 선이 아니라 따로 만든 오버레이인지** 설명할 수 있다"* |
| **새것** | *"구간 경계의 흰 점이 **어디서 나온 좌표인지** 짚을 수 있고, **화면에서 크기가 고정돼야 한다는 요구가 왜 오버레이와 렌더러를 직접 만들게 했는지**, 그때 **내가 채워야 하는 것이 무엇인지** 설명할 수 있다"* |

- *"왜 선이 아니라"* — **선(`MKPolyline`)이 반대편이었던 적이 없다.** 코드에도 기록에도 없다.
- *"따로 만든 오버레이인지"* — **어노테이션→오버레이 전환의 「왜」는 파트 2 구간 ④가 이미 답했다**
  (`p2s4`). 파트 2가 *"흰 점을 오버레이로 만드는 이야기 자체는 파트 4가 다룬다"*로 넘겼으므로
  파트 4의 몫은 **「점 쪽에서 무엇을 했나」**다.
- 🔴 **advisor가 `MKCircle`을 능력 선언에서 내리게 했다** — *"`MKCircle`을 실제로 검토했다는 기록이
  없다"*고 내가 직접 적어놓고 그것을 채점 기준에 올리면 **확인 문제가 기록 없는 반사실에서 나온다.**
  근거 있는 쪽은 커밋 `fb1069f`의 *"줌에 무관하게 화면상 고정 크기로 그림"*이고, `MKCircle`은
  **능력 선언이 아니라 구간 ④ 본문의 예시**로 내렸다.

### 확정 구간 5개

| 구간 | 제목 | 첫 조각(③-2) |
|---|---|---|
| ① | 흰 점이 찍히는 좌표는 어디서 오나 — 핀이 아닌 경계만 맡는다 | `waypointCoordinates`가 무엇을 하나 / 없으면 어디서 끊겼는지 안 보인다 |
| ② | 오버레이 타입을 직접 만들면 채워야 하는 것 둘 — 그리고 점 전부를 하나에 담았다 | `WaypointDotsOverlay`가 무엇을 하나 / 없으면 MapKit이 어디를 다시 그릴지 모른다 |
| ③ | 렌더러를 직접 만들면 그리는 코드를 내가 쓴다 | `draw`가 무엇을 하나 / 없으면 올라갔는데 아무것도 안 그려진다 |
| ④ | 줌으로 나눠야 화면에서 크기가 고정된다 — 직접 만들어서 치른 값 | `zoomScale`이 무엇을 하나 / 안 나누면 줌아웃할 때 점이 사라진다 |
| ⑤ | 겹칠 때 누가 양보하나 — 설계에 있던 두 줄이 지금 코드에 없는 이유 | **현재 코드의 사실**(겹침을 조정하는 줄이 하나도 없다)로 열고 뒤로 걸어간다 |

**advisor가 고치게 한 경계:** 원안은 ③이 *"MapKit이 렌더러를 고른다"*, ④가 *"그 렌더러가 이렇게
그린다"*여서 **렌더러 개념 하나가 두 구간에 걸쳐 있었다**(2026-08-14에 잡힌 것과 같은 모양).
경계를 **렌더러 안쪽으로** 옮겨 ②가 「지도에 올리기」까지 흡수하고, ③이 「렌더러를 만든다」,
④가 「그 안에서 크기를 정한다」로 갈랐다.

**⑤ 첫 조각도 바꿨다** — 원안(*"설계 문서의 두 줄이 무엇을 하는 값인가"*)은 `displayPriority`/
`collisionMode`를 **다시 여는 셈**이라 「✅ `displayPriority` 판정」의 **「다시 열지 말 것」**과 충돌했다.

### 1차 출처로 확인한 것 (`apple-docs` MCP · `search_apple_docs` 우회 경로 1번)

| 무엇 | 문서 문장 |
|---|---|
| `MKCircle.radius` | *"The radius of the circular area, **in meters**."* → 줌아웃하면 화면에서 점이 작아진다 |
| `draw(_:zoomScale:in:)` | *"**The default implementation of this method does nothing.** Subclasses can override…"* |
| `zoomScale` | *"The zoom factor to apply to the map content. You can use this value for **configuring the stroke width of lines or other attributes** that the scale of the map's contents might affect."* → `5 / zoomScale`이 그 용법 |
| `point(for:)` | *"Returns the point in the overlay renderer's drawing area corresponding to the specified point on the map."* |

### 만든 과정 세 막 (커밋으로 확인 — 구간 ⑤ 재료)

1. `158e6ac`(2026-07-03) — **애노테이션으로 만들었다.** `displayPriority = .defaultLow` +
   `collisionMode = .circle`로 *"조용한 위계"*. 설계 문서 `:112-115`가 그 두 값을 명시했고,
   리뷰(`[P2 design-lens]`)에서 *"미지정 → 명시"*로 들어온 항목이었다.
2. `69d1a9f`(2026-07-04) — 실기기 QA에서 흰 점이 거리 라벨을 가림. `layer.zPosition = 1` 시도 → 재발.
3. `fb1069f`(2026-07-04) — 추가 순서 조정도 재발. **오버레이로 전환.** 그러면서
   `displayPriority`/`collisionMode`가 **함께 사라졌다.**

🔑 **체크리스트에서 나온 사실 하나** — 설계 문서 QA 항목(`:161`)의 *"경유점 마커의 시각 위계
(지저분하지 않은지, **양보 동작이 자연스러운지**) 확인"* 중 **「양보 동작」이 최종 체크리스트
(`history/mvp9/2026-07-04-edit-consistency-device-checklist.md:67-73`)에서 사라졌다.**
남은 것은 ①점이 보이는지 ②라벨을 가리지 않는지 ③라벨 위치 셋뿐이다.
**양보 장치가 코드에서 사라진 것과 짝이다.**
(사인오프 자체는 **전환 뒤** 재확인이라 지금 버전에 대한 것이 맞다 — advisor 확인 요청 해소.)

### 🔴 구간 ④ 발행에 함께 고칠 것 — 파트 1의 틀린 원인

`note.html:1325-1345`: *"그쪽은 **점 여러 개라** MapKit이 주는 것으로는 안 되는 모양이었고"*
→ **틀렸다.** `MKCircle`을 N개 추가하는 길이 있으므로 개수가 막은 것이 아니고,
막은 것은 **반경 단위**다. **이미 발행돼 사용자가 읽은 문장이므로 고친 사실을 채팅으로 알린다.**
같은 발행에 묶을 것 둘 — `:1055`(전환 사건을 파트 4에 귀속했는데 실제로는 파트 2 구간 ④가 다뤘다) ·
`:920`(*"오버레이를 **처음부터** 만든"*이 ①밑바닥부터 직접(참) / ②처음부터 오버레이였다(거짓)로 갈린다).

### 발행

파트 머리(한 줄 · 능력 선언 · **구간 표 5개 전체**) + 목차 · 플로팅 목차 · 3분 복습 파트 4 줄까지
켜서 같은 URL에 재발행했다(`url` 인자 + 사전 `WebFetch`, `force` 안 씀, favicon `🗺️🎨` 유지).
**구간 본문은 아직 없다.**

---

## 🔴 파트 4 구간 ⑤를 접었다 — 5구간 → 4구간 (2026-08-19)

**사용자가 잡았다.** *"이게 프로젝트를 이해하려고 하는 스터디 입장에서 이것까지 알아야 하나?
알아야 하면 대체 결론이 무엇인지를 알고 싶은거야?"* — 두 번 다시 썼는데도 「그래서 뭐」가
안 나왔고, 그 자체가 신호였다.

**접은 근거: 능력 선언에 답하지 않는다.**

| 능력 선언 조각 | 답하는 구간 |
|---|---|
| 흰 점 좌표가 어디서 나오나 | ① |
| 화면 고정 크기 요구가 왜 직접 만들게 했나 | ③ |
| 직접 만들 때 무엇을 채워야 하나 | ②·④ |
| — | **⑤ (빈칸)** |

그리고 **어노테이션 → 오버레이 전환 사건 자체는 파트 2 구간 ④가 이미 다뤘다.**
⑤에 남는 것은 사실 두어 줄뿐인데 구간 하나를 세웠다.

🔴 **파트 3에서 같은 일이 있었고**(옛 구간 ③을 접어 6→5), `(b-4)`의 「반대편」 검사를
**구간 단위로는 돌렸는데 ⑤에는 안 댔다.** 착수 메모의 「조심할 것 ①」이 정확히 이것이었다 —
*"각 구간이 능력 선언의 어느 조각에 답하는지 열을 넣는다. 빈칸이면 그 구간을 접거나 능력 선언이 좁은 것."*
**표를 만들 때는 채웠는데, ⑤는 (b-2)「깨지고 고친 과정」 근거로 세워서 그 검사를 면제했다.**
⚠️ **(b-2)가 (b-4)를 면제하지 않는다** — 깨진 과정이라도 능력 선언에 안 닿으면 다른 파트 몫이거나 백로그다.

### 살린 것 / 보낸 것

- **노트(구간 ④ 끝 곁줄)** — *"겹침을 조정하는 값이 흰 점 쪽에 하나도 없다. 핀·라벨보다 아래인 것은
  구조가 보장하고, 흰 점끼리는 양보하지 않는다."* 코드를 읽을 때 *"왜 아무 설정도 없지"*를 막는 사실.
- **백로그** — 「흰 점끼리 양보 동작 없음 + 설계의 QA 항목에서 그 확인이 빠진 채 끝남」. 미검증·확인 방법 포함.
- **여기(집필 이력)** — 설계 문서가 `displayPriority = .defaultLow` / `collisionMode = .circle`을
  **리뷰 지적을 받아 명시**했다는 이력과, 🔑 **분석 하나**: `collisionMode`는 **겹치면 감출지**를
  정하는 값이고 실기기 문제는 **누가 위에 그려지는지**였다. **다른 문제인데 한 값으로 둘 다
  잡으리라 본 것**이 1·2차 시도가 실패한 뿌리다. (노트에는 안 넣는다 — 파트 2 구간 ④의 몫이고
  능력 선언 밖이다.)

### 겹침 조합 셋 — 지금 상태 (사실)

```
흰 점 ↔ 출발·도착 핀      핀이 위          구조가 보장 (오버레이는 항상 어노테이션 아래)
흰 점 ↔ 거리 라벨         라벨이 위        같은 이유 — 이것이 고치려던 문제였다
흰 점 ↔ 다른 흰 점        둘 다 그려진다    양보가 사라졌고 대체 코드 없음 → 백로그
```

---

## ✅ 파트 4 `(d)` advisor 팩트체크 — 지적 6건 반영 (2026-08-19)

🔑 **호출 방식이 결과를 갈랐다.** 사용자 요청대로 **범위를 「파트 4 본문만 · 구간 ①~④ 균등」으로
못박고, 발행본 전문을 컨텍스트에 먼저 올린 뒤** 걸었다 — *"advisor 팩트체크를 할때는 최근 대화에
대해서 치우치지 말고 파트 전체를 보면서 진행해야돼."*
**효과가 실측됐다: 지적 6건 중 5건이 파트 머리와 구간 ①에 있었다.** 최근에 쓴 ③④는 깨끗했다.
(청크 2에서 고침 4건이 전부 마지막 파트에서 나온 것과 정반대 결과다 — **전문을 올리는 것이
「마지막에 읽은 것이 가장 선명하게 걸린다」를 상쇄한다.**)

| # | 지적 | 고친 것 |
|---|---|---|
| 1 | 구간 ③의 정정이 **파트 머리·3분 복습에 안 옮겨져** 같은 절 안에서 반대말 | 셋 다 **「대체로 그대로」**로 맞춤 |
| 2 | `point(for:)`를 ②는 「화면 위치」, ③은 「화면 좌표가 아니다」 — **문서 기준 ②가 틀림** | **「그리는 영역 안의 위치」**로 고정 |
| 3 | 파트 머리 앞참조 `(③④)`가 **구간 재배치 전 번호** | `(②③)`으로 |
| 4 | *"저장분은 `entries` 하나"*가 과함(`redoStack`·`nextOrder` 있음) | **「화면에 그려지는 것의 뿌리」**로 좁힘 |
| 5 | 「왜 계산 프로퍼티인가」에 **출처 없음** | `git log -S`로 확인 → **커밋 `158e6ac`·설계 문서 어디에도 없음** → 「기록에 없다」 명시 |
| 6 | *"이음매에서 앞 끝과 뒤 시작이 **겹쳐 있다**"*가 미검증 | **「거의 같은 지점」**으로 낮추고, 그리기 모드는 맞춘 뒤 길찾기를 한 번 더 거친다고 명시 |

추가로 **`2000`의 실패 모드**(*"박스가 안 겹치면 `draw`가 안 돈다"*)가 **문서로 확인된 범위를 넘는다**는
한 줄을 곁줄에 더했다 — 애플 문서가 뒷받침하는 것은 *"영역이 겹칠 때 렌더러를 부른다"*까지이고,
**박스 밖 그림이 잘리는지는 문서에 없고 재보지도 않았다.**

🔴 **반복해서 걸린 실패 둘 — 다음 파트에서 먼저 볼 것**
1. **「한 번 고치면 이미 쓴 곳도 함께 고친다」가 또 안 돌았다**(#1). 구간 안에서 정정하면
   **파트 머리 · 3분 복습 `sum` · 붙들 것**까지 같이 본다.
2. **구간 순서를 바꿀 때 표만 고쳤다**(#3). 파트 머리 hint의 **내용 참조(원문자)**도 함께 훑는다 —
   원문자 검사로는 안 걸리고 문장을 읽어야 걸린다.

---

## 🧹 ⚠️ 블록 정리 — 노트에서 곁줄 형식을 없앴다 (2026-08-19)

**사용자 지적에서 시작했다** — *"내용을 검토할 때마다 계속 추가해도 하는게 그 내용들이 진짜로
내가 볼 필요가 있는건지는 잘 모르겠네 … 굳이 내가 보는 파일에는 필요없는거 아니야?"*

### 실측이 진단을 확인했다

| 청크 | 구간 | 곁줄 | 구간당 |
|---|---|---|---|
| 1 | 36 | 7 | **0.19** |
| 2 | 34 | 49 | **1.44** |
| 3 | 24 | 38 | **1.58** |

**구간 밀도는 그대로인데 곁줄만 7배.** 그리고 92개를 종류별로 가르니 **한 종류가 아니었다**:
폐기된 구간 머리 라벨 40 · **⚠️ 미검증 표시 15(전부 청크 3)** · 곁가지 5 · 본문성 사실 32.
**⚠️ 15개가 청크 1·2에 하나도 없다는 것이 결정적이었다** — `(d)` advisor 팩트체크를 본격적으로
걸기 시작한 시점과 정확히 겹친다.

### 규칙 (스킬 절대 원칙에 기록)

**⚠️ 블록이라는 형식을 노트에서 쓰지 않는다.** 코드의 사실 → 본문 / 본문 주장의 한계 →
그 문장에 한 구절 / 그 밖의 미검증·곁가지 → 백로그 또는 여기.
🔴 **숫자 상한은 두지 않는다** — 2026-08-19에 「구간당 1개」를 제안했다가 사용자가 물렸다
(*"진짜 중요한 내용을 전해야 할 상황도 있는데 상한으로 두는게 의미가 있을까?"*).
**상한은 판단을 담지 못한다.**

### 청크 3에 한 것

- **노트 밖으로 6건** — 테스트 이야기(검증은 노트 밖) · `RunWaypoint` 이름 겹침 ·
  `draw`의 `mapRect` 미사용 · 빈 박스 방어 분기 · 백로그에 이미 있는 항목 ·
  `2000` 곁줄의 「문서로 확인된 범위」 뒷문장.
- **본문으로 32건** — 곁줄 서식만 잘못 쓴 것들. ⚠ 마커를 떼고 일반 문단으로.
- **골격 파트 옛 라벨 8건 → 「이 구간이 다루는 것」으로 변환.** 2026-08-18 개편이 소급 안 된
  자리였다. 이제 **청크 3의 24구간 전부가 「이 구간이 다루는 것」 + 「알고 갈 것」**을 갖는다.
- 남긴 ⚠ 둘은 **3분 복습 안내문**과 **팩트체크 수정 내역 절** — 둘 다 정식 자리다.

### 🔴 청크 1·2는 소급하지 않는다 (사용자 결정)

**청크 2는 34구간 중 32개가 옛 라벨만 있고 새 형식 머리가 0개**라, 지우면 머리가 사라진다 —
정리가 아니라 **재집필**이다. 그리고 **내용은 팩트체크를 통과했고 확인 문제까지 풀어 복원 도구로
작동하고 있다.** 사용자 표현 — *"이전것을 다시 들춰서 파악하고 그것에 맞게 분석하고 다시
재작성하는건 비용이 너무 많이 들어. 그렇게 까지 할만한 메리트도 없는것 같고."*
**뒤집는 조건 하나** — `/trace-study MVP{N}`으로 그 청크에 덧쓸 일이 생기면 그때 함께 맞춘다.

---

# 🔑 파트 5 착수 메모 — 다음 세션은 여기서 시작한다 (2026-08-19 작성)

**파트 5 — 고른 구간이 굵어지고 후광이 깔린다** (장면 ⑥ 계열)

## 화면에서 무엇인가

구간 패널에서 구간 하나를 고르면, 지도의 그 선이 **굵어지고** 아래에 **연한 초록 후광**이 깔린다.
고른 것을 바꾸면 후광이 그쪽으로 옮겨 가고, 선택을 풀면 사라진다.

⚠️ **능력 선언은 아직 안 정했다.** 발행본 3분 복습에 옛 문장이 있다 —
*"고른 구간이 **왜 굵어지고 후광이 깔리는지, 그 순서가 어떻게 정해지는지** 설명할 수 있다."*
**파트가 실제로 다룰 것보다 좁거나 넓은지 `(b-4)`에서 먼저 판정한다**(파트 4에서 같은 문제로 넓혔다).

## 재료 경로 (2026-08-19 코드에서 확인 · ⚠️ 전수는 `(b-4)`에서 다시 뽑는다)

| 무엇 | 어디 |
|---|---|
| `polylineHaloColor` — 앱 강조색 35% 투명 | `MapViewRepresentable.swift:10` |
| `SegmentHaloPolyline` 선언 + **주석이 의도를 적어뒀다** | `:59-61` |
| `var selectedSegmentIndex: Int?` 입력 | `:175` |
| 선택이 바뀔 때 블록 — **오버레이를 다시 만들지 않고 보관된 렌더러만 고친다** | `:295-309` |
| 색 선 굵기: `configureRenderer(...)` | `:300-301`, `:346` |
| 케이싱 굵기: 선택이면 11, 아니면 9.5 | `:305`, `:432` |
| 🔑 **`syncHalo` — 기존 후광 제거 → 선택 구간의 케이싱을 찾아 같은 좌표로 새 후광을 만들고 `insertOverlay(_:below:)`로 그 아래 끼워 넣는다** | `:351-368` |
| `currentHaloOverlay`(Coordinator 보관) · `lastSelectedIndex` | `:407-408` |
| `rendererFor`의 후광 분기 — 굵기 18 · 둥근 끝 | `:421-427` |
| 구간 목록이 바뀔 때도 `syncHalo`를 부른다 | `:292` |
| 후광이 들어온 커밋 | `a4da480` |

## 앞 파트가 넘긴 것 — 이어서 받는다

1. 🔑 **파트 4와 정확히 대비된다.** 파트 4의 순서는 **「오버레이는 항상 어노테이션 아래」라는 구조가
   정한 것**이라 코드에 정하는 줄이 없었다. **후광은 `insertOverlay(_:below:)`로 순서를 직접 지정한다.**
   ⚠️ 파트 4의 목차 줄이 이미 그렇게 예고해 뒀다 — *"순서를 직접 지정하는 것은 파트 5"*.
   **다시 세우지 말고 이어받는다.**
2. **파트 1 구간 ③이 넘긴 것** — *"선택이 바뀌면 오버레이를 다시 만들지 않고 보관된 렌더러의 굵기만
   고친 뒤 `setNeedsDisplay()`로 알린다."* **굵기 쪽은 거기서 이미 다뤘다.** 파트 5가 더할 것은
   **후광 쪽**이다(그쪽은 반대로 **매번 지우고 새로 만든다**). 그 비대칭이 이 파트의 재료다.
3. **골격 1 구간 ⑦** — `rendererFor`의 네 갈래 중 **후광이 파트 5 몫**이라고 이미 적혀 있다.

## ⚠️ 조심할 것 — 오늘(파트 4) 값을 치른 것들

1. 🔴 **⚠️ 블록을 노트에 만들지 않는다** (2026-08-19 신설 · `SKILL.md` 절대 원칙).
   코드의 사실 → **본문** / 주장의 한계 → **그 문장에 한 구절** / 그 밖 → **백로그·여기**.
   **`(d)` 지적을 반영할 때 「어디에」를 고치기 전에 정한다.**
2. 🔑 **`(d)` 팩트체크는 「발행본 전문을 컨텍스트에 먼저 올리고, 범위를 파트 본문만·구간 균등으로
   못박아」 건다.** 오늘 그렇게 했더니 **지적 6건 중 5건이 파트 머리와 구간 ①에서 나왔다**
   (청크 2는 전부 마지막 파트에서 나왔다). **호출 방식이 결과를 갈랐다.**
3. 🔴 **한 곳을 고치면 파트 머리 · 3분 복습 `sum` · 붙들 것까지 함께 본다.** 오늘 구간 ③만 고쳐서
   **3분 복습 안에서 요약과 붙들 것이 반대말**을 하고 있었다.
4. 🔴 **구간 순서를 바꾸면 표만 고치지 말고 파트 머리 hint의 「내용 참조」도 읽는다.**
   원문자 검사로는 안 걸리고 문장을 읽어야 걸린다.
5. 🔴 **능력 선언에 답하지 않는 구간은 접는다. `(b-2)`「깨지고 고친 과정」이 `(b-4)`를 면제하지 않는다.**
   오늘 구간 ⑤를 그렇게 세웠다가 통째로 접었다(5구간 → 4구간).
6. 🔴 **「그래서 뭐」가 안 나오면 구간이 잘못된 것이다.** 두 번 다시 써도 안 나오면 접는 신호다.
   사용자가 *"결론이 뭐야 / 이것까지 알아야 하나"*로 물으면 **그 자리에서 능력 선언으로 판정한다.**
7. **사용자가 막히면 갈래를 접고 「처음 이 구간을 연다고 생각하고」 다시 쓴다.** 오늘 구간 ②를
   그렇게 두 번 다시 썼고, **덧붙이기로는 복구가 안 됐다.**
8. 🔴 **지어낸 표현 금지 — 오늘 추가된 것 넷.** ❌*"이 앱이 더한 것"* ❌*"우리가 쓴 것"*(주어 모호)
   ❌*"반대편"*(스킬 내부 용어가 샜다) ❌*"곁줄"* →
   ✅**「프로토콜이 요구하는 프로퍼티 / 이 클래스가 스스로 정의한 프로퍼티」** ·
   ✅**「애플이 준 것이 아니라 이 프로젝트 안에 있다」** · ✅**「원래는 이렇게 되어 있었다」** · ✅**「⚠️ 표시」**.
   그리고 **채우기·테두리처럼 한 동작을 쪼개 말하지 않는다** — *"흰 점을 그린다"* 한 줄로.
9. 🔑 **「코드 ↔ 화면 사이에 한 칸이 있다」는 관점을 이어간다**(사용자가 낸 것 · 노트 💡 박스에 있음).
   파트 5는 **그리는 시점을 MapKit이 쥔 자리**가 또 나오므로(`setNeedsDisplay` 이후 언제 그리는지)
   같은 함정이 있다. **코드에서 읽은 것을 화면에서 본 것과 같다고 단정하지 않는다.**
10. **처음 나오는 이름에 한 줄 설명** — `insertOverlay(_:below:)` · `removeOverlay` ·
    `lineCap`/`lineJoin` · `withAlphaComponent`. `(b-4)` 입력에 「설명 있음/없음」을 채운다.
11. **재료 전수는 이 메모가 아니라 코드에서 다시 뽑는다.** 파트 4에서도 메모의 경로 하나가 실제와 달랐다.

---

## ✅ 파트 5 `(d)` advisor 팩트체크 — 지적 5건 + 안 열어본 파일 3건 (2026-08-21)

**거는 방법은 파트 4에서 통한 것을 그대로 썼다** — 범위를 **「파트 5 본문만 · 구간 ①~⑤ 균등」**으로
못박고, **발행본 파트 5 전문(673줄)을 컨텍스트에 먼저 올린 뒤** 걸었다. 여기에 하나를 더했다 —
**「뺄 문장 후보도 함께」**. 사용자가 구간 ①~③이 길다고 지적했고, 지적 반영과 같은 자리를
손대게 되므로 한 번에 처리하려고 그렇게 요청했다.

### 🔴 가장 값진 지적 — 「내가 grep해놓고 안 연 파일」

advisor가 **내 앞선 `grep -rn -il "halo" docs/ history/` 결과를 되짚어**, 목록에 있었는데
내가 안 연 파일 둘을 짚었다 — `history/daily-retro/260713_daily_retro.md`,
`history/mvp12/260713_mvp12_completion_retro.md`. **둘 다 후광 커밋(`a4da480`, 07-12)의 다음 날**이라
*"왜 18인지, 왜 35%인지, 왜 케이싱에서 좌표를 꺼냈는지"*가 가장 있을 법한 자리였다.
`history/mvp7/260701_mvp7_completion_retro.md`도 같은 이유로(1.6·0.003) 지목됐다.

**셋 다 열었고 셋 다 침묵이었다** — 두 회고는 halo를 *"P2에서 당겨왔다"*로만 적었고,
MVP7 회고에는 카메라 관련 언급이 아예 없다. **「기록에 없다」 세 문장이 그대로 섰고,
이제 근거가 「설계·플랜·`git log -S`」에서 「+ 회고 셋」으로 넓어졌다.**

🔑 **교훈: `grep -l`로 후보 목록을 뽑아놓고 일부만 여는 것이 「기록에 없다」의 최대 위험이다.**
「없다」고 쓰려면 **뽑은 목록을 전부 열었는지** 먼저 확인할 것.

### 고친 것 5건

| # | 무엇이 틀렸나 | 어떻게 고쳤나 |
|---|---|---|
| 1 | *"여덟 곳 모두 배열을 바꾸는 코드 **바로 다음 줄**에서 번호를 지운다"* — **둘이 아니다.** `clear()`는 사이에 `pendingTapStart = nil`이 있고 `applyLoadedCourse`도 같다 | **「같은 함수 안에서」**로 되돌렸다. ⚠️ **내가 채팅에서 쓴 원래 표현이 맞았는데 노트로 옮기면서 조여서 거짓이 됐다** |
| 2 | *"이 앱에서 좌표를 카메라 범위로 바꾸는 곳은 여기 하나뿐"* — **아니다.** `cameraRegion`은 `:164`·`:174`·`:321`에서도 만들어진다(앱 시작·내 위치) | **「좌표 목록 전체를 감싸는 범위를 만드는 것은 여기뿐」**으로 좁혔다 |
| 3 | `clear()` 실패 예시가 **실제로 안 깨진다** — *"번호가 남아서 지도가 그 번호로 후광을 찾는다"*고 썼는데, `syncHalo`는 케이싱을 못 찾고 그냥 `return`한다(크래시 없음·화면 변화 없음) | **「값이 실제 상태와 어긋난 채 남는다」**로 낮추고 후광 구절을 뺐다 |
| 4 | **원문자 충돌** — `②가 구간 ②에서…`, `currentHaloOverlay(⑤에서 적어둔 것)` 바로 뒤에 `골격 구간 ⑤` | **「단계 ②」·「단계 ⑤」**로 접두어를 붙였다. 노트 자신의 「번호는 무엇의 번호인지 밝힌다」 규칙이다 |
| 5 | **「세 곳」(구간 ①: 시트·카메라·지도) ↔ 「세 가지」(구간 ③: 색 선·케이싱·후광)** 가 다른 셋인데 둘 다 *"고르면 셋이 바뀐다"*로 읽힌다 | 구간 ③을 **「세 층이 함께 바뀐다」**로 바꿨다 |

**낮은 우선순위 둘도 같이 처리했다.** ①*"4m씩 옆으로 비켜 그린"* → 코드 확인
(`offsetStepMeters = 4.0`, `counts.map { Double($0) * step }`)으로 **겹치는 선이 하나 늘 때마다
4m씩**이고 첫 선은 제자리인데, 파트 5에서 숫자가 필요하지 않아 **「옆으로 밀어 그린」**으로 낮췄다(파트 6 몫).
②`:292`의 `syncHalo` 호출을 *"후광도 다시 맞춰야 한다"*로 썼는데, **여덟 자리가 전부 배열 변경과
같은 함수에서 번호를 지우므로 그 호출은 남은 기록을 정리할 뿐**이다 → **「정리한다」**로 바꿨다.

### 길이도 같은 패스에서 잡았다

advisor 방침: **문장을 압축하지 말고 「주장을 뺀다」.** 고침 2·3이 각각 한 문단 → 한 구절로 줄었다.
추가로 둘을 합쳤다 — **구간 ① 소제목 2**(5문단 → 3문단, `clear()` 문단을 두 문장으로),
**구간 ③ 소제목 3**(색/굵기/둥근 끝 3문단 → 1문단). 남긴 것: 구간 ① 소제목 3의
「행이 두 번호를 다 들고 있다」 문단 — **구간 ⑤가 값을 치르는 자리**라 빼면 ⑤가 붕 뜬다.

### 그대로 선 것 (재검증 불필요)

- **여덟 곳 분류** — 여덟이 전부 배열을 바꾸고, `attach` 실패는 배열을 안 건드려 선택이 유지된다
  (`testFailedAttach_doesNotResetSelectedSegmentIndex`와 일치).
- **「사용자가 직접 풀 방법이 없다」** — `selectSegment` 비테스트 호출부는 `BottomSheetComponent:365` 하나뿐.
- **파트 1·골격과의 경계** — 중복도 모순도 없음. 구간 ③의 굵기 표는 재교육이 아니라 재연결이라
  「파트가 바뀌면 다시 이어준다」 규칙이 의도대로 작동한 것.
- **산수 전부** — 5.5/7 · 9.5/11 · 18 · 띠 2 · 후광 3.5 · `clear()` 일곱 · 0.003 ≈ 330m.
- **후광 좌표 ↔ 비켜 그린 좌표** — 케이싱이 `displayCoordinates`로 만들어지고(`:262-272`),
  `OverlapOffsetResolver`(`9dcf1a7`, 07-03)가 후광(07-12)보다 먼저다.
  **「결과적으로 맞는다 / 이유는 기록에 없다」 분리가 옳은 판단.**

### 백로그에 올린 것 (2026-08-21)

**「고른 구간을 사용자가 직접 풀 방법이 없다」** — 사용자가 구간 ⑤ 진행 중 직접 물어서 나왔다
(*"배열이 바뀌지 않았을때 선택한것을 해제할 방법이 있어야 한다던가"*). 코드로 확인한 사실은
**푸는 경로가 배열 변경 여덟 자리뿐이고 행 버튼은 토글이 아니라는 것**까지이고,
**실사용에서 거슬리는지는 재본 적 없다.**

---

# 🔑 파트 6 착수 메모 — 다음 세션은 여기서 시작한다 (2026-08-21 작성)

**파트 6 — 되짚어 그리면 두 줄로 보인다** (장면 ⑦ 계열 · 청크 3의 마지막 기능 파트)

## 화면에서 무엇인가

왔던 길을 되짚어 그으면 지도에 **두 선이 나란히** 보인다 — 좌표는 완전히 같은데도.
출발·도착 핀 자리에서는 다시 **한 점으로 모인다**. 세 번 겹치면 더 벌어진다.

## 능력 선언 — 발행본에 있는 것을 그대로 쓸지 `(b-4)`에서 판정한다

3분 복습에 있는 문장: *"좌표가 같은 두 구간이 **왜 화면에서는 두 줄로 보이는지** 설명할 수 있다."*
⚠️ **파트 5에서 이 판정을 먼저 한 것이 값을 했다** — 옛 문장이 굵기 쪽으로 넓고 카메라 쪽으로 좁았다.
여기서도 **파트가 실제로 다룰 것과 맞는지** 먼저 본다. 후보로 걸리는 것:
**「도메인 좌표는 안 건드린다」는 경계**와 **「줌아웃하면 다시 겹친다」는 한계**가 문장에 안 들어 있다.

## 재료 경로 (2026-08-21 코드에서 확인 · ⚠️ 전수는 `(b-4)`에서 다시 뽑는다)

| 무엇 | 어디 |
|---|---|
| `OverlapOffsetResolver` — **전체 152줄**, `enum` + `static func`만. MapKit을 import하지 않는다 | `Trace/Pages/CoursePlannerPage/OverlapOffsetResolver.swift` |
| `Parameters` 넷 — 감지 `10m` · 오프셋 간격 `4m` · 테이퍼 `15m` · 최소 런 `3점` | `:7-12` |
| 🔑 유일한 공개 함수 `displayCoordinates(segments:colorKeys:)` | `:17` |
| 겹침 세기 — 좌표마다 「앞서 처리한 구간 몇 개와 겹치나」를 센다 | `:33-63` |
| `key(at:colorKeys:)` — **생성 순서로 누가 먼저인지 가른다**(파트 1의 `colorKey`) | `:70` |
| `isWithin` — 겹침 판정 | `:74` |
| `suppressShortRuns` — **1~2점짜리 잡음성 겹침(수직 교차)을 무시** | `:91` |
| `exemptPins` — **양 끝은 밀지 않는다**(핀 자리에서 한 점으로 모이는 이유) | `:107` |
| 🔑 `taperedOffsets` — 목표 오프셋을 **`taperLength`에 걸쳐 선형으로 램프**. 앞→뒤, 뒤→앞 두 패스 | `:120-137` |
| `offsets = counts.map { Double($0) * step }` — **겹치는 선이 하나 늘 때마다 4m씩**(첫 선은 제자리) | `:124` |
| `applyOffsets` — 실제로 옆으로 미는 곳 | `:139` |
| 호출부 — `updateUIView`의 구간 재생성 게이트 안 | `MapViewRepresentable.swift:262-272` |
| 테스트 10개(이름만 읽는다) | `TraceTests/OverlapOffsetResolverTests.swift` |

**설계 문서:** `history/mvp8/2026-07-02-overlap-offset-design.md` (MVP8 마일스톤 2/2)
**커밋:** `9dcf1a7`(2026-07-03) *"지도 렌더링을 겹침 오프셋 표시 좌표로 전환"*

## 🔑 「왜」의 재료가 이미 다 있다 — 기각된 대안 넷이 문서에 있다

파트 5는 「왜」를 찾느라 커밋·회고까지 뒤졌는데, **여기는 설계 문서가 대안 다섯을 적어뒀다**
(`-design.md` 배경 절). **이것이 이 파트의 중심 재료다.**

| 대안 | 결정 | 기각 이유 |
|---|---|---|
| 라벨에 **구간 번호** 표기 | ❌ 기각 | 번호는 공간 순서라 prepend 시 전부 밀린다 — **MVP7 색상 문제와 같은 뿌리**이고 `colorKey`처럼 분리해서 안정화할 수 없다 |
| **점선·반투명** 등 스타일 | ❌ 기각 | 혼동의 뿌리(두 구간이 동시에 안 보임)를 못 건드린다 |
| 단일 색 + **방향 화살표** | 📦 백로그 | α로도 혼동이 남으면 |
| **화면 포인트 기준** 오프셋(커스텀 렌더러) | 📦 백로그 | α의 줌아웃 병합이 실사용에서 문제되면 (`docs/backlog.md:108`, 여전히 `open`) |
| **좌표 오프셋(α)** | ✅ 채택 | 렌더러를 안 건드리는 순수 기하 계산이라 마일스톤 1개 규모 |

🔑 **문제의 출발점도 문서에 있다** — 나중 구간이 앞 구간을 **완전히 덮는데 거리 라벨은 원래 자리에
그대로 떠 있어서, 라벨 색과 보이는 선 색이 어긋났다**(MVP7 실기기 피드백). **파트 2(거리 라벨)가
이 파트의 동기다.** 파트 2 구간 ②(`:1908`)가 *"라벨도 그 비켜난 선을 따라간다 — 그 처리 자체는
파트 6이 다룬다"*로 이미 넘겨뒀다.

## 앞 파트가 넘긴 것 — 이어서 받는다

1. 🔑 **파트 5 구간 ④가 넘긴 것** — 후광이 좌표를 **구간이 아니라 케이싱에서** 꺼내는데,
   그 케이싱이 **여기서 만든 비켜 그린 좌표**로 만들어진 것이다. 파트 5는 *"결과적으로 맞는다 /
   그렇게 고른 이유는 기록에 없다"*까지 적었다. **여기서 그 좌표가 무엇인지 완성한다.**
2. **파트 5에서 숫자를 낮춰 넘겼다** — *"4m씩"*을 **「옆으로 밀어 그린」**으로 낮췄다.
   정확한 뜻(**겹치는 선이 하나 늘 때마다 4m씩 · 첫 선은 제자리**)은 여기서 처음 밝힌다.
3. **소개 2 「지금도 열려 있는 것 ②」** — *"줌아웃하면 두 선이 다시 겹쳐 보인다"*가
   **파트 6 몫**이라고 노트에 적혀 있다(`:995`). 설계 문서가 **의도된 한계**로 미리 적어뒀다.
4. **파트 1 `colorKey`** — 누가 먼저 만들어진 구간인지 가르는 데 그대로 쓰인다(`key(at:)`).
   ⚠️ **파트가 바뀌었으니 다시 이어준다.**

## ⚠️ 조심할 것 — 파트 5에서 값을 치른 것들

1. 🔴 **「기록에 없다」를 쓰려면 뽑은 후보 목록을 전부 열었는지 먼저 확인한다.**
   파트 5에서 `grep -l`로 뽑아놓고 회고 셋을 안 열었고, **advisor가 그것을 짚었다.**
   (열어보니 전부 침묵이라 문장은 살았지만, 안 열고 쓴 것은 사실이다.)
2. 🔴 **채팅에서 맞게 쓴 표현을 노트로 옮기며 조이지 않는다.** 파트 5에서
   *"같은 함수 안에 이어져 있다"*(맞음)를 *"바로 다음 줄에서"*(틀림)로 조여서 거짓이 됐다.
3. 🔴 **원문자를 쓸 때 「무엇의 번호인지」를 붙인다** — **단계 ② / 구간 ② / 골격 구간 ⑤**가
   한 문단에 섞여 지적받았다. 이 파트는 **겹침 횟수(1겹·2겹·3겹)**까지 나오므로 더 위험하다.
4. 🔴 **같은 파트 안에서 「셋」이 두 번 나오면 다른 이름을 준다** — 파트 5의
   「세 곳」(시트·카메라·지도)과 「세 가지」(색 선·케이싱·후광)가 부딪혔다.
5. 🔑 **분량은 「주장을 빼서」 줄인다. 문장을 압축하지 않는다**(파트 5에서 통한 방법).
   사용자가 파트 5 도중 **두 번** 길이를 지적했다 — *"너무 자세하고 복잡해서 이해를 못하겠는데
   핵심만 잡고 넘어가고 싶어"* · *"그냥 덧붙히면 길이만 늘어나고 오히려 이해가 안돼."*
6. 🔑 **소제목 머리에 「이것만 알고 넘어가도 된다」 상자를 쓴다**(파트 5 구간 ②에서 신설).
   핵심 한 줄 + **왜 그런지까지** 담고, 아래는 *"그 한 줄을 코드가 어떻게 만들어내는지"*로 잇는다.
   **구간 ③④⑤에도 썼고 효과가 있었다.**
7. 🔴 **질의가 오면 덧붙이지 말고 그 소제목을 다시 짠다.** 파트 5 구간 ②는 **세 번** 다시 썼고,
   반려 사유가 *"이미 한 번 본 사람에 대한 추가설명 같은 느낌"*이었다.
8. 🔴 **처음 나오는 함수는 명패 카드로 연다 — 「무엇을 하나 / 없으면 뭐가 깨지나」.**
   파트 5 구간 ②에서 `regionFitting`을 소개 없이 쓰다가 반려됐다.
   이 파트에서 처음 나오는 이름: `OverlapOffsetResolver` · `displayCoordinates` ·
   `suppressShortRuns` · `exemptPins` · `taperedOffsets` · `applyOffsets` ·
   **「테이퍼」·「런(run)」이라는 말 자체**. `(b-4)` 입력에 「설명 있음/없음」을 채운다.
9. ⚠️ **「도메인 좌표는 안 건드린다」가 이 파트의 경계다.** 설계 문서가 명시했고
   테스트(`testOriginalSegmentsUntouched`)가 못박고 있다. **거리·라우팅·저장은 전부 원본을 쓴다** —
   이걸 안 밝히면 *"그럼 거리도 4m씩 늘어나나"*가 바로 나온다.
10. **기하 계산을 한 줄씩 따라가지 않는다**(깊이 D). 파트 4에서 *"내부 과정도 이해를 해야돼?"* →
    **아니오**였다. `taperedOffsets`의 두 패스도 **무엇을 보장하는지**까지만 쓴다.
