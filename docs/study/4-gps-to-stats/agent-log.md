# 청크 4 작업 기록 — GPS가 거리·페이스가 되는 법

> 사용자용 노트는 `note.html`. 이 파일은 **에이전트용**이다 — 팩트체크 내역·집필 이력·막힌 자리.

## 2026-08-25 — 청크 열기 (소개 3절)

브랜치 `docs/study-chunk-4-gps-to-stats`.

### `(b-5)` advisor 검토에서 걸린 것

1. **넘길 것 넷 중 「장면↔부품 표」를 빠뜨렸다.** 초안에 제목만 있고 내용이 없었다.
2. **파트 1(거리)이 파트 2(상태)를 앞지른다는 지적.** 판별은 "초안을 써보고" 하라는 규칙대로
   확인했다 — 파트 1 첫 조각에 필요한 상태 설명이 **두 문장으로 끝난다**(카운트다운이 끝나는
   순간이 세션 시작 시각 → 그 이전 타임스탬프는 버려진다). 기준상 순서를 그대로 뒀다.
3. **파트 2가 상태 골격과 시간 정의 둘을 겸한다는 지적.** 골격을 파트로 세우지 않고 분산하기로
   했다(청크 3에서 골격 파트를 접은 선례 `b6b52dd`). 소유권은 소개 3에 명시했다.

### 판정 셋 — 사용자 승인

전부 추천대로 확정(2026-08-25).

- **(ㄱ) `RunStats`를 청크 4에 둔다.** 스펙 §3 표가 여기 배정했고 청크 8 재료 목록에 받을 자리가
  없다. 대신 경계선 문장을 "뛰는 동안 쌓는 것 + 다 뛴 뒤 집계하는 것 둘 다"로 넓혔다.
- **(ㄴ) `RunGoal`을 파트 6으로 편입한다.** 스펙 표에 없었으나 결정이 실재한다(MVP14 §1 결정 6·7,
  "별도 타이머 없음"이라는 반대편이 코드 주석에 있다).
- **(ㄷ) 포인트 구간 거리를 파트 1에 붙인다.** 스펙상 청크 7에는 `MarkRunWaypointIntent`만 있어
  구간거리 계산이 어느 청크에도 없었다. 테스트 두 개가 파트 1·2 규칙을 재검증한다.

### 코드로 센 것 (착수 메모에 기대지 않고 전수)

- **평균 페이스 계산 자리 다섯**: `RunTrack.averagePaceSecondsPerKm` /
  `RunPageViewModel.liveAveragePace…` / `…summaryAveragePace…` /
  `RunAudioCoach.averagePace(elapsed:)` / `SavedRunSummary.averagePaceSecondsPerKm`.
  **넷은 활동 시간 기준, `RunTrack` 것만 GPS 샘플 구간 기준.**
  → 화면에서 안 쓴다. `RunPageViewModelTests:152`가 `buggyPace`라는 이름으로
  **"이걸 쓰면 틀린다"는 회귀 테스트**를 걸어뒀다. 파트 3의 핵심 재료.
- **활동 시간 식을 직접 쓰는 자리 다섯**: `activeElapsedSeconds` / `summaryActiveElapsedSeconds` /
  `startRecordSave`의 `duration:` / `RunSplitCalculator.activeSeconds` / `displayTimerStart`(반대 방향).
  가운데 둘은 완전히 같은 식의 중복.
- `RunTrack.duration`은 요약 화면에서 **폴백으로만** 쓰인다
  (`RunPage+StatsPanelComponent.swift:267`의 `summaryElapsedSeconds ?? …track.duration`).

### 주석↔코드 불일치 2건 — 백로그로 보낼 것 (아직 안 적음)

1. `Trace/Domain/RunTracking/Entity/SavedRun.swift:11` — `/// 벽시계 경과 시간(초)`인데
   `RunSession.startRecordSave:358`이 넣는 값은 **활동 시간**(일시정지 제외). MVP14 §4-2가 의미를 바꿨다.
   뒷문장("트래킹 화면·요약이 보여준 시간과 같은 기준")은 지금도 맞고 **앞부분만 틀렸다.**
2. `Trace/Pages/RunPage/UIComponent/RunPage+StatsPanelComponent.swift:265` —
   `// …벽시계 경과 시간과 맞춘다`인데 실제로 쓰는 `summaryElapsedSeconds`는 활동 시간.

⚠️ **다음 세션이 파트 1을 열기 전에 이 2건을 `docs/backlog.md`에 적는다.**

### 안 한 것

- `(d)` 팩트체크는 **파트를 하나 다 쓴 뒤**가 자리라 아직 안 걸었다.
- 3분 복습 절은 파트가 쌓이며 자라는 것이라 아직 없다.

### 배정 정정 — 권한·정확도 게이트는 청크 5가 아니라 두 청크에 걸친다 (2026-08-25)

`(b-5)` 단계에서 **「`lastStartFailure`·정확도 게이트 → 청크 5」로 배정했는데 부정확했다.**
사용자 질문(*"청크5에서 위치 권한과 정확한 위치에 대해서 왜 청크4에서 다룰 필요가 없는거야?"*)을
계기로 코드를 다시 열어 확인했다.

| 코드 | 하는 일 | 청크 |
|---|---|---|
| `RunSession.prepareStart:139-148` | `currentAccuracy()`로 확인 → `.reduced`면 `requestSessionFullAccuracy()` 호출 → **거부되면 `state = .idle`로 되돌리고 `lastStartFailure = .reducedAccuracy`** | **4 · 파트 1** |
| `RunLocationTracker.requestSessionFullAccuracy:30-34` | 실제로 `requestTemporaryFullAccuracyAuthorization`을 불러 시스템 창을 띄운다 | 5 |
| `RunLocationTracker.startUpdates:36-49` | `authorizationStatus` 분기 — `.notDetermined`면 요청, `.denied`면 스트림 종료 | 5 |
| `RunSession.streamEnded:308-338` | 스트림이 끊겼을 때 **수집분을 버릴지 요약으로 넘길지** | **4 · 파트 1** |

**가르는 기준: 「시스템에 무엇을 어떻게 물어보나」는 청크 5, 「그 답에 따라 러닝을 어떻게 하나」는 청크 4.**

→ 노트 소개 1에 「권한과 "정확한 위치"만은 두 청크에 걸쳐 있다」 절을 넣어 반영했다.
→ **파트 1 배정에 `prepareStart`의 정확도 게이트 · `lastStartFailure` · `streamEnded`가 추가된다.**
  `(b-3)`/`(b-4)`에서 구간을 짤 때 빠뜨리지 말 것.

### 도식에서 놓친 것 (2026-08-25)

경계선 카드 문구를 고치면서 **같은 사실을 담은 도식을 안 고쳤다.** 사용자가 찾아냈다 —
*"여전히 도식도에서 청크5에 대한 설명은 바뀌지 않은 것 같은데."*
게다가 옛 도식은 **「GPS 점 하나」 상자를 청크 5 박스 안에** 그리고 있어서
**오해를 만든 원인 자체**였다. 점 상자를 청크 4 박스 맨 위로 옮기고 라벨을 고쳤다.

**교훈: 본문과 도식은 같은 사실을 담으므로 한쪽을 고치면 다른 쪽도 본다.**

### 소개 3절 마감 (2026-08-25)

- **백로그 등록 완료** — 주석 불일치 2건을 한 항목으로 묶어 `docs/backlog.md`
  「학습 중 발견 (2026-08-25, 따라잡기 청크 4 소개)」에 적었다. 진행 상태 파일의 🔴 표시는 해소했다.
- **노트에서 걷어낸 것** — 청크 경계를 두고 오간 질의를 절로 만들어 넣었던 두 절(약 55줄)을
  통째로 지우고 한 문단으로 압축했다. 사용자 지적: *"필요없는 설명에 대해서 굳이 노트 파일에
  넣지 말아줘."* → `saving.md`에 규칙으로 반영했다.
- **마지막 검토에서 잡은 누락 셋**: ①포인트 찍기(판정 ㄷ)가 장면·표 어디에도 없었다 →
  장면 ⑨로 넣고 뒤 번호를 밀었다. ②`prepareStart`·`streamEnded`가 소개 3 표의 파트 1 행에
  없었다 → 넣었다. ③일시정지 카드의 파트 안내가 ⑨(일시정지)를 가리켰는데 내용은 재개(⑪)였다 → 고쳤다.
- 본문 462줄(CSS 583줄 제외). 장면 번호 ①~⑭ 전수와 표 참조가 일치하는 것을 스크립트로 확인했다.

## 2026-08-25 — 파트 1 착수

### `(b-3)` 판정 순서 (줄 순서와 다르다)

가장 먼저 걸러지는 것은 `ingest` 안이 아니라 **스트림 루프의 `startedAt` 가드**다
(`prepareStart` 안 `for await`). 이후 `isActive` → `state != .paused` →
`timestamp >= sessionStart` → 수평 정확도(0 초과 · 30m 이하) 순.

### `(b-4)` advisor 검토에서 고친 것 셋

1. **구간 ④에 `markGap`을 배정했던 것이 오류.** `markGap()`을 부르는 곳은 `resume` 한 곳뿐이라
   (`RunSession:215`) GPS 끊김과 무관하다. **파트 2로 넘겼다.**
2. **거리 관련 두 구간을 하나로 합쳤다.** `RunTrack.append`에 공백/이상치를 가르는 분기가
   **하나도 없다**(조건은 `pendingGap == false` 뿐). 터널이 직선으로 이어지는 것과 순간이동이
   그대로 합산되는 것은 **같은 한 줄의 두 얼굴**이라 결정 단위로는 하나다.
3. **구간 ①이 결정 셋을 담고 있어 둘로 쪼갰다** — 「시작 게이트」(①)와 「시작 경계」(②).
   advisor 지적: 이 쪼개기가 `(b-5)`의 "파트 1을 파트 2보다 앞에 둔다" 근거(*"두 문장으로 끝난다"*)를
   되살린다.
4. `finishAcquiringCancelled`를 ①에서 ⑥으로 옮겼다 — `streamEnded`와의 **대비가 곧 ⑥의 결정**이다
   (취소는 버리고, 끊김은 남긴다. 가르는 기준은 유효 샘플 유무).

⚠️ **advisor가 남긴 것:** 전수 목록에 `finish`·`dismissSummary`가 없었다. 둘 다 파트 2 몫이라
파트 1 구성은 안 바뀌지만 **파트 2를 열 때 챙길 것.**

### 절차 실패 두 건 (같은 구조)

둘 다 **규칙은 있는데 읽히는 시점이 어긋난** 경우였고, 둘 다 `session-cycle.md`로 옮겨 적었다.

1. `(a)+(b)`를 채팅으로만 내고 파트로 갈 뻔했다 — 규칙은 `saving.md`에 있었다.
2. `(b-4)` 통과한 구간 표를 노트에 안 넣고 곧바로 구간 ①을 설명했다 — 규칙(「파트를 처음 저장할 때
   골격을 함께 만든다」)도 `saving.md`에 있었다. 사용자 지적: *"파트와 구간 분리까지 하고 그것까지
   노트에 반영을 하고 구간을 1개씩 진행하는거야."*

### 구간 ① 질의에서 나온 정정

*"거부가 실패로 처리된다"*가 모호하다는 지적 — **무엇이 실패한 것인지가 문장에 없었다.**
노트에는 *"시작 버튼을 누른 것이 없던 일이 되고 카운트다운도 돌지 않는다"*로 고쳐 넣었고,
실제 알림창 문구(`RunPage.swift:16-20`)도 함께 실었다.

### 구간 ②③ 진행 중 나온 것 (2026-08-25)

**형식 개선 셋 — 사용자 지적에서 나왔고 전부 `note-format.md`에 반영했다.**

1. **흐름 그림을 먼저 놓고 그 위에서 설명한다.** 레인 타임라인이 기본 형태
   (화면 · GPS 상태 · 들어온 점 · 그 점을 어떻게 하나). 사용자 요청이 *"왠만한 모든 설명"*이었다.
   메모리 `flow-explanations-need-visual-form`도 갱신했다.
2. 🔴 **CSS 버그 — 구간 본문의 소제목에 스타일이 안 걸리고 있었다.** `.pbody > h4`가 직계 자식
   선택자라 `.segbody > h4`에는 적용되지 않는다. **청크 3부터 있던 문제**다.
   `.pbody > h4, .segbody > h4`로 고치고 왼쪽 세로선 + 위 여백을 줬다.
   그리고 `h3`(1.02rem) < `h4`(1.06rem)로 **위계가 뒤집혀** 있어 h3를 1.1rem으로 올렸다.
3. **굵은 글씨 남발.** 실측 소개 3이 문단당 평균 2.7개(최다 6개). 문단당 2개 이하로 정리했다
   (12개 제거, 전체 평균 1.52).

**1차 출처로 확인한 것 둘**

- `CLLocation.horizontalAccuracy` — *"The location's latitude and longitude identify the center of
  the circle, and this value indicates the radius of that circle. A negative value indicates that
  the latitude and longitude are invalid."* → 구간 ③ 본문에 인용했다.
  코드의 `> 0` 조건은 Apple이 말한 「음수 = 무효」보다 **한 칸 더 엄격**하다(0도 버린다).
- `startUpdatingLocation`·`didUpdateLocations`·`CLLocation.timestamp` 세 문서에
  **「캐시된 옛 위치를 먼저 준다」는 서술은 없다.** 가장 가까운 것은 *"multiple locations arrived
  before they could be delivered … organized in the order in which they occurred"*뿐이다.
  → 구간 ②에 그 수준으로만 쓰고 **「이 앱에서 실제로 일어난 기록은 없다」**를 한 구절로 붙였다.

**30m·10초의 근거** — `history/mvp13/2026-07-13-run-tracking.md:29`(계획 문서)에
*"상수 기본값(전부 구현 중 튜닝 가능): 수평 정확도 필터 30m, 약신호 타임아웃 10초, 현재 페이스
윈도 30초, 고도 임계 3m, 지도 폴리라인 스로틀 3초/20m, distanceFilter 5m"*로 **여섯 개가 한 줄에**
있다. 개별 근거도, 이후 튜닝 기록도 없다. → 구간 ③에 사실로 적었다.
**파트 4(고도 3m)·파트 3(페이스 30초)에서 같은 줄을 다시 쓰게 된다.**

### 구간 ③ — 두 번 만들고 한 번 되돌렸다 (2026-08-25)

**절차 실패:** 구간 ③을 만들자마자 노트에 넣고 발행했다. 사용자가 멈춰 세웠다 —
*"구간을 진행하는 방식은 너가 만들고 그냥 바로 넣고 하는게 아니라 나한테 보여주고 내가 질의를
하면서 이해가 안되거나 더 쉬운 형태로 재구성 한다음에 파일에 넣는 구조인거야."*
**노트에서 다시 빼고 채팅부터 다시 했다.**
원인은 구간 ②에서 사용자가 재구성을 요청해 파일에 바로 넣은 것을 **기본 방식으로 굳힌 것**이다.

🔴 **`check-draft.py` 신설.** 사용자가 *"결국 내가 계속 찾아내야 하는거야?"*라고 물었고,
**규칙 텍스트를 늘리는 방식이 반복해서 실패**했으므로 실행되는 검사로 바꿨다
(2026-08-13 *"룰즈나 어디 스킬이나 다 적어두면은 뭐해?"* 와 같은 처방).
폐기한 구간 ③ 초안으로 검증했더니 **사용자가 한 질문 여섯 개가 전부 걸렸다.**
검사 항목: 소제목이 답을 담았나 · 문단 첫머리 지시어 · 대상 없는 지시 명사구 · 느낌 표현 ·
문단당 강조 · 그림에 화면 상태 · 구간 머리에 「언제」.
예외 처리 둘 — **인용문**(원문이라 못 고침) · **라벨**(📌/✅ 뒤 굵은 글씨는 제목).

### 구간 ③에서 바로잡은 사실 하나

**버려진 구간의 거리가 사라지지 않는다.** 처음에 *"거리가 멈춰 있다가 다시 올라간다"*로 썼는데,
`RunTrack.append`가 `samples.last`(마지막으로 **쓴** 점)와 새 점 사이를 재므로
**신호가 돌아오는 순간 그 사이가 직선으로 한 번에 더해진다.**
`RunTrack` 머리 주석 — *"연속으로 버려진 공백 구간은 다음 유효 샘플과의 직선 거리로 자동 가산된다."*
설계 문서는 이것을 **"과소 계상 수용 — 러닝앱 관례"**로 적었다(MVP13 §2).

⚠️ **사용자가 두 답의 모순을 짚었다**(*"질문1에서는 거리가 다 들어온다고 하고 질문2에서는 유실이
된다고 하는데"*). 갈린 지점은 **「빠지지 않는다」와 「정확하다」를 구별해 쓰지 않은 것**이었고,
빠져 있던 전제는 **「거리는 원래도 직선 조각의 합」**이다.
→ **구간 ④의 본론이 그것이다.** 구간 ③에는 예고만 남겼다.

### 구간 ④ — 질의로 셋을 고치고 셋을 걷어냈다 (2026-08-25)

**질의에서 고친 것 셋**
1. **검사가 두 단계**라는 것을 구간 머리에 명시하고, 2번을 「통과한 점으로 하는 일 넷」 표로 바꿨다
   (거리에만 조건이 붙는다). 사용자 질문 — *"더하든지 말던지 검사는 추후 과정인것이고?"*
2. **점 간격은 앱이 정한다** — `RunLocationTracker.init`의 `distanceFilter = 5`. 처음에 *"애플이 주는 것"*으로
   읽히게 써서 사용자가 물었다. **조절되는 것(받는 하한)과 안 되는 것(신호 품질)**을 갈라 적었다.
3. **일시정지와 GPS 튐의 차이** — 갈리는 기준은 **앱이 그 상황을 아는가**다(일시정지는 사실, 튐은 추측).
   그리고 **「못 막는 것」(GPS가 튀는 것)과 「안 막은 것」(튄 점 걸러내기)**을 갈랐다.
   사용자 질문 — *"두 상황은 다른거 아니야? 그리고 튀는것도 우리가 제어할수 있는건 아니지 않나?"*

**사용자가 「알 필요 없는 내용」을 물어 걷어낸 것 셋**
- **GPX 재생 되감기 문단 통째로** — 테스트 도구 사정이다(`saving.md` *"검증 이야기는 노트에 넣지 않는다"*).
  *"튄 점은 그대로 더해진다"*는 사실만 남겼다.
- **백로그 인용 전문 → 한 문장** — 판정·처방은 백로그 몫이고, 노트가 백로그 상태를 따라다니면 안 된다.
- **미검증 표시 두 문장 → 한 구절**(*"실제로 뛰다가 겪은 사례는 아직 없다"*).

⚠️ **이 셋은 `check-draft.py`가 못 잡는다** — 형태가 아니라 **무엇을 담을지의 판단**이라서다.
사용자가 *"내가 알 필요가 없는 내용이 들어간 부분은 없는것 같아?"*로 물어 걸렸다.
**구간을 내기 전 스스로 한 번 이 질문을 던지는 것이 다음 개선 후보다.**

### 구간 ⑤ — 두 번 재구성했다 (2026-08-26)

**첫 구성의 실패:** *"구간 거리는 뺄셈이다"*부터 열고 좌표 이야기를 맨 뒤에 붙였다.
그러자 사용자가 *"그러면 포인트를 찍으면 다른거 하나도 없이 그 순간의 거리만 기억을 한다는거야?
좌표가 있어야 … 위치를 찾을수 있는거 아닌가?"*로 막혔다.
**원인은 「좌표로 계산하지 않는다」를 「좌표를 저장하지 않는다」로 읽히게 쓴 것.**

**재구성 방향:** 「무엇을 남기나」를 **표로 맨 앞에** 놓고 셋의 쓰임을 갈라 보인 뒤 계산으로 갔다.
그리고 4번의 시간축 도식을 **지도를 위에서 본 그림**으로 바꿨다 — 시간축으로는 *"자리"*가 안 보여서
사용자가 *"이거는 ✕의 제일 마지막 부분을 말하는건가? 먼가 중간으로 표현되어있어"*로 막혔다.

**질의로 확정한 사실 둘**
- **마커가 꽂히는 자리는 「마지막으로 쓴 점」이고, 신호가 회복돼도 안 옮겨진다.**
  `markWaypoint`가 값을 복사해 `append`하고 나중에 고치는 코드가 없다.
- **공백 구간 거리는 다음 구간에 귀속된다** — 테스트 이름이 그대로 적고 있다
  (`test_GPS공백중_탭은_마지막_유효샘플_기준이고_공백거리는_다음구간에_귀속된다`).

**「내가 볼 필요 없는 내용」 점검에서 나온 셋**
- 뺀 것: *"따로 저장했다면 반올림이 쌓여 합계가 달라졌을 것"* — **재본 적 없는 추측**이었다.
- 약하게: *"최대 5m"* → *"5m쯤"*.
- 🔑 **오히려 넣은 것: 포인트 삭제 기능이 실재한다**
  (`RunHistoryViewModel:54` `waypoints.remove(at:)`, 기록 상세에 삭제 확인 알럿).
  **차분으로 만드는 셋째 이유**가 그것이다 — 지운 뒤 같은 뺄셈만 다시 돌리면 된다.
  ⚠️ **「필요 없는 것 빼기」 점검이 「빠진 것 넣기」로도 이어진 사례다.**

## 2026-08-26 — 파트 1 `(d)` 팩트체크

### advisor가 잡은 것 셋 (전부 반영)

1. 🔴 **「1초에 한 번」과 「5m마다」가 서로 다른 말을 하고 있었다.** 코드는 `distanceFilter = 5`이고,
   `~1Hz`는 MVP13 설계 문서의 **지도 폴리라인 스로틀** 논의(= `distanceFilter` 적용 전) 표현이었다.
   **소개 1 본문·도식 · 구간 ③ 머리 다섯 자리**를 「5m를 움직일 때마다(뛰는 속도면 1~2초에 하나쯤)」로 통일했다.
2. **구간 ③ 도식이 본문과 어긋났다.** 「GPS 신호 찾는 중」 구간에 「약함」 표시가 없었는데,
   `RunPage.swift:145-158`의 `acquiringPanel`은 **약함과 찾는 중을 같은 패널에 함께** 띄운다.
   도식의 찾는 중 상자에 *"+ 약함 (첫 ✕부터)"*를 넣었다.
3. **구간 ⑥ 도식이 코드에 없는 합류점을 그렸다.** `finishAcquiringCancelled`는 `streamEnded`를 지나지 않고
   `samples.isEmpty`를 검사하지도 않는다. *"묻는 것은 하나뿐"*은 **개념적 통합**이지 공유 함수가 아니다.
   → figcaption에 *"취소 쪽은 코드가 따로인데, 그 상태에는 쓸 만한 점이 0이라 같은 결론이 된다"*를 붙였다.
   그리고 구간 ⑤의 *"같은 뺄셈에서 나온다"*를 *"같은 방식"*으로 낮췄다(카드는 `lastSegmentMeters`,
   상세 표는 `RunWaypointSegmentsCalculator` — 함수가 다르다).

### 🔴 `check-draft.py`에 구간별 전수 모드를 넣었다 (사용자 요청)

사용자 지적 — *"어드바이저가 최신 대화를 기준으로 좀 치우쳐져서 검사를 하더라고. 파트 전체를
평등하게 확인을 해주고 … 내가 굳이 볼 필요가 없는것들도 검토를 해줬으면해."*

- `--note <파일>`로 **구간을 하나씩 갈라 검사**한다. advisor가 뒤쪽에 쏠리는 것을 막는 자리다.
- **「볼 필요 없는 것」 검사 추가** — 테스트·도구 이야기 / 추측(*"~했을 것"*) / 내 작업 이력 / 판정·처방.
  `saving.md`의 목적지 규칙(노트 / 백로그 / agent-log)을 기계로 옮긴 것이다.
- **오탐을 다섯 번 줄였다.** 가장 컸던 것: 문단 정규식의 `<p`가 **`<path`에도 매치돼** SVG를 통째로
  문단으로 삼켰다(`<(?:p|li)(?=[\s>])`로 수정). 그 밖에 인용문 엔티티(`&#8220;`) · 라벨(`<span class="tag">`) ·
  구간 제목(`<summary>`는 목차라 주제형이 맞다) · `"그 자리에서 묻는다"`(관용) · 참조가 줄바꿈 뒤에 오는 경우.
- 최종 결과 **파트 1 여섯 구간 전부 통과.** 그 과정에서 실제로 고친 것 —
  느낌 표현(*"껑충"*) · 추측(*"했을 것"*) · 그림 위치 참조(*"그림의 GPS 줄을 보면"*) ·
  용어 이어주기 넷(구간 ②④⑤의 「쓸 만한 점」·「오차 반경」).

⚠️ **청크 끝 파트 간 일관성 검토를 이것으로 대체하지 않는다.** 위 1번(1초/5m 충돌)이
**소개 절과 구간에 걸쳐** 있었다는 것이 그 증거다 — 파트 단위로는 구조적으로 안 걸린다.

## 2026-08-26 — 파트 2 착수 (구간 표 확정·발행)

### `(b-3)` 판정 순서 — 이 파트는 분기가 아니라 「읽는 자리」가 갈린다

`pause()`/`resume()`은 가드가 하나씩이라 순서 문제가 없다. 대신 **시간을 읽는 자리가 셋인데
서로 다른 함수를 쓴다**: 뛰는 중은 `displayTimerStart`(SwiftUI가 스스로 그린다) · 일시정지 중은
`activeElapsedSeconds()` 고정 문자열 · 종료 후는 `summaryElapsedSeconds`(화면)와
`summaryActiveElapsedSeconds`(발화). 가르는 코드는 `RunPage+StatsPanelComponent`의
`elapsedText`에 있는 `if isPaused` 한 줄이다.

### `(b-4)` advisor 검토에서 고친 넷

1. 🔴 **구간 ⑤의 반대편이 가짜였다.** 「앱이 매초 갱신하는 타이머」로 적었는데 **옛 코드도
   `Text(startedAt, style: .timer)`라 같은 메커니즘**이었다(MVP14 Task 5 플랜).
   **청크 3 구간 ⑥이 네 번 무너진 것과 같은 실패다.** 진짜 반대편은 설계 §3.1에 기각으로 적혀
   있다 — *"불리언 추가만으로는 정지·재개를 표현할 수 없다."* 시작 시각을 **미는 것**과 뷰를
   **갈아 끼우는 것**이 둘 다 `elapsedText` 안에 네 줄 간격으로 있다.
2. **①②를 뒤집었다.** `activeElapsedSeconds`가 `now − startedAt − totalPausedSeconds(now:)`라,
   「무엇을 빼는가」를 말하려면 「멈춘 시간을 어떻게 아는가」가 먼저여야 했다.
   MVP13 이력(신호 대기 시간이 기록에 들어가던 문제, 설계 §0)은 **파트 여는 한 줄**로 올렸다.
3. **구간 ⑥을 둘로 쪼갰다** — 닫기 / 붙잡기 / 값 두 벌 / 폴백 / 비우기로 다섯이라
   「넷이면 구간이 아니라 파트다」에 걸렸다. → ⑥ 종료 순간에 닫히는 것, ⑦ 붙잡아 두는 자리.
4. **전수의 빈칸 하나** — `RunSplitCalculator.straddlesPause`가 목록에 없었다. 주석이
   *"라이브 markGap과 동일 규칙"*이라 적고 있어 **구간 ④의 주제가 다른 데서 다시 구현된 것**이다.
   ④에서 한 줄로 이어주고 계산 자체는 파트 5로 보낸다.

### 코드로 다시 뽑은 것 (착수 메모에 안 기댐)

- **활동 시간 식 다섯 자리:** `activeElapsedSeconds`(구간 ②) · `summaryActiveElapsedSeconds`(⑦) ·
  `startRecordSave`의 `duration:`(⑦) · `RunSplitCalculator.activeSeconds`(파트 5) ·
  `displayTimerStart`(⑤, 반대 방향으로 민다). **가운데 둘이 같은 식**이고,
  `summaryActiveElapsedSeconds`의 실사용처는 `RunAudioCoach:66` **하나뿐**이라
  두 벌의 갈림은 **화면 / 발화**다(발화는 청크 6).
- **`finish`·`dismissSummary`** — 착수 메모가 지적한 누락. ⑥·⑦에 배정했다.

### 낡은 용어의 세 번째 자리 — 백로그로 보냈다

`RunPageViewModelTests.test_종료하면_시작시각부터의_벽시계_경과시간을_캡처한다`는 **일시정지가
없는 케이스**라 값은 맞지만 이름이 MVP14 이전 용어다. 바로 아래 테스트가 같은 값을 「활동시간」이라
부른다. **주석 2건과 같은 잔재**라 그 백로그 항목에 덧붙였다(노트 본문에는 넣지 않는다).

### 🔴 절차 실패 — 골격을 파일에 넣고 발행을 안 했다

파트 2 골격을 `note.html`에 넣고 **Artifact 재발행을 건너뛰었다.** 사용자가 찾아냈다 —
*"파트2가 제대로 발행이 안된거 같은데 확인해줘."* 발행본에는 `id="p2"`가 0건이었다.
**원인:** `saving.md`의 발행 규칙이 *"구간이 끝날 때마다"*로 쓰여 있어서, **구간이 아직 없는
골격 저장**이 그 문장에 안 걸렸다. → `saving.md`의 골격 항목에 발행을 한 마디 붙였다.

### 구간 ① — 두 번 재구성했다 (2026-08-26)

**첫 구성의 실패:** 「쌍」·「닫힌 것」·「열린 것」이라는 **내가 만든 말**로 설명했고,
「목록」이 무엇의 목록인지 안 밝혔다. 사용자 질문이 일곱 개 나왔고 그중 넷(1·3·5·7)이
**전부 같은 것**을 묻고 있었다 — *"어디에 무엇이 저장되는가."*
*"3번에서 말하는 쌍이 뭐야? 닫힌것 무엇이고 안 닫힌것 그리고 열러있는것 또 뭐야?"* ·
*"어렵다....."*

**복구는 코드를 편 것이다.** 사용자가 직접 요청했다 — *"이거는 어떤식으로 구성이 되어있는지
코드 타입을 간단하게 알고 싶어."* `RunSession`의 네 프로퍼티(`startedAt` · `track` ·
`completedPauses` · `pausedAt`)를 실제 값과 함께 펴자 질문 넷이 한꺼번에 풀렸다.
**절대 원칙 ③(실물을 먼저)을 어긴 것이 원인이었다.**

**질의에서 통한 형태 하나 — `pausedAt`의 존재 이유를 숫자로 보였다.**
*"내가 정지를 한 상태에서 조금 걷다가 취소를 할수도 있는데 이때 전체 러닝시간은 계속흐르고
있어서 이것도 추가를 해서 계산을 해서 빼줘야 하는거야?"* → **사용자가 스스로 정확히 추론했다.**
09:14:30 시점의 계산표(벽시계 14:30 − 닫힌 것 0 − `pausedAt`부터 8초 = 14:22)를 그대로
그림으로 옮겼다.

**용어 고정** — 「쌍/닫힘/열림」을 버리고 **`completedPauses`(끝난 일시정지들) / `pausedAt`(값 하나)**
으로 통일했다. 코드 이름을 그대로 쓰는 것이 지어낸 말보다 쉬웠다(절대 원칙 ①과 같은 뿌리).

**빼거나 옮긴 것**
- *"거리쪽에 표시를 남긴다"* — 구간 ④ 몫인데 미리 꺼내서 질문만 만들었다. **삭제.**
- *"파일 전체가 아홉 줄인데"* — 파일 줄 수는 내 분석 흔적이다. **삭제.**
- 설계 문서 인용 문단이 💡 박스와 **같은 말을 두 번** 하고 있었다. **박스로 합쳤다.**
- *"일시정지해도 전체 러닝 시간이 흐르나"* — 구간 ②의 본론 전체라 **한 줄로만** 남겼다.

### 🔴 `check-draft.py`에 「그림 배치」 검사를 넣었다 (사용자 지적)

*"먼가 지금 스타일이 맨 처음 부분만 그렇게 하는데 … 중간 부분에 대해서도 시각적으로 표현하면
더 좋을것 들이 훨씬 많을것 같아."* → **소제목 3개 이상 + 그림 1개 이하**를 경고로 잡는다.
⚠️ **사용자가 그 자리에서 좁혔다** — *"무조건 넣으라고 한건 아닌데 … 특정 시나리오 및 흐름에
대해서 설명하는 구간이면"*. 값 하나의 정의·선언뿐인 소제목은 그림 없이 넘어간다.
`note-format.md`에도 같은 단서와 함께 반영했다.

**전수 검사에서 파트 1의 구간 넷이 걸렸다**(`p1s1`·`p1s3`·`p1s4`·`p1s6` — 소제목 3~5개에 그림 1개).
**손대지 않았다** — 사용자가 요청한 것은 앞으로의 규칙이고, 소급은 사용자 판단이다.

### 구간 ② — 재료가 한 번 정확해졌고, 질의로 갈림 하나를 세웠다 (2026-08-26)

**`(b-4)` 단계의 판정이 틀렸다.** 옛 기준이 `RunTrack.duration`이라고 봤는데, MVP13 커밋
`f4afa07`을 열어 보니 그때 저장에 쓴 식은 `endedAt − startedAt`(벽시계)이었고 주석이
*"GPS 샘플 구간 아님"*이라고 명시했다. **옛 기준과 `RunTrack.duration`은 서로 다른 것**이라,
구간을 「셋 중 하나를 고른 것」(벽시계 / GPS 샘플 구간 / 활동 시간)으로 다시 세웠다.
`Text(startedAt, style: .timer)`가 `685ea3c`(MVP13)에서 들어와 `b3f07a5`(MVP14 Task 5)에서
나간 것도 확인했다.

**질의로 드러난 구멍 — 「거리는 점에서, 시간은 시계에서」가 구간에 없었다.**
사용자 질문: *"GPS 샘플구간에 대해서 앞뒤 공백이 빠진다는게 무슨 말이야? 어쨌든 이게 우리 뛴거리를
제대로 측정하는 값이고 이것을 활동시간과 같이 계산을 해야하는거 아닌가?"* → **사용자 추론이 맞았다.**
앞뒤 22초는 거리가 0인 구간이라 시간에서도 빼는 쪽이 더 정확하다.
그래서 **소제목 3을 새로 세웠다** — 어긋남의 원인 · 안 고른 이유 셋 · 남은 대가.

**두 번째 오해 — 그림이 만든 것.** 첫 구성의 그림 1이 러닝 전체(09:00~09:35)를 펴 놓아서
사용자가 *"러닝이 다끝나고 계산하는것을 말하는거야?"*로 읽었다. → **뛰는 도중 09:20 시점**으로
바꾸고 *"1초 뒤에 같은 계산을 또 하면 19분 41초"*를 넣었다.

**사용자가 스스로 정리한 것을 채점했다.** *"최종적은 결과보다 그 순간 유저의 인터랙션을 좀더
중요하게 본건가?"* → 1·2는 맞고 **축 하나만 달랐다.** 최종 결과도 같은 값을 쓰므로 순간 대 최종의
대립이 아니라 **하나의 기준을 모든 자리가 공유하는 것**이 고른 것이다. 그 근거로 MVP14에서
기준이 갈려 두 번 깨진 이력을 소제목 4에 넣었다.

**「매 순간 계산된다」가 부정확했다** (사용자 질문 — *"기준은 없는거야? 어느정도 텀이있다던가"*).
`activeElapsedSeconds` 호출처를 전수로 보니 **뛰는 중 화면 타이머는 그 함수를 아예 안 부른다**
(`Text(timerInterval:)`이 SwiftUI 자체 기능). 부르는 것은 페이스·목표·발화·잠금화면이고
전부 **점이 들어올 때**(5m마다)다. 텀이 둘로 갈린다는 사실을 소제목 1에 넣었다.

**분량 정리** (사용자 — *"내용이 너무 길고 굳이 내가 볼필요없는 것들은 생략해도돼"*):
산문 17문단 → 13문단. 뺀 것 둘 — `durationText` 주석 인용(같은 말 중복) ·
설계 문서의 사이클 순서 인용(개발 순서 이야기라 기능 이해에 안 걸린다).

### 구간 ③ — 지어낸 말과 수량 불일치 (2026-08-26)

**「듣는다」를 썼다.** *"일시정지 버튼은 뛰는 중에만 듣는다"* — 사용자가 두 번 물었다
(*"이건 무슨 말이야? 뭘 들어?"* · *"이것도 무슨 말인가?"*). **절대 원칙 ②-2(의인화)와 (가)(없는 말)
동시 위반.** → *"뛰는 중이 아닐 때 누르면 아무 일도 일어나지 않는다"*로 고쳤다.
takeaway에도 같은 표현이 있어서 함께 고쳤다.

**「상태 하나」가 틀렸다.** 사용자 지적 — *"상태가 바뀌면 그에 따라서 화면이 pause에 맞게
바뀌는거 아니야? 그러면 안에서도 여러개가 바뀌는거 아닌가?"* `pause()`가 직접 바꾸는 것은
**`pausedAt`과 `state` 둘**이다. 구간 ①에서 `pausedAt`을 다뤘다고 여기서 뺀 것이 원인이다.

🔴 **본문만 고치고 구간 머리·figcaption을 안 고쳤다.** 사용자가 *"재구성해서 보여달라"*고 해서
파일을 텍스트로 뽑아 보고서야 발견했다 — 두 자리가 아직 「상태 하나」였다.
**`note-format.md`의 「본문을 고치면 같은 사실을 담은 도식도 함께 본다」가 정확히 이 실패인데
또 걸렸다.** 다음에 수량·개수를 고칠 때는 **구간 머리 · figcaption · takeaway 셋을 같이 연다.**

**「따로」의 대비항을 안 밝혔다.** *"넷 중 둘에는 따로 정한 이유가 붙어 있다"* →
사용자 — *"뭐가 따로인지를 먼저 설명하고 이런 설명을 붙혀야 하는거 아닌가?"*
→ *"시간이 서는 것과 「일시정지됨」은 상태를 그대로 비춘 것이라 달리 할 여지가 없지만,
두 버튼은 다르게 만들 수도 있었던 것"*으로 대비를 먼저 세웠다.

**사용자가 정리한 표현을 본문에 가져왔다.** *"GPS는 끄지 않고 계속 점을 주는데 우리가 일시정지
이기 때문에 해당 점에 대해서 저장을 하거나 사용하지 않는 상태를 만든다는거지?"* → 맞는 정리라
*"아무 흔적도 남기지 않는다"*를 **"어디에도 안 쓰이고 안 저장된다"**로 바꿨다.

**「볼 필요 없는 것」 점검에서 뺀 둘** — 설계 문서 인용의 *"스트림을 껐다 켜면 acquiring 상태가
다시 필요해져 상태기계가 복잡해진다"*(개발 사정이고 다음 문단이 같은 것을 사용자 관점으로
다시 말한다) · 그림 안 문구와 본문의 중복 한 줄.

### 구간 ④ — 통째로 다시 썼다. 원인은 `(b-4)` 구성 실패 (2026-08-26)

🔴 **구간 ①이 구간 ④의 내용을 미리 다 써버렸다.** 구간 ①의 소제목 4에서 *"왜 점에서 파생하지
않고 따로 적나"*를 설명하려고 **거리 처리가 정반대라는 것을 근거로 썼는데**, 그것이 구간 ④의
본론이었다. 사용자 지적 — *"먼가 그냥 이전에 했던 내용들에 대해서 다시 말하는 느낌 … 특히 이전
구간과 많이 겹치는것 같기도 하고 … 대체 무엇을 알아야하는거야?"*

| 첫 구성이 다룬 것 | 실제로는 |
|---|---|
| 멈춘 동안 이동은 거리에 안 들어간다 | 구간 ① 소제목 4에 이미 있다 |
| 신호 공백은 이어 붙이고 일시정지는 끊는다 | 구간 ① 소제목 4에 이미 있다 |
| 가르는 기준은 앱이 그 상황을 아는가 | 구간 ① 소제목 4에 이미 있다 |
| 「건너뛰기 표시」가 켜졌다 꺼진다 | **목표 깊이 밖**(「그 기준을 어떤 계산으로 재나」) |

**교훈: 근거로 쓴 대비가 뒤 구간의 본론일 수 있다.** `(b-4)`가 「한 개념이 두 구간에 걸치지
않는가」를 보는데, 이번 것은 **개념이 걸친 것이 아니라 앞 구간이 뒤 구간을 소비한 것**이라
표만 봐서는 안 걸렸다. 다음 `(b-4)`에서는 **각 구간의 「왜」가 다른 구간의 본론을 근거로 쓰는지**를
함께 본다.

**남긴 것 둘** (사용자에게 A: 구간 접기 / B: 둘만 남기기를 제시하고 B로 갔다):
①**빼는 것은 정확히 한 칸**이라는 범위(두 칸이면 실제로 뛴 구간까지 사라진다) ·
②**끝난 뒤에는 시각 쌍으로 같은 판단**을 한다(구간 ①의 「길이가 아니라 시각」이 여기서 회수된다).
제목도 *"재개하면 멈춘 동안 움직인 거리는 왜 안 들어가나"*(구간 ①이 이미 답한 질문) →
**"거리에서 빼는 것은 정확히 어느 한 칸인가"**로 바꾸고 구간 표·순서 근거도 함께 고쳤다.
분량: 소제목 3·그림 2·12문단 → 소제목 2·그림 1·6문단.

**사용자가 판단을 넘겼다** — *"이 내용이 들어가는게 맞는지도 모르겠고"*. 넣기로 한 근거는
**10분 복원**이다. 없으면 나중에 코드에서 *"왜 한 번만 건너뛰지"* · *"저장 쪽에 왜 같은 판단이
또 있지"*에서 막힌다.

### 파트 1의 낡은 참조 하나를 고쳤다

파트 1 구간 ④ — *"멈춰 있는 동안 이동한 것을 거리로 치지 않기 위해서다(구간 ⑥)"*.
파트 1 구간 ⑥은 신호 끊김 처리라 무관한 자리였다. **파트 2 구간 ④**로 정정했다.
파트 1을 쓸 때는 파트 2 구간이 아직 안 나뉘어 있어서 정확히 가리킬 수 없었던 것이다.
⚠️ **파트를 넘어가는 참조는 뒤 파트를 나눈 뒤에 다시 봐야 한다** — 청크 끝 「파트 간 일관성」
검토에서 이 종류를 전수로 확인할 것.

### 파트 2 마감 — 구간 7개를 5개로 줄였다 (2026-08-26)

**접은 둘, 사유가 다르다**
- 옛 구간 ⑤ 「스스로 도는 타이머에 멈춤을 어떻게 표현하나」 → **목표 깊이 밖.**
  「시작 시각을 민다 / 뷰를 갈아 끼운다」는 ❌행(「그 기준을 어떤 계산으로 재나」)이다.
  **능력 선언 어느 절에도 없다**는 것이 결정적 근거였다.
- 옛 구간 ⑦ 「끝난 시간을 어디에 붙잡아 두나」 → 옛 ⑥과 합쳐 새 ⑤로.

🔴 **한 번 잘못 접을 뻔했다 — 사용자 반응에 치우쳤다.** 새 구간 ⑤까지 접자고 제안했는데,
사용자가 물었다 — *"내가 말할때마다 먼가 나한테 치우치는 느낌이 없지않아있어."* **맞는 지적이었다.**
구간 ④(중복)와 옛 ⑤(깊이 밖)는 근거가 있었지만, **새 ⑤는 「이해가 안 된다」는 반응만이 근거**였다.
검토에서 「능력 선언 셋째 절이고 결과가 있는 결정」이라는 판정이 이미 나와 있었는데 새 근거 없이 뒤집었다.
**판별법: 접자는 근거가 사용자 반응 말고 무엇인가.** 능력 선언·목표 깊이·중복 중 하나여야 한다.

### 🔑 「사용자 장면으로 연다」가 통했다 — 규칙으로 반영

구간 ⑤를 세 번째로 다시 쓸 때 **유저가 겪는 장면**으로 열었다 —
*"신호등에서 일시정지를 누르고 물을 마시다가 「오늘은 여기까지」 하고 종료를 누르는 장면이다."*
사용자 반응 — *"훨씬더 해당 기능에 대해서 상황이 몰입되고 하니까 좋은것 같아 … 어떤 상황에서
쓰이는 상황인지에 대해서 들어가니까 더 몰입이 빠르게 되고 집중이 되는것 같아."*
그리고 **다른 설명에도 반영해 달라**고 요청했다.

- `note-format.md`에 「구간은 사용자가 겪는 장면으로 연다 — 그림만이 아니라 설명 전체가 그렇다」 추가.
  **기존 규칙이 그림에만 걸려 있어서** 구간 머리와 본문은 여전히 코드 동작에서 출발하고 있었다.
- `check-draft.py`가 구간 머리에 화면·조작 표현이 없으면 잡는다.

### 「닫는다」 — 고친 표현을 다시 썼다

구간 ①에서 「닫힌 것/안 닫힌 것」을 지적받아 **「목록에 들어간 것/아직 못 들어간 것」**으로 바꿨는데,
구간 ⑤에서 「닫는다」를 다시 썼다. 사용자 — *"닫는다는게 무엇을 다는다는거야?"*
**「지적받아 고친 표현은 그 세션 내내 다시 쓰지 않는다」를 명시적으로 어겼다.**

### 수정 결과는 조각이 아니라 전체로 보인다

고친 자리만 발췌해 보였더니 사용자가 막혔다 — *"어디를 바꾼다는거야? 대체 너가 하는 말을 내가
이해를 못하겠어."* **같은 요청이 이 세션에서 두 번 나왔다**(구간 ③에서도 *"재구성해서 보여줄래?"*).
**수정본은 파일에서 뽑아 전체를 보인다.**

### 파트 2 최종 구성

| 구간 | 제목 |
|---|---|
| ① | 멈춰 있던 시간을 앱이 어떻게 아나 |
| ② | 화면의 시간은 무엇에서 무엇을 뺀 값인가 |
| ③ | 일시정지를 누르면 무엇이 멈추나 |
| ④ | 거리에서 빼는 것은 정확히 어느 한 칸인가 |
| ⑤ | 종료하면 시간이 어떤 값으로 굳나 |

⚠️ **능력 선언의 셋째 절은 그대로 뒀다** — 새 ⑤가 그 절에 답한다.
⚠️ **`pauseTime` 건은 버렸다.** `Text(timerInterval:)`에 `pauseTime`(iOS 16+)이 있고 이 앱은 안 쓰지만,
코드가 정상 동작하고 고칠 이유가 없으며 더 나은지 재본 적도 없다. 사용자 지적 —
*"백로그는 이후에 수정을 할것들이고 … 뭐 어쩌라는거야?"* **발견했다는 이유로 자리를 찾지 않는다.**

## 2026-08-26 — 파트 2 `(d)` 팩트체크

### 🔴 구간 ②에 계산 오류가 있었다 — 논지가 통째로 잘못 서 있었다

**발행된 숫자로 검산하니 어긋났다.** 벽시계 2100초 / GPS 샘플 구간 2078초 / 활동 시간 2012초.
뒤의 둘 차이는 **66초**인데 본문은 *"남은 둘의 차이는 앞뒤 22초뿐"*이라고 적었다.
그림이 34:38과 33:32를 나란히 보이는데 산문이 22초라 했으니 **그림과 산문이 서로 다른 말**을 했다.

**더 큰 것은 그 위에 쌓은 논지다.** 소제목 3이 *"22초를 시간에서도 빼면 거리와 시간의 구간이
딱 맞는다 — GPS 샘플 구간이 그 방법이다"*라고 했는데, `RunTrack.duration`은
`last.timestamp − first.timestamp`라 **일시정지를 하나도 안 뺀다.** 그것을 쓰면 서 있던 88초가
뛴 시간으로 들어가서 파트 2의 전제가 무너진다. 거리·시간을 맞추는 후보는
**「GPS 샘플 구간 − 일시정지」**인데 **코드에 없는 넷째 방법**이다.

→ 고친 것 셋: ①GPS 샘플 구간이 탈락하는 **첫째 이유를 「멈춤을 안 뺀다」로** 바꿨다
②세 값이 각각 무엇을 빼는지 나란히 적고 **66초**를 밝혔다 ③*"그렇게 재는 방법은 코드에 없다"*로
소제목 3의 논지를 세우고 그림 문구도 함께 고쳤다.

⚠️ **교훈: 예시 숫자를 만들면 그 자리에서 검산한다.** 세 값을 그림에 나란히 놓고도 산문에서
차이를 잘못 적었다. **사용자가 소제목 3에서 계속 막힌 것이 이 어긋남 때문일 수 있다.**

### 구간 ⑤의 서술 범위를 한정했다

*"러닝은 언제나 멈춘 상태에서 끝난다"* — **종료 버튼 경로에만 맞는 말**이다.
스트림이 밖에서 끊기는 경로(파트 1 구간 ⑥)는 멈춤 없이 요약으로 간다.
→ *"종료 버튼으로 끝낼 때는"*으로 한정하고, **앱이 스스로 끝내는 경로**를 괄호로 한 줄 넣었다.
소제목 제목과 takeaway도 함께 고쳤다.

### 대조하고 이상 없던 것

- 구간 ⑤의 30:47 계산 — 09:31:07 시점과 09:32:15 시점이 같은 값으로 나온다. 맞다.
- 파트 1↔2 참조 전수 — 전부 *"파트 N 구간 M"* 형태로 파트를 명시하고 있다. 낡은 참조 없음.

## 2026-08-26 — 파트 3 골격 + `(b-4)` 구성 검토

### `(b-3)` 판정 — 분기 순서가 아니라 「어느 화면의 어느 숫자냐」로 갈린다

파트 2와 같은 결론이다. 다만 **거르는 자리가 두 층**인 것은 확인했다 —
계산 층은 다섯 자리 전부 `guard distance > 0, elapsed > 0 else { return nil }`이고,
표시 층(`RunPaceFormatter.string`)은 `seconds < 3600`으로 **60분/km를 넘는 값을 한 번 더 버린다**.
3600 상한의 근거는 `git log -S "3600"`으로 찾았으나 **도입 커밋(`685ea3c`)에 설명이 없다** —
구간으로 세우기엔 얇아 구간 ①의 마무리로 접었다.

### `(b-4)` advisor 검토 — 넷이 걸렸고 전부 반영했다

1. 🔴 **소개 2 장면 ⑥이 코드와 다르다**(아래 절에서 따로 다룬다).
2. **구간 ④ 제목의 수량이 배정과 안 맞았다** — *"네 군데"*라 썼는데 `liveAverage`를 구간 ①에
   배정해 실제로는 셋이었다. 구간을 다시 짜면서 해소했다.
3. **초안 ②③ 순서가 뒤집혀 있었다** — 잠금화면 이야기를 열려면 *"왜 거기에만 있나"*가 먼저
   필요한데 그게 ③이었다. ③을 ②의 여는 장면으로 흡수했다.
4. **초안 ②의 제목이 사실과 달랐다** — *"왜 나눗셈을 안 쓰나"*라고 썼으나 `1000 / averageSpeed`도
   나눗셈이다. 갈리는 것은 **입력**(최근 30초 속도 평균 대 누적 거리·시간)이라 제목을 그렇게 고쳤다.

**advisor의 ④+⑤ 병합 제안은 부분만 받았다.** 담을 것이 넷을 넘어(네 자리의 시간 재료 · 왜 안 모았나 ·
죽은 경위 두 건 · 테스트가 양쪽에 있는 상황) **「한 구간에 넷이면 파트다」에 걸린다.**
대신 구간 표 아래 「왜 이 순서인가」에서 **둘이 한 이야기의 앞뒤**임을 밝히는 쪽으로 처리했다.

### 🔴 소개 2 장면 ⑥이 앱과 다르다 — 사용자 판정 대기

노트는 *"**현재 페이스**와 **평균 페이스**가 나란히 뜬다"*고 적었는데 **앱 화면에는 평균 페이스만**
있다(`RunPage+StatsPanelComponent:12-21`, 주석도 *"보조 행: 시간 · 평균 페이스"*).
현재 페이스의 **유일한 소비처는 `RunActivityController:89` → 잠금화면 Live Activity**다.

그리고 **역사의 어느 쪽과도 안 맞는다** — MVP13 설계는 트래킹 패널에 *"거리·경과 시간·현재 페이스"*였고
(`history/mvp13/2026-07-13-run-tracking-design.md` §3), MVP16 화면 재구성에서 **평균 페이스로 바뀌었다**
(`ui-direction §3` · 커밋 `0fc8fb1` *"보조 행(시간·평균 페이스) 위에 누적 거리를 주인공으로 배치"*).
장면 ⑥이 말하는 「둘 다 뜬다」는 **어느 시점에도 없었다.**
게다가 「정한 것과 어긋난 지점」 다섯에 **이 건이 빠져 있다** — 뒤집힌 설계 결정을 담는 절인데도.

### 파트 3의 「왜」 재료 — 「자동으로 된다」던 전제가 틀렸다

MVP14 플랜이 *"평균 페이스는 duration 의미 변경으로 자동으로 활동 기준이 된다"*라고 적었는데
(`history/mvp14/2026-07-15-run-pause-resume.md:720`) **자동이 아니었다.**
`RunTrack.duration`은 지금도 `last.timestamp - first.timestamp`라 일시정지가 섞인다.
그래서 **두 번 따로 고쳤다**:

| 언제 | 무엇이 발견했나 | 무엇을 고쳤나 |
|---|---|---|
| 2026-07-16 `2e418e5` | **최종 브랜치 리뷰** | 요약 화면이 옛 기준을 쓰고 있었다 → `summaryAveragePaceSecondsPerKm` 추가 |
| 2026-07-17 `f306791` | **실기기 QA** | km별 페이스가 첫/마지막 GPS 샘플 기준이었다 → 세션 시작·활동 시간 기준으로 |
| 2026-07-20 `0fc8fb1` | (MVP16 화면 재구성) | 트래킹 화면이 평균 페이스로 바뀌며 `liveAveragePaceSecondsPerKm` 추가 |

그리고 `RunTrack.averagePaceSecondsPerKm`은 **아무도 안 쓰는데 테스트가 양쪽에 남아 있다** —
`RunTrackTests`는 정상 동작으로 검증하고(`test_평균페이스는_전체거리와_시간으로_계산된다`),
`RunPageViewModelTests:152`는 **`buggyPace` 대조군**으로 쓴다.

### 발행이 막혔다

`Artifact` 재발행이 *"identical content already refused ... resent unchanged"*로 거절됐다.
발행본과 직전 커밋을 diff한 결과 **차이는 발행 랩퍼 2줄뿐**이라(다른 세션의 변경 없음)
병합할 것이 없는데, 도구는 병합 흔적을 요구한다. **`force:true`는 사용자 승인 사항이라 멈췄다.**

### 사용자 확인에서 갈린 것 둘 (2026-08-26)

**①「③④가 무엇인지」를 사용자가 되물었다.** 사용자 정리 — *"파트3는 결국 페이스라는 부분에
대해서 실제 앱 화면과 잠금화면에서 다르게 처리하고 있다는것에 대해서 알라는거야?"*
**앞 절반(①②)만 맞고 뒤 절반(③④)은 화면 갈림이 아니다** — ③④는 러닝 탭 화면의 평균 페이스
**하나**가 코드 다섯 자리에 흩어진 이야기다. **구간 표만 보면 이 갈림이 안 보인다**(제목 넷이
나란히 놓여 있어서 넷 다 화면 이야기로 읽힌다). 구간 표 아래 「왜 이 순서인가」에
**「③④는 화면 갈림이 아니다」를 명시**해서 막았다.

**②「화면」이 어느 화면인지 안 밝혔다.** 사용자 지적 — *"여기서 말하는 화면 보조행이 실제 앱
화면인지 잠금화면인지에 대해서 표현들을 명확하게 해줘."* 이름 둘을 고정했다(**러닝 탭 화면** ·
**잠금화면**) 그리고 `check-draft.py`에 **「어느 화면인지」 검사**를 넣었다 — 화면 이름이 나온
문단에서만 수식 없는 「화면」을 잡아 *"화면에 뜬다"* 같은 일반 표현까지 걸리지 않게 했다.
🔴 **그 검사가 파트 1·2에서 3건을 더 잡았다** — 사용자에게 알렸고 판단 대기 중이다.

**그리고 페이스 두 종류를 갈라서 쓰기로 했다**(사용자 요청 — *"페이스에 대해서 명확하게 분리해서
설명을 해주면 좋을것 같아"*). 한 줄 소개를 **평균 = 시작부터 지금까지 전체 / 현재 = 최근 30초**로
나눠 다시 썼다. **재는 구간이 다르다는 것이 두 숫자가 다른 이유**라 거기서부터 갈라야 한다.

### 발행이 다섯 번 거절됐다 — `force`로 뚫었다

이전 세션에 발행한 아티팩트를 새 세션에서 `url`로 재발행하는데 두 에러가 번갈아 나왔다 —
*"hadn't viewed the live version"*(→ 저장본 3226줄 전체 Read) ↔ *"identical content ... resent
unchanged"*(→ `action: "read"` 재조회). **저장본 전체를 두 번 읽고도 안 뚫렸다.**
발행본과 로컬을 diff해 **발행본에만 있는 줄이 0임을 확인**하고 사용자 승인을 받아 `force`로 밀었다.
URL·제목·다른 아티팩트 전부 무사하다. ⚠️ **그 뒤 같은 세션에서의 재발행은 `url` 없이 한 번에 통과했다** —
막힌 원인은 「이전 세션 것을 새 세션에서 올리는 것」쪽으로 보이나 **확증은 못 했다.**

## 2026-08-30 — 파트 3 구간 ①: 축을 갈아엎고 구간 넷을 둘로 줄였다

### 🔴 첫 판이 무너졌다 — 원인은 구간이 아니라 파트 골격의 축

사용자 지적 — *"결국 모든 부분에서 다 똑같이 나오도록 해야하는거 아니야? 무슨 페이스가
상황마다 다르게 표현된다는게 말이 안된다고 생각해."* **지적이 맞았다.**

원인은 구간 ① 본문이 아니라 **파트 3 골격이 「화면」을 축으로 잡은 것**이다 — 한 줄 소개가
*"자리가 두 군데인데 거기 뜨는 것이 서로 다른 페이스다"*였고 구간 표 아래도
*"①②는 두 화면의 숫자가 왜 다른지를 가른다"*였다. 화면을 축에 놓으면 **같은 값이 화면마다
다르게 나오는 이야기로 읽힌다.**

🔴 **같은 오해가 두 번째다.** 2026-08-26에 사용자가 *"파트3는 결국 페이스라는 부분에 대해서
실제 앱 화면과 잠금화면에서 다르게 처리하고 있다는것에 대해서 알라는거야?"*라고 물었을 때
**뒤 절반(③④)만 「화면 갈림이 아니다」로 고치고 앞 절반(①②)의 축은 그대로 뒀다.**
남겨둔 자리에서 그대로 다시 터졌다. **지적을 구성 전체로 읽지 않고 지적받은 줄만 고친 실패다.**

고친 것 셋 — ①한 줄 소개를 *"페이스라는 이름으로 뜨는 값이 둘이고, 둘은 서로 다른 것을 재는
별개의 지표다"*로 ②구간 ①② 제목을 지표 주어로 앞세우고 화면을 뒤에 ③구간 표 hint를
*"두 지표가 각각 무엇을 재는지"*로.

### 🔴 둘째 판도 걸렸다 — 목표 깊이 밖을 노트에 넣고 있었다

사용자 지적 — *"굳이 내가 이것을 기능을 위해서 알아야될 필요가 없지 않아? 특히 3,4번은 내가
이것에 대해서 「다 만들었다고 여긴 뒤」 이런것들이 왜 있어야 하는지 모르겠어."*
그리고 *"학습 깊이를 낮췄는데도 결국에 시간도 계속 오래걸리고 아무것도 달라지는게 없는것 같은
느낌이 들어."* **둘 다 맞았다.**

목표 깊이 표로 재판정한 결과:

| 초안의 절 | 판정 |
|---|---|
| 1. 왜 활동 시간을 골랐나 | ✅ 「무엇을 왜 그렇게 정했나」 — 남긴다 |
| 2. 계산은 한 줄이다 | ✅ 「무엇을 기준으로 삼았나」 — 남긴다 |
| 3. 세 번에 걸쳐 고쳤다(커밋 셋·날짜·발견 경로) | ⚠️ **기록이지 학습 재료가 아니다** — 「처음부터 그랬던 게 아니다」 한 문장만 남기고 흡수 |
| 4. 60분/km 상한 + 위젯 중복 | ❌ 「어떤 계산으로 재나」 + 판정·처방 — **통째로 백로그로** |

### 🔴 구간 ③④를 접었다 — 파트 3은 구간 2개다

같은 잣대를 나머지 구간에도 댔다. **별도 구간이 설 근거가 없었다.**

- **구간 ③(같은 평균 페이스를 네 자리가 각자 계산한다)** — 「네 자리가 같은 숫자여야 한다」는
  구간 ①의 「왜 활동 시간인가」에 이미 들어간다. 네 자리가 어디인지는 한 줄 열거로 충분하다.
  중복 자체는 **판정이라 백로그 몫**이다.
- **구간 ④(다섯째 자리는 옛 기준으로 남아 아무도 안 쓴다)** — *"안 고른 쪽이 코드에 그대로
  남아 있다"* 한 문장이면 된다. 죽은 프로퍼티에 테스트가 둘 붙어 있는 것은 **검증 이야기라
  노트에 안 넣는다**(`saving.md`).

능력 선언도 줄였다 — *"다섯 군데인 이유와 그중 하나가 왜 죽은 채 남아 있는지"* →
*"평균 페이스가 뜨는 네 자리에서 왜 같은 숫자가 나오는지"*.

### 개념 대화가 먼저 필요했다 — 골격이 그 갈림을 안 세워 놨다

구간 ①을 내기 전에 사용자가 개념 질문을 셋 던졌다. ①*"평균페이스랑 최근 페이스(현재 페이스?)가
다른거야?"* ②*"각 1km마다의 페이스를 정하고 나누는거야?"* ③*"현재 페이스가 최근 30초를 기준으로
원래 잡는거야?"*

**셋 다 파트 3 골격이 답했어야 할 질문이다.** 특히 ②는 **킬로미터별 페이스**라는 세 번째 값의
존재를 골격이 한 번도 안 밝혀서 나왔다(그 값은 파트 5 몫이라 파트 3에 없지만, **없다는 사실
자체를 안 밝히면 평균 페이스를 쪼갠 것으로 오해한다**). ③의 답 — 30초는 MVP13이 정한 잠정값이고
(`history/mvp13/2026-07-13-run-tracking.md:29`, *"전부 구현 중 튜닝 가능"*) **왜 30초인지는 기록에
없다.** 도입 커밋 `10c3d3a`에도 설명이 없다. **구간 ②에서 쓸 재료다.**

용어도 하나 고정했다 — 사용자가 쓴 *"각 구간마다의 페이스"*의 「구간」이 학습 단위(구간 ①②)와
겹쳐서, 러닝의 1km 칸은 **킬로미터별 페이스**로 부르기로 했다(앱 화면 라벨과 같다).

### 🔴 구조적 원인 — 사이클에 「빼는 단계」가 없다

사용자가 *"시간도 계속 오래걸리고 아무것도 달라지는게 없는것 같은 느낌"*이라고 한 것의 실체는
**깊이를 낮췄는데 분량이 안 줄어든 것**이고, 원인은 **깊이 밖을 뺀 게 아니라 깊이 안을 더 자세히
쓰는 쪽으로 간 것**이다. 오늘 3번·4번 절이 그 증거다.

⚠️ **이 진단은 `SKILL.md`에 이미 적혀 있다** — *"스터디 사이클에는 빼는 단계가 없다 — 구성
검토는 구간을 더하고, 질의는 본문을 더하고, 팩트체크는 표시를 더한다."* 그런데 **그 조항이
「⚠️ 미검증 표시」에만 걸려 있고 본문 절·구간 자체에는 안 걸린다.** 오늘 접은 것은
미검증 표시가 아니라 **절 하나와 구간 둘**이었다.
**규칙을 어디에 어떻게 걸지는 사용자 판단이 필요해 아직 안 고쳤다.**

## 2026-08-30 — 파트 3 구간 ②, 그리고 「내 말로 잡기」 블록을 도입했다

### 🔴 절차 실패 — 구간 ①을 채팅에 안 보이고 노트에 바로 넣었다

사용자 지적 — *"먼가 구간1도 수정한것 같은데 왜 나한테 안보여주고 바로 노트파일에 넣은건지도 모르겠고."*
**「알아서 진행해줘」를 「보여주는 단계까지 건너뛰라」로 넓혀 받았다.** 구간 진행 순서(🔴)의
③채팅 → ④질의를 통째로 건너뛴 것이다. 구간 ②는 순서대로 진행했다.

### 💬 「한 줄로 잡으면」 블록을 새로 만들었다 (사용자 요청)

사용자 — *"구간1에 대해서 내가 말했던 내 이해 표현들도 넣어줄수 있어? … 내 이해 바탕에 대해서
내가 물어본것이기 때문에 나중에 다시 보거나 했을때 더 잘이해가 되지 않을까? (바로 몰입을 할수
있는 상황으로 들어가도록 해주는거지.)"*

`<div class="mywords">`로 **구간 머리 다음, 첫 소제목 앞**에 놓는다. 구간 ①에는 *"영화에서
세상이 멈춘 것처럼"*을, 구간 ②에는 *"「그 순간의 속도를 준다」까지만 알면 된다"*를 담았다.
**형식 규칙은 `note-format.md`에 넣었다** — 사용자 표현을 그대로 쓰고, 내 해명은 넣지 않는다.

### 🔑 GPS 속도를 잘못 읽게 쓸 뻔했다 — 사용자 질문에서 걸렸다

초안이 *"GPS 점에 함께 실려 오는 속도를 본다"*라고만 써서 사용자가 **5m 구간을 계산한 값**으로
읽었다 — *"이 5m마다의 내 페이스를 알아서 계산해서 준다는건가?"* 두 군데가 틀리게 잡혔다:
**①속도이지 페이스가 아니다 ②앞 점과 비교한 값이 아니라 그 순간의 속도다.**

1차 출처로 확인했다(`CLLocation.speed`) — *"The instantaneous speed of the device, measured in
meters per second"* · *"A negative value indicates an invalid speed"* · 그리고
🔑 ***"actual speed can change many times between the delivery of location events, use this
property for informational purposes only"*** — **Apple이 스스로 흔들리는 값이라고 못박았다.**
마지막 문장은 30초 창의 배경이라 백로그에 적었다.

본문에 문단 하나를 더해 바로잡았고, **평균을 내는 순서도 밝혔다** — 점마다 페이스를 구해
평균 내는 것이 아니라 **속도를 먼저 평균 내고 그 하나를 뒤집는다.**

### 🔴 사용자 실사용 관찰이 백로그 두 건이 됐다

사용자 — *"방금 오디오로 페이스를 이야기 해줬는데 시간이 잠깐 몇초 지나고 잠금화면을 봤는데
페이스가 갑자기 너무 달라져있는거야. 이래서 뭐가 문제인지에 대해서 이야기를 하려고 했거든."*

**두 지표가 있다는 사실 자체를 모르고 오작동으로 읽은 것이다.** 발화는 평균 페이스, 잠금화면은
현재 페이스이므로 동작은 설계대로다. 고칠 대상은 값이 아니라 **어느 페이스인지 알 수 없는 표시**다.
30초 창 재검토(사용자가 *"5~10초로 알고 있고 30초는 너무 길다"*고 들었다)와 함께 백로그로 보냈다.

### 뺀 것 — 새 「깊이 점검」이 아니라 판단으로 걸렀다

3번 절 마지막 문단(*"짧게 잡을수록 민감해지고 자주 튄다"*)을 **재본 적 없는 추론**이라 뺐다.
백로그의 조사 항목과 같은 내용이고, 사용자도 *"지금보다 그때 개념을 공부하고 이해하는게
맞는것같기도하고"*라고 했다. **깊이 점검 출력이 소제목을 펴 준 상태에서 이 판단을 했다.**

## 2026-08-30 — 파트 3 `(d)` 팩트체크

### 🔴 3분 복습 절이 통째로 빠져 있었다 — 세 파트가 밀린 상태였다

advisor가 잡았다. **청크 0·1·2·3에는 다 있는데 청크 4에만 없었고**, `.recap` CSS만 있고
`<div class="recap">`가 한 번도 안 만들어졌다. `saving.md`는 3분 복습을 **첫 발행부터 있어야 할
절 다음**에 두고 *"파트가 쌓이며 자란다"*고 적었는데, 파트 1·2·3이 다 차도록 안 만들었다.
**목차에도 항목이 없어서 빠진 것이 안 보였다.** 파트 1·2·3을 구간 순서대로 채워 넣었다.

### 🔴 검사에 사각지대가 있었다 — 소개 절과 파트 골격은 어떤 검사도 안 지났다

`check_note_by_segment`가 `<details class="seg">`만 훑어서 **소개 3절 · 파트 골격 · 3분 복습이
통째로 빠져 있었다.** 그날 소개 2를 세 번 고쳤는데 한 번도 검사를 안 지난 것이다.
`--note` 모드에 **구간 밖 블록 셋**을 추가했다(소개 `<section>` · 파트 골격 · 3분 복습).
구간용 검사(그림 · 그림 배치 · 구간 머리)는 그 자리에 안 맞아 뺐고,
**3분 복습은 「앞에서 정한 말」도 뺐다** — 파트를 가로지르는 요약이라 구조적으로 걸린다.

새 검사가 곧바로 **소개 절 「어느 화면인지」 4건**을 잡았다(소개 1·3, 그리고 advisor가 따로 짚은
소개 2). 전부 고쳤다 — 소개 2의 *"한 화면에 같이 뜨는 자리는 없다"*는 구간 ②에서 오늘 고친
것과 **같은 패턴**이었다(→ *"한자리에 같이 뜨는 곳은 없다"*).

### 맨 아래 「다음」 안내도 없었다

`.next` CSS만 있고 블록이 없었다. 청크 0·1·3에는 있다. 착수 메모의 만성 ⚠️(*"목차 부제와
노트 맨 아래 「다음」 안내를 함께 고친다"*)가 **이 청크에서는 안내가 아예 없어서 안 걸렸던 것**이다.
만들어 넣었다.

### 대조하고 이상 없던 것 (다시 재지 말 것)

**산수 전부 확인** — 1171 ÷ 3.42 = 342.4(`5'42"`) · 1201 ÷ 3.42 = 351.2(`5'51"`) ·
1186 ÷ 3.42 = 346.8(`5'47"`) · 1201 ÷ 3.48 = 345.1(`5'45"`) · 초속 2m → 500초(`8'20"`).
두 구간의 그림도 서로 맞는다 — 구간 ②의 3.48km/20:01은 구간 ①의 3.42km/19:31에
**초속 2m로 30초에 60m를 더한 값**이다.

**코드 주장 전부 확인** — `liveAveragePaceSecondsPerKm`의 식과 두 guard · `RunTrack.duration`이
아직 `last − first`인 것 · `speedMetersPerSecond > 0` 필터와 주석 · 현재 페이스의 소비처가
`RunActivityController:89` 하나인 것 · 위젯 라벨이 `"페이스"`인 것 · 30초가 오차 반경 30m와
같은 MVP13 줄에 있는 것. **네 자리가 같은 기준인 근거도 확인됐다** —
`SavedRunSummary.duration`이 `endedAt − startedAt − totalPausedSeconds`라 활동 시간이다.

### 🔑 정밀하게는 이렇다 (노트 수정은 불필요)

구간 ①의 마무리가 네 자리가 *"같은 숫자를 낸다"*고 하는데, 엄밀히는 `liveAverage`가
`activeElapsedSeconds()`(지금 기준, 종료 후에도 자란다 — `RunSession.swift:111`)를 읽고
`summaryAverage`는 **종료 시점에 굳은 값**을 읽는다. **각자 자기 화면이 떠 있는 동안만 읽히므로
어긋나는 순간이 없다.** 사용자가 *"항상 같냐"*고 물으면 이것이 답이다.
